[package]
name = "forge-discovery"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
# T15: artifact ZIP-bomb check uses zip crate
zip = "2"
[lints]
workspace = true
