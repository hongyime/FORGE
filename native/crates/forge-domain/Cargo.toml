[package]
name = "forge-domain"
version = "0.1.0"
edition = "2024"
rust-version = "1.94.1"
publish = false

[dependencies]
serde = { version = "=1.0.229", features = ["derive"] }
serde_json = { version = "=1.0.151", features = ["raw_value"] }
time = "=0.3.55"
rand = "=0.8.8"

[dev-dependencies]
sha2 = "=0.10.9"

[lints.rust]
unsafe_code = "forbid"
