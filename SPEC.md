# FORGE Spec

Last updated: 2026-07-25

Goal lock: `FORGE-DETERMINISTIC-ASM-PIPELINE-v1`.

End goal now: FORGE must ship one comprehensive deterministic authorized ASM
engagement pipeline, not a UI-only dashboard, scanner collection, or provider
count project.

This file is the compact root spec for implementers. It does not replace the
normative goal in `END_GOAL.md`; it restates the contract as invariants and
execution tasks so agents can decide what to build next without moving the goal.

## G

FORGE must be one comprehensive, deterministic, authorized engagement pipeline
that turns scoped multi-seed intake into bounded recursive discovery,
non-destructive validation, rule-engine findings, graph/dashboard/report/audit
review, and guaranteed template/raw exports when LLM providers fail.

## C

- Authorized scope is mandatory for live probing, follow-on tool execution, and
  credential/resource validation.
- Passive discovery, static artifact parsing, and deterministic synthesis are
  the default automation path.
- Live work must be ROE-gated, scope-manifest-gated, bounded, paced, logged,
  resumable, dry-run capable, and non-destructive.
- URL scope entries are same-host path constraints for crawler,
  remote-artifact, scheduled URL-task, and other URL-fetch gates. Explicit
  domain/IP scope still authorizes its own host; generic host gates may use URL
  entries only as host authorization.
- LLMs may write narrative only. They must not create findings, set severity,
  weaken validation, alter evidence, or decide reportability.
- Provider failures, missing keys, quota errors, rate limits, and token limits
  are normal product paths and must degrade to deterministic template output
  plus raw exports.
- Runtime `/goal` labels, chat summaries, and old handoff notes are advisory
  only; repository goal docs override stale runtime state.
- New code must stay modular. Large legacy files should receive thin adapters,
  not more embedded feature logic, when a focused helper can own the behavior.

## I

- CLI: `forge kill-chain`, report commands, engagement creation, artifact
  ingestion, and scoped active-assessment flags.
- Backend/API: engagement CRUD, seed CRUD, run status, dashboard payloads,
  findings, validation inventory, graph/export/report surfaces.
- Frontend: main dashboard, engagement detail route, graph/report/raw-export
  review, audit log review.
- Persistence: engagement SQLite stores, monotonic master ID store, migrations,
  audit logs, graph/report/export artifacts.
- Providers/tools: passive OSINT, identity enrichment, web mining, static
  artifact parsers, cloud/key validators, LLM narrative cascade.

## V

- V1. Same scoped inputs, fixtures, config, and cached provider responses must
  produce the same findings, severities, reportability, graph/export metadata,
  audit metadata, and fallback path.
- V2. Engagement IDs are monotonic and are not reused after deletion.
- V3. Every seed, discovered pivot, validation attempt, finding, export, and
  report render is traceable to an engagement and audit context.
- V4. Recursive discovery terminates only by explicit depth, iteration, queue,
  stable-snapshot, budget, or operator-stop conditions.
- V5. Static artifact analysis never executes the artifact being parsed.
- V6. Unvalidated, dead, unsupported, placeholder, honeypot-suspected, or
  low-signal evidence may be analyst inventory, but cannot become a reportable
  deterministic finding.
- V7. Severity and report gates are deterministic rule-engine outputs only.
- V8. Dashboard, graph exports, reports, raw exports, validation inventory, and
  audit logs expose the same engagement facts.
- V9. LLM/API narrative failure must still produce deterministic template
  reports and raw JSON/CSV exports with checksums and failure metadata.
- V10. Automated tests must clean up or isolate test engagement data and must
  not rely on real external targets unless an explicit scoped target is provided.
- V11. Automated cleanup may remove only proven test-owned engagement artifacts;
  it must not delete broad pytest owner containers or persistent engagement DBs
  that are not proven artifacts from the current test run.
- V12. Deterministic reports and LLM prompts must frame Section 6 as validation
  boundaries and evidence handling; they must not force post-exploitation,
  persistence, lateral movement, shell access, or data-exfiltration narratives
  into authorized ASM reports.
- V13. Deterministic reports and LLM prompts must frame Section 5 as
  vulnerability and exposure/finding correlation; they must not require exploit
  correlation headings or mandatory exploit narratives for authorized ASM
  reports.

## T

| id | status | task | cites |
|---|---|---|---|
| T1 | x | Prove one representative multi-seed engagement path from intake through recursion, validation, graph/report/audit review, fallback exports, and cleanup with focused mocked E2E tests. | V1,V2,V3,V4,V8,V9,V10,V11 |
| T2 | . | Continue safe recursive discovery upgrades for concrete passive parser, provider payload, identity, artifact, and source-gated metadata gaps. | V1,V3,V4,V5 |
| T3 | ~ | Harden validation proof gates and report gates so only latest validated evidence can create deterministic findings. | V6,V7 |
| T4 | ~ | Keep dashboard, API, graph, report, raw export, validation inventory, and audit surfaces in factual parity. | V3,V8 |
| T5 | . | Preserve deterministic report fallback through LLM cascade failures, quota/token failures, missing keys, local/template degradation, and raw export availability. | V7,V9,V12,V13 |
| T6 | . | Keep scoped active checks ROE-gated, non-destructive, bounded, paced, logged, and dry-run capable. | V3,V4,V6,V10 |
| T7 | . | Split or wrap large legacy modules only when it reduces risk without changing verified behavior. | V1,V10 |
| T8 | . | Commit meaningful checkpoints to `main` with docs and handoffs updated for continuation. | V3,V10 |

## B

| id | date | cause | fix |
|---|---|---|---|
| B1 | 2026-07-20 | Goal and invariant notes were split across long handoff/tasklist history, including stale historical text saying no root spec existed. | Added root `SPEC.md` and linked it from the active goal docs while preserving `docs/end_goal.md` as normative. |
| B2 | 2026-07-20 | Runtime `/goal` state can be stale while repo docs contain the real goal lock, which can confuse future agents. | Recorded that repo goal docs override stale runtime/chat/handoff goal text and that stale continuation material should be corrected, not used to redefine the project. |
| B3 | 2026-07-20 | The locked end goal existed, but continuation docs could still require reading long sections before the one-sentence product target was obvious. | Added one-glance end-goal quick answers to the root goal file, active tasklist, and Claude continuation docs. |
| B4 | 2026-07-20 | Broad inventory-name artifact labeling could outrank explicit multi-suffix SBOM names such as `inventory.spdx.json`, weakening deterministic artifact review metadata. | Classified explicit SBOM multi-suffix names before broad inventory heuristics; covered by V8 artifact metadata parity. |
| B5 | 2026-07-20 | Pytest engagement cleanup treated `pytest-of-*` owner containers as removable pytest run directories when a nested `engagement.db` existed. | Added V11 and tightened cleanup to remove only actual nested `pytest-*` run directories that contain engagement DBs, leaving owner containers and persistent workspace DBs intact. |
| B6 | 2026-07-20 | Deterministic Phase 6 reports and fallback prompts still forced a legacy post-exploitation section despite the current authorized ASM goal. | Added V12 and reframed Section 6 as validation boundaries and evidence handling in mandatory sections, fallback prompts, Jinja template instructions, and deterministic template output. |
| B7 | 2026-07-20 | Deterministic Phase 6 reports and fallback prompts still forced a legacy exploit-correlation section despite the current authorized ASM goal. | Added V13 and reframed Section 5 as vulnerability and exposure correlation in mandatory sections, fallback prompts, Jinja template instructions, and deterministic template output. |
| B8 | 2026-07-20 | Runtime `/goal` text remained stale enough to obscure the real deterministic product target even though repository goal docs existed. | Re-anchored the active goal statement in `END_GOAL.md`, `docs/end_goal.md`, `docs/deterministic_engagement_contract.md`, and the active continuation checklists; repository goal docs remain authoritative over runtime labels. |
| B9 | 2026-07-23 | The locked end goal existed, but the newest continuation prompt showed the answer still needed to be more immediate for future agents under low context. | Added top-level "end goal now" wording to the root spec and refreshed the source-of-truth docs so agents answer with the deterministic authorized ASM pipeline target before choosing tasks. |
| B10 | 2026-07-23 | Deterministic finding synthesis trusted a `VALIDATED` cloud row even when the validation method was not a known deterministic proof method. | Gated cloud findings by explicit reportable validation-method allowlists and required linked key confirmations to pass the stable proof parser before they can keep or create deterministic findings. |
| B11 | 2026-07-23 | Phase 6, graph, and dashboard review surfaces could still trust stale deterministic cloud finding rows when the latest validation row used an unknown `VALIDATED` method, because the validation-method allowlist lived only in synthesis. | Moved the reportable cloud validation-method policy into `forge.utils.cloud_exposure_gate` and reused it across deterministic synthesis, Phase 6 report/raw exports, graph vuln-node gating, and dashboard severity/finding review tables. |
| B12 | 2026-07-23 | Dashboard review counts and imported graph payloads could still treat stale `ACTIVE` key scanner rows or `APIKEY` snapshot nodes as reportable when their stored validation detail failed the stable proof parser. | Reused the stable key proof parser for dashboard `key_scanner_findings` counts and imported graph payload `APIKEY` filtering while leaving downgraded key rows visible as analyst inventory. |
| B13 | 2026-07-23 | The live `/api/engagements/{id}/vuln-summary` route grouped `vulnerability_findings` directly from SQLite, so stale deterministic cloud findings with non-reportable validation methods could reappear as active `HIGH` summary counts after dashboard/report gates suppressed them. | Reused `_reportable_vulnerability_rows` for the live vulnerability summary route so API active-finding counts follow the same cloud validation-method gate as dashboard/detail/report surfaces. |
| B14 | 2026-07-23 | Operational automation still used raw `vulnerability_findings` rows: report suggestions counted stale unreportable findings, and the RCE trigger selected any high/critical row by a non-canonical `host_id` column without checking reportability or RCE-specific evidence. | Reused `_reportable_vulnerability_rows` for automation report suggestions and RCE trigger candidates, required RCE-specific finding text plus host metadata before scheduling RCE playbooks, and skipped safely when canonical findings lack legacy `host_id`. |
| B15 | 2026-07-23 | The legacy cloud-leak manual playbook trusted `key_scanner_findings.validation_state='ACTIVE'` without checking stable validation proof, so stale active key rows could enter validation/enumeration flow even after report/dashboard gates downgraded them. | Required stable key proof parsing or linked reportable cloud validation before the cloud-leak playbook trusts an existing `ACTIVE` key row, while keeping the auto-trigger disabled and provider/enumeration behavior unchanged. |
| B16 | 2026-07-23 | Automation suggestions still offered `post:lateral` from validated credentials, which conflicted with the locked authorized ASM goal and could steer operators toward post-exploitation work from the review surface. | Suppressed the legacy lateral-movement suggestion path so automation no longer emits post-exploitation actions from validated credential rows. |
| B17 | 2026-07-23 | Automation suggestions still offered `osint:validate` credential-use actions against discovered services by default, without an explicit scoped live credential-validation model. | Suppressed the default credential-validation suggestion path so unvalidated credential rows no longer emit live credential-use actions from the review surface. |
| B18 | 2026-07-23 | Automation suggestions still offered `exploit:correlate` from passive service version strings, and the web automation executor had no scoped passive correlation task handler for that action. | Suppressed the legacy exploit-correlation suggestion path until a scoped passive vulnerability/exposure correlation task model exists. |
| B19 | 2026-07-23 | `/api/automation/execute` accepted arbitrary action strings and scheduled the suffix as a task type, allowing unsupported or sensitive names such as `exploit:safe_check`, `post:lateral`, `auth:spray`, and unknown tasks to enter the queue. | Added route-level action admission so only currently supported passive/recon automation actions are scheduled, and unsupported or sensitive actions are rejected before queue writes. |
| B20 | 2026-07-23 | Automation suggestions still emitted `osint:dehashed` and `report:generate`, but `/api/automation/execute` correctly rejected them because no supported scheduled action path existed for those suggestions. | Suppressed unsupported suggestion actions and shared the executable automation action allowlist between the route and suggestion parity tests. |
| B21 | 2026-07-23 | Scraped Terraform DNS resources were parsed for existing IaC cloud references, but record names and CNAME targets did not have a focused source-gated path into recursive host/subdomain seed promotion. | Added compact static Terraform DNS parsing for public record names and targets, wired it through the existing artifact host-seed path, and covered it with focused helper plus engagement-backed recursion tests. |
| B22 | 2026-07-23 | SAML federation metadata artifacts were treated as generic XML, so source-gated IdP/SP endpoint fields such as `Location`, `ResponseLocation`, `entityID`, and `OrganizationURL` lacked focused passive recursion coverage and remote `/saml/metadata` cache filenames lost analyst-visible format labels. | Added compact static SAML metadata parsing for source-gated endpoint/document URLs, wired it through the existing artifact URL-seed path and remote cache classification, stripped SAML protocol query secrets from generic URL extraction, and covered helper plus local/remote engagement-backed recursion tests. |
| B23 | 2026-07-24 | Kill-chain recursion/validation budgets existed as internal hardcodes, so run review could not tell which synthesis depth or pending validation batch limit governed an engagement. | Added bounded env controls `FORGE_KILL_CHAIN_SYNTHESIS_DEPTH` and `FORGE_KILL_CHAIN_VALIDATION_BATCH_LIMIT`, failed closed on invalid values, and wrote the effective budgets into engagement run metadata. |
| B24 | 2026-07-24 | React engagement detail counted key-scanner rows and all cloud-validation inventory rows under the "Validated findings" panel, so unverified/dead/suspect inventory could look reportable in dashboard review. | Split reportable validated findings from validation inventory in the React detail page; key/cloud validation rows now render under a separate inventory panel with their status columns preserved. |
| B25 | 2026-07-24 | `conflicts_with` was allowed by schema and consumed by confidence synthesis, but no deterministic producer created conflict relations for obvious identity/entity collisions. | Added conservative same-anchor identity collision detection in synthesis: when the same email/phone anchor has incompatible `same_entity` name/company targets, FORGE writes `conflicts_with` relations with evidence and refreshes conflict counts in seed confidence metadata. |
| B26 | 2026-07-24 | Default kill-chain prerequisite detection surfaced evasion, brute-force, auth-bypass, IDOR, and post-exploitation manual hints inside normal ASM completion metadata/review output. | Added explicit `--include-offensive-prereqs` opt-in; default ASM runs suppress those hints while safe runnable enrichment prereqs still work, and run metadata/audit rows record whether offensive prerequisite hints were included. |
| B27 | 2026-07-24 | Kill-chain prerequisite detection remained embedded inside the large CLI finalization path, making safe/offensive hint policy harder to review and test independently. | Extracted prerequisite detection into `forge.kill_chain_prereqs.detect_kill_chain_prerequisites()` with helper-level tests while preserving CLI audit, display, auto-run, prompt, non-TTY, and completion metadata behavior. |
| B28 | 2026-07-24 | The remaining prerequisite display/execution/completion branch still lived inline in `forge kill-chain`, keeping prompt, non-TTY, auto-run, and completion-mode behavior hard to test independently. | Extracted the branch into `handle_kill_chain_prerequisite_flow()` behind CLI-provided callbacks, leaving run finalization and dashboard refresh in `cli.py` while adding helper tests for none/manual-only/non-TTY/prompt/auto-run modes. |
| B29 | 2026-07-24 | Dashboard/API review gates still failed open for malformed deterministic cloud exposure rows and graph VULN nodes when a validation asset or identifier was missing, while Phase 6 already failed closed. | Required deterministic cloud findings and graph VULN nodes to have a reportable validation-index proof before appearing as reportable findings, severity counts, API summaries, or graph vulnerability nodes; malformed rows remain excluded from reportable surfaces. |
| B30 | 2026-07-24 | Linked key/cloud reportability indexes kept older reportable validation rows when a newer validation row for the same asset became `UNVERIFIED`, `DEAD`, honeypot-suspected, or otherwise non-reportable. | Added a shared latest-row cloud validation reportability helper and reused it across deterministic finding synthesis, Phase 6 linked-key gates, dashboard/API reportability indexes, and cloud-leak playbook admission; linked cloud proof now authorizes reportability only when the latest matching row is reportable. |
| B31 | 2026-07-24 | `forge kill-chain --dry-run` still scheduled prereport `vuln passive` and `exploit correlate` finalizers, relying on the outer dry-run dispatcher to prevent execution and overstating the dry-run finalization queue. | Global kill-chain dry-run now skips those network-capable finalizers before scheduling, records an audit row naming the skipped labels, and passes `--dry-run` to the HIBP finalizer so the preview contract is explicit even if dispatch guards change later. |
| B32 | 2026-07-24 | Direct deterministic cloud finding synthesis and some review surfaces still trusted `VALIDATED` status plus an allowlisted method even when the row proof was generic, scaffold-only, or otherwise rejected by the stable proof parser. | Centralized stable-proof-aware cloud validation reportability for proof-bound data/listing methods, reused it in deterministic findings, dashboard/API gates, and attack graph VULN gating, while preserving LOW storage reachability findings for metadata-only probe methods. |
| B33 | 2026-07-24 | Public runtime frontend config artifacts such as `runtime-env.js` and `env-config.js` were queued as text/config artifacts but lacked a source-gated JS runtime label and env-assignment parser, so host-only API endpoints and Firebase/Supabase project refs could fail to become same-run recursive URL/cloud candidates. | Added a narrow `runtime-js-config` label for explicit runtime/env config files and public/static/build-path `config.js`, parsed uppercase env-style endpoint/cloud keys through the bounded ordered candidate path, and proved local public runtime config artifacts persist recursive URL seeds plus Firebase/Supabase cloud assets while arbitrary `notes.js` and root generic `config.js` stay outside JS runtime structured parsing. |
| B34 | 2026-07-24 | Public service-worker and precache artifacts were likely crawler targets, but lacked a source-gated JS label and parser for `importScripts()` URLs, API endpoint config, and Firebase messaging `projectId` refs, so those artifacts could be inventoried without feeding recursive discovery. | Added a narrow `service-worker-js` label for service-worker, Workbox, precache-manifest, Firebase messaging SW, and OneSignal worker filenames; parsed `importScripts()` URLs and Firebase project refs through the existing bounded candidate path; and proved local service-worker artifacts persist recursive URL seeds plus Firebase cloud assets while arbitrary `app.js` stays outside structured JS parsing. |
| B35 | 2026-07-24 | Service-worker/precache recursion had focused parser proof but lacked an end-to-end kill-chain contract proving remote page/manifest -> service-worker -> precache/chunk recursion reaches validation, deterministic findings, fallback report lineage, graph/dashboard/raw exports, and unsupported storage inventory without findings. | Added a mocked local-safe service-worker/precache E2E fixture that forces LLM provider failure, validates Firebase/Supabase proof-bound assets, inventories unsupported storage without findings, and asserts recursive seeds, report fallback lineage, graph export, dashboard summary, validation inventory, raw export, and cleanup isolation. |
| B36 | 2026-07-24 | The service-worker parser accepted absolute `importScripts()` URLs only, missing common root-relative and path-relative precache/Workbox imports discovered from remote service-worker artifacts. | Resolved relative `importScripts()` values against the remote service-worker source URL for `http(s)` bases, kept local artifacts from inventing URLs without a base, and switched the E2E fixture to prove root-relative precache recursion. |
| B37 | 2026-07-24 | The service-worker/precache E2E proved recursive `seed_relations` in SQLite, but did not prove those relations were visible in the exported attack graph that the dashboard and Maltego workflow consume. | Extended the mocked E2E to assert exported attack-graph `derived_from` edges for `manifest -> service-worker -> precache -> chunk`, including service-worker/precache provenance metadata. |
| B38 | 2026-07-24 | Stable kill-chain exits could include parsed and failed artifact queue rows from terminal K2 processing, but future reviewers lacked a hard contract that those rows were visible in run metadata, audit logs, and dashboard artifact inventory. | Added terminal artifact queue audit logging and E2E assertions that final run metadata exposes artifact status counts, cumulative processed/failed counts, zero pending work, dashboard artifact queue rows, and the `artifact_queue_terminal_metrics` audit summary. |
| B39 | 2026-07-24 | Phase 6 report/raw-export filtering reused the cloud validation method/status gate but did not require stable proof for pre-existing deterministic cloud finding rows, so weak `VALIDATED`-looking evidence could enter report context even after deterministic finding, graph, dashboard, and API gates rejected it. Raw CSV finding rows also lacked target identity fields, making asset-level reportability audits weaker. | Required stable proof in Phase 6 deterministic-cloud filtering, added non-sensitive finding identity fields to raw CSV exports, and added a focused local integration fixture proving weak `VALIDATED` Firebase/S3 rows and honeypot Supabase rows remain validation inventory only across deterministic findings, Phase 6 template/JSON/CSV output, attack graph, dashboard, and web API summaries. |
| B40 | 2026-07-24 | Deterministic key-exposure rows could still use stale embedded `VALIDATED` proof text when a newer linked cloud validation row for the same asset/identifier was weak, placeholder, unverified, or honeypot-suspected. | Added latest-linked cloud validation precedence for key exposures across deterministic synthesis, Phase 6 counts/report context, attack graph VULN nodes, dashboard/API finding tables, key-scanner counts, and imported graph payload filtering; expanded the stable-proof surface fixture with stale Firebase/Supabase key exposures and corrected an older S3 fixture to use concrete listing proof. |
| B41 | 2026-07-24 | Scheduled URL tasks without an explicit scope manifest could preflight against DB `scope_json` URL entries through the generic host gate, allowing same-host path drift before the downstream crawler/auth module enforced URL prefixes. | Added prefix-aware scheduled target preflight for URL-shaped targets when DB engagement scope contains URL entries, preserving generic host-gate behavior for non-URL targets and manifest-backed prefix checks. |
| B42 | 2026-07-24 | Scheduled crawl validated the initial target against a scope manifest URL prefix but then invoked the crawler without manifest-derived `url_prefixes`, allowing recursive same-host links to rely on broader DB host scope. | Propagated manifest domains/IP ranges and URL prefixes into scheduled `crawl_target_sync(..., require_scope=True)` and added a regression where an allowed `/app/` seed cannot fetch discovered `/admin`. |
| B43 | 2026-07-24 | Scheduled `crawl_stealth` validated the initial target against scope but launched Playwright without manifest/DB URL-prefix scope, allowing same-host out-of-prefix subresources or final redirects during browser navigation. | Passed scheduled manifest/DB scope into `run_crawl_stealth`, added a Playwright route abort guard plus final URL scope check, converted runtime browser scope denials into scheduled task audit denials, and added focused scheduler/stealth regressions. |
| B44 | 2026-07-24 | Crawler HTTP requests used automatic redirects, so an in-prefix URL could fetch an out-of-prefix redirect before the final URL scope check ran. | Disabled automatic redirects in `_crawl_http`, checked redirect `Location` against same-host URL-prefix scope before enqueueing, and added a regression proving `/app/` cannot auto-fetch redirected `/admin`. |
| B45 | 2026-07-24 | Scheduled `passive`/`auth-bypass` could authorize a manifest target but leave downstream modules to reload stale DB scope, and scheduled `searxng_passive` accepted caller-controlled provider base URLs. | Passed manifest/DB scope options into scheduled passive/auth dispatch, added explicit passive scope inputs, restricted scheduled SearXNG provider URLs to default-local or environment allowlisted bases, and covered the dispatcher paths with focused tests. |
| B46 | 2026-07-24 | Scheduled URL-bound safeguards were proven by separate focused tests but lacked one combined workflow fixture tying crawl redirect denial, stealth/passive/auth scope propagation, and provider URL denial together. | Added a mocked distributed scheduler fixture that exercises crawl, crawl_stealth, passive, auth-bypass, and searxng_passive under one narrow URL manifest and verifies no out-of-prefix redirect/provider dispatch occurs. |
| B47 | 2026-07-25 | Legacy or loosely typed `vulnerability_findings` rows could embed `validation=UNVERIFIED:*` key/cloud evidence and still appear as reportable findings in Phase 6 reports, raw CSV/JSON exports, dashboard detail sections, severity summaries, and `/vuln-summary`. | Added a shared validation-inventory finding gate that excludes inventory-only vulnerability types and non-reportable embedded validation details from report/dashboard/API finding surfaces while leaving stable validated findings intact. |
| B48 | 2026-07-25 | Recovered `fanout_d_host_surface` work could be starved after resume because retryable hosts were ordered after all never-attempted known hosts, and the D-stage scheduler only takes the first bounded batch. | Prioritized retryable pending host surfaces before never-attempted hosts so abandoned/failed host-surface work is retried in the next resumed batch while the remaining new-host backlog stays visible in pending-work metadata. |
| B49 | 2026-07-25 | Concurrent cloud-asset validation sweeps could double-claim alias-equivalent assets such as `s3/shared-assets` and `aws_s3/shared-assets` because active validation-claim joins compared raw asset types while validation-result joins used normalized aliases. | Normalized asset-type and identifier comparisons when checking active asset claims, so a live alias claim blocks canonical duplicates until released without changing raw claim storage or release semantics. |
| B50 | 2026-07-25 | Failed root-domain `fanout_f_keyscan` work was retryable, but absent from pending-work/stability accounting, so a stable snapshot could terminate with root keyscan work still eligible for the next iteration. | Added `root_keyscan_domains` pending-work accounting and a regression proving repeated failed root keyscan attempts keep `last_iteration_stable` false and visible in run metadata until success or max-iteration exhaustion. |
| B51 | 2026-07-25 | Failed GitHub-org keyscan targets could be lost across a fresh resume because discovered orgs lived only in memory, while completed host-surface parsing could be skipped and never rediscover the failed org. | Reloaded failed `fanout_f_keyscan` composite org seed-run targets, constrained them to current scoped root domains, counted them in `github_orgs` pending work, and scheduled them through the existing scoped keyscan path without rediscovery. |
| B52 | 2026-07-25 | Cloud key validation worker exceptions were counted as failed but did not persist validation receipts or mark key rows attempted, so full exception batches could be reclaimed repeatedly and partial batches could hide pending key work. | Converted provider exceptions into non-reportable `UNVERIFIED / provider_exception` key validation receipts, updated key validation state/`validated_at`, released claims normally, and proved a second `only_unattempted` sweep does not reclaim the same rows. |
| B53 | 2026-07-25 | Attack-graph snapshot cloud nodes omitted scrubbed `cloud_assets.metadata_json`, so dashboard graph review could lose artifact-derived provenance once saved snapshots took precedence over the richer fallback graph. | Loaded allowlisted cloud-asset provenance into Phase 4 CLOUD nodes, preserved artifact provenance fields in saved snapshots, stripped variant sensitive metadata keys, dropped raw config blobs, sanitized URL provenance, and added dashboard fallback plus snapshot regressions. |
| B54 | 2026-07-25 | Phase 6 deterministic cloud finding gates re-parsed sanitized/truncated validation summaries even when the latest raw validation row was already proven reportable, so redaction could make a genuinely validated exposure disappear from reports and raw finding exports. | Propagated and trusted the raw-derived `validation_reportable` flag for structured cloud/key validation matches, leaving summary re-parsing as the legacy fallback, and added a regression where sanitized S3 listing evidence loses the `<Key>` proof but the raw validation row remains reportable. |
| B55 | 2026-07-25 | Kill-chain finalization marked runs `completed` whenever a report artifact existed, even if max iterations ended with retryable recursive work still pending in run metadata. | Finalization now fails the run with a pending-work error when `pending_work_total > 0`, keeps report-failure behavior unchanged, updates console wording for pending-work exits, and extends the root-keyscan retry regression to assert latest run status/error. |
| B56 | 2026-07-25 | Dashboard/API fallback graph cloud nodes still loaded arbitrary `cloud_assets.metadata_json` through a weaker key denylist than saved attack-graph snapshots, so variant secret keys and unsanitized URL provenance could leak when no snapshot/graph artifact existed. | Extracted a shared allowlisted cloud-asset graph metadata sanitizer, reused it in Phase 4 snapshots and dashboard/API fallback graphs, and added static/live fallback regressions for variant secret keys, raw config blobs, URL userinfo, and sensitive URL query parameters. |
| B57 | 2026-07-25 | Non-graph cloud asset inventory surfaces still parsed `cloud_assets.metadata_json` with local broad scrubbing, so live assets API, Phase 6 report JSON/raw CSV, and static dashboard cloud tables could expose arbitrary safe-looking nested metadata or unsanitized URL provenance even after graph surfaces were hardened. | Reused the shared allowlisted cloud-asset metadata sanitizer across live assets API rows, Phase 6 cloud inventory/raw exports, and static dashboard cloud asset provenance, with regressions for nested/raw metadata removal plus URL userinfo and sensitive query stripping. |
| B58 | 2026-07-25 | The mocked service-worker/precache kill-chain E2E proved recursive seed persistence and graph/report/dashboard output, but not that derived seeds were dispatched through their own recursive fan-outs with audit-visible `seed_runs`; the fixture also lacked explicit ROE/scope inputs and had stale assumptions about artifact retry metrics and inventory-only report text. | Added explicit ROE/scope manifest to the E2E, mocked RDAP/archive/API URL paths for deterministic convergence, asserted completed derived email/username/URL seed runs, asserted over-depth skip receipts, asserted dashboard seed-run visibility, and updated inventory-only report/metric assertions to match current deterministic review surfaces. |
| B59 | 2026-07-25 | The stable-proof surface fixture covered weak `VALIDATED` rows and honeypot-suspected rows, but did not include explicit `DEAD` and `ACCESSIBLE_BUT_NO_DATA` validation statuses in the same cross-surface matrix or prove live cloud-asset inventory parity. | Extended the integration matrix so `DEAD` Firebase and `ACCESSIBLE_BUT_NO_DATA` S3 rows with stale deterministic findings remain validation inventory only across Phase 6 report/JSON/CSV, cloud asset inventory/raw asset rows, attack graph, dashboard/API detail, live `/assets`, vuln summary, helper gates, and deterministic finding cleanup. |
| B60 | 2026-07-25 | Reportable findings were provable through DB/report/graph/dashboard surfaces, but the audit log did not explicitly tie deterministic rule decisions and final report inclusion to the same finding target/checksum. | Added deterministic finding rule applied/skipped audit receipts, kill-chain synthesis batch receipts, and Phase 6 `report_findings_included` receipts with provider lineage, finding count, checksum, and included targets; extended the service-worker/precache E2E to prove a Supabase finding from derived seed/artifact through validation, rule severity, report checksum, graph/dashboard review, and audit traceability. |
| B61 | 2026-07-25 | PDFs inside archives took the byte extraction path and returned early when Tesseract/OCR was unavailable, even though barcode extraction from rendered pages does not require OCR. | Removed the OCR-binary requirement from embedded PDF page extraction, preserving the rasterizer requirement and adding a ZIP-contained scanned-PDF regression proving barcode URLs become recursive seeds without OCR. |
| B62 | 2026-07-25 | Legacy/imported Discord and Telegram key rows could still become report/dashboard/graph/deterministic findings from standalone stable-looking `VALIDATED:*` strings without a linked provider validation result bound to the discovered bot identifier. | Added a shared key validation gate requiring Discord/Telegram reportability to come from the latest linked `cloud_validation_results` row, made provider-key validation methods reportable only with stable proof, and reused the gate across Phase 6, dashboard/graph filtering, deterministic findings, and attack graph API-key loading. |
| B63 | 2026-07-25 | Static artifact recursion handled quoted JS/manifest routes and live crawler CSS links, but artifact-queued remote CSS/HTML payloads missed common unquoted CSS `url(...)`, `srcset`, unquoted route attributes, and meta-refresh pivots. | Added source-gated CSS/HTML route families to the artifact relative-route extractor, resolving through the existing safe URL resolver so remote artifact payloads promote stylesheet, bundle, manifest, image/font, and route pivots into recursive URL seeds and queued artifacts. |
| B64 | 2026-07-25 | Slack bot-token rows still trusted standalone stable-looking `VALIDATED:slack_auth_test` evidence across report, dashboard, graph, and deterministic finding surfaces, even though fresh validation persists a provider-bound `team_id/actor_id` validation row. | Extended the shared linked bot-token proof gate to Slack, made `slack_auth_test` reportable only through stable latest cloud validation rows, and covered unlinked/linked Slack behavior across Phase 6, dashboard, deterministic findings, and attack graph loading. |
| B65 | 2026-07-25 | After adding artifact-side CSS/HTML route parsing, bare same-directory CSS dependencies such as `@import "theme.css"`, `@import url(print.css)`, and `url(hero.png)` still did not resolve from remote stylesheet artifacts. | Expanded source-gated artifact CSS route extraction to accept bare file dependencies through the existing safe route resolver, preserving unsafe-scheme rejection while promoting same-directory CSS/image/font pivots into recursive URL seeds and queued artifacts. |
| B66 | 2026-07-25 | Artifact-queued HTML payloads still missed bare same-directory asset references such as `src=app.js`, `href=style.css`, manifest links, image sources, and meta-refresh URLs, so static page bundles could fail to recurse without the live crawler path. | Expanded source-gated HTML route extraction to accept bare file dependencies through the existing safe route resolver, preserving unsafe-scheme rejection while promoting same-directory bundle, stylesheet, manifest, image, and route pivots into recursive URL seeds and queued artifacts. |
| B67 | 2026-07-25 | QR/barcode extraction stored `barcode_payload_count` on parsed artifact summaries, but artifact-derived recursive seeds and relation context did not preserve that count, making barcode pivots harder to distinguish from generic secondary seeds during review. | Added `barcode_payload_count` to the safe artifact relation and seed metadata allowlists, and added a seed-backed downloaded-image regression proving barcode-derived email/URL seeds retain passive artifact provenance. |
| B68 | 2026-07-25 | Artifact-derived cloud assets used a separate metadata allowlist that dropped passive payload proof counts such as `barcode_payload_count`, so QR/barcode-discovered cloud references lost reviewable extraction provenance before validation and graph review. | Added non-zero passive payload proof counts to artifact cloud-asset metadata, avoiding zero-count preview noise, and added a seed-backed QR-to-Firebase regression proving cloud assets and attack-graph nodes retain barcode provenance. |
| B69 | 2026-07-25 | Hugging Face `whoami` validation treated `user_profile_present=true` as stable proof without a non-sensitive profile hash, unlike similar profile-provider gates, allowing low-detail account proofs to become reportable. | Hugging Face validation now emits `profile_hash=<16hex>` from stable profile-presence fields, and core/Phase 4 proof gates require that hash before deriving reportable Hugging Face identifiers. |
| B70 | 2026-07-25 | Artifact-side HTML/CSS route recursion still ignored common static image/favicon suffixes such as `.avif`, `.heic`, `.tiff`, `.bmp`, and `.ico`, so scraped static pages could miss passive image pivots unless another crawler path found them. | Added modern passive image/favicon suffixes to the safe artifact route allowlist, keeping unsafe-scheme filtering intact and proving modern image assets recurse into URL seeds plus queued document artifacts where classification supports them. |
| B71 | 2026-07-25 | Phase 4 provider identifier extraction used a duplicated provider-proof parser while report, dashboard, graph, and helper gates used the shared validation proof parser, creating drift risk for reportability and stable identifiers. | Added `parse_provider_validation_identity()` on top of `parse_validated_detail()`, delegated Phase 4 provider identifiers to it, and added a cross-provider parity contract covering reportable and inventory-only provider proofs. |
| B72 | 2026-07-25 | Dashboard/API scope-denial review only surfaced `scheduled_task_scope_denied`, so recursive seed, remote artifact, cloud validation, key validation, and automation scope-boundary denials could disappear once they fell out of the recent audit slice. | Added one dedicated scope-boundary denial section for all current denial actions, reused it in static dashboard and live API detail payloads, and redacted scope-manifest payload assignments before rendering denial results. |
| B73 | 2026-07-25 | Run audit manifests hashed report `.md`, `.json`, and `.pdf` companions but omitted deterministic `.html` and report `.csv` exports, weakening tamper coverage for the current report family. | Added `.html` and report `.csv` to the report-artifact suffix whitelist, preserved fixed-family hashing instead of broad globbing, and added manifest/bundle tests proving HTML/CSV inclusion and tamper detection. |
| B74 | 2026-07-25 | Static artifact queue processing inherited full global `--parallel-fanout`, unlike direct identity and validation lanes that already had separate slow-and-steady worker caps. | Added `FORGE_ARTIFACT_PROCESSOR_MAX_WORKERS`, used `min(parallel_fanout, cap)` for `ArtifactQueueProcessor`, recorded the effective/cap values in run metadata, and documented the operator knob. |
| B75 | 2026-07-25 | Legacy Firebase/AWS/Azure audit rows and Firebase credential-status inventory could still enter report/dashboard/graph finding surfaces without explicit stable validation proof, while new authenticated audit writers persisted unlabelled provider evidence. | Added a shared legacy cloud-audit reportability gate requiring latest linked reportable validation or stable authenticated-audit receipts, made credential-status rows inventory-only, reused the gate in Phase 6, dashboard, and attack graph, and taught Firebase/AWS/Azure audit writers to persist compact non-secret validation receipts. |
| B76 | 2026-07-25 | Helm `index.yaml` chart archive safety was enforced in the Helm helper, but generic direct URL extraction could still queue unsafe archive URLs from the same index and chart-derived outputs did not reliably preserve index-to-chart provenance. | Suppressed direct chart-archive URL extraction for Helm indexes so chart archives come through the Helm parser, added queue-time scope rejection for recursively discovered artifact URLs when a scope checker is configured, and propagated `helm_index_url`/`source_rule` through artifact queue metadata, seed relations, recursive seed metadata, and cloud-asset provenance. |
| B77 | 2026-07-25 | Local static OCI/Docker-save artifacts could produce recursive seeds and cloud assets, but non-HTTP artifact sources had no seed anchor, so review graph relation lineage was weaker than remote artifact lineage. | Added synthetic completed `artifact://queue/{id}` source seeds for non-HTTP parsed artifacts, preserving passive parser metadata without creating live recursive targets, and added an OCI/Docker-save parity regression covering referenced-layer-only parsing, decoy/path-traversal suppression, validation/finding gates, graph/dashboard/report/raw export provenance, and secret scrubbing. |
| B78 | 2026-07-25 | The release audit found strong slice coverage but no closure proof, a representative dashboard smoke that did not yet carry explicit ROE/scope manifest, a stale multi-seed export expectation missing HTML, and raw-export fallback lacking a `report_findings_included` receipt. | Added `docs/end_goal_completion_audit.md`, upgraded the dashboard smoke to launch non-dry-run with ROE/scope manifest and scoped cloud-resource validation, updated the multi-seed fixture for HTML report-family exports, and made raw-export fallback emit report inclusion audit receipts with raw-export lineage, render path, format, checksum, and targets. |
| B79 | 2026-07-25 | The end-goal audit still lacked one compact representative proof tying API-created multi-seed intake, explicit ROE/scope launch, recursion, static artifact parsing, validation-before-reporting, deterministic scoring, graph/MTGX export, fallback reporting, dashboard/API/download parity, run audit manifests, and cleanup/no-ID-reuse together. | Added `tests/integration/test_canonical_release_e2e.py`, which uses mocked external execution but real FORGE persistence, artifact parsing, synthesis, deterministic findings, graph export, template-to-raw fallback, dashboard/live API artifact surfaces, report-inclusion audit receipts, manifest verification, cleanup helper scoping, and monotonic ID assertions. |
| B80 | 2026-08-14 | The canonical release E2E existed, but it still skipped on removed `python-jose` instead of the current PyJWT dependency and did not prove that the enterprise CTEM roadmap primitives were reviewable in the same representative engagement path. | Switched the canonical E2E guard to `jwt` and extended the same local mocked engagement to prove literal `cloud_ref` intake, connector catalog free-first/read-only-gated readiness without secret leakage, active-validation lab proof, monitoring snapshot/diff/alerting, secret lifecycle owner routing/remediation guidance, local standards/STIX enrichment, dashboard CTEM sections, raw fallback/API parity, audit manifest verification, and cleanup/no-ID-reuse. |
| B81 | 2026-08-14 | The asset graph could score critical assets, attack paths, choke points, and fix candidates, but operator-facing path explanations and workflow creation from graph candidates were still disconnected. | Added per-path exposure summaries/recommended actions, allowed `asset_graph` remediation sources in schema/migrations, and added an idempotent local remediation draft service that turns minimal-fix candidates into owner/SLA/review-queue items without ticket sync, active validation, network calls, or intrusive behavior. |
| B82 | 2026-08-14 | Graph-derived remediation drafts existed as a local workflow service, but operators could not trigger them through the web API, leaving the graph-to-remediation handoff disconnected from normal role-gated workflows. | Added `POST /api/engagements/{engagement_ref}/remediation/draft-from-asset-graph`, requiring `remediation:write` plus `assets:read`, returning refreshed remediation items/summary/review queue, validating draft limits, and covering success, idempotence, permission denial, invalid input, and secret-scrubbed graph metadata in route/helper tests. |
| B83 | 2026-08-14 | `forge doctor` could report remediation queue status, but it did not tell operators when asset-graph minimal-fix candidates were waiting to be drafted, so the new graph-to-remediation path was still easy to miss during setup/readiness review. | Made the Remediation Review Queue doctor row count undrafted graph fix candidates when graph tables are present, return a warning with CLI/API draft guidance, avoid live provider behavior, and added redaction-safe doctor regressions proving the guidance without leaking graph evidence. |
| B84 | 2026-08-14 | Doctor/readme guidance named `forge remediation draft-from-asset-graph`, but the remediation Typer app did not actually register that operator command, leaving the graph-to-remediation workflow reachable by service/API but not by the documented CLI setup path. | Added the `forge remediation draft-from-asset-graph` command with JSON and human summaries, wired it to the idempotent asset-graph draft service, documented it in README, and added CLI regressions proving JSON output, owner/status persistence, idempotent row count, registry coverage, and secret-scrubbed graph evidence. |
| B85 | 2026-08-14 | The graph-derived remediation workflow was reachable through service/API/CLI/doctor, but the live dashboard Remediation panel still only exposed owner propagation, leaving graph fix drafting outside the primary operator review workflow. | Added a Remediation panel action that calls `/remediation/draft-from-asset-graph`, refreshes live remediation state, reports drafted/candidate counts, preserves the live-token/busy/error pattern, and extended the React contract/build verification for the new type, endpoint, and button. |
| B86 | 2026-08-14 | Graph-derived remediation drafts created workflow rows, but the graph was not immediately resynced and `asset_graph` remediation references could point at synthetic `finding:asset_graph:*` nodes instead of the original graph entity, weakening remediation-node/path review. | Resynced the asset graph after successful graph draft upserts, resolved `asset_graph` remediation `finding_ref` values back to existing graph entity keys before falling back to synthetic findings, and added regressions proving drafted items appear as remediation nodes with `remediates` edges to the original candidate without duplicate rows or leaked evidence. |
| B87 | 2026-08-14 | Asset ownership conflicts were available in graph APIs and static reporting, but the live React dashboard did not let operators resolve competing owner claims from the graph review surface. | Added typed `ownership_conflicts` payload support, a live `/asset-graph/ownership-conflicts/resolve` client action, a graph-panel conflict queue with per-owner `Resolve to ...` controls, live snapshot refresh, success/error state, and a React contract regression covering the type, endpoint, and visible action. |
| B88 | 2026-08-14 | Remediation owner propagation applied the highest-confidence graph owner by default and only exposed an overwrite toggle, leaving teams without a policy control for unresolved ownership conflicts or low-confidence owner claims. | Added shared propagation policy controls for `highest_confidence` versus `skip_conflicts`, a bounded minimum-confidence floor, skipped-conflict/low-confidence counters, policy metadata/audit receipts, CLI/API arguments, React controls, README guidance, and focused service/route/dashboard regressions while preserving the existing default behavior. |
| B89 | 2026-08-14 | Scheduled passive provider-report refresh accepted Shodan/Censys/urlscan imports, but one shared `report_file` made real multi-provider monitoring policies awkward and easy to misconfigure. | Added per-connector `report_files`/`provider_reports` mapping support for monitoring connector refresh, preserved the legacy single-file fallback, returned sanitized report-file labels and URL/crawl counters in refresh summaries, documented the multi-provider policy shape, and added a regression proving separate Shodan and urlscan files produce scoped scheduled diffs without leaking report bodies. |
| B90 | 2026-08-14 | Production hardening had doctor and Compose coverage, but operators still lacked checked reverse-proxy examples and a systemd artifact for the self-host baseline. | Added Caddy and nginx reverse-proxy examples aligned to loopback-bound API/web ports, added a systemd unit that preflights and manages the hardened Compose stack with `/etc/forge/forge.env`, documented the operator path, and extended packaging tests to keep those artifacts aligned with the production hardening contract. |
| B91 | 2026-08-14 | Production packaging still named Helm/Kubernetes as a future artifact family after Docker/systemd, leaving cluster operators without a checked baseline manifest set. | Added a first Helm chart under `docker/helm/forge` for API, web UI, and worker deployments with required production URL/secrets, non-root/read-only pod contexts, ClusterIP services, persistent data and remote-audit claims, JWT/scope-manifest/security-header env, Postgres/Redis URL wiring, append-only audit storage, README/handoff guidance, and packaging tests that pin the chart's hardening contract. |
| B92 | 2026-08-14 | The first Helm chart had internal services and hardening env, but no checked ingress or managed-service values example for cluster operators. | Added an optional Helm Ingress template that splits `/api` traffic to the API service and web traffic to the web UI service, added `values.production-example.yaml` for managed Postgres, managed Redis, TLS secret, encrypted storage class, and required secret values, updated operator docs, and extended packaging tests to pin the ingress and managed-service contract. |
| B93 | 2026-08-14 | The Helm baseline still required secret values directly in chart values, which was weaker than the requested encrypted/operator-managed secrets posture for self-hosted deployments. | Added `secrets.existingSecretName` support, gated the chart-managed Secret so operators can provide their own Secret, added an optional External Secrets Operator template for web secret, bootstrap token, Postgres password, and audit remote scope values, documented the supported secret modes, and extended packaging tests to pin all three paths. |
| B94 | 2026-08-14 | Docker/systemd/Helm artifacts existed, but operators still had to hand-compose install, upgrade, render, and status commands. | Added `scripts/self_host_operator.sh` with preflight, install-systemd, upgrade-compose, helm-template, and status commands, dry-run support for mutating actions, compose config/build/up sequencing with `/etc/forge/forge.env`, systemd unit install/enable, Helm render entry point, README/handoff guidance, and packaging tests for command coverage and safe shell structure. |
| B95 | 2026-08-14 | The live remediation panel had the necessary controls, but selected context, queue health, and live-action readiness were buried in a long flat workflow surface. | Added a compact remediation command strip ahead of the detailed forms, showing selected item status/owner/ticket, review-queue blocker counts, live API readiness, graph-owner coverage, retest count, and ticket-sync failures; added responsive CSS and web UI contract assertions while preserving existing remediation API behavior. |
| B96 | 2026-08-14 | Remediation owner approval was still implicit in queue state and owner propagation metadata, so operators could not explicitly approve, reject, or re-review an assigned owner with audit lineage from the live workflow. | Added metadata-backed owner review state/history, the `review_remediation_owner_assignment` workflow helper, a live `/remediation/{item}/review-owner` route, React selected-item approval controls, refreshed summaries/review queues, audit receipts, and focused route/dashboard/build verification. |
| B97 | 2026-08-14 | The self-host Helm chart had static contract tests, but no repeatable real-Helm lint/render gate, so operator packaging could still hide template regressions until a cluster install attempt. | Added a `helm-lint` operator helper command, optional real-Helm packaging test coverage keyed by `HELM_BIN`/`helm`, and verified Helm v3.15.4 lint/template behavior for production example values, chart-managed Secrets, existing Secrets, ExternalSecret mode, and fail-closed missing secret values. |
| B98 | 2026-08-14 | Cloud graph identity context handled explicit IAM metadata, but common GCP IAM binding and Azure role-assignment export shapes did not become identity-to-cloud/account paths or least-privilege remediation candidates. | Normalized GCP `bindings[].members[]` and Azure `role_assignments[]` into scrubbed service-account/managed-identity graph nodes, inferred privilege from role names, linked identities to cloud resources/accounts, and added graph regressions for permission summaries, critical identity tags, minimal-fix candidates, and secret scrubbing. |
| B99 | 2026-08-14 | `forge.webui.app.create_app()` still owned workspace route request shaping, validation, membership mutation, and control-audit writes inline, keeping enterprise tenancy code harder to review. | Extracted workspace route payload logic into `forge.webui.workspace_routes`, leaving `create_app()` as a thin auth/permission/DB/HTTP wrapper, and verified workspace/RBAC integration behavior plus auth/route helper tests. |
| B100 | 2026-08-14 | Active-validation proof could flow into the asset graph, but graph-ranked paths, critical assets, and minimal-fix candidates did not flow back into active-validation recommendations, leaving the Wiz/Orca-style graph prioritization and BAS-style validation lane disconnected. | Added `forge.active_validation.graph_scenarios` to draft safe, non-destructive graph-recommended validation jobs from attack paths and fix candidates, exposed bounded `graph_scenarios` in the active-validation list payload without creating jobs or live network execution, and added route/service regressions for dry-run/lab-only behavior and secret-scrubbed graph context. |
| B101 | 2026-08-14 | The active-validation API could return graph-recommended scenario drafts, but the React operator workflow still showed only manual job creation, leaving the graph-prioritized validation path invisible in the primary dashboard. | Added a typed Active Validation “Graph recommendations” panel with graph draft counts, method/mode/target/risk context, and a `Use draft` action that pre-fills the existing create-job form without creating jobs, approving runs, or arming live execution; verified the React contract and production build. |
| B102 | 2026-08-14 | `Use draft` could prefill the active-validation create form, but creating the job dropped the graph scenario metadata, so queued jobs lost the asset-graph reason, expected result, and path context that made the recommendation reviewable. | Extended the React create payload to carry selected graph-scenario metadata into the existing active-validation job create API, shows a graph-lineage token while the form is bound to a draft, clears stale lineage on manual target/kind/method/mode edits, and verified the React contract plus production build. |
| B103 | 2026-08-14 | The graph-draft lineage path had UI coverage, but backend route/storage tests did not explicitly prove graph scenario metadata was forwarded and scrubbed when an operator created the active-validation job. | Added focused route and runner regressions proving `metadata.source=asset_graph`, reason, expected result, and graph path/entity context survive create-job forwarding/storage while token, secret, and sensitive URL query material is removed. |
| B104 | 2026-08-14 | The connector/plugin catalog advertised future active-validation plugins, but data-only plugin manifests rejected the `active_validation` domain entirely, blocking operators from inventorying approved validation adapters without custom code. | Allowed catalog-only `active_validation_gated` plugin manifests with required `approval`, `roe_id`, `scope_manifest`, and `live_gate` gates, kept Forge runner command claims forbidden, and added registry/CLI validation regressions proving valid manifests load while missing live gates fail closed. |
| B105 | 2026-08-14 | Active-validation plugin manifests could load after B104, but `forge doctor` still reported only a generic local plugin manifest count, so operator readiness reviews could miss whether active-validation adapters were present and properly gated. | Updated the Connector Catalog doctor row to count active-validation plugin manifests separately and include the required catalog-only approval/ROE/scope/live-gate guidance, with regressions using the default `FORGE_DATA_DIR/connector_plugins` discovery path. |
| B106 | 2026-08-14 | The live connector catalog API/dashboard still collapsed plugin manifests into generic connector rows, so operators could not quickly see local plugin-manifest count, active-validation plugin-manifest count, catalog-only execution state, or runner support from the primary UI. | Added `active_validation_plugin_manifest_count` to connector summaries, typed/rendered source, execution status, runner support, plugin manifest counts, active-validation plugin counts, plugin-catalog counts, and runner-path counts in the React Connector Catalog panel, with focused connector and web contract/build verification. |
| B107 | 2026-08-14 | Invalid connector plugin manifests could make `forge doctor` or the live connector catalog route raise during operator readiness review, instead of producing a controlled fail-closed signal with validation guidance. | Doctor now pre-validates connector plugin manifests and returns a Connector Catalog `WARN` with `forge connectors plugin-validate --json` remediation when any manifest is invalid; the live connector catalog API maps registry validation errors to HTTP 400 so the dashboard can show an actionable load error. |
| B108 | 2026-08-14 | Connector catalog and secret route payload construction still lived inline in `forge.webui.app.create_app()`, mixing plugin-manifest validation, secret-store shaping, and route HTTP mapping inside the app factory. | Extracted connector catalog and secret payload logic into `forge.webui.connector_routes`, leaving `create_app()` as the auth/permission/DB/HTTP wrapper while preserving secret redaction, RBAC denial, optional-paid filtering, and fail-closed invalid plugin-manifest behavior. |
| B109 | 2026-08-14 | Audit-review list/record payload construction still lived inline in `forge.webui.app.create_app()`, mixing audit review normalization and summary assembly into the web app factory. | Extracted audit-review route payload logic into `forge.webui.audit_review_routes`, leaving `create_app()` as the auth/permission/DB/HTTP wrapper while preserving role-scoped review reads/writes, attestation redaction, run annotation, and manifest review summaries. |
| B110 | 2026-08-14 | Retention route policy parsing, preview/apply request shaping, and retention-day validation still lived inline in `forge.webui.app.create_app()`, mixing enterprise retention workflow logic into the web app factory. | Extracted retention route payload logic into `forge.webui.retention_routes`, leaving `create_app()` as the auth/permission/DB/HTTP wrapper while preserving role-scoped retention reads/writes, confirm-gated apply, legal-hold behavior, and cross-workspace isolation. |
| B111 | 2026-08-14 | Asset-graph route payload construction still lived inline in `forge.webui.app.create_app()`, mixing graph rebuild audits, ownership claim validation, attribution import, and conflict resolution into the web app factory. | Extracted asset-graph route payload logic into `forge.webui.asset_graph_routes`, leaving `create_app()` as the auth/permission/DB/HTTP wrapper while preserving graph rebuild/read behavior, ownership claims, attribution import, conflict resolution, audit receipts, and secret-scrubbed graph responses. |
| B112 | 2026-08-14 | Engagement list/detail/tombstone response shaping still lived inline in `forge.webui.app.create_app()`, duplicating generated-at envelopes across API and dashboard data routes. | Extracted engagement index response helpers into `forge.webui.engagement_index_routes`, leaving `create_app()` as the auth/permission/discovery/HTTP wrapper while preserving list/detail JSON, workspace isolation, tombstone filtering/purging, and `/data/engagements*.json` parity. |
| B113 | 2026-08-14 | HTMX engagement tab rendering still lived inline in `forge.webui.app.create_app()`, mixing valid-tab state, template rendering, no-store headers, and 404 handling into the web app factory. | Extracted HTMX rendering helpers into `forge.webui.htmx_routes`, leaving `create_app()` as the auth/discovery/HTTP wrapper, and tightened legacy engagement access so same-workspace operators can read pre-membership DBs only when the workspace has no membership rows. |
| B114 | 2026-08-14 | Run listing, stop/pause request shaping, and log list/download/tail payload assembly still lived inline in `forge.webui.app.create_app()`, keeping operational run review and control routes tied to the app factory. | Extracted run/log route helpers into `forge.webui.run_log_routes`, leaving `create_app()` as the auth/permission/resolution/FileResponse wrapper while preserving run audit/review summaries, stop/pause markers and progress events, log listing, log downloads, and bounded tail responses. |
| B115 | 2026-08-14 | Engagement seed list/create/update/delete route payload dispatch still lived inline in `forge.webui.app.create_app()`, keeping seed CRUD response envelopes tied to the app factory even after seed mutation logic had been split out. | Extracted seed route helpers into `forge.webui.seed_routes`, leaving `create_app()` as the auth/permission/resolution/DB/HTTP wrapper while preserving seed list envelopes, canonical seed CRUD behavior, scope updates, relation cleanup, and conflict/error mapping. |
| B116 | 2026-08-14 | Kill-chain launch routes still kept launchable seed ordering, active-run detection, option/spec construction, process spawn, response payloads, and progress event publishing inline in `forge.webui.app.create_app()`. | Moved launchable seed ordering and launch-run payload orchestration into `forge.webui.kill_chain_launch`, leaving `create_app()` as the auth/permission/resolution/HTTP wrapper while preserving no-seed and active-run status mapping, launch option validation, process-spawn monkeypatch seams, log handling, response payloads, and progress events. |
| B117 | 2026-08-14 | Task/queue/scan routes still kept start-scan mutation/event construction and task-enqueue scheduling/event construction inline in `forge.webui.app.create_app()`, despite lower-level task payload helpers already existing. | Extended `forge.webui.task_routes` with route-level start-scan and task-enqueue helpers, leaving `create_app()` as the auth/permission/resolution/broker wrapper while preserving scan progress upserts, scoped task enqueue audits, scheduler dispatch, queue/worker/metrics/progress payloads, and progress events. |
| B118 | 2026-08-14 | Active-validation route permission gates and route dispatch were still encoded inline in `forge.webui.app.create_app()`, even though preview/create/approve/run payload construction already lived in `forge.webui.active_validation_routes`. | Added active-validation route-level permission and dispatch helpers, leaving `create_app()` as the auth/resolution/DB/HTTP wrapper while preserving approval-required permission escalation, live-run permission escalation, list summaries, preview/create/approve/run payloads, 400/404 mapping, and environment-live override blocking. |
| B119 | 2026-08-14 | Continuous-monitoring routes still repeated policy/route/suppression/snapshot/run-due/update/escalation dispatch inside `forge.webui.app.create_app()`, even though monitoring payload logic already lived in `forge.webui.monitoring_routes`. | Added monitoring route-level dispatch helpers, leaving `create_app()` as the auth/permission/resolution/DB/HTTP wrapper while preserving overview enrichment, policy upserts, alert routes, suppressions, snapshots, due-policy runs, alert updates, remediation escalation, audit writes, 400/404 mapping, and continuous-monitoring dashboard/report evidence. |
| B120 | 2026-08-14 | Remediation routes still repeated list/review/export/owner-propagation/graph-draft/create/update/review/retest/ticket-sync dispatch inside `forge.webui.app.create_app()`, keeping workflow orchestration mixed into the app factory. | Added remediation route-level dispatch and multi-permission helpers, leaving `create_app()` as the auth/permission/resolution/DB/HTTP-response wrapper while preserving remediation summaries, exports, owner propagation, graph drafting, risk acceptance/retest dynamic permission checks, owner review, ticket sync, audit writes, and 400/404 mapping. |
| B121 | 2026-08-14 | Automation suggestion/action/playbook routes still repeated payload and dispatch calls inside `forge.webui.app.create_app()`, keeping automation route orchestration mixed into the app factory after the underlying parser/queue/playbook helpers had been split out. | Added automation route-level dispatch helpers in `forge.webui.automation_routes`, leaving `create_app()` as the auth/permission/resolution/DB/HTTP wrapper while preserving suggestion payloads, action parsing, scope-gated queueing, playbook execution, progress events, audit denial writes, and 400 mapping. |
| B122 | 2026-08-14 | Web shell/static/generated-data routes still kept SPA entry fallback, frontend asset lookup, generated engagement JSON routing, and data-asset boundary checks inline in `forge.webui.app.create_app()`. | Added `forge.webui.shell_routes` for shell/static route dispatch, leaving `create_app()` as the auth/permission/response-class/HTTP wrapper while preserving React-vs-legacy entry fallback, command-center legacy response behavior, favicon/icons 404 mapping, generated engagement collection/detail JSON, workspace-scoped detail lookup, and generated-data traversal protection. |
| B123 | 2026-08-14 | Engagement index API routes still assembled list, tombstone, and detail payload dispatch inside `forge.webui.app.create_app()`, despite collection/detail/tombstone envelope helpers already living in `forge.webui.engagement_index_routes`. | Added engagement index route-level dispatch helpers, leaving `create_app()` as the auth/permission/timestamp/env/HTTP wrapper while preserving authorized engagement collection payloads, tombstone retention metadata, workspace-scoped missing-index rows, detail lookup by ref, and 404 mapping for missing details. |
| B124 | 2026-08-14 | Workspace admin routes still called workspace list/upsert/member/audit/delete payload helpers directly inside `forge.webui.app.create_app()`, leaving route dispatch mixed with control-DB lifecycle and HTTP error mapping. | Added workspace route-level dispatch helpers in `forge.webui.workspace_routes`, leaving `create_app()` as the auth/permission/control-DB/HTTP wrapper while preserving workspace list scoping, workspace upsert owner seeding, member role/permission normalization, append-only audit writes, self-delete rejection, cross-workspace access denial, 400/403 mapping, and sanitized metadata. |
| B125 | 2026-08-14 | Engagement create/update routes still kept record mutation plus control-index refresh dispatch inside `forge.webui.app.create_app()`, even though lifecycle normalization and mutation helpers already lived in `forge.webui.engagement_lifecycle`. | Added engagement lifecycle route-level dispatch helpers for create/update, leaving `create_app()` as the auth/permission/workspace-access/allocation/DB/HTTP wrapper while preserving seed-backed engagement creation, workspace-owner bootstrapping, metadata/tag normalization, detail payload construction, control index refresh, update metadata merging, 400/404/500 mapping, and workspace-boundary enforcement. |
| B126 | 2026-08-14 | Engagement listing skipped unindexed numeric DBs whenever the control-index path returned at least one item, so a surviving pre-indexed DB such as `1001.db` could disappear from `/api/engagements` after another indexed DB was deleted and a newer engagement was created. | Changed engagement discovery to scan numeric DB files that are not already represented in the control index, preserving the fresh-index fast path for indexed DBs while restoring mixed indexed/unindexed listings, monotonic sequence gap behavior, tombstone exclusion for deleted DBs, and control-index backfill for rediscovered DBs. |
| B127 | 2026-08-14 | Command-center host/action/sentry/timeline routes still called lower-level payload helpers directly inside `forge.webui.app.create_app()`, keeping operator workflow route dispatch mixed with auth and engagement authorization. | Added command-center route-level dispatch helpers, leaving `create_app()` as the auth/permission/engagement-authorization/service-factory/HTTP wrapper while preserving host context, host actions, execute/approve ROE scope inheritance, sentry toggle, emergency stop, timeline payloads, progress event publishing, and 400 mapping for command-center failures. |
| B128 | 2026-08-14 | Engagement data routes still called lower-level assets, vulnerability summary, and asset-tree payload helpers directly inside `forge.webui.app.create_app()`, keeping report-data route dispatch mixed with auth and DB lifecycle. | Added engagement data route-level dispatch helpers in `forge.webui.engagement_data`, leaving `create_app()` as the auth/permission/engagement-authorization/DB wrapper while preserving crawl/port/passive/auth/cloud asset payloads, reportability-gated vulnerability summaries, asset tree grouping, and route permission boundaries. |
| B129 | 2026-08-14 | Run and log routes still called lower-level run listing, run-control, log listing, log download, and log-tail helpers directly inside `forge.webui.app.create_app()`, keeping run/log dispatch mixed with FastAPI auth, permission checks, engagement resolution, and response mapping. | Added run/log route-level dispatch helpers in `forge.webui.run_log_routes`, leaving `create_app()` as the auth/permission/engagement-authorization/DB/FileResponse/HTTP wrapper while preserving audit manifest run summaries, stop/pause marker side effects, progress publishing, log listing/download/tail payloads, and 404/500 route error boundaries. |
| B130 | 2026-08-14 | Task, worker, queue, and scan routes still imported lower-level payload helpers with `build_*` aliases and called them directly inside `forge.webui.app.create_app()`, keeping task route dispatch mixed with auth, permission checks, engagement authorization, DB lifecycle, queue construction, and broker publishing. | Added task route-level dispatch wrappers in `forge.webui.task_routes`, leaving `create_app()` as the auth/permission/engagement-authorization/DB/queue/broker wrapper while preserving scan-start upserts/events, task enqueue scope enforcement and audit denial behavior, worker/task list payloads, queue metric limit clamping, scan progress payloads, and 400 route error mapping. |
| B131 | 2026-08-14 | Artifact download routes still resolved engagement artifacts directly inside `forge.webui.app.create_app()`, keeping artifact route dispatch and missing-artifact mapping mixed with auth, permission checks, `FileResponse`, and discovery context closure handling. | Added an artifact download route resolver in `forge.webui.artifacts`, leaving `create_app()` as the auth/permission/FileResponse/HTTP wrapper while preserving authorized engagement lookup, basename/path traversal gating through discovery, artifact filename responses, and 404 mapping for missing artifacts. |
| B132 | 2026-08-14 | Progress WebSocket routes still parsed subprotocol headers and serialized progress events inline inside `forge.webui.app.create_app()`, keeping socket payload shaping mixed with auth, engagement authorization, broker subscription, and close-code handling. | Added progress WebSocket state helpers in `forge.webui.state`, leaving `create_app()` as the auth/engagement-authorization/socket-lifecycle wrapper while preserving `forge-progress` subprotocol negotiation, engagement-scoped event filtering, JSON message shape, stale subscriber cleanup, and existing 1008 close behavior. |
| B133 | 2026-08-14 | Progress bridge paths still constructed `ProgressEvent` objects and queue-message validation inline inside `forge.webui.app.create_app()`, keeping event construction mixed with bridge loops, broker publishing, and run-progress polling. | Added progress event construction, synchronous publish adapter, queued-event validation, and run-progress event helpers in `forge.webui.state`, leaving `create_app()` as the bridge-loop/broker/lifecycle wrapper while preserving invalid queue-message dropping, non-dict payload normalization, `engagement_run_progress` event shape, and existing WebSocket delivery behavior. |
| B134 | 2026-08-14 | `forge.cli` still manually wired the extracted root/web/kb/recon/evasion/exploit/clean/vuln/auth command registration sequence after building the Typer apps, keeping registration orchestration in the oversized entrypoint instead of the CLI registry layer. | Added `register_extracted_cli_commands()` in `forge.cli_registry` and switched `forge.cli` to call it, leaving the entrypoint responsible for callback setup, compatibility adapters, and kill-chain handler registration while preserving public/hidden group visibility, extracted command names, README parity guards, and legacy direct-import adapters. |
| B135 | 2026-08-14 | `forge.cli` root callback still implemented offline socket guarding, `--no-tor` proxy cleanup, Tor bootstrap decisions, and Tor failure handling inline, keeping runtime guard policy mixed with Typer callback glue in the oversized entrypoint. | Added `forge.cli_runtime` runtime guard helpers and switched the root callback to delegate to them, leaving `forge.cli` responsible for version output, resilient parsing, config loading, and Typer exit mapping while preserving `FORGE_OFFLINE_STRICT` socket denial, `FORGE_PROXY` cleanup under `--no-tor`, Tor start gating, stop registration, and failed Tor bootstrap exit behavior. |
| B136 | 2026-08-14 | `forge.cli` still kept exploit/vuln/auth/clean direct-import compatibility adapter implementations inline, even though their command bodies already lived in extracted command modules. | Added `forge.cli_compat` for legacy direct-import adapters and re-exported those symbols from `forge.cli`, leaving the entrypoint responsible for callback/setup and kill-chain implementation while preserving adapter signatures, Typer option metadata, direct import identity, and CLI registry/README parity guards. |
| B137 | 2026-08-14 | `ArtifactQueueProcessor` still constructed its artifact queue runtime-service adapter inline, keeping callback binding responsibilities inside the oversized `forge.engagement_orchestrator` monolith even after queue processing moved into orchestration helpers. | Added `artifact_processor_runtime_services()` and an adapter protocol in `forge.orchestration.artifact_processor_runtime`, exported them through `forge.orchestration`, and switched `ArtifactQueueProcessor._runtime_services()` to delegate while preserving ingestion/process callback binding, remote download reconciliation, status/local-path updates, parsing, persistence, and existing artifact processor runtime coverage. |
| B138 | 2026-08-14 | `ArtifactQueueProcessor._emit_stage_progress()` still duplicated artifact queue progress-label and snapshot emission logic inline, keeping progress adapter behavior inside the oversized orchestrator even though the artifact queue progress primitives already lived in orchestration helpers. | Added `artifact_processor_progress_stage_label()` and `emit_artifact_processor_stage_progress()` in `forge.orchestration.artifact_processor_runtime`, exported them through `forge.orchestration`, and switched `ArtifactQueueProcessor` compatibility wrappers to delegate while preserving blank-label/callback skip behavior, staged progress labels, bounded queue metrics, failed counts, and completed-run ETA shape. |
| B139 | 2026-08-14 | `ArtifactQueueProcessor.ingest_local_artifacts()` and `.process()` still wired db path, engagement id, runtime services, local ingest, queue processing, search roots, and progress callbacks inline, keeping public entrypoint adapter responsibilities inside the oversized orchestrator. | Added `ingest_local_artifacts_for_processor()` and `process_artifact_queue_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported them through `forge.orchestration`, and switched the processor entrypoints to delegate while preserving search-root binding, progress callback propagation, runtime service construction, and artifact queue processing summary behavior. |
| B140 | 2026-08-14 | `ArtifactQueueProcessor._artifact_queue_rows_process_callbacks()` still assembled artifact queue row-processing callback bundles inline, duplicating runtime callback construction and keeping dispatch/download/reconcile/status/local-path/parse/persist/commit wiring inside the oversized orchestrator. | Added `artifact_processor_callbacks_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the processor compatibility wrapper to delegate while preserving stage callback field names, progress-aware download/parse wrappers, status/local-path adapter calls, context-bound persistence, and acquisition/processing commit hooks. |
| B141 | 2026-08-14 | `ArtifactQueueProcessor._local_artifact_record()`, `_local_artifact_metadata()`, and `_local_artifact_metadata_matches()` still wrapped orchestration artifact helpers inline, keeping local artifact record/metadata adapter policy inside the oversized orchestrator. | Added `artifact_processor_local_artifact_record()`, `artifact_processor_local_artifact_metadata()`, and `artifact_processor_local_artifact_metadata_matches()` in `forge.orchestration.artifact_processor_runtime`, exported them through `forge.orchestration`, and switched the processor compatibility wrappers to delegate while preserving artifact classification, resolved source URL shape, stat metadata, and exact metadata match semantics. |
| B142 | 2026-08-14 | `ArtifactQueueProcessor._artifact_queue_dispatch_entry()` still adapted artifact queue rows to local work items, remote download requests, and skipped-row status tuples inline, keeping dispatch adapter policy inside the oversized orchestrator. | Added `artifact_processor_dispatch_entry()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the processor compatibility wrapper to delegate while preserving local path resolution, artifact classification, remote request construction, skipped-row reasons, and existing dispatch tuple shape. |
| B143 | 2026-08-14 | `ArtifactQueueProcessor._remote_download_reconciliation_entry()` still adapted remote artifact download results to failed/skipped/local-path update/work-item tuples inline, keeping remote reconciliation adapter policy inside the oversized orchestrator. | Added `artifact_processor_remote_download_reconciliation_entry()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the processor compatibility wrapper to delegate while preserving classifier-based type inference, failed download rows, skipped download rows, local path updates, metadata propagation, and ready work item construction. |
| B144 | 2026-08-14 | `ArtifactQueueProcessor._parse_local_artifacts()` and `_download_remote_artifacts()` still bound parse/download batch helpers inline, keeping max-worker, progress, remote-scope, denial, and download-one adapter policy inside the oversized orchestrator. | Added `parse_local_artifacts_for_processor()` and `download_remote_artifacts_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported them through `forge.orchestration`, and switched the processor compatibility wrappers to delegate while preserving max-worker binding, parse worker callbacks, remote scope checks, denied-download callbacks, download request handling, progress labels/callbacks, and batch return behavior. |
| B145 | 2026-08-14 | `ArtifactQueueProcessor._parse_local_artifact()` still bound the single work-item parser to mobile/text scan callbacks and artifact-format labeling inline, keeping parser adapter policy inside the oversized orchestrator. | Added `parse_artifact_work_item_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the processor compatibility wrapper to delegate while preserving work-item parsing, mobile bundle scan callback binding, text scan callback binding, artifact-format labeling, and parsed artifact return behavior. |
| B146 | 2026-08-14 | `ArtifactQueueProcessor._scan_mobile_bundle_artifact()` and `_scan_text_artifact()` still bound scan helpers to runtime batch execution, extraction callbacks, cloud-config extraction, payload summaries, and dedupe callbacks inline, keeping scan adapter policy inside the oversized orchestrator. | Added `scan_mobile_bundle_artifact_for_processor()` and `scan_text_artifact_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported them through `forge.orchestration`, and switched the processor compatibility wrappers to delegate while preserving mobile/text artifact scanning, ordered batch binding, extraction-stage callbacks, cloud-config extraction, payload metadata summaries, Firebase/Supabase dedupe, and scan tuple return behavior. |
| B147 | 2026-08-14 | `ArtifactQueueProcessor._extract_mobile_bundle_family()` and `_extract_text_artifact_stage()` still bound extractor/text-stage helper dependencies inline, keeping APK/IPA payload extraction and nested-mobile text stage adapter policy inside the oversized orchestrator. | Added `extract_mobile_bundle_family_for_processor()` and `extract_text_artifact_stage_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported them through `forge.orchestration`, and switched the processor compatibility wrappers to delegate while preserving payload extraction callback binding, APK/IPA Firebase and Supabase extractor selection, text payload extraction, nested mobile bundle config extraction, stage result construction, and return behavior. |
| B148 | 2026-08-14 | `ArtifactQueueProcessor._extract_mobile_bundle_text_payloads()` and `_run_ordered_local_batch()` still bound archive text payload extraction callbacks and max-worker ordered local batch execution inline, keeping two reusable runtime adapter policies inside the oversized orchestrator. | Added `extract_mobile_bundle_text_payloads_for_processor()` and `run_ordered_local_batch_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported them through `forge.orchestration`, and switched the processor compatibility wrappers to delegate while preserving ZIP/TAR text payload extraction callback binding, default factory propagation, worker invocation, max-worker binding, ordered batch return behavior, and existing callers of `_run_ordered_local_batch()`. |
| B149 | 2026-08-14 | `ArtifactQueueProcessor._extract_cloud_configs_from_payloads()`, `_payload_cloud_config_job()`, and `_payload_cloud_config_result_entry()` still bound payload job/result callbacks and ordered extraction batching inline, keeping cloud-config payload adapter policy inside the oversized orchestrator. | Added `extract_cloud_configs_from_payloads_for_processor()`, `payload_cloud_config_job_for_processor()`, and `payload_cloud_config_result_entry_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported them through `forge.orchestration`, and switched the processor compatibility wrappers to delegate while preserving blank-payload filtering, ordered batch execution, per-payload Firebase/Supabase extraction, result-entry normalization, and accumulated cloud config return behavior. |
| B150 | 2026-08-14 | `ArtifactQueueProcessor._extract_cloud_configs_from_payload()` and `_extract_cloud_config_family()` still bound single-payload cloud-config batching and Firebase/Supabase extractor selection inline, keeping another cloud-config adapter layer inside the oversized orchestrator. | Added `extract_cloud_configs_from_payload_for_processor()` and `extract_cloud_config_family_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported them through `forge.orchestration`, and switched the processor compatibility wrappers to delegate while preserving Firebase/Supabase family ordering, ordered batch/default behavior, source/extract-path/text propagation, Firebase extractor binding, Supabase extractor binding, and tuple/list return behavior. |
| B151 | 2026-08-14 | `ArtifactQueueProcessor._extract_nested_mobile_bundle_configs()` and nested mobile ZIP/TAR/7z member-entry wrappers still bound archive dispatch, `py7zr` availability, suffix gates, size limits, and safe-name handling inline, keeping nested-mobile adapter policy inside the oversized orchestrator. | Added `extract_nested_mobile_bundle_configs_for_processor()`, `nested_mobile_zip_member_entry_for_processor()`, `nested_mobile_tar_member_entry_for_processor()`, and `nested_mobile_7z_member_entry_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported them through `forge.orchestration`, and switched the processor compatibility wrappers to delegate while preserving archive handler dispatch, ZIP/TAR/7z member filtering, safe archive member normalization, nested mobile suffix gates, remote artifact size limits, and nested config tuple return behavior. |
| B152 | 2026-08-14 | `ArtifactQueueProcessor._extract_nested_mobile_configs_from_zip()` and `_extract_nested_mobile_configs_from_tar()` still bound ordered archive member filtering, member job normalization, and nested member-job processing inline, keeping ZIP/TAR nested-mobile adapter policy inside the oversized orchestrator. | Added `extract_nested_mobile_configs_from_zip_for_processor()` and `extract_nested_mobile_configs_from_tar_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported them through `forge.orchestration`, and switched the processor compatibility wrappers to delegate while preserving ordered batch execution, ZIP/TAR member-entry callbacks, nested member job filtering, member job processing callback binding, source path propagation, and nested config tuple return behavior. |
| B153 | 2026-08-14 | `ArtifactQueueProcessor._extract_nested_mobile_configs_from_7z()`, `_nested_mobile_member_job()`, `_extract_nested_mobile_configs_from_member_jobs()`, and `_nested_mobile_member_result_entry()` still bound 7z extraction, member normalization, member-byte processing, and result normalization inline, keeping nested-mobile job adapter policy inside the oversized orchestrator. | Added `extract_nested_mobile_configs_from_7z_for_processor()`, `nested_mobile_member_job_for_processor()`, `extract_nested_mobile_configs_from_member_jobs_for_processor()`, and `nested_mobile_member_result_entry_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported them through `forge.orchestration`, and switched the processor compatibility wrappers to delegate while preserving 7z factory/size binding, ordered batch execution, 7z member-entry callbacks, member job normalization, member byte processing, result-entry normalization, source path propagation, and nested config tuple return behavior. |
| B154 | 2026-08-14 | `ArtifactQueueProcessor._extract_mobile_configs_from_member_bytes()` and the rebased mobile member payload/project/config wrappers still bound nested member-byte parsing, scan/bundle callbacks, suffix/size gates, and Firebase/Supabase type rebasing inline, keeping another nested-mobile adapter layer inside the oversized orchestrator. | Added `extract_mobile_configs_from_member_bytes_for_processor()`, `rebased_mobile_member_payload_entry_for_processor()`, `rebased_mobile_member_project_entry_for_processor()`, and `rebased_mobile_member_config_entry_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported them through `forge.orchestration`, and switched the processor compatibility wrappers to delegate while preserving nested/archive suffix gates, remote artifact size limits, ordered batch binding, text scan callback binding, mobile bundle family binding, source/member rebasing, and Firebase/Supabase type-specific return behavior. |
| B155 | 2026-08-14 | `ArtifactQueueProcessor._safe_artifact_relation_context()`, `_merge_artifact_relation_context()`, `_artifact_cloud_asset_metadata()`, and `_artifact_relation_context()` still bound parse metadata filtering, relation merge, cloud asset provenance callbacks, and engagement queue lookup inline, keeping artifact relation-context adapter policy inside the oversized orchestrator. | Added `safe_artifact_relation_context_for_processor()`, `merge_artifact_relation_context_for_processor()`, `artifact_cloud_asset_metadata_for_processor()`, and `artifact_relation_context_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported them through `forge.orchestration`, and switched the processor compatibility wrappers to delegate while preserving parse/artifact metadata filtering, relation metadata precedence, source-seed provenance binding, engagement-id queue lookup, and cloud asset artifact-provenance metadata behavior. |
| B156 | 2026-08-14 | `ArtifactQueueProcessor._persist_parsed_artifact()` still bound parsed-artifact persistence to relation context, source-seed lookup/creation, discovery payload expansion, generic text discovery persistence, Firebase dedupe/store, and Supabase dedupe/store callbacks inline, keeping parse persistence adapter policy inside the oversized orchestrator. | Added `persist_parsed_artifact_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the processor compatibility wrapper to delegate while preserving parsed artifact persistence callback wiring, local source-seed fallback creation, structured discovery expansion, generic discovery seed counts, Firebase/Supabase dedupe/store behavior, source URL propagation, artifact context propagation, and parse metadata return behavior. |
| B157 | 2026-08-14 | `ArtifactQueueProcessor._artifact_discovery_payloads()`, `_expand_structured_discovery_jobs()`, `_structured_discovery_payload_job()`, `_structured_discovery_result_entry()`, `_structured_discovery_jobs_for_payload()`, and `_structured_discovery_payload_entry()` still bound artifact discovery payload normalization, structured discovery batch expansion, per-payload family dispatch, result flattening, source hints, and payload-entry filtering inline, keeping structured discovery adapter policy inside the oversized orchestrator. | Added `artifact_discovery_payloads_for_processor()`, `expand_structured_discovery_jobs_for_processor()`, `structured_discovery_payload_job_for_processor()`, `structured_discovery_result_entry_for_processor()`, `structured_discovery_jobs_for_payload_for_processor()`, and `structured_discovery_payload_entry_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported them through `forge.orchestration`, and switched the processor compatibility wrappers to delegate while preserving source URL payload rebasing, blank payload filtering, ordered batch/default behavior, structured discovery family ordering, source-hint construction, fragment-builder callback binding, payload-entry callback binding, and result flattening. |
| B158 | 2026-08-14 | `ArtifactQueueProcessor._decode_data_uri_bytes()`, `_data_uri_payload_entry()`, `_data_uri_structured_payload_text()`, `_data_uri_image_payload_entry()`, and `_data_uri_image_structured_payload_text()` still bound data-URI decoding, member-size limits, text decoding, ordered batch execution, OCR image suffix/size gates, content-type suffix mapping, OCR/barcode/image metadata callbacks, and data-URI regex dispatch inline, keeping data-URI structured discovery adapter policy inside the oversized orchestrator. | Added `decode_data_uri_bytes_for_processor()`, `data_uri_payload_entry_for_processor()`, `data_uri_structured_payload_text_for_processor()`, `data_uri_image_payload_entry_for_processor()`, and `data_uri_image_structured_payload_text_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported them through `forge.orchestration`, and switched the processor compatibility wrappers to delegate while preserving base64/URL decoding, text decode size limits, ordered batch/default behavior, data-URI regex limits, OCR image suffix/size filtering, content-type suffix selection, OCR/barcode/image metadata callback binding, and structured payload text return behavior. |
| B159 | 2026-08-14 | `ArtifactQueueProcessor._iac_text_structured_payload_text()` and `_iac_text_structured_payload_family()` still bound IaC structured discovery family ordering, ordered batch execution, duplicate-line folding, Terraform/Bicep adapter calls, and CloudFormation/Serverless/SAM/Pulumi/SST/AWS CDK candidate dispatch inline, keeping IaC structured discovery adapter policy inside the oversized orchestrator. | Added `iac_text_structured_payload_text_for_processor()` and `iac_text_structured_payload_family_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported them through `forge.orchestration`, and switched the processor compatibility wrappers to delegate while preserving IaC family ordering, source-hint propagation, ordered batch/default behavior, case-insensitive duplicate-line suppression, Terraform/Bicep callback binding, candidate-function dispatch, joined candidate return formatting, and unknown-family empty results. |
| B160 | 2026-08-14 | `ArtifactQueueProcessor._store_firebase_projects()`, `_store_supabase_configs()`, `_firebase_project_persistence_entry()`, and `_supabase_config_persistence_entry()` still bound mobile config persistence to child-depth calculation, ordered batch execution, cloud asset references, artifact provenance metadata, seed insertion/linking, relation metadata merging, URL seed creation, key finding storage, redaction, and encryption callbacks inline, keeping mobile config storage adapter policy inside the oversized orchestrator. | Added `store_firebase_projects_for_processor()`, `store_supabase_configs_for_processor()`, `firebase_project_persistence_entry_for_processor()`, and `supabase_config_persistence_entry_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported them through `forge.orchestration`, and switched the processor compatibility wrappers to delegate while preserving Firebase/Supabase storage callback wiring, source seed propagation, source URL propagation, artifact context propagation, child-depth binding, ordered batch/default behavior, cloud asset/key finding persistence, relation merge behavior, and Supabase secret redaction/encryption behavior. |
| B161 | 2026-08-14 | `ArtifactQueueProcessor._collect_generic_text_discovery_batches()`, `_generic_text_discovery_job()`, `_collect_generic_text_discovery_job_result()`, `_collect_generic_text_discoveries()`, `_artifact_text_discovery_family_entry()`, and `_artifact_text_discovery_merge_entry()` still bound generic text discovery job filtering, ordered batch execution, discovery result collection, per-family discovery callbacks, family-entry cloning, merge-entry cloning, and batch merge callbacks inline, keeping generic text discovery adapter policy inside the oversized orchestrator. | Added `collect_generic_text_discovery_batches_for_processor()`, `generic_text_discovery_job_for_processor()`, `collect_generic_text_discovery_job_result_for_processor()`, `collect_generic_text_discoveries_for_processor()`, and `artifact_text_discovery_family_entry_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported them through `forge.orchestration`, and switched the processor compatibility wrappers to delegate while preserving blank discovery-job filtering, source hint fallback, ordered batch/default behavior, exception-safe discovery job results, per-family discovery callback binding, family batch cloning, merge-entry cloning, merge callback behavior, and discovery batch return shape. |
| B162 | 2026-08-14 | `ArtifactQueueProcessor._collect_generic_text_discovery_family()` still bound generic text family dispatch for emails, phones, IPs, network hosts, URLs, contact identities, keys, and cloud assets inline, keeping the highest-level text discovery family adapter policy inside the oversized orchestrator. | Added `collect_generic_text_discovery_family_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the processor compatibility wrapper to delegate while preserving source-label fallback, simple/network/URL/identity/key/cloud family ordering, ordered batch binding, URL/contact/key/cloud candidate callbacks, key pattern/redaction/Azure parsing/encryption callbacks, and unknown-family empty batch behavior. |
| B163 | 2026-08-14 | `ArtifactQueueProcessor._artifact_text_key_pattern_findings()` still built base secret findings, source/repo/file/backend context, match-group extraction, bad-group suppression, and contextual secret-finder expansion inline, keeping generic key finding adapter policy inside the oversized orchestrator. | Added `artifact_text_key_pattern_findings_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the processor compatibility wrapper to delegate while preserving no-match behavior, invalid regex group suppression, base finding shape, source/repo/file/backend metadata, key value string coercion, contextual finding callback invocation, and contextual finding append order. |
| B164 | 2026-08-14 | `ArtifactQueueProcessor._artifact_text_url_family_candidates()` and `_artifact_text_direct_url_candidate()` still bound generic URL-family dispatch, direct URL normalization, source-label gates, metadata parser selection, package/container extraction, Helm archive suppression, fragment stripping, and duplicate filtering inline, keeping URL candidate adapter policy inside the oversized orchestrator. | Added `artifact_text_url_family_candidates_for_processor()` and `artifact_text_direct_url_candidate_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported them through `forge.orchestration`, and switched the processor compatibility wrappers to delegate while preserving ordered direct URL normalization, HTTP/HTTPS/netloc filtering, manifest fragment stripping, duplicate suppression, Helm chart archive suppression for Helm-index sources, label-gated metadata families, open metadata families, package/container candidate callbacks, and unknown-family empty results. |
| B165 | 2026-08-14 | `ArtifactQueueProcessor._artifact_text_contact_identity_candidates()` and its calendar/vCard line/value cleanup helpers still bound contact marker detection, source-label gates, title enrichment, line parsing, value normalization, name/company classification, dedupe, and candidate limits inline, keeping contact identity discovery adapter policy inside the oversized orchestrator. | Added `artifact_text_contact_identity_candidates_for_processor()`, `calendar_contact_identity_line_entry_for_processor()`, `calendar_contact_title_line_value_for_processor()`, `calendar_contact_identity_value_for_processor()`, and `clean_calendar_contact_identity_value_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported them through `forge.orchestration`, and switched the processor compatibility wrappers to delegate while preserving vCard/calendar marker fallback, source-label allowlist behavior, title extraction, `FN`/`N`/`ORG` parsing, person-name gate binding, cleanup rejection rules, dedupe behavior, and the 40-candidate cap. |
| B166 | 2026-08-14 | `ArtifactQueueProcessor._artifact_text_cloud_asset_family_candidates()` still bound AWS S3 URI/ARN extraction, AWS KMS ARN extraction, generic AWS ARN delegation, lowercasing, dedupe, and non-AWS fallthrough inline, keeping AWS cloud candidate adapter policy inside the oversized orchestrator. | Added `artifact_text_aws_cloud_asset_family_candidates_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the processor cloud candidate wrapper to delegate AWS subfamilies before the remaining non-AWS branches while preserving S3 URI/ARN ordering, S3 bucket lowercasing, KMS ARN lowercasing, KMS dedupe, generic AWS ARN callback dispatch, and `None` fallthrough for non-AWS cloud families. |
| B167 | 2026-08-14 | `ArtifactQueueProcessor._artifact_text_cloud_asset_family_candidates()` still bound GCS URI/resource bucket extraction, GCP KMS resource extraction, identifier case behavior, dedupe, and non-GCP fallthrough inline, keeping GCP cloud candidate adapter policy inside the oversized orchestrator. | Added `artifact_text_gcp_cloud_asset_family_candidates_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the processor cloud candidate wrapper to delegate GCP subfamilies after AWS and before remaining Azure/manifest branches while preserving GCS URI/resource ordering, GCS bucket lowercasing, GCP KMS resource string preservation, KMS dedupe, and `None` fallthrough for non-GCP cloud families. |
| B168 | 2026-08-14 | `ArtifactQueueProcessor._artifact_text_cloud_asset_family_candidates()` still bound Azure Blob resource extraction, Azure Key Vault URL extraction, Microsoft identity association app ID extraction, URL-decoding, lowercasing, dedupe, and non-Azure fallthrough inline, keeping Azure cloud candidate adapter policy inside the oversized orchestrator. | Added `artifact_text_azure_cloud_asset_family_candidates_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the processor cloud candidate wrapper to delegate Azure subfamilies after AWS/GCP and before remaining manifest branches while preserving Azure Blob account/container lowercasing, Key Vault vault/family/name lowercasing, URL-decoded Key Vault object names, Key Vault/app ID dedupe, Microsoft identity app ID lowercasing, and `None` fallthrough for non-Azure cloud families. |
| B169 | 2026-08-14 | `ArtifactQueueProcessor._artifact_text_cloud_asset_family_candidates()` still bound ads.txt, app-ads.txt, sellers.json, ai-plugin.json, Android assetlinks, Android manifest, Apple app site association, and web manifest candidate routing inline, keeping app/mobile manifest adapter policy inside the oversized orchestrator after the cloud-provider branches. | Added `artifact_text_app_manifest_family_candidates_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the processor cloud candidate wrapper to delegate app/mobile manifest families after AWS/GCP/Azure and before Kubernetes/GitOps/workflow branches while preserving source-format gates, app-ads mode forwarding, Android package candidate source labels, Android manifest inline `<manifest` fallback, iOS app ID wrapping, webmanifest/manifest.json allowlist behavior, callback dispatch, and `None` fallthrough for non-app manifest families. |
| B170 | 2026-08-14 | `ArtifactQueueProcessor._artifact_text_cloud_asset_family_candidates()` still bound Kubernetes secret manifest, GitOps manifest, and workflow manifest URI family maps, URL-decoding, lowercasing, dedupe, and Amplify source/case exceptions inline, keeping orchestration-manifest adapter policy inside the oversized orchestrator before the Cloudflare branch. | Added `artifact_text_orchestration_manifest_family_candidates_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the processor cloud candidate wrapper to delegate Kubernetes/GitOps/workflow manifest families after app/mobile manifests and before Cloudflare while preserving Kubernetes asset type mapping, Amplify identifier case preservation/source labeling, non-Amplify lowercasing, URL-decoded identifiers, slash trimming, GitOps/workflow source labels, dedupe behavior, and `None` fallthrough for non-orchestration manifest families. |
| B171 | 2026-08-14 | `ArtifactQueueProcessor._artifact_text_cloud_asset_family_candidates()` still bound Cloudflare structured URI extraction inline, keeping Cloudflare R2/D1/KV/Worker/Pages candidate tuple construction and lowercasing inside the oversized orchestrator after the orchestration-manifest branches. | Added `artifact_text_cloudflare_asset_family_candidates_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the processor cloud candidate wrapper to delegate Cloudflare families after orchestration manifests while preserving structured URI matching, `cloudflare_{service}` asset type construction, identifier lowercasing, `artifact_cloudflare_config` source labeling, ordering, and `None` fallthrough for non-Cloudflare families. |
| B172 | 2026-08-14 | `ArtifactQueueProcessor._persist_generic_text_discovery_batch()` still bound child-depth, ordered batch execution, per-family persistence-entry callbacks, seed/email/url/key/cloud persistence callbacks, relation-context merging, metadata merge, and cloud-asset metadata callbacks inline, keeping generic text discovery persistence adapter wiring inside the oversized orchestrator. | Added `persist_generic_text_discovery_batch_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, expanded the runtime adapter protocol to include the needed persistence callbacks, and switched the processor compatibility wrapper to delegate while preserving source seed/artifact context forwarding, every lower persistence callback binding, key/cloud/email/seed/url store callback binding, relation metadata merge binding, and return-count behavior. |
| B173 | 2026-08-14 | `ArtifactQueueProcessor._store_generic_text_discoveries()` still coordinated generic text discovery collection and persistence inline, keeping source-file forwarding, source-seed forwarding, and collection-to-persistence batch handoff policy inside the oversized orchestrator. | Added `store_generic_text_discoveries_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the processor compatibility wrapper to delegate while preserving text/source-file forwarding to collection, discovered batch handoff to persistence, source seed forwarding, and inserted-count return behavior. |
| B174 | 2026-08-14 | `ArtifactQueueProcessor._artifact_url_seed_persistence_entry()` still bound URL templating/standards/mobile classification callbacks, ordered family execution, family-entry dispatch, and family-merge callbacks inline, keeping artifact URL seed persistence adapter wiring inside the oversized orchestrator. | Added `artifact_url_seed_persistence_entry_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, expanded the runtime adapter protocol to include URL seed family callbacks, and switched the processor compatibility wrapper to delegate while preserving relation metadata forwarding, templated/standards/mobile classifier callback binding, ordered-batch callback binding, URL seed family-entry callback binding, family-merge callback binding, return entry shape, and invalid URL suppression behavior inherited from the lower helper. |
| B175 | 2026-08-14 | `ArtifactQueueProcessor._artifact_url_seed_family_entry()` still dispatched URL seed family work for social pivots, related seeds, and cloud assets inline, keeping URL family branch selection and source labeling inside the oversized orchestrator. | Added `artifact_url_seed_family_entry_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, expanded the runtime adapter protocol to include URL social/related/cloud callbacks, and switched the processor compatibility wrapper to delegate while preserving social pivot relation metadata forwarding, related seed hostname forwarding, cloud asset URL forwarding, `artifact_url_extract` source labeling, empty dict behavior for unknown families, and returned family-entry keys. |
| B176 | 2026-08-14 | `ArtifactQueueProcessor._artifact_url_related_seed_entries()` still bound social-platform host suppression, managed-cloud host suppression, and root-domain normalization callbacks inline, keeping URL related-seed adapter policy inside the oversized orchestrator. | Added `artifact_url_related_seed_entries_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the processor compatibility wrapper to delegate while preserving host normalization, social/managed-cloud suppression callback ordering, root-domain normalization callback binding, domain vs subdomain entry behavior, confidence values, and empty-list suppression behavior. |
| B177 | 2026-08-14 | `ArtifactQueueProcessor._artifact_url_social_pivot_entries()` still bound social profile platform, handle extraction, seed classification, company-name, and profile-name callbacks inline, keeping URL social-pivot adapter policy inside the oversized orchestrator. | Added `artifact_url_social_pivot_entries_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the processor compatibility wrapper to delegate while preserving relation metadata forwarding, `artifact_social_url_extract` rule override behavior, platform callback binding, handle extraction, Bluesky domain-handle promotion, company/name pivot entries, confidence values, and callback ordering. |
| B178 | 2026-08-14 | `ArtifactQueueProcessor._artifact_url_cloud_asset_entries()` still bound ordered URL cloud-asset family execution and cloud-asset family callback forwarding inline, keeping URL cloud-asset adapter policy inside the oversized orchestrator. | Added `artifact_url_cloud_asset_entries_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the processor compatibility wrapper to delegate while preserving ordered-family execution callback binding, URL forwarding, hostname normalization, source forwarding, default empty-list factory behavior, flattened family-entry output, and cloud-asset family callback dispatch. |
| B179 | 2026-08-14 | `ArtifactQueueProcessor._artifact_url_cloud_asset_family_entries()` still bound AWS S3, DigitalOcean Spaces, GCS, Azure Blob/static website/key vault, and Cloudflare URL matcher bundles inline, keeping URL cloud-asset family adapter policy inside the oversized orchestrator. | Added `artifact_url_cloud_asset_family_entries_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the processor compatibility wrapper to delegate while preserving family dispatch, URL/hostname/source forwarding, AWS S3 matcher tuple binding, DigitalOcean/GCS/Azure matcher tuple binding, Azure static website/key vault regex binding, Cloudflare worker/pages/R2 regex binding, identifier normalization, and empty-list fallthrough behavior. |
| B180 | 2026-08-14 | `ArtifactQueueProcessor._store_social_profile_url_pivots()` still coordinated URL seed lookup, optional social-pivot extraction, ordered pivot normalization, seed insertion, and relation insertion callback binding inline, keeping social profile pivot storage adapter policy inside the oversized orchestrator. | Added `store_social_profile_url_pivots_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, expanded the runtime adapter protocol to include pivot-entry, lookup, and relation callbacks, and switched the processor compatibility wrapper to delegate while preserving lookup-miss suppression before pivot extraction, relation metadata forwarding for generated pivots, supplied pivot-entry passthrough, depth forwarding, ordered pivot normalization, seed insertion, second lookup behavior inherited from the lower helper, and relation insertion metadata. |
| B181 | 2026-08-14 | `ArtifactQueueProcessor._store_cloud_asset_from_url()` still coordinated optional URL cloud-asset extraction, ordered cloud URL entry normalization, cloud asset metadata construction, and cloud asset reference storage callback binding inline, keeping cloud URL asset storage adapter policy inside the oversized orchestrator. | Added `store_cloud_assets_from_url_entries_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, expanded the runtime adapter protocol to include the cloud URL entry callback, and switched the processor compatibility wrapper to delegate while preserving generated vs supplied cloud entry behavior, source forwarding, source seed forwarding, relation metadata forwarding, ordered normalization, default `None` handling, metadata construction with `artifact_context=None`, and cloud asset reference storage. |
| B182 | 2026-08-14 | `ArtifactQueueProcessor._social_profile_url_pivot_entry()` and `_cloud_asset_url_entry()` still directly bound artifact queue URL entry normalizers inline, keeping pivot/cloud URL entry coercion and suppression adapters inside the oversized orchestrator. | Added `artifact_social_profile_url_pivot_entry_for_processor()` and `artifact_cloud_asset_url_entry_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported them through `forge.orchestration`, and switched the processor compatibility wrappers to delegate while preserving social pivot field stripping, confidence coercion, metadata dict copying, non-dict suppression, cloud asset type/identifier/source stripping, missing-field suppression, and returned entry shapes. |
| B183 | 2026-08-14 | `ArtifactQueueProcessor._store_cloud_asset_reference()` still created the artifact-lineage audit closure and called cloud asset reference persistence inline, keeping cloud asset reference storage adapter policy inside the oversized orchestrator. | Added `store_artifact_cloud_asset_reference_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, expanded the runtime adapter protocol to include audit-lineage binding, and switched the processor compatibility wrapper to delegate while preserving engagement id forwarding, asset type/identifier/source forwarding, optional metadata forwarding, audit callback action/target/result forwarding, and database connection reuse. |
| B184 | 2026-08-14 | `ArtifactQueueProcessor._firebase_match_entry()` still normalized Firebase URL match tuples inline, keeping Firebase project id cleanup and RTDB URL classification inside the oversized orchestrator before the larger Firebase text extraction flow. | Added `firebase_match_entry_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the processor compatibility wrapper to delegate while preserving project id stripping/lowercasing, empty project suppression, RTDB URL retention for `.firebaseio.com` matches, non-RTDB URL suppression, and returned entry shape. |
| B185 | 2026-08-14 | `ArtifactQueueProcessor._extract_firebase_from_text()` still coordinated Firebase API key capture/encryption, storage bucket normalization, URL-pattern matching, ordered match normalization, duplicate suppression, and `FirebaseProject` construction inline, keeping Firebase text extraction adapter policy inside the oversized orchestrator. | Added `extract_firebase_from_text_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the processor compatibility wrapper to delegate while preserving Firebase URL pattern binding, API key regex/encryption callback binding, storage bucket regex/normalizer binding, ordered match-entry callback execution, project id dedupe, RTDB URL carry-forward behavior, source file/extract path forwarding, `bundle_id=None`, storage bucket forwarding, and project factory construction. |
| B186 | 2026-08-14 | `ArtifactQueueProcessor._extract_terraform_state_payload_family()` still dispatched Terraform state payload families inline, keeping structured/text family branch selection and callback forwarding inside the oversized orchestrator. | Added `terraform_state_payload_family_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the processor compatibility wrapper to delegate while preserving structured payload callback forwarding, text payload callback forwarding, text/source-file/member-name argument forwarding, returned payload tuple lists, and empty-list behavior for unknown families. |
| B187 | 2026-08-14 | `ArtifactQueueProcessor._extract_terraform_state_text_payloads()` still built Terraform state raw text payload tuples inline, keeping whitespace suppression and source/member forwarding inside the oversized orchestrator. | Added `terraform_state_text_payloads_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the processor compatibility wrapper to delegate while preserving blank-text suppression, source file forwarding, member name forwarding, raw text preservation, and returned payload tuple shape. |
| B188 | 2026-08-14 | `ArtifactQueueProcessor._extract_terraform_state_structured_payloads()` still wrapped Terraform state structured payload text inline, keeping empty structured-payload suppression and `#tfstate-structured` member labeling inside the oversized orchestrator. | Added `terraform_state_structured_payloads_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the processor compatibility wrapper to delegate while preserving structured payload text callback forwarding, empty payload suppression, source file forwarding, `#tfstate-structured` member-name suffixing, structured payload forwarding, and returned payload tuple shape. |
| B189 | 2026-08-14 | `ArtifactQueueProcessor._terraform_state_structured_payload_text()` still coordinated Terraform state JSON loading, resource-value iteration, ordered resource candidate extraction, structured candidate normalization, dedupe, and joined payload text inline, keeping Terraform state structured payload text adapter policy inside the oversized orchestrator. | Added `terraform_state_structured_payload_text_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the processor compatibility wrapper to delegate while preserving non-object JSON suppression, resource-value iterator callback binding, ordered resource candidate callback execution, structured candidate entry callback execution, invalid entry suppression, dedupe by lowered value, insertion ordering, newline joining, and empty output behavior. |
| B190 | 2026-08-14 | `ArtifactQueueProcessor._terraform_block_assignments()` still coordinated Terraform block line enumeration, ordered assignment parsing, invalid parser-result suppression, duplicate-key overwrite behavior, and assignment dictionary construction inline, keeping Terraform text block assignment adapter policy inside the oversized orchestrator. | Added `terraform_block_assignments_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the processor compatibility wrapper to delegate while preserving block text string coercion, splitline enumeration, assignment-line parser callback binding, ordered batch callback execution, invalid entry suppression, later duplicate-key overwrite behavior, empty input behavior, and returned assignment dictionary shape. |
| B191 | 2026-08-14 | `ArtifactQueueProcessor._terraform_assignment_line_entry()` still parsed Terraform assignment lines inline, keeping comment suppression, quoted-assignment matching, key normalization, value trimming, and empty result suppression inside the oversized orchestrator. | Added `terraform_assignment_line_entry_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the processor compatibility wrapper to delegate while preserving blank/comment line suppression, `#`/`//`/`/*`/`*` comment-prefix handling, quoted-value-only assignment matching, key lowercasing, surrounding value whitespace stripping, empty key/value suppression, non-assignment suppression, and returned `(key, value)` tuple shape. |
| B192 | 2026-08-14 | `ArtifactQueueProcessor._iter_terraform_text_blocks()` still scanned Terraform text blocks inline, keeping line coercion, block-start regex matching, resource-type normalization, brace-depth tracking, nested block retention, unclosed-block retention, non-block line skipping, and block tuple construction inside the oversized orchestrator. | Added `iter_terraform_text_blocks_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the processor compatibility wrapper to delegate while preserving injected block-start regex binding, text string coercion, splitline scanning, skipped non-block lines, lowercased resource type extraction, brace-depth accounting, nested brace handling, block text newline joining, unclosed block retention, multiple block ordering, and returned `(resource_type, block_text)` tuple list shape. |
| B193 | 2026-08-14 | `ArtifactQueueProcessor._terraform_structured_candidate_entry()` still normalized Terraform structured candidate lines inline, keeping whitespace trimming, empty candidate suppression, lowered dedupe-key construction, and candidate tuple shaping inside the oversized orchestrator even though both Terraform state and text paths share it. | Added `terraform_structured_candidate_entry_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the processor compatibility wrapper to delegate while preserving candidate string coercion, surrounding whitespace trimming, empty candidate suppression, lowered value generation, original-trimmed candidate retention, returned `(candidate, lowered)` tuple shape, and compatibility with both state/text ordered batch normalization callers. |
| B194 | 2026-08-14 | `ArtifactQueueProcessor._terraform_text_structured_payload_text()` still coordinated Terraform text block iteration, ordered text-block candidate extraction, backend/terragrunt source-hint candidate expansion, structured candidate normalization, dedupe, ordering, and joined payload text inline, keeping Terraform text structured payload policy inside the oversized orchestrator. | Added `terraform_text_structured_payload_text_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the processor compatibility wrapper to delegate while preserving text-block iterator callback binding, text-block candidate callback binding, backend config candidate callback binding, terragrunt remote-state callback binding, source-hint predicate callback binding, ordered batch execution for block candidates and final candidate normalization, backend candidate inclusion for backend/terragrunt hints, terragrunt candidate inclusion for terragrunt hints only, invalid entry suppression, first-seen dedupe by lowered value, candidate ordering, newline joining, empty output behavior, and compatibility with existing Terraform text payload callers. |
| B195 | 2026-08-14 | `ArtifactQueueProcessor._terraform_text_block_candidate()` still converted Terraform text resource blocks into cloud/Firebase URL candidates inline, keeping assignment extraction, AWS S3, DigitalOcean Spaces, Google Cloud Storage, Firebase RTDB, and Azure Blob resource dispatch policy inside the oversized orchestrator. | Added `terraform_text_block_candidate_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the processor compatibility wrapper to delegate while preserving Terraform block assignment callback binding, empty assignment suppression, AWS S3 bucket normalization and validation, DigitalOcean Spaces bucket/region normalization and validation, GCS name/bucket fallback and validation, Firebase project/project_id/name fallback and validation, Azure container/account validation, unknown resource suppression, invalid candidate suppression, and returned URL candidate shapes. |
| B196 | 2026-08-14 | `ArtifactQueueProcessor._digitalocean_spaces_url_from_endpoint()` still normalized Terraform backend DigitalOcean Spaces endpoints inline, keeping bucket validation, endpoint coercion, scheme fallback, hostname cleanup, endpoint host regex matching, region extraction, and Spaces URL construction inside the oversized orchestrator. | Added `digitalocean_spaces_url_from_endpoint_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the processor compatibility wrapper to delegate while preserving injected endpoint-host regex binding, bucket lowercasing/trimming, bucket validation, endpoint trimming and trailing slash stripping, implicit `https://` parsing for schemeless endpoints, hostname lowercasing/trailing-dot stripping, non-matching endpoint suppression, empty-region suppression, invalid input suppression, and returned `https://{bucket}.{region}.digitaloceanspaces.com` URL shape. |
| B197 | 2026-08-14 | `ArtifactQueueProcessor._terraform_backend_config_candidates()` still coordinated Terraform backend assignment parsing and S3, DigitalOcean Spaces, GCS, and Azure Blob backend candidate selection inline, keeping backend resource policy inside the oversized orchestrator. | Added `terraform_backend_config_candidates_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the processor compatibility wrapper to delegate while preserving Terraform assignment callback binding, empty assignment suppression, bucket normalization and validation, DigitalOcean endpoint callback binding and priority, S3 fallback key detection, GCS key detection, Azure account/container fallback handling, Azure URL callback binding, invalid bucket suppression, candidate ordering, empty output behavior, and returned candidate list shape. |
| B198 | 2026-08-14 | `ArtifactQueueProcessor._iter_terragrunt_remote_state_blocks()` still scanned Terragrunt `remote_state` blocks inline, keeping text coercion, case-insensitive block-start matching, brace-depth tracking, nested block retention, unclosed-block retention, non-block line skipping, and joined block text construction inside the oversized orchestrator. | Added `iter_terragrunt_remote_state_blocks_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the processor compatibility wrapper to delegate while preserving string coercion, splitline scanning, case-insensitive `remote_state` block matching, non-block line skipping, brace-depth accounting, nested brace handling, newline joining, unclosed block retention, multiple block ordering, and returned block text list shape. |
| B199 | 2026-08-14 | `ArtifactQueueProcessor._terragrunt_remote_state_backend_candidates()` still coordinated Terragrunt remote-state block extraction, ordered backend candidate parsing, candidate trimming, empty suppression, case-insensitive dedupe, and flattened candidate list assembly inline, keeping Terragrunt backend candidate aggregation policy inside the oversized orchestrator. | Added `terragrunt_remote_state_backend_candidates_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the processor compatibility wrapper to delegate while preserving remote-state block iterator callback binding, Terraform backend candidate callback binding, ordered batch execution, default empty-list factory behavior, candidate string coercion/trimming, empty candidate suppression, first-seen case-insensitive dedupe, batch flattening, candidate ordering, empty block behavior, and returned candidate list shape. |
| B200 | 2026-08-14 | `ArtifactQueueProcessor._iter_bicep_text_blocks()` still scanned Bicep resource blocks inline, keeping text coercion, resource-start regex matching, resource-type normalization, brace-depth tracking, nested block retention, unclosed-block retention, non-block line skipping, and block tuple construction inside the oversized orchestrator. | Added `iter_bicep_text_blocks_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the processor compatibility wrapper to delegate while preserving injected Bicep resource-start regex binding, text string coercion, splitline scanning, skipped non-block lines, lowercased resource type extraction, brace-depth accounting, nested brace handling, block text newline joining, unclosed block retention, multiple block ordering, and returned `(resource_type, block_text)` tuple list shape. |
| B201 | 2026-08-14 | `ArtifactQueueProcessor._bicep_block_assignments()` still coordinated Bicep block line enumeration, ordered assignment parsing, invalid parser-result suppression, duplicate-key overwrite behavior, and assignment dictionary construction inline, keeping Bicep text block assignment adapter policy inside the oversized orchestrator. | Added `bicep_block_assignments_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the processor compatibility wrapper to delegate while preserving block text string coercion, splitline enumeration, Bicep assignment-line parser callback binding, ordered batch callback execution, invalid entry suppression, later duplicate-key overwrite behavior, empty input behavior, and returned assignment dictionary shape. |
| B202 | 2026-08-14 | `ArtifactQueueProcessor._bicep_assignment_line_entry()` still parsed Bicep assignment lines inline, keeping blank/comment suppression, quoted-assignment matching, key normalization, value trimming, and empty result suppression inside the oversized orchestrator. | Added `bicep_assignment_line_entry_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the processor compatibility wrapper to delegate while preserving blank line suppression, `//`/`/*`/`*` comment-prefix handling, quoted-value-only assignment matching, key lowercasing, surrounding value whitespace stripping, empty key/value suppression, non-assignment suppression, and returned `(key, value)` tuple shape. |
| B203 | 2026-08-14 | `ArtifactQueueProcessor._bicep_text_structured_payload_text()` still coordinated Bicep text block iteration, ordered text-block candidate extraction, structured candidate normalization, dedupe, ordering, and joined payload text inline, keeping Bicep text structured payload policy inside the oversized orchestrator. | Added `bicep_text_structured_payload_text_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the processor compatibility wrapper to delegate while preserving text-block iterator callback binding, text-block candidate callback binding, structured candidate normalization callback binding, ordered batch execution for block candidates and final candidate normalization, invalid entry suppression, first-seen dedupe by lowered value, candidate ordering, newline joining, empty output behavior, and compatibility with existing Bicep text payload callers. |
| B204 | 2026-08-14 | `ArtifactQueueProcessor._bicep_text_block_candidate()` still converted Bicep resource blocks into IaC structured candidates inline, keeping assignment extraction, mapping construction, normalized mapping callback use, candidate dispatch, empty-assignment suppression, empty-candidate suppression, and first-candidate selection inside the oversized orchestrator. | Added `bicep_text_block_candidate_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the processor compatibility wrapper to delegate while preserving Bicep assignment callback binding, resource-type/block-text unpacking, empty assignment suppression, `type`/`properties` mapping construction, assignment flattening into the mapping, YAML normalization callback binding, IaC resource candidate callback binding, first-candidate return behavior, no-candidate empty output behavior, and compatibility with existing Bicep text structured payload callers. |
| B205 | 2026-08-14 | `ArtifactQueueProcessor._azure_blob_url_from_parts()` still normalized Azure Blob account/container values inline, keeping string coercion, whitespace trimming, lowercasing, account validation, container validation, URL construction, and invalid input suppression inside the oversized orchestrator. | Added `azure_blob_url_from_parts_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the processor compatibility wrapper to delegate while preserving account/container string coercion, trimming, lowercasing, `[a-z0-9-]{3,24}` account validation, `[^/?#]+` container validation, invalid account/container suppression, and returned `https://{account}.blob.core.windows.net/{container}` URL shape. |
| B206 | 2026-08-14 | `ArtifactQueueProcessor._azure_blob_parts_from_composite_name()` still parsed slash-separated Azure Blob composite names inline, keeping string coercion, slash splitting, whitespace trimming, empty segment suppression, lowercasing, outer-part extraction, and short-value suppression inside the oversized orchestrator. | Added `azure_blob_parts_from_composite_name_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the processor compatibility wrapper to delegate while preserving composite value string coercion, slash-separated segment parsing, whitespace trimming, empty segment filtering, lowercasing, first/last segment return behavior for three-or-more parts, short-value suppression, and returned `(account, container)` tuple shape. |
| B207 | 2026-08-14 | The Azure branch inside `ArtifactQueueProcessor._iac_resource_structured_candidates()` still selected Azure Blob candidates inline, keeping Azure resource-type detection, account/container reference lookup, Blob URL construction, composite-name fallback, and empty candidate suppression inside the oversized orchestrator. | Added `iac_resource_azure_blob_candidate_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the Azure branch to delegate while preserving Azure type-hint gating, `storageAccountName`/`accountName`/`account` lookup ordering, `containerName`/`container`/`name` lookup ordering, account/container lowercasing, Azure Blob URL callback binding, composite `name` fallback callback binding, no-candidate empty output behavior, and compatibility with existing IaC resource candidate callers. |
| B208 | 2026-08-14 | The Firebase branch inside `ArtifactQueueProcessor._iac_resource_structured_candidates()` still selected Firebase RTDB URL candidates inline, keeping Firebase type-hint detection, project reference lookup ordering, project-ref validation, URL construction, and invalid result suppression inside the oversized orchestrator. | Added `iac_resource_firebase_candidate_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the Firebase branch to delegate while preserving Firebase type-hint gating, `projectId`/`project-id`/`project_id`/`project`/`name` lookup ordering, project-ref validation callback binding, invalid project suppression, returned `https://{project_ref}.firebaseio.com` URL shape, and compatibility with existing IaC resource candidate callers. |
| B209 | 2026-08-14 | The Supabase branch inside `ArtifactQueueProcessor._iac_resource_structured_candidates()` still selected Supabase URL candidates inline, keeping Supabase type-hint detection, project reference lookup ordering, project-ref validation, URL construction, and invalid result suppression inside the oversized orchestrator. | Added `iac_resource_supabase_candidate_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the Supabase branch to delegate while preserving Supabase type-hint gating, `projectRef`/`project-ref`/`project_ref`/`ref`/`name` lookup ordering, project-ref validation callback binding, invalid project suppression, returned `https://{project_ref}.supabase.co` URL shape, and compatibility with existing IaC resource candidate callers. |
| B210 | 2026-08-14 | The AWS S3 branch inside `ArtifactQueueProcessor._iac_resource_structured_candidates()` still selected S3 URL candidates inline, keeping AWS S3 type-hint detection, bucket reference lookup ordering, bucket-name validation, URL construction, regex suppression, and invalid result suppression inside the oversized orchestrator. | Added `iac_resource_s3_candidate_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the AWS S3 branch to delegate while preserving `aws::s3::bucket`/`aws:s3`/`aws.s3` type-hint gating, `bucketName`/`bucket-name`/`bucket_name`/`bucket`/`name` lookup ordering, bucket validation callback binding, `[a-z0-9.-]{3,63}` regex validation, invalid bucket suppression, returned `s3://{bucket}` URL shape, and compatibility with existing IaC resource candidate callers. |
| B211 | 2026-08-14 | The GCS branch inside `ArtifactQueueProcessor._iac_resource_structured_candidates()` still selected Google Cloud Storage URL candidates inline, keeping GCS type-hint detection, bucket reference lookup ordering, bucket-name validation, URL construction, and invalid result suppression inside the oversized orchestrator. | Added `iac_resource_gcs_candidate_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the GCS branch to delegate while preserving `gcp:storage`/`google.storage`/`google::cloud::storage`/`google_storage_bucket` type-hint gating, `bucketName`/`bucket-name`/`bucket_name`/`bucket`/`name` lookup ordering, bucket validation callback binding, invalid bucket suppression, returned `gs://{bucket}` URL shape, and compatibility with existing IaC resource candidate callers. |
| B212 | 2026-08-14 | The DigitalOcean Spaces branch inside `ArtifactQueueProcessor._iac_resource_structured_candidates()` still selected Spaces URL candidates inline, keeping DigitalOcean/space type-hint detection, bucket reference lookup ordering, bucket-name validation, region lookup ordering, region regex validation, URL construction, and invalid result suppression inside the oversized orchestrator. | Added `iac_resource_digitalocean_spaces_candidate_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the DigitalOcean Spaces branch to delegate while preserving DigitalOcean/space type-hint gating, `bucketName`/`bucket-name`/`bucket_name`/`bucket`/`name` lookup ordering, bucket validation callback binding, `region`/`spaceRegion`/`space-region`/`space_region` lookup ordering, `[a-z0-9-]{2,32}` region validation, invalid bucket/region suppression, returned `https://{bucket}.{region}.digitaloceanspaces.com` URL shape, and compatibility with existing IaC resource candidate callers. |
| B213 | 2026-08-14 | `ArtifactQueueProcessor._iac_resource_structured_candidates()` still coordinated IaC resource type-hint lookup, nested properties normalization, provider lookup merging, provider candidate ordering, callback binding, and empty candidate suppression inline after the individual provider candidate helpers had been extracted. | Added `iac_resource_structured_candidates_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the processor compatibility wrapper to delegate while preserving type/resource/kind lookup ordering, empty type-hint suppression, `properties`/`config`/`inputs` child lookup, normalized property override behavior, AWS S3/GCS/DigitalOcean/Firebase/Supabase/Azure provider ordering, provider callback binding, first-seen non-empty candidate collection semantics, and compatibility with existing IaC resource candidate callers. |
| B214 | 2026-08-14 | `ArtifactQueueProcessor._parse_key_value_scalar()` still normalized scalar assignment values inline, keeping quote stripping, triple-quote stripping, comment suffix removal, trailing comma suppression, whitespace trimming, and empty-value handling inside the oversized orchestrator despite being reused by multiple artifact parser paths. | Added `parse_key_value_scalar_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the processor compatibility wrapper to delegate while preserving single/double quote stripping, triple quote stripping, inline `#`/`;`/`//` comment suffix removal for unquoted values, trailing comma removal, whitespace trimming, empty-value suppression, and compatibility with existing key-value parser callers. |
| B215 | 2026-08-14 | `ArtifactQueueProcessor._key_value_section_path()` still normalized key-value section and assignment key paths inline, keeping bracket stripping, quote removal, dot/slash splitting, lowercasing, invalid-character stripping, empty-part suppression, and tuple shaping inside the oversized orchestrator. | Added `key_value_section_path_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the processor compatibility wrapper to delegate while preserving bracket stripping, single/double quote removal, dot/slash path splitting, lowercasing, `[^a-z0-9_-]` character stripping, empty-part suppression, empty-input behavior, tuple return shape, and compatibility with existing key-value line parser callers. |
| B216 | 2026-08-14 | `ArtifactQueueProcessor._key_value_line_entry()` still classified key-value config lines inline, keeping comment/structural-line suppression, section header matching, export assignment parsing, key normalization, scalar cleanup callback use, empty-value suppression, and line-entry tuple shaping inside the oversized orchestrator. | Added `key_value_line_entry_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the processor compatibility wrapper to delegate while preserving blank/comment/JSON-ish structural-line suppression, single/double bracket section parsing, assignment/export regex behavior, section-path callback binding, scalar parser callback binding, empty raw key/value/path suppression, returned section tuple shape, returned assignment tuple shape, and compatibility with existing key-value entry aggregation callers. |
| B217 | 2026-08-14 | `ArtifactQueueProcessor._parse_key_value_entries()` still coordinated key-value config line batching, ordered line parsing, section state carry-forward, malformed line-entry suppression, assignment tuple assembly, and empty input handling inline inside the oversized orchestrator. | Added `parse_key_value_entries_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the processor compatibility wrapper to delegate while preserving ordered batch callback binding, line-entry callback binding, default `None` factory behavior, current section-path tracking, malformed line-entry suppression, assignment tuple coercion, input line ordering, empty-text behavior, and compatibility with existing key-value structured payload callers. |
| B218 | 2026-08-14 | `ArtifactQueueProcessor._key_value_structured_payload_text()` still built key-value structured payload inputs inline, keeping env-map aliasing, section map population, YAML fingerprint aliasing, section-prefixed env names, direct URL/email candidate extraction, direct candidate dedupe, and empty input behavior inside the oversized orchestrator. | Added `key_value_structured_inputs_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the processor payload method to delegate input construction while preserving env key uppercasing, hyphen-to-underscore env aliases, section map insertion order, normalized and fingerprinted section keys, section-prefixed env aliases, direct `http`/`https`/`s3`/`gs` URL capture, lowercased email capture, case-insensitive direct dedupe, returned `(env_map, section_maps, direct_candidates)` shape, and compatibility with the existing key-value payload candidate pipeline. |
| B219 | 2026-08-14 | `ArtifactQueueProcessor._key_value_structured_payload_text()` still assembled key-value structured payload output inline, keeping candidate job batching, candidate batch dispatch, family flattening, direct candidate preparation, append-entry filtering, case-insensitive dedupe, line ordering, newline joining, and malformed append-entry suppression inside the oversized orchestrator. | Added `key_value_structured_payload_lines_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the processor payload method to delegate output assembly while preserving structured candidate job callback binding, candidate validation callback binding, batch callback binding, family/direct preparation callbacks, append-entry callback binding, ordered batch default factories, malformed append-entry suppression, case-insensitive first-seen dedupe, final line ordering, empty input behavior, newline-joined return shape, and compatibility with the existing key-value payload pipeline. |
| B220 | 2026-08-14 | `ArtifactQueueProcessor._key_value_structured_payload_text()` still coordinated the full key-value payload flow inline, keeping source-hint gating, key-value entry parsing, empty-entry suppression, structured input construction, structured payload line assembly, and final payload return policy inside the oversized orchestrator. | Added `key_value_structured_payload_text_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the processor compatibility wrapper to delegate the full key-value payload flow while preserving config-name gating callback binding, parse-entry callback binding, empty-entry suppression, structured input callback binding, structured line callback binding, returned text shape, non-config empty behavior, and compatibility with existing artifact structured payload dispatch. |
| B221 | 2026-08-14 | `ArtifactQueueProcessor._strip_jsonc_comments()` still stripped JSONC comments inline, keeping string-state tracking, escaped quote handling, line-comment removal, block-comment removal, newline preservation inside block comments, unterminated block handling, and comment-marker preservation inside strings inside the oversized orchestrator. | Added `strip_jsonc_comments_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the processor compatibility wrapper to delegate while preserving line comment removal, block comment removal, newline preservation, string literal protection, escape handling, unterminated block behavior, returned text shape, and compatibility with existing JSONC document parsing callers. |
| B222 | 2026-08-14 | `ArtifactQueueProcessor._json_document_from_line()` still parsed JSONL/NDJSON candidate lines inline, keeping whitespace trimming, empty-line suppression, safe JSON loading, object/list filtering, scalar suppression, invalid-line suppression, and returned document shape inside the oversized orchestrator. | Added `json_document_from_line_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the processor compatibility wrapper to delegate while preserving safe loader callback binding, whitespace trimming before parsing, empty-line suppression, object/list return behavior, scalar/non-container suppression, invalid-line suppression, and compatibility with existing JSON document parsing callers. |
| B223 | 2026-08-14 | `ArtifactQueueProcessor._json_documents_from_text()` still coordinated JSON document extraction inline, keeping source-hint gating, empty text suppression, JSONC stripped fallback generation, direct JSON parse priority, JSONL/NDJSON ordered line fallback, object/list filtering, and returned document list shaping inside the oversized orchestrator. | Added `json_documents_from_text_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the processor compatibility wrapper to delegate while preserving config/container-image source gating callback binding, whitespace trimming, empty-text suppression, JSONC suffix detection, JSONC comment stripping callback binding, direct safe JSON parse priority, object/list direct parse return behavior, JSONL ordered batch callback binding, per-line parser callback binding, object/list line filtering, empty fallback behavior, and compatibility with existing JSON structured payload callers. |
| B224 | 2026-08-14 | `ArtifactQueueProcessor._json_structured_payload_text()` still coordinated JSON structured payload candidate extraction inline, keeping document extraction, Docker auth gating, ECS/Lambda/Amplify candidate batches, generic structured document lines, ordered batch preparation, case-insensitive dedupe, final line ordering, newline joining, and empty-document suppression inside the oversized orchestrator. | Added `json_structured_payload_text_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the processor compatibility wrapper to delegate while preserving JSON document callback binding, Docker auth predicate/candidate callback binding, ECS task callback binding, Lambda callback binding, Amplify callback/source-hint binding, generic structured document line callback binding, ordered line batch callback binding, ordered batch default factories, case-insensitive first-seen dedupe, final line ordering, empty-document behavior, newline-joined return shape, and compatibility with existing artifact structured payload dispatch. |
| B225 | 2026-08-14 | `ArtifactQueueProcessor._json_document_looks_like_docker_auth_config()` still detected Docker auth JSON documents inline, keeping dict-only gating, `.dockercfg`/`.dockerconfigjson` filename detection, YAML-normalized key lookup, and Docker auth key matching inside the oversized orchestrator. | Added `json_document_looks_like_docker_auth_config_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the processor compatibility wrapper to delegate while preserving dict-only suppression, source-name normalization, `.dockercfg`/`.dockerconfigjson` filename matches, YAML-normalized mapping callback binding, `auths`/`credhelpers`/`credsstore`/`credstore` key detection, no-match false behavior, and compatibility with existing JSON structured payload Docker auth dispatch. |
| B226 | 2026-08-14 | `ArtifactQueueProcessor._firebaserc_project_ref_url()` still converted `.firebaserc` project references inline, keeping YAML project-ref validation and Firebase realtime database URL construction inside the oversized orchestrator. | Added `firebaserc_project_ref_url_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the processor compatibility wrapper to delegate while preserving string coercion, YAML project-ref validator callback binding, invalid-ref suppression, realtime database URL formatting, and compatibility with existing `.firebaserc` structured payload extraction. |
| B227 | 2026-08-14 | `ArtifactQueueProcessor._firebaserc_structured_payload_text()` still coordinated `.firebaserc` structured payload extraction inline, keeping source-name gating, JSON payload parsing, `projects` value collection, `targets` key collection, ordered URL conversion, duplicate suppression, and newline joining inside the oversized orchestrator. | Added `firebaserc_structured_payload_text_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the processor compatibility wrapper to delegate while preserving `.firebaserc` filename gating, safe JSON loader callback binding, dict-only payload handling, `projects` values traversal, existing `targets` key traversal behavior, ordered project URL callback binding, ordered batch default factory binding, first-seen duplicate suppression, invalid URL suppression, newline-joined return shape, and compatibility with artifact structured payload dispatch. |
| B228 | 2026-08-14 | `ArtifactQueueProcessor._observability_structured_document_candidates()` still normalized observability document candidates inline, keeping default HTTP inheritance, worker-enabled node traversal, string coercion, whitespace trimming, case-insensitive duplicate suppression, empty suppression, and candidate ordering inside the oversized orchestrator. | Added `observability_structured_document_candidates_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the processor compatibility wrapper to delegate while preserving label discard behavior, observability node callback binding, inherited `http` scheme, worker-enabled traversal flag, string coercion, whitespace trimming, case-insensitive first-seen dedupe, empty candidate suppression, final candidate ordering, and compatibility with existing observability structured payload extraction. |
| B229 | 2026-08-14 | `ArtifactQueueProcessor._observability_child_candidate_values()` still dispatched observability child traversal inline, keeping child-job tuple unpacking, inherited scheme propagation, recursive node callback invocation, worker suppression for child recursion, and returned candidate list passthrough inside the oversized orchestrator. | Added `observability_child_candidate_values_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the processor compatibility wrapper to delegate while preserving child index discard behavior, child payload propagation, inherited scheme callback binding, `use_workers=False` recursion guard, returned list passthrough, and compatibility with existing observability dict/list traversal. |
| B230 | 2026-08-14 | `ArtifactQueueProcessor._observability_endpoint_jobs()` still flattened observability endpoint values inline, keeping scalar endpoint collection, nested list traversal, per-list 4096 item slicing, global 4096 job cap, scheme propagation, non-scalar suppression, and returned job ordering inside the oversized orchestrator. | Added `observability_endpoint_jobs_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the processor compatibility wrapper to delegate while preserving `str`/`int`/`float` endpoint collection, nested list recursion, per-list slice cap, total job cap, tuple shape `(raw_value, scheme)`, inherited scheme propagation, dict/non-scalar suppression, first-seen traversal order, and compatibility with existing observability target URL conversion. |
| B231 | 2026-08-14 | `ArtifactQueueProcessor._observability_scheme_candidate()` still normalized observability schemes inline, keeping string coercion, whitespace trimming, lowercase normalization, `http`/`https` allowlisting, and unsupported-value suppression inside the oversized orchestrator. | Added `observability_scheme_candidate_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the processor compatibility wrapper to delegate while preserving string coercion, whitespace trimming, lowercase normalization, allowed `http`/`https` return values, empty fallback for unsupported values, and compatibility with observability inherited-scheme and target URL callers. |
| B232 | 2026-08-14 | `ArtifactQueueProcessor._observability_target_url_candidate()` still converted observability endpoint jobs inline, keeping raw target/scheme tuple unpacking, quote trimming, template/wildcard suppression, explicit URL normalization/classification, host:port regex validation, localhost/example filtering, domain/subdomain classification, port bounds, scheme fallback, path retention, and URL formatting inside the oversized orchestrator. | Added `observability_target_url_candidate_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the processor compatibility wrapper to delegate while preserving URL normalizer callback binding, seed classifier callback binding, scheme candidate callback binding, explicit URL handling, host normalization, invalid target suppression, `localhost`/`example.com` filtering, port range checks, fallback `http` scheme behavior, path preservation, and compatibility with existing observability endpoint discovery. |
| B233 | 2026-08-14 | `ArtifactQueueProcessor._observability_structured_payload_text()` still coordinated observability structured YAML extraction inline, keeping source label gating, structured-label allowlisting, optional YAML dependency suppression, safe YAML document loading, YAML parse failure suppression, ordered document candidate extraction, ordered line batch preparation, case-insensitive duplicate suppression, empty candidate suppression, and newline joining inside the oversized orchestrator. | Added `observability_structured_payload_text_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the processor compatibility wrapper to delegate while preserving label callback binding, structured-label collection binding, YAML loader callback binding, document candidate callback binding, ordered line batch callback binding, ordered batch callback binding, YAML unavailable behavior, parse-error fallback, first-seen dedupe, final candidate ordering, newline-joined return shape, and compatibility with artifact structured payload dispatch. |
| B234 | 2026-08-14 | `ArtifactQueueProcessor._edge_proxy_structured_payload_text()` still coordinated edge proxy structured line extraction inline, keeping source label gating, structured-label allowlisting, 4096-line cap, ordered line candidate extraction, ordered line batch preparation, candidate string normalization, case-insensitive duplicate suppression, empty candidate suppression, and newline joining inside the oversized orchestrator. | Added `edge_proxy_structured_payload_text_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the processor compatibility wrapper to delegate while preserving edge proxy label callback binding, structured-label collection binding, line candidate callback binding, ordered line batch callback binding, ordered batch callback binding, line slicing, first-seen dedupe, final candidate ordering, newline-joined return shape, and compatibility with artifact structured payload dispatch. |
| B235 | 2026-08-14 | `ArtifactQueueProcessor._edge_proxy_endpoint_url_candidate()` still normalized edge proxy endpoint tokens inline, keeping slash unescaping, wrapper punctuation trimming, template/wildcard/protocol suppression, relative-path suppression, `grpc`/`h2c` downgrade, `grpcs` upgrade, schemeless URL handling, default-scheme fallback, and API-spec URL candidate normalization inside the oversized orchestrator. | Added `edge_proxy_endpoint_url_candidate_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the processor compatibility wrapper to delegate while preserving API-spec URL candidate callback binding, default scheme validation, unsupported protocol suppression, relative target suppression, grpc/grpcs/h2c scheme translation, schemeless host handling, returned URL shape, and compatibility with edge proxy, orchestration, and API client endpoint parsing callers. |
| B236 | 2026-08-14 | `ArtifactQueueProcessor._edge_proxy_line_url_candidates()` still parsed edge proxy config lines inline, keeping comment/empty suppression, Host/HostSNI rule extraction, keyed endpoint extraction, template suppression, YAML-ish value extraction, contextual endpoint token scanning, endpoint URL normalization, case-insensitive duplicate suppression, empty candidate suppression, and candidate ordering inside the oversized orchestrator. | Added `edge_proxy_line_url_candidates_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the processor compatibility wrapper to delegate while preserving injected regex pattern binding, endpoint URL callback binding, Host rule parsing, keyed-value parsing, marker suppression, fallback line scanning, YAML value trimming, first-seen dedupe, returned list shape, and compatibility with edge proxy, orchestration, and API structured payload callers. |
| B237 | 2026-08-14 | `ArtifactQueueProcessor._orchestration_annotation_endpointish_key()` still classified annotation keys inline, keeping substring marker detection for endpoint/hostname/serveralias/vhost/domain/externaldns and suffix matching for host/url/address inside the oversized orchestrator. | Added `orchestration_annotation_endpointish_key_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the processor compatibility wrapper to delegate while preserving marker matching, suffix matching, false behavior for unrelated keys, and compatibility with orchestration annotation endpoint discovery. |
| B238 | 2026-08-14 | `ArtifactQueueProcessor._orchestration_endpoint_values()` still flattened orchestration endpoint values inline, keeping `str`/`int`/`float` scalar collection, string stripping, nested list traversal, per-list 4096 slicing, global 4096 collection cap, non-list container suppression, empty-value suppression, and traversal ordering inside the oversized orchestrator. | Added `orchestration_endpoint_values_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the processor compatibility wrapper to delegate while preserving scalar coercion, list-only recursion, cap behavior, empty filtering, returned list shape, and compatibility with orchestration endpoint URL candidate extraction. |
| B239 | 2026-08-14 | `ArtifactQueueProcessor._orchestration_text_values()` still flattened orchestration text values inline, keeping string-only collection, nested list/dict traversal, per-container 4096 slicing, global 4096 collection cap, non-string scalar suppression, empty-string suppression, and original string preservation inside the oversized orchestrator. | Added `orchestration_text_values_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the processor compatibility wrapper to delegate while preserving string preservation, list/dict traversal, cap behavior, empty filtering, returned list shape, and compatibility with orchestration manifest text extraction. |
| B240 | 2026-08-14 | `ArtifactQueueProcessor._kopia_structured_payload_text()` still parsed Kopia repository config JSON inline, keeping source-name gating, safe JSON loading, storage/config merging, endpoint extraction, S3/GCS/Azure bucket URL derivation, case-insensitive dedupe, empty suppression, and newline-joined return shape inside the oversized orchestrator. | Added `kopia_structured_payload_text_for_processor()` in `forge.orchestration.artifact_processor_runtime`, exported it through `forge.orchestration`, and switched the processor compatibility wrapper to delegate while preserving injected config-name, JSON, YAML lookup, bucket validation, and managed-cloud URL callbacks plus Kopia provider extraction behavior. |
| B241 | 2026-08-14 | `ArtifactQueueProcessor` still owned Duplicacy preferences parsing inline, including source-name gating, hinted single-entry detection, list/dict JSON entry selection, storage URL extraction, S3/GCS/Azure URL normalization, cloud endpoint preservation, nested storage context merging, per-entry and final dedupe, and newline-joined return shape inside the oversized orchestrator. | Added Duplicacy preferences runtime helpers in `forge.orchestration.artifact_processor_runtime`, exported them through `forge.orchestration`, and switched the processor compatibility wrappers to delegate while preserving injected config-name, JSON, YAML lookup/normalization, ordered batch, bucket validation, and managed-cloud URL callbacks plus existing passive Duplicacy cloud candidate behavior. |
| B242 | 2026-08-14 | `ArtifactQueueProcessor` still owned Borg repository candidate normalization inline, including template/whitespace suppression, S3 endpoint and bucket extraction, GCS bucket extraction, Azure Blob URL derivation from context, SSH/SFTP userinfo stripping, SCP-style repository normalization, local Windows path suppression, and candidate dedupe inside the oversized orchestrator. | Added Borg repository runtime helpers in `forge.orchestration.artifact_processor_runtime`, exported them through `forge.orchestration`, and switched the processor compatibility wrappers to delegate while preserving injected YAML lookup, bucket validation, managed-cloud URL, and network-DSN sanitization callbacks plus existing passive Borg cloud and host candidate behavior. |
| B243 | 2026-08-14 | `ArtifactQueueProcessor._borg_structured_payload_text()` and `_borg_repository_candidates_from_env_map()` still coordinated Borg location/config parsing inline, keeping source-kind gating, raw location line extraction, key-value parsing, context/env-map construction, repository-key detection, ordered candidate expansion, `BORG_*` env repository fallback, final dedupe, and newline-joined return shape inside the oversized orchestrator. | Added Borg structured payload and env-map runtime helpers in `forge.orchestration.artifact_processor_runtime`, exported them through `forge.orchestration`, and switched the processor compatibility wrappers to delegate while preserving injected source-kind, key-value parser, key fingerprint, ordered batch, repository candidate, and env-map candidate callbacks plus existing passive Borg config behavior. |
| B244 | 2026-08-14 | `ArtifactQueueProcessor` still owned Duplicati target URL normalization inline, including target key selection from env maps, HTTP managed URL preservation, S3 endpoint and bucket extraction, GCS bucket extraction, Azure Blob URL derivation from auth/account context, empty target suppression, and bucket parsing inside the oversized orchestrator. | Added Duplicati target URL runtime helpers in `forge.orchestration.artifact_processor_runtime`, exported them through `forge.orchestration`, and switched the processor compatibility wrappers to delegate while preserving injected YAML lookup/normalization, bucket validation, and managed-cloud URL callbacks plus existing passive Duplicati SQLite target cloud candidate behavior. |
| B245 | 2026-08-14 | `ArtifactQueueProcessor._duplicati_structured_payload_text()`, `_duplicati_env_map_from_entries()`, `_looks_like_duplicati_payload_hint()`, and `_duplicati_nested_option_entries()` still coordinated Duplicati row/config parsing inline, keeping key-value parsing, nested settings/options/metadata parameter expansion, env-map normalization, Duplicati/SQLite source gating, target candidate expansion, final dedupe, and newline-joined return shape inside the oversized orchestrator. | Added Duplicati structured payload runtime helpers in `forge.orchestration.artifact_processor_runtime`, exported them through `forge.orchestration`, and switched the processor compatibility wrappers to delegate while preserving injected key-value parser, key fingerprint, target-candidate, env-map, and source-hint callbacks plus existing passive Duplicati SQLite target behavior. |
| B246 | 2026-08-14 | `ArtifactQueueProcessor._ci_text_structured_payload_text()`, `_appveyor_ci_document_candidate()`, and `_yaml_mapping_looks_like_appveyor_ci()` still parsed AppVeyor CI YAML inline, keeping source-label gating, optional YAML dependency suppression, safe YAML document loading, AppVeyor mapping detection, pipeline name normalization, fallback pipeline naming, ordered candidate processing, dedupe, and newline-joined return shape inside the oversized orchestrator. | Added AppVeyor CI runtime helpers in `forge.orchestration.artifact_processor_runtime`, exported them through `forge.orchestration`, and switched the processor compatibility wrappers to delegate while preserving injected source-label, YAML, mapping, key lookup, external-ref normalization, and ordered batch callbacks plus existing passive AppVeyor pipeline evidence behavior. |
| B247 | 2026-08-14 | `ArtifactQueueProcessor._gitpod_structured_payload_text()`, `_gitpod_document_structured_candidates()`, and `_yaml_mapping_looks_like_gitpod_config()` still coordinated Gitpod YAML parsing inline, keeping source-label gating, optional YAML dependency suppression, safe YAML document loading, document type suppression, Gitpod mapping detection, ordered candidate batch processing, dedupe, and newline-joined return shape inside the oversized orchestrator. | Added Gitpod payload runtime helpers in `forge.orchestration.artifact_processor_runtime`, exported them through `forge.orchestration`, and switched the processor compatibility wrappers to delegate while preserving injected source-label, YAML, structured candidate, mapping detection, and ordered batch callbacks plus existing passive Gitpod workspace config behavior. |
| B248 | 2026-08-14 | `ArtifactQueueProcessor._yaml_gitpod_config_structured_candidates()` and `_gitpod_repository_url_candidates()` still extracted Gitpod explicit-registry image values and `additionalRepositories` URL seeds inline, keeping repository fallback normalization, GitOps delegate precedence, ordered batch callbacks, candidate dedupe, and GitHub/GitLab/Bitbucket host-path handling inside the oversized orchestrator. | Added Gitpod config/repository runtime helpers in `forge.orchestration.artifact_processor_runtime`, exported them through `forge.orchestration`, and switched the processor compatibility wrappers to delegate while preserving injected YAML normalization, Gitpod mapping detection, container image candidate, YAML ref collection/value, GitOps repository candidate, repository suffix stripping, ordered batch, and passive static parsing behavior. |
| B249 | 2026-08-14 | The canonical CTEM release E2E proved monitoring, asset graph ownership, remediation workflow, and active validation in one engagement, but the primitives were still adjacent: no assertion required a monitoring alert to become an owner-routed remediation item, request an active-validation retest, apply the run result back to remediation, and surface that resolved/passed state in dashboard sections. | Extended `tests/integration/test_canonical_release_e2e.py` so the scheduled monitoring diff selects a `finding_added` alert, attaches a graph ownership claim, creates monitoring-alert remediation from that owner, requests a scoped lab `fix_verification` retest, runs active validation, asserts the linked remediation retest resolves as passed, and verifies dashboard remediation plus active-validation job/run sections expose the full loop. |
| B250 | 2026-08-14 | CTEM web routes had role-scoped read/write tests and workspace admin tests, but no single proof required a cross-workspace token with full CTEM permissions to be denied across connectors, connector secrets, asset graph, active validation, remediation, and monitoring route families. | Added `test_ctem_engagement_routes_are_workspace_isolated` so an alpha engagement is visible to its alpha principal, while a beta principal carrying full CTEM read/write permissions cannot list, read, or mutate the alpha engagement across those route families and receives only `404 Engagement not found.` without leaking alpha identifiers. |
| B251 | 2026-08-14 | Dashboard release verification was not hermetic: `generate_dashboard()` defaulted to legacy cwd engagement discovery even when callers supplied an explicit `data_dir`, so reporting tests could silently render hundreds of local `.forge_data/engagements` DBs and time out in dirty operator workspaces. | Made direct dashboard generation explicit-data-dir scoped by default, kept operator CLI refresh paths opted into legacy discovery, and added `test_generate_dashboard_defaults_to_explicit_data_dir` so temp dashboard builds ignore cwd legacy DBs unless a caller explicitly requests legacy inclusion. |
| B252 | 2026-08-14 | The theprawnhunter scheduled target-import task no longer failed through inline `python -c` quote stripping, but live logs showed optional stale-watchdog process cleanup still performing WMI Python-process scans that repeatedly timed out before imports, leaving a scheduler instance stuck in `Running`. | Made stale watchdog helper process scanning opt-in with `-StaleHelperProcessCleanup`, kept stale temp-file cleanup enabled by default, preserved explicit stale import-process cleanup behavior, and verified the watchdog self-test plus launcher/import suites while stopping the old stuck scheduled-task instance so the next run can load the patched script. |
| B253 | 2026-08-14 | Asset-graph attack paths carried risk tags/factors, but they did not explicitly call out Wiz/Orca-style toxic combinations or compact cloud account/workload/identity context in the exposure summary, making public-sensitive-data-plus-privileged-IAM chains harder to review. | Added path-level toxic-combination labels and scrubbed cloud context summaries for account refs, regions, sensitivity tiers, workloads, and identity refs; graph regressions now prove public sensitive S3 exposure chained to wildcard/admin IAM is surfaced as `public_sensitive_data_exposure`, `privileged_identity_to_sensitive_data`, and `public_to_privileged_sensitive_data_path`. |
| B254 | 2026-08-14 | Secret lifecycle prevention plans grouped free/local commands by pre-commit, pull-request, and push workflow, but did not return reviewable artifact template content, leaving operators to hand-compose `.pre-commit-config.yaml`, GitHub Actions, or pre-push hook snippets from commands. | Added value-free artifact templates to each prevention workflow using existing Gitleaks, detect-secrets, TruffleHog, and provider push-protection commands; tests now prove the plan includes copyable pre-commit, pull-request CI, and pre-push hook content without raw or encrypted secret material. |
| B255 | 2026-08-14 | Remediation workflow had live optional adapters for local JSONL/stdout, GitHub Issues, Jira, ServiceNow, Tines, Splunk HEC, and Torq, but operators still lacked a free-first review-only handoff artifact that showed exactly what would be sent before enabling paid or write-capable systems. | Added `remediation_ticket_handoff_plan()` plus `forge remediation handoff-plan` to generate sanitized per-connector request/body templates without network calls, file writes, ticket-event inserts, or secret-bearing webhook paths; remediation regressions now prove GitHub/Jira/ServiceNow/SOAR/SIEM previews are redacted and operator-reviewable before live sync. |
| B256 | 2026-08-15 | Remediation integrations could push or preview ticket/SOAR/SIEM payloads, but they still lacked a free-first inbound status reconciliation path, leaving external ticket closure/reopen state disconnected from Forge remediation status unless an operator edited items manually. | Added `import_remediation_ticket_statuses()` plus `forge remediation import-ticket-statuses` for JSON/JSONL ticket status exports; tests now prove dry-run safety, deterministic external-state mapping, unmatched/unknown status review handling, audit receipts, and secret-stripped ticket URLs before applying resolved/risk/open/in-progress state back to remediation items. |
| B257 | 2026-08-15 | Inbound ticket-status reconciliation was implemented for one engagement at a time, but scheduled/operator imports still could not consume a shared JSONL export across all engagement DBs in `FORGE_DATA_DIR`, limiting practical bidirectional workflow use. | Added data-dir batch ticket-status import through `import_remediation_ticket_statuses_for_data_dir()` and `forge remediation import-ticket-statuses --data-dir`; tests now prove shared exports are grouped by `engagement_id`, dry-run/apply summaries aggregate per DB, foreign engagements are preserved for review, and matched ticket closure updates the intended engagement item. |
| B258 | 2026-08-15 | Data-dir ticket-status imports could run from the CLI, but operators still lacked a repeatable Windows scheduled-task wrapper with dry-run-by-default behavior, timeout logging, and explicit apply gating. | Added `run_remediation_ticket_status_import_task.ps1` and `install_remediation_ticket_status_import_task.ps1` so shared JSON/JSONL ticket exports can be reconciled on a schedule; launcher regressions and self-test now prove the task defaults to `--dry-run`, requires explicit `-Apply` for DB mutation, redirects stdout/stderr, budgets execution time, and installs with `MultipleInstances IgnoreNew`. |
| B259 | 2026-08-15 | The remediation ticket-status scheduled importer existed, but `forge doctor` did not surface its scripts, status-file readiness, scheduled-task state, or dry-run/apply setup path, leaving operator discovery weaker than the rest of the setup flow. | Added a value-free `Remediation Ticket Status Import` doctor row that stays `OFF` until configured, verifies runner/installer scripts, checks the configured JSON/JSONL status file exists without reading contents, queries the Windows scheduled task only when relevant, and guides dry-run-first installation; doctor regressions prove ready and missing-task/file states without printing status export secrets. |
| B260 | 2026-08-15 | Ticket-status imports could trust external closure directly, but enterprise remediation workflows often require internal retest/fix verification before a ticket-system `closed` state is treated as resolved in Forge. | Added `--close-policy require_retest_for_resolved` for single-engagement, data-dir, and scheduled ticket-status imports; external fixed/closed states can now move items to `retest_pending` with `retest_status=pending` and policy metadata instead of `resolved`, preserving dry-run/apply behavior and making fix verification explicit before closure. |
| B261 | 2026-08-15 | Remediation integrations had preview, sync, inbound status import, scheduler, doctor, and close-policy controls, but no structured operator runbook tying per-system setup, dry-run-first review, status import, scheduling, and closure policy into one value-free artifact. | Added `remediation_integration_runbook()` plus `forge remediation integration-runbook` to emit secret-free setup/runbook JSON for local JSONL/stdout, GitHub Issues, Jira, ServiceNow, Tines, Splunk HEC, and Torq; tests now prove it includes dry-run handoff, sync, status import, scheduled importer, and retest-required closure policy guidance without network calls, file writes, or secret-bearing URLs. |
| B262 | 2026-08-15 | Asset-graph choke points had path counts and blast-radius counts, but they did not summarize the reachable cloud/data/identity/workload context that makes Wiz/Orca-style triage useful when prioritizing a shared control point. | Added `blast_radius_summary` to choke points with reachable counts, critical asset refs, entity/risk-tier counts, risk tags/factors, toxic combinations, and scrubbed cloud context; graph regressions now prove finding choke points summarize proof/ownership reachability while cloud-asset choke points expose account, region, sensitivity, workload, identity, and toxic-combination blast radius. |
| B263 | 2026-08-15 | Choke-point `blast_radius_summary` existed in asset-graph JSON, but the dashboard/report rows still showed only counts, score, remediation, and action, forcing operators to inspect raw graph payloads to see cloud account, sensitivity, identity, workload, and toxic-combination context. | Extended asset-graph choke-point report rows with critical asset refs, risk mix, toxic combinations, and scrubbed cloud context; dashboard regressions now prove a public sensitive S3 choke point with privileged IAM/workload context renders account, sensitivity, critical refs, and `public_sensitive_data_exposure` without leaking IAM tokens. |
| B264 | 2026-08-15 | `forge doctor --json` exposed flat checks and per-row remediation, but the operator setup path still lacked one machine-readable action plan that grouped free runnable connectors, missing local binaries, optional keys, catalog-only rows, gated active-validation plugins, and hidden paid adapters. | Added connector `action_items` to `DoctorCheck`, a top-level doctor `action_plan`, and a `Connector Action Plan` row with install/run/configure/review/gating phases; doctor regressions now prove the plan is secret-free, includes free runnable/missing-binary/optional-key/catalog-only/active-validation/paid groups, and still preserves the existing table/check payload shape. |
| B265 | 2026-08-15 | The doctor action plan grouped connector readiness, but live LLM provider probing, paid-backend enablement, and live active-validation gates still lived as separate row text, leaving provider discovery and BAS-style live validation safety outside the machine-readable setup path. | Added structured action items to paid LLM, active-validation, and LLM provider checks so `doctor_payload().action_plan` now includes `review_paid_llm_backends`, `enable_live_validation_only_after_roe`, and `run_live_provider_probes_if_intended`; doctor regressions prove static, live, missing-provider, paid-enabled, and live-validation-enabled states are reflected without secret values. |
| B266 | 2026-08-15 | README public command coverage had a group-level drift guard, but it could still miss newly added public modular subcommands such as remediation integration/status commands or workspace audit, recreating the older “7 commands” documentation drift problem at command level. | Added a CLI registry regression that compares README public command lines against every registered public modular subcommand for targets, monitoring, remediation, active-validation, connectors, standards, workspaces, and retention; README now documents `handoff-plan`, `integration-runbook`, `import-ticket-statuses`, and `workspaces audit`, and the full CLI registry suite proves the guard. |
| B267 | 2026-08-15 | README command docs still had default/option drift risk: standalone `forge report generate` uses `--provider` while kill-chain uses `--report-provider`, demo proof packs have a stable default engagement id, `--engagement` auto-derivation only applies to kill-chain, and doctor `action_plan` IDs are snake_case JSON contracts rather than prose labels. | Added CLI help-backed README regressions for report provider defaults, demo proof-pack default engagement id, common engagement-flag wording, and exact doctor action-plan IDs; README now distinguishes `--provider` from `--report-provider`, documents the `9901` demo default, narrows the `--engagement` default claim, and lists exact action IDs. |
| B268 | 2026-08-15 | Demo proof packs generated DB/report/dashboard/graph/audit/standards artifacts, but the proof manifest only listed surfaces and counts, leaving operators to infer which roadmap gaps were actually demonstrated; the run audit manifest also omitted explicit dashboard and standards export coverage. | Added manifest `proof_assertions` that explicitly map continuous monitoring, asset graph, remediation workflow, active validation, secrets lifecycle, standards exchange, dashboard evidence, audit manifest bundle, and free-local safety to artifact/count evidence; demo regressions now inspect graph JSON, monitoring/remediation/active-validation/secret lifecycle DB rows, STIX/TAXII standards markers, dashboard provenance, and audit manifest artifact coverage. Run audit manifests can include explicit report-directory artifact paths from run metadata, so the demo audit bundle now covers dashboard and standards exports while preserving manifest verification. |
| B269 | 2026-08-15 | The CTEM workspace-isolation proof covered core connector, asset graph, remediation, active-validation, and monitoring routes, but enterprise read surfaces such as report artifact downloads, logs, run/progress, task/queue/worker telemetry, legacy numeric asset/finding endpoints, and `/ws/progress` were not proven against cross-workspace leakage. | Extended the CTEM RBAC integration test so an alpha engagement with report and log artifacts is readable by alpha, while a beta principal with broad read/write permissions cannot list, download, or subscribe to alpha artifacts, logs, run/progress telemetry, queue/task/worker state, numeric asset/finding routes, or the progress websocket; denied responses stay 404/1008 and do not leak alpha slug/name/body text. |
| B270 | 2026-08-15 | Active-validation previews exposed approval/ROE/scope/live gates, but persisted run evidence did not consistently record which gates passed or blocked execution, and approved live jobs lacked a focused proof that out-of-scope targets stop before any HTTP request. | Active-validation run evidence now includes compact `gates` and `budgets` receipts for dry-run, lab, read-only-live blocked, live-disabled, scope-denied, and live-executed paths. Regressions prove dry/lab runs remain offline, live-disabled runs record a blocked live gate, successful live runs record the two-request budget, and an approved out-of-scope live HTTP job returns `scope_manifest_denied` with `network_execution=false`, zero live budget, no HTTP client requests, and redacted target material. |
| B271 | 2026-08-15 | Self-host packaging required web/bootstrap/Postgres/audit env names, but the operator preflight did not enforce the documented production value contract and Helm/Compose secret wiring did not consistently include the engagement encryption key. | Compose and Helm now require/project `FORGE_ENGAGEMENT_KEY` through inline, existing-secret, and ExternalSecret modes. `scripts/self_host_operator.sh preflight` validates required production env names plus redacted value policy: web secret, bootstrap token, and engagement key must be at least 32 characters, public base URL must be HTTPS, and audit scope must match a safe token pattern. Packaging regressions prove missing-key and weak-value failures without leaking env values; `py_compile`, Ruff, and the focused Docker packaging suite passed (`13 passed, 1 skipped` because Helm is not installed locally). |
| B272 | 2026-08-15 | `forge/cli.py` still carried pure kill-chain seed helper policy inside the command body, making seed classification/routing maintenance part of the monolith even after command registration and finalization splits. | Extracted company/person detection, managed-host exclusion, placeholder-IP detection, host context JSON shaping, root-domain normalization, and initial-seed dedupe policy to `forge.utils.kill_chain_seed_helpers`, with direct helper regressions plus existing kill-chain option and CLI registry coverage. `py_compile`, Ruff, and the focused helper/kill-chain/registry suite passed (`34 passed`). |
| B273 | 2026-08-15 | Initial kill-chain seed canonicalization and route preparation still lived inside `forge.cli.kill_chain`, leaving email/domain/IP/cloud-ref normalization and initial seed route payload shaping coupled to the CLI body. | Moved canonical initial seed value shaping, classified seed entry preparation, hostname/domain derivation, reverse-DNS injection, and initial seed route payload construction into `forge.utils.kill_chain_seed_helpers`. CLI now keeps only command-specific classifier/canonicalizer wiring while helper regressions prove injected URL/cloud normalization, reverse-DNS domain derivation, managed-host exclusion, and route payload compatibility; focused verification passed (`37 passed`). |
| B274 | 2026-08-15 | Kill-chain cloud URL seed parsing still lived inline in `forge.cli.kill_chain`, coupling managed-hosting provider pattern maintenance to the CLI body even though cloud-asset persistence was the only DB-bound part. | Extracted URL-to-cloud-asset seed-ref parsing to `forge.utils.kill_chain_seed_helpers.extract_cloud_asset_seed_refs`; `forge.cli.kill_chain` now keeps only the DB promotion wrapper and literal cloud-ref merge. Helper regressions cover Supabase, Firebase, S3, DigitalOcean Spaces, GCS, Azure Blob/static website, Cloudflare Pages/Workers, GitHub Pages, Vercel, invalid URLs, and overlap dedupe; focused verification passed (`39 passed`). |
| B275 | 2026-08-15 | Kill-chain cloud-asset seed promotion still owned SQL inserts/upserts inside `forge.cli.kill_chain`, keeping DB persistence policy coupled to the CLI after the URL parser extraction. | Added `forge.orchestration.seed_promotion` for kill-chain cloud seed DB promotion. `promote_cloud_asset_seed_refs` preserves literal/cloud-URL merge, `INSERT OR IGNORE`, source labels, and swallowed insert failures; `promote_pending_cloud_targets` preserves ref normalization, malformed-target skipping, provider-identifier upgrade policy, and rowcount accounting. CLI now keeps only local wrappers with engagement context; focused SQLite/helper/CLI verification passed (`42 passed`). |
| B276 | 2026-08-15 | Kill-chain seed lookup and relation insertion still lived inside `forge.cli.kill_chain`, keeping generic engagement seed and `seed_relations` persistence coupled to the CLI after cloud seed promotion was split. | Added `lookup_engagement_seed_id` and `insert_seed_relation` to `forge.orchestration.seed_promotion`, preserving engagement-scoped lookup, invalid-row/error-to-None behavior, missing/self relation skips, `INSERT OR IGNORE`, sorted evidence JSON, swallowed relation insert failures, and local CLI wrappers. Focused SQLite/helper/CLI verification passed (`45 passed`). |
| B277 | 2026-08-15 | Social URL and email-localpart seed cross-reference promotion still lived inside `forge.cli.kill_chain`, keeping profile-context expansion, email normalization, seed upserts, and relation evidence policy embedded in the CLI after generic persistence helpers were split. | Moved social URL and email-localpart promotion algorithms to `forge.orchestration.seed_promotion` behind injected upsert/classifier/synthesis helpers. The extraction preserves unknown-platform skips, URL-seed lookup gating, username/company/name profile expansion, Bluesky domain-handle promotion, email normalization, empty-handle skips, discovered-email creation, username creation, relation evidence/confidence, and CLI compatibility wrappers. Focused SQLite/helper/CLI verification passed (`47 passed`). |
| B278 | 2026-08-15 | Kill-chain engagement-run metadata and websocket progress payload shaping still lived inside `forge.cli.kill_chain`, duplicating run-tracking normalization policy near finalization/reporting code after queue metrics and run trackers had moved to orchestration. | Added `kill_chain_engagement_run_metadata` and `current_run_progress_payload` to `forge.orchestration.run_tracking` and exported them through `forge.orchestration`. CLI now keeps only command-specific counters/policy inputs while orchestration owns recent-step trimming, ETA coercion, queue metric normalization, pending work fields, runtime metadata merge, and progress event shape. Focused run-tracking/seed-helper/CLI verification passed (`70 passed`). |
| B279 | 2026-08-15 | Kill-chain progress publishing still kept console-markup stripping and phase inference inside `forge.cli.kill_chain`, leaving progress event normalization split between CLI-local helpers and `forge.orchestration.run_tracking`. | Added `strip_console_markup` and `infer_kill_chain_run_phase` to `forge.orchestration.run_tracking` and exported them through `forge.orchestration`. CLI progress publishing now delegates Rich-tag stripping, whitespace collapse, iteration label parsing, numeric fan-out prefix parsing, normalized phase slugging, and fallback phase behavior. Focused run-tracking/seed-helper/CLI verification passed (`72 passed`). |
| B280 | 2026-08-15 | `_publish_run_progress` still mutated kill-chain run-progress state inline in `forge.cli.kill_chain`, keeping duplicate suppression, timestamp/elapsed shaping, recent-step append/trim policy, and phase assignment in the CLI after the underlying normalization helpers were extracted. | Added `update_kill_chain_run_progress_state` to `forge.orchestration.run_tracking` and exported it through `forge.orchestration`. CLI progress publishing now computes elapsed time and timestamp, delegates state mutation, and flushes only when the helper reports a change. Regressions preserve empty-step skips, duplicate suppression, forced duplicate publication, Rich-cleaned/truncated step/message fields, phase inference, elapsed rounding, timestamp propagation, and eight-entry recent-step retention. Focused run-tracking/seed-helper/CLI verification passed (`75 passed`). |
| B281 | 2026-08-15 | Artifact processor cumulative progress counters still merged inside `forge.cli.kill_chain`, leaving local artifact intake and artifact summary counter policy embedded in the CLI next to progress flushing. | Added `update_artifact_processor_cumulative_metrics` to `forge.orchestration.run_tracking` and exported it through `forge.orchestration`. CLI now delegates cumulative artifact progress state mutation and only flushes afterward. Regressions preserve non-dict queue recovery, existing counter carry-forward, local intake accumulation, invocation counting, processed/failed/skipped/firebase/supabase/discovered seed accumulation, negative delta clamping, and unrelated queue metric preservation. Focused run-tracking/seed-helper/CLI verification passed (`77 passed`). |
| B282 | 2026-08-15 | Resume-time artifact queue progress restoration still copied prior `artifact_processor` and `artifact_processor_cumulative` queue metrics inline in `forge.cli.kill_chain`, keeping payload validation/coercion and prior-run artifact metric replay in the CLI. | Added `restore_prior_artifact_queue_metrics` to `forge.orchestration.run_tracking` and exported it through `forge.orchestration`. CLI now keeps DB/JSON lookup local and delegates prior artifact metric replay. Regressions preserve malformed payload no-op behavior, partial artifact group restore from bad state, string/int/None count coercion, unrelated queue metric preservation, ignored non-artifact groups, and artifact processor/cumulative replacement semantics. Focused run-tracking/seed-helper/CLI verification passed (`80 passed`). |
| B283 | 2026-08-15 | Kill-chain stop/pause run-control marker cleanup, marker payload parsing, malformed-marker fallback, and engagement-run metadata flag lookup still lived inline in `forge.cli.kill_chain`, keeping runner lifecycle control policy inside the CLI body after run tracking had moved to orchestration. | Added `clear_run_control_marker_paths`, `read_run_control_marker_request`, and `run_control_request_from_run_metadata` to `forge.orchestration.run_tracking` and exported them through `forge.orchestration`. CLI now keeps marker paths and command wiring local while orchestration owns tolerant marker deletion, missing/malformed marker handling, non-dict marker fallback, read-only metadata lookup, bad JSON no-op behavior, and flag-gated payload return. Focused run-tracking/seed-helper/CLI verification passed (`84 passed`). |
| B284 | 2026-08-15 | Remote artifact URL scope decisions still lived inline in `forge.cli.kill_chain`, keeping URL normalization, dry-run/no-manifest behavior, scope-manifest URL validation, denial source shaping, and invalid URL handling in the CLI next to artifact processor wiring. | Added `remote_artifact_url_scope_decision` to `forge.orchestration.artifact_queue` and re-exported it through `forge.orchestration.artifacts` and `forge.orchestration`. CLI now delegates pure remote artifact URL scope policy while retaining audit and processor callback wiring. Regressions preserve empty/invalid URL rejection, missing-manifest fail-closed behavior, dry-run no-manifest allowance, hostname normalization, manifest validator entry shape, allow/deny handling, and denial source propagation. Focused artifact/CLI verification passed (`323 passed`). |
| B285 | 2026-08-15 | Kill-chain stop/pause interrupt transition shaping still lived inline in `forge.cli.kill_chain`, leaving stop-over-pause precedence, default request context, lifecycle metadata, audit action selection, dashboard reason, and console label policy embedded in the CLI after marker parsing moved to run tracking. | Added `RunControlInterruptTransition` and `run_control_interrupt_transition` to `forge.orchestration.run_tracking` and exported them through `forge.orchestration`. CLI now keeps side effects local while delegating pure stop/pause transition policy. Regressions preserve stop precedence over pause, cancelled/paused lifecycle metadata, pause resume recommendation, default requested-by/reason values, audit action/status/dashboard/console labels, and no-op behavior when no request exists. Focused run-tracking/seed-helper/CLI verification passed (`88 passed`). |
| B286 | 2026-08-15 | Module seed-run finalization batch orchestration still lived inline in `forge.cli.kill_chain`, keeping prep/apply batch labels, parallel prep logging, order-preserving apply policy, and finish callback wiring in the CLI even though the seed finalization entry/apply primitives were already in run tracking. | Added `finalize_seed_run_batch` to `forge.orchestration.run_tracking` and exported it through `forge.orchestration`. CLI now delegates module seed-run finalization coordination while keeping local compatibility wrappers for scattered one-off call sites. Regressions preserve tracker/empty-handle no-op behavior, parallel prep log shape, prep/apply progress labels, order-preserved apply note, metadata precedence, error/status/output propagation, and finish callback invocation. Focused run-tracking/seed-helper/CLI verification passed (`90 passed`). |
| B287 | 2026-08-15 | Artifact source metadata parsing, source-row candidate normalization, classification reduction shaping, ordered source candidate accumulation, and artifact queue total halt semantics still lived inline in `forge.cli.kill_chain`, keeping artifact discovery glue in the CLI after queue persistence and remote-scope policy had moved to orchestration. | Added `artifact_source_metadata`, `prepare_artifact_source_candidate_item`, `prepare_artifact_source_reduction_item`, `prepare_artifact_classification_reduction_item`, `apply_artifact_source_candidate_item`, and `apply_artifact_queue_total_item` to `forge.orchestration.artifact_queue`, re-exported through `forge.orchestration.artifacts` and `forge.orchestration`. CLI wrappers now delegate to artifact queue helpers. Regressions preserve metadata allow-listing, alias handling, list bounds/filtering, crawl/seed row shaping, source/seed/artifact normalization, candidate append order, and negative queue total halt behavior. Focused artifact/CLI verification passed (`327 passed`). |
| B288 | 2026-08-15 | The artifact URL discovery coordinator still orchestrated source prep, source reduction, source apply, artifact classification, classification reduction, queue-candidate dedupe, queue writes, and total aggregation inline in `forge.cli.kill_chain`, keeping artifact queue batch policy in the CLI after the pure helper pieces moved to orchestration. | Added `queue_discovered_artifact_candidates` to `forge.orchestration.artifact_queue`, re-exported through `forge.orchestration.artifacts` and `forge.orchestration`. CLI now supplies DB source rows and queue-write callbacks while artifact queue orchestration owns batch labels, parallel parse logging, apply order notes, classification callback dispatch, URL dedupe, queue write ordering, and negative-total halt behavior. Focused artifact/CLI verification passed (`328 passed`). |
| B289 | 2026-08-15 | The post-processor static artifact metadata sweep still opened the engagement DB, selected completed local artifacts, invoked the passive parser, wrote bounded metadata JSON, committed, logged success, and swallowed parser/setup failures inline in `forge.cli.kill_chain`, keeping artifact queue metadata refresh policy in the CLI. | Added `sweep_completed_artifact_metadata` to `forge.orchestration.artifact_queue`, re-exported through `forge.orchestration.artifacts` and `forge.orchestration`. CLI now keeps import/logging callbacks local while artifact queue persistence owns completed-row selection, local-path existence checks, parser result normalization, bounded metadata updates, commit/log behavior, and best-effort failure suppression. Focused artifact verification passed (`300 passed`). |
| B290 | 2026-08-15 | Artifact processing summary log formatting still lived inline in two `forge.cli.kill_chain` call sites, duplicating the processed/firebase/supabase/skipped policy for initial processing and per-iteration K3 processing. | Added `artifact_processing_summary_log_message` to `forge.orchestration.artifacts` and exported it through `forge.orchestration`. CLI now logs only when orchestration returns a message, preserving the previous processed-or-skipped gate and exact summary fields. Focused artifact verification passed (`301 passed`). |
| B291 | 2026-08-15 | Artifact queue count log formatting still lived inline in `forge.cli.kill_chain` for local artifact intake and per-iteration K2 discovered artifact URL queue messages, keeping positive-count gating and Rich-marked message text duplicated in the CLI. | Added `local_artifact_intake_log_message` and `discovered_artifact_queue_log_message` to `forge.orchestration.artifact_queue`, re-exported through `forge.orchestration.artifacts` and `forge.orchestration`. CLI now keeps labels and metric updates local while artifact queue helpers own zero/negative suppression and exact local/discovered queue message formatting. Focused artifact verification passed (`302 passed`). |
| B292 | 2026-08-15 | Seed synthesis summary log formatting still lived inline in two `forge.cli.kill_chain` call sites, with the initial synthesis log reporting corroborated count and the per-iteration K4 log reporting root-domain count. | Added `synthesis_summary_log_message` to `forge.orchestration.synthesis` and exported it through `forge.orchestration`. CLI now keeps labels and root-domain refresh side effects local while synthesis owns the seeds/relations positive gate and the initial-versus-K4 message shapes. Focused synthesis/artifact/CLI verification passed (`348 passed`). |
| B293 | 2026-08-15 | Deterministic finding synthesis audit/log result formatting still lived inline in `forge.cli.kill_chain`, coupling the CLI to inserted/updated/removed/active finding summary text and severity-summary JSON shaping. | Added `finding_synthesis_audit_result` and `finding_synthesis_log_message` to `forge.deterministic_findings`. CLI now keeps audit/log side effects local while deterministic findings owns pass-label audit result shaping, sorted severity JSON serialization, and inserted/updated/removed positive-gate log formatting. Focused deterministic/artifact/synthesis/CLI verification passed (`368 passed`). |
| B294 | 2026-08-15 | Resume-time abandoned seed-run recovery logging still formatted the positive-count message inline in `forge.cli.kill_chain`, keeping seed-run lifecycle display policy in the CLI after run tracking owned the recovery operation. | Added `abandoned_seed_run_recovery_log_message` to `forge.orchestration.run_tracking` and exported it through `forge.orchestration`. CLI now keeps recovery execution and `_log` side effects local while run tracking owns zero/negative suppression and exact abandoned seed-run recovery message formatting. Focused run-tracking/artifact/synthesis/deterministic/CLI verification passed (`412 passed`). |
| B295 | 2026-08-15 | Resume-time persisted fan-out reuse logging still formatted the positive-count message inline in `forge.cli.kill_chain`, keeping resume display policy for reused seed/loop targets in the CLI. | Added `persisted_fanout_resume_reuse_log_message` to `forge.orchestration.run_tracking` and exported it through `forge.orchestration`. CLI now keeps reuse counting and `_log` side effects local while run tracking owns zero/negative suppression and exact persisted fan-out reuse message formatting. Focused run-tracking/artifact/synthesis/deterministic/CLI verification passed (`413 passed`). |
| B296 | 2026-08-15 | Resume-skip completed-target label/message shaping still lived inline in `forge.cli.kill_chain`, including the central resume-skip prepared-log helper and passive-domain schedule reduction skip logs. | Added `resume_completed_skip_log_entry` to `forge.orchestration.run_tracking` and exported it through `forge.orchestration`. CLI now keeps prepared-log emission local while run tracking owns normalized stage/target label shaping and the exact completed-target resume-skip message. Focused run-tracking/artifact/synthesis/deterministic/CLI verification passed (`414 passed`). |
| B297 | 2026-08-15 | `ArtifactQueueProcessor` still owned GoReleaser scalar flattening, templated image candidate callback binding, and blob bucket URL normalization inline, keeping passive GoReleaser config extraction details in the oversized orchestrator. | Added `goreleaser_scalar_values_for_processor`, `goreleaser_image_template_values_for_processor`, and `goreleaser_blob_bucket_value_for_processor` to `forge.orchestration.artifact_processor_runtime` and exported them through `forge.orchestration`. The processor compatibility wrappers now delegate while preserving bounded scalar traversal, explicit-registry image template extraction, S3/GCS provider handling, bucket validation callbacks, and empty/unsupported suppression. Focused artifact processor runtime verification passed (`234 passed`). |
| B298 | 2026-08-15 | GoReleaser root mapping/path-hint classification still lived inline in `ArtifactQueueProcessor._yaml_mapping_looks_like_goreleaser_config`, keeping passive GoReleaser config detection policy in the oversized orchestrator. | Added `yaml_mapping_looks_like_goreleaser_config_for_processor` to `forge.orchestration.artifact_processor_runtime` and exported it through `forge.orchestration`. The processor compatibility wrapper now delegates while preserving root marker matching, docker/blob marker pairing, path-hint fallback behavior, and unrelated-map suppression. Focused artifact processor runtime verification passed (`235 passed`). |
| B299 | 2026-08-15 | GoReleaser structured-candidate coordination still lived inline in `ArtifactQueueProcessor._yaml_goreleaser_config_structured_candidates`, coupling config classification, root traversal callback binding, candidate string normalization, empty suppression, and case-insensitive dedupe to the oversized orchestrator. | Added `yaml_goreleaser_config_structured_candidates_for_processor` to `forge.orchestration.artifact_processor_runtime` and exported it through `forge.orchestration`. The processor compatibility wrapper now delegates while preserving classifier gating, root traversal with `use_workers=True`, candidate trimming, first-seen ordering, case-insensitive dedupe, and empty/unrelated config suppression. Focused artifact processor runtime verification passed (`236 passed`). |
| B300 | 2026-08-15 | GoReleaser child-node dispatch still lived inline in `ArtifactQueueProcessor._yaml_goreleaser_child_candidate_values_for_node`, coupling key fingerprinting, docker-path image-template detection, image candidate extraction, child-path construction, and non-worker recursive traversal to the oversized orchestrator. | Added `yaml_goreleaser_child_candidate_values_for_node_for_processor` to `forge.orchestration.artifact_processor_runtime` and exported it through `forge.orchestration`. The processor compatibility wrapper now delegates while preserving key fingerprint callback binding, docker/dockers/dockermanifests image-template gating, image candidate ordering, child-path propagation, recursive traversal with `use_workers=False`, and non-docker image suppression. Focused artifact processor runtime verification passed (`238 passed`). |
| B301 | 2026-08-15 | GoReleaser recursive candidate-value traversal still lived inline in `ArtifactQueueProcessor._yaml_goreleaser_candidate_values_for_node`, coupling blob-path bucket extraction, ordered child job construction, worker-pool selection, list recursion bounds, and scalar suppression to the oversized orchestrator. | Added `yaml_goreleaser_candidate_values_for_node_for_processor` to `forge.orchestration.artifact_processor_runtime` and exported it through `forge.orchestration`. The processor compatibility wrapper now delegates while preserving blob-path candidate ordering, ordered child-job payload shape, worker versus local traversal behavior, list item cap, recursive `use_workers=False` behavior, and scalar empty results. Focused artifact processor runtime verification passed (`240 passed`). |
| B302 | 2026-08-15 | GoReleaser child-job adapter unpacking still lived inline in `ArtifactQueueProcessor._yaml_goreleaser_child_candidate_values`, keeping ordered child job tuple interpretation in the oversized orchestrator after the node dispatcher moved out. | Added `yaml_goreleaser_child_candidate_values_for_processor` to `forge.orchestration.artifact_processor_runtime` and exported it through `forge.orchestration`. The processor compatibility wrapper now delegates while preserving ignored index behavior, `(key, child, path)` unpacking, and child-node callback binding. Focused artifact processor runtime verification passed (`241 passed`). |
| B303 | 2026-08-15 | Git repository suffix stripping still lived inline in `ArtifactQueueProcessor._strip_git_repository_suffix`, keeping repository URL cleanup policy in the oversized orchestrator even though Gitpod/GitOps repository normalization already used it as a callback. | Added `strip_git_repository_suffix_for_processor` to `forge.orchestration.artifact_processor_runtime` and exported it through `forge.orchestration`. The processor static compatibility wrapper now delegates while preserving string coercion, whitespace trimming, trailing slash stripping, case-insensitive `.git` suffix removal, and no-suffix passthrough. Focused artifact processor runtime verification passed (`242 passed`). |
| B304 | 2026-08-15 | GitOps repository child-job adapter unpacking still lived inline in `ArtifactQueueProcessor._yaml_gitops_repository_child_values`, keeping ordered child job tuple interpretation in the oversized orchestrator. | Added `yaml_gitops_repository_child_values_for_processor` to `forge.orchestration.artifact_processor_runtime` and exported it through `forge.orchestration`. The processor compatibility wrapper now delegates while preserving ignored index behavior, child extraction, and recursive repository-value traversal with `use_workers=False`. Focused artifact processor runtime verification passed (`243 passed`). |
| B305 | 2026-08-15 | GitOps repository candidate URL normalization still lived inline in `ArtifactQueueProcessor._yaml_gitops_repository_candidates`, keeping HTTP(S), OCI/docker, git shorthand, SSH/git URL conversion, suffix stripping, and dedupe policy in the oversized orchestrator. | Added `yaml_gitops_repository_candidates_for_processor` to `forge.orchestration.artifact_processor_runtime` and exported it through `forge.orchestration`. The processor compatibility wrapper now delegates through injected URL normalization, container image, and repository suffix callbacks while preserving quoted-value cleanup, unsupported-value suppression, and case-insensitive candidate dedupe. Focused artifact processor runtime verification passed (`245 passed`). |
| B306 | 2026-08-15 | GitOps repository mapping candidate coordination still lived inline in `ArtifactQueueProcessor._yaml_gitops_repository_candidates_from_mapping`, keeping worker-enabled value collection, ordered candidate batch conversion, and case-insensitive cross-value dedupe in the oversized orchestrator. | Added `yaml_gitops_repository_candidates_from_mapping_for_processor` to `forge.orchestration.artifact_processor_runtime` and exported it through `forge.orchestration`. The processor compatibility wrapper now delegates through injected value, candidate, and ordered-batch callbacks while preserving first-seen candidate ordering, empty batch handling, and duplicate suppression across values. Focused artifact processor runtime verification passed (`246 passed`). |
| B307 | 2026-08-15 | GitOps repository value walking still lived inline in `ArtifactQueueProcessor._yaml_gitops_repository_values_for_node`, keeping direct repo field extraction, repository `name`/`value` hint handling, dict/list child job construction, worker traversal, and local recursion in the oversized orchestrator. | Added `yaml_gitops_repository_values_for_node_for_processor` to `forge.orchestration.artifact_processor_runtime` and exported it through `forge.orchestration`. The processor compatibility wrapper now delegates through injected YAML normalization, reference lookup, key fingerprint, child-value, and ordered-batch callbacks while preserving direct repo keys, source URL hints, child ordering, `use_workers=True` worker dispatch, and `use_workers=False` recursive traversal. Focused artifact processor runtime verification passed (`248 passed`). |
| B308 | 2026-08-15 | Flux source reference candidate mapping still lived inline in `ArtifactQueueProcessor._yaml_flux_source_ref_candidates`, keeping Flux kind-to-family mapping and namespace/name identifier formatting in the oversized orchestrator. | Added `yaml_flux_source_ref_candidates_for_processor` to `forge.orchestration.artifact_processor_runtime` and exported it through `forge.orchestration`. The processor compatibility wrapper now delegates through injected YAML normalization, reference lookup, kind fingerprinting, and safe segment callbacks while preserving GitRepository/HelmRepository/OCIRepository/Bucket URI families, optional namespace prefixes, and missing/unknown reference suppression. Focused artifact processor runtime verification passed (`250 passed`). |
| B309 | 2026-08-15 | Flux bucket structured candidate mapping still lived inline in `ArtifactQueueProcessor._yaml_flux_bucket_structured_candidates`, keeping provider-specific bucket URI selection in the oversized orchestrator. | Added `yaml_flux_bucket_structured_candidates_for_processor` to `forge.orchestration.artifact_processor_runtime` and exported it through `forge.orchestration`. The processor compatibility wrapper now delegates through injected YAML normalization, reference lookup, bucket validation, safe segment, and DigitalOcean endpoint regex callbacks while preserving empty-spec/bucket suppression, GCP `gs://`, Azure Blob, DigitalOcean Spaces, and S3 fallback behavior. Focused artifact processor runtime verification passed (`252 passed`). |
| B310 | 2026-08-15 | Crossplane manifest detection still lived inline in `ArtifactQueueProcessor._yaml_manifest_looks_like_crossplane`, keeping provider API group recognition policy in the oversized orchestrator. | Added `yaml_manifest_looks_like_crossplane_for_processor` to `forge.orchestration.artifact_processor_runtime` and exported it through `forge.orchestration`. The processor static compatibility wrapper now delegates while preserving API group splitting, lowercasing, `crossplane.io` detection, `.upbound.io` suffix detection, and unrelated/empty API suppression. Focused artifact processor runtime verification passed (`254 passed`). |
| B311 | 2026-08-15 | Crossplane provider-family normalization still lived inline in `ArtifactQueueProcessor._crossplane_provider_family`, keeping provider precedence and fallback group formatting in the oversized orchestrator. | Added `crossplane_provider_family_for_processor` to `forge.orchestration.artifact_processor_runtime` and exported it through `forge.orchestration`. The processor static compatibility wrapper now delegates while preserving API group splitting, lowercasing, AWS/GCP/Azure/Kubernetes/DigitalOcean/Cloudflare precedence, dotted-group hyphen fallback, and empty `crossplane` fallback. Focused artifact processor runtime verification passed (`256 passed`). |
| B312 | 2026-08-15 | Crossplane external-name annotation extraction still lived inline in `ArtifactQueueProcessor._yaml_crossplane_external_name`, keeping metadata/annotation traversal and annotation key matching in the oversized orchestrator. | Added `yaml_crossplane_external_name_for_processor` to `forge.orchestration.artifact_processor_runtime` and exported it through `forge.orchestration`. The processor compatibility wrapper now delegates through injected child-mapping and safe-segment callbacks while preserving metadata/annotations lookup, stripped/lowercase `crossplane.io/external-name` key matching, formatted value return, and missing/non-matching annotation suppression. Focused artifact processor runtime verification passed (`258 passed`). |
| B313 | 2026-08-15 | Crossplane cloud candidate mapping still lived inline in `ArtifactQueueProcessor._yaml_crossplane_cloud_candidates`, keeping AWS/GCP/Azure storage candidate selection in the oversized orchestrator. | Added `yaml_crossplane_cloud_candidates_for_processor` to `forge.orchestration.artifact_processor_runtime` and exported it through `forge.orchestration`. The processor compatibility wrapper now delegates through injected YAML child mapping, normalization, reference lookup, and bucket validation callbacks while preserving ignored mapping input, API group splitting/lowercasing, AWS S3 bucket candidates, GCP storage bucket candidates, Azure Blob container candidates, resource-name fallback, and unsupported/invalid suppression. Focused artifact processor runtime verification passed (`260 passed`). |
| B314 | 2026-08-15 | Crossplane structured candidate coordination still lived inline in `ArtifactQueueProcessor._yaml_crossplane_structured_candidates`, keeping provider config, composition/XRD, provider ref, resource, and cloud candidate ordering in the oversized orchestrator. | Added `yaml_crossplane_structured_candidates_for_processor` to `forge.orchestration.artifact_processor_runtime` and exported it through `forge.orchestration`. The processor compatibility wrapper now delegates through injected provider-family, YAML lookup, external-name, and cloud-candidate callbacks while preserving early returns for provider configs/compositions/XRDs, providerConfigRef candidate emission, external-name precedence over object identifiers, resource candidate ordering, and cloud candidate append order. Focused artifact processor runtime verification passed (`262 passed`). |
| B315 | 2026-08-15 | Kubernetes object identifier formatting still lived inline in `ArtifactQueueProcessor._yaml_kubernetes_object_identifier`, keeping metadata lookup and namespace/name formatting in the oversized orchestrator. | Added `yaml_kubernetes_object_identifier_for_processor` to `forge.orchestration.artifact_processor_runtime` and exported it through `forge.orchestration`. The processor compatibility wrapper now delegates through injected child-mapping, YAML normalization, reference lookup, and safe segment callbacks while preserving missing metadata/name suppression, name-only identifiers, and namespaced `namespace/name` formatting. Focused artifact processor runtime verification passed (`264 passed`). |
| B316 | 2026-08-15 | ExternalSecret store reference formatting still lived inline in `ArtifactQueueProcessor._yaml_external_secret_store_refs`, keeping SecretStore/ClusterSecretStore URI selection in the oversized orchestrator. | Added `yaml_external_secret_store_refs_for_processor` to `forge.orchestration.artifact_processor_runtime` and exported it through `forge.orchestration`. The processor compatibility wrapper now delegates through injected YAML child-mapping, normalization, reference lookup, safe segment, and key-fingerprint callbacks while preserving ignored object identifier behavior, missing store/name suppression, `secret-store://` defaults, and `cluster-secret-store://` handling. Focused artifact processor runtime verification passed (`266 passed`). |
| B317 | 2026-08-15 | ExternalSecret remote-ref entry parsing still lived inline in `ArtifactQueueProcessor._yaml_external_secret_remote_ref_entry_keys`, keeping `data.remoteRef` and `dataFrom.extract/find` key lookup in the oversized orchestrator. | Added `yaml_external_secret_remote_ref_entry_keys_for_processor` to `forge.orchestration.artifact_processor_runtime` and exported it through `forge.orchestration`. The processor compatibility wrapper now delegates through injected YAML child-mapping, normalization, and reference lookup callbacks while preserving `data` remoteRef lookup, `data_from` extract/find ordering, unknown-family suppression, and missing remote-ref suppression. Focused artifact processor runtime verification passed (`268 passed`). |
| B318 | 2026-08-15 | ExternalSecret remote-ref key coordination still lived inline in `ArtifactQueueProcessor._yaml_external_secret_remote_ref_keys`, keeping data/dataFrom job collection, ordered batching, and key dedupe in the oversized orchestrator. | Added `yaml_external_secret_remote_ref_keys_for_processor` to `forge.orchestration.artifact_processor_runtime` and exported it through `forge.orchestration`. The processor compatibility wrapper now delegates through injected ordered-batch and entry-key callbacks while preserving `data` and `dataFrom`/`datafrom` job ordering, non-dict suppression, default batch factory use, quote/whitespace trimming, case-insensitive dedupe, and first-seen key ordering. Focused artifact processor runtime verification passed (`270 passed`). |
| B319 | 2026-08-15 | ExternalSecret provider candidate mapping still lived inline in `ArtifactQueueProcessor._yaml_external_secret_provider_candidates`, keeping AWS/GCP/Azure/Vault/webhook/gitlab candidate emission in the oversized orchestrator. | Added `yaml_external_secret_provider_candidates_for_processor` to `forge.orchestration.artifact_processor_runtime` and exported it through `forge.orchestration`. The processor compatibility wrapper now delegates through injected YAML lookup, safe segment, project validation, Vault address, and URL normalization callbacks while preserving AWS Secrets Manager vs Parameter Store selection, GCP project expansion, Azure Key Vault URL handling, HashiCorp Vault host/path identifiers, webhook/gitlab endpoint emission, empty-provider suppression, and candidate ordering. Focused artifact processor runtime verification passed (`272 passed`). |
| B320 | 2026-08-15 | ExternalSecret reference segment sanitization still lived inline in `ArtifactQueueProcessor._yaml_external_secret_ref_segment`, keeping trimming, unsafe-value rejection, and URL quoting policy in the oversized orchestrator. | Added `yaml_external_secret_ref_segment_for_processor` to `forge.orchestration.artifact_processor_runtime` and exported it through `forge.orchestration`. The processor static compatibility wrapper now delegates while preserving quote/slash trimming, empty/oversized/whitespace/template suppression, and `%` encoding with `/._:@+=-` as safe characters. Focused artifact processor runtime verification passed (`274 passed`). |
| B321 | 2026-08-15 | SOPS section entry selection still lived inline in `ArtifactQueueProcessor._yaml_sops_section_entries`, keeping section alias matching and dict/list entry filtering in the oversized orchestrator. | Added `yaml_sops_section_entries_for_processor` to `forge.orchestration.artifact_processor_runtime` and exported it through `forge.orchestration`. The processor compatibility wrapper now delegates through injected key-fingerprint callbacks while preserving alias matching, dict-as-single-entry handling, list dict filtering, unsupported-value suppression, and missing-section suppression. Focused artifact processor runtime verification passed (`276 passed`). |
| B322 | 2026-08-15 | SOPS metadata entry candidate mapping still lived inline in `ArtifactQueueProcessor._yaml_sops_metadata_entry_candidate`, keeping KMS/Vault family validation in the oversized orchestrator. | Added `yaml_sops_metadata_entry_candidate_for_processor` to `forge.orchestration.artifact_processor_runtime` and exported it through `forge.orchestration`. The processor compatibility wrapper now delegates through injected reference lookup, AWS/GCP regex, URL normalization, and Vault address callbacks while preserving AWS KMS ARN validation, GCP KMS resource validation, Azure Key Vault URL normalization, HashiCorp Vault address normalization, invalid-ref suppression, and unknown-family suppression. Focused artifact processor runtime verification passed (`278 passed`). |
| B323 | 2026-08-15 | SOPS metadata structured-candidate coordination still lived inline in `ArtifactQueueProcessor._yaml_sops_metadata_structured_candidates`, keeping SOPS mapping selection, section job construction, ordered candidate batching, and dedupe in the oversized orchestrator. | Added `yaml_sops_metadata_structured_candidates_for_processor` to `forge.orchestration.artifact_processor_runtime` and exported it through `forge.orchestration`. The processor compatibility wrapper now delegates through injected hint, section-entry, batch, and entry-candidate callbacks while preserving direct SOPS-file detection, nested `sops` metadata fallback, AWS/GCP/Azure/Vault job ordering, empty candidate suppression, quote trimming, case-insensitive dedupe, and first-seen candidate ordering. Focused artifact processor runtime verification passed (`280 passed`). |
| B324 | 2026-08-15 | Vault address candidate normalization still lived inline in `ArtifactQueueProcessor._yaml_vault_address_candidate`, keeping URL passthrough and bare-host fallback policy in the oversized orchestrator. | Added `yaml_vault_address_candidate_for_processor` to `forge.orchestration.artifact_processor_runtime` and exported it through `forge.orchestration`. The processor static compatibility wrapper now delegates through injected URL normalization while preserving quote trimming, empty suppression, HTTP(S) URL passthrough, bare dotted-host lowercasing with `https://` fallback, trailing-dot trimming, and invalid scheme/host suppression. Focused artifact processor runtime verification passed (`282 passed`). |
| B325 | 2026-08-15 | Cloudflare reference validation still lived inline in `ArtifactQueueProcessor._cloudflare_valid_ref`, keeping Cloudflare identifier trimming and syntax policy in the oversized orchestrator. | Added `cloudflare_valid_ref_for_processor` to `forge.orchestration.artifact_processor_runtime` and exported it through `forge.orchestration`. The processor static compatibility wrapper now delegates while preserving quote trimming, lowercase normalization, allowed alphanumeric/dot/underscore/hyphen syntax, first-character alphanumeric requirement, length bounds, and invalid-value suppression. Focused artifact processor runtime verification passed (`284 passed`). |
| B326 | 2026-08-15 | Cloudflare URI candidate assembly still lived inline in `ArtifactQueueProcessor._cloudflare_uri_candidate`, keeping provider URI formatting coupled to the oversized orchestrator. | Added `cloudflare_uri_candidate_for_processor` to `forge.orchestration.artifact_processor_runtime` and exported it through `forge.orchestration`. The class compatibility wrapper now delegates while preserving injected reference validation, invalid-ref suppression, family-specific `cloudflare-{family}://{ref}` formatting, and existing Cloudflare candidate call sites. Focused artifact processor runtime verification passed (`286 passed`). |
| B327 | 2026-08-15 | Cloudflare URI candidate tuple adaptation still lived inline in `ArtifactQueueProcessor._cloudflare_uri_candidate_entry`, keeping ordered-batch `(family, value)` coercion coupled to the oversized orchestrator. | Added `cloudflare_uri_candidate_entry_for_processor` to `forge.orchestration.artifact_processor_runtime` and exported it through `forge.orchestration`. The class compatibility wrapper now delegates while preserving tuple unpacking, blank-value coercion, injected URI candidate formatting, and ordered-batch call behavior. Focused artifact processor runtime verification passed (`288 passed`). |
| B328 | 2026-08-15 | Cloudflare URI candidate-entry batch merging still lived inline in `ArtifactQueueProcessor._yaml_cloudflare_structured_candidates`, keeping ordered batch dispatch, empty-candidate filtering, and first-seen dedupe coupled to the oversized orchestrator. | Added `cloudflare_uri_candidate_entries_for_processor` to `forge.orchestration.artifact_processor_runtime` and exported it through `forge.orchestration`. The structured Cloudflare parser now delegates candidate-entry batching while preserving ordered local batch invocation, `default_factory=str`, non-empty filtering, duplicate suppression, and first-seen output order. Focused artifact processor runtime verification passed (`290 passed`). |
| B329 | 2026-08-15 | Cloudflare structured marker and key-family detection still lived inline in `ArtifactQueueProcessor._yaml_cloudflare_structured_candidates`, keeping Cloudflare path hints and R2/D1/KV/Worker/Pages marker tables coupled to the oversized orchestrator. | Added `yaml_cloudflare_structured_marker_flags_for_processor` to `forge.orchestration.artifact_processor_runtime` and exported it through `forge.orchestration`. The structured Cloudflare parser now delegates marker detection while preserving the exact path-hint set, Worker marker keys, R2/D1/KV/Worker/Pages key-family flags, and downstream candidate branch behavior. Focused artifact processor runtime verification passed (`292 passed`). |
| B330 | 2026-08-15 | Cloudflare R2 candidate-ref selection still lived inline in `ArtifactQueueProcessor._yaml_cloudflare_structured_candidates`, keeping explicit R2 key lookup, hinted generic bucket fallback, and append gating coupled to the oversized orchestrator. | Added `yaml_cloudflare_r2_candidate_ref_for_processor` to `forge.orchestration.artifact_processor_runtime` and exported it through `forge.orchestration`. The structured Cloudflare parser now delegates R2 ref selection while preserving explicit R2 key priority, Cloudflare-hinted generic bucket/name fallback, suppression for unhinted generic buckets, and downstream `r2` candidate append behavior. Focused artifact processor runtime verification passed (`295 passed`). |
| B331 | 2026-08-15 | Cloudflare D1 candidate-ref selection still lived inline in `ArtifactQueueProcessor._yaml_cloudflare_structured_candidates`, keeping explicit D1 key lookup, hinted generic database fallback, and append gating coupled to the oversized orchestrator. | Added `yaml_cloudflare_d1_candidate_ref_for_processor` to `forge.orchestration.artifact_processor_runtime` and exported it through `forge.orchestration`. The structured Cloudflare parser now delegates D1 ref selection while preserving explicit D1 key priority, Cloudflare-hinted generic database/name fallback, suppression for unhinted generic database values, and downstream `d1` candidate append behavior. Focused artifact processor runtime verification passed (`298 passed`). |
| B332 | 2026-08-15 | Cloudflare KV candidate-ref selection still lived inline in `ArtifactQueueProcessor._yaml_cloudflare_structured_candidates`, keeping explicit KV namespace lookup, hinted generic namespace/id/name fallback, and append gating coupled to the oversized orchestrator. | Added `yaml_cloudflare_kv_candidate_ref_for_processor` to `forge.orchestration.artifact_processor_runtime` and exported it through `forge.orchestration`. The structured Cloudflare parser now delegates KV ref selection while preserving explicit KV key priority, Cloudflare-hinted generic namespace/id/name fallback, suppression for unhinted generic namespace values, and downstream `kv` candidate append behavior. Focused artifact processor runtime verification passed (`301 passed`). |
| B333 | 2026-08-15 | Cloudflare Worker candidate-ref selection still lived inline in `ArtifactQueueProcessor._yaml_cloudflare_structured_candidates`, keeping explicit Worker key lookup, Worker-marker/name fallback, path-hint gating, and append behavior coupled to the oversized orchestrator. | Added `yaml_cloudflare_worker_candidate_ref_for_processor` to `forge.orchestration.artifact_processor_runtime` and exported it through `forge.orchestration`. The structured Cloudflare parser now delegates Worker ref selection while preserving explicit Worker key priority, Cloudflare/Worker marker `name` fallback, worker/workers/wrangler path-hint gating, suppression for ungated generic worker names, and downstream `worker` candidate append behavior. Focused artifact processor runtime verification passed (`305 passed`). |
| B334 | 2026-08-15 | Cloudflare Pages candidate-ref selection still lived inline in `ArtifactQueueProcessor._yaml_cloudflare_structured_candidates`, keeping explicit Pages project lookup, Pages/build-output fallback, path-hint gating, and append behavior coupled to the oversized orchestrator. | Added `yaml_cloudflare_pages_candidate_ref_for_processor` to `forge.orchestration.artifact_processor_runtime` and exported it through `forge.orchestration`. The structured Cloudflare parser now delegates Pages ref selection while preserving explicit Pages key priority, Pages path-hint `name` fallback, Pages build-output marker fallback, suppression for ungated generic project names, and downstream `pages` candidate append behavior. Focused artifact processor runtime verification passed (`309 passed`). |
| B335 | 2026-08-15 | Cloudflare structured candidate coordination still lived in `ArtifactQueueProcessor._yaml_cloudflare_structured_candidates`, even after marker detection, family ref selection, and URI batching were extracted. | Added `yaml_cloudflare_structured_candidates_for_processor` to `forge.orchestration.artifact_processor_runtime` and exported it through `forge.orchestration`. The processor method is now a compatibility wrapper that delegates full Cloudflare structured coordination while preserving marker detection, R2/D1/KV/Worker/Pages ref ordering, generic-config suppression, ordered URI candidate batching, and existing call sites. Focused artifact processor runtime verification passed (`311 passed`). |
| B336 | 2026-08-15 | Generic YAML candidate batch, family, and merge helpers still lived inline in `ArtifactQueueProcessor`, keeping ordered batch flattening and blank-candidate suppression coupled to the oversized orchestrator. | Added `yaml_candidate_batch_entries_for_processor`, `yaml_candidate_family_entries_for_processor`, and `yaml_candidate_merge_entry_for_processor` to `forge.orchestration.artifact_processor_runtime` and exported them through `forge.orchestration`. The processor methods are now compatibility wrappers that preserve batch/family order, candidate string stripping, empty-value suppression, and existing ordered local batch call sites. Focused artifact processor runtime verification passed (`314 passed`). |
| B337 | 2026-08-15 | Restic repository candidate parsing still lived inline in `ArtifactQueueProcessor`, keeping REST/S3/GCS/Azure repository normalization and env-map lookup coupled to the oversized orchestrator. | Added `restic_repository_candidates_from_env_map_for_processor`, `restic_repository_candidates_for_processor`, `restic_s3_repository_candidates_for_processor`, and `restic_bucket_from_pathish_for_processor` to `forge.orchestration.artifact_processor_runtime` and exported them through `forge.orchestration`. The processor methods are now compatibility wrappers that preserve Restic repository env lookup, REST URL normalization, S3 endpoint/bucket extraction, GCS bucket extraction, Azure account/container URL assembly, dedupe, and existing call sites. Focused artifact processor runtime verification passed (`318 passed`). |
| B338 | 2026-08-15 | The environment candidate-family coordinator still lived inline in `ArtifactQueueProcessor._yaml_env_candidate_family`, keeping env-map routing for cloud buckets, managed hosting, Cloudflare, backup tools, Docker auth, and env-value candidates coupled to the oversized orchestrator. | Added `yaml_env_candidate_family_for_processor` to `forge.orchestration.artifact_processor_runtime` and exported it through `forge.orchestration`. The processor method is now a compatibility wrapper that preserves Firebase/Supabase/S3/GCS/DigitalOcean Spaces/Azure env extraction, managed-hosting dedupe, Cloudflare normalization delegation, Amplify/Sanity/Docker auth/Restic/Borg/Duplicati delegation, env-value filtering, source hints, and existing ordered local batch call sites. Focused artifact processor runtime verification passed (`322 passed`). |
| B339 | 2026-08-15 | Managed-hosting env entry and generic env-value candidate extraction still lived inline in `ArtifactQueueProcessor`, keeping URL marker gating, managed URL normalization, email lowering, direct cloud URI preservation, and non-match suppression coupled to the oversized orchestrator. | Added `yaml_managed_hosting_env_entry_for_processor` and `yaml_env_value_candidate_entry_for_processor` to `forge.orchestration.artifact_processor_runtime` and exported them through `forge.orchestration`. The processor methods are now compatibility wrappers that preserve managed-hosting marker checks, injected cloud URL normalization, email candidate lowering, URL/URI/endpoint/portal/base marker handling, direct `http`/`https`/`s3`/`gs` URI preservation, and existing env-family call sites. Focused artifact processor runtime verification passed (`325 passed`). |
| B340 | 2026-08-15 | Docker auth registry/principal leaf parsing still lived inline in `ArtifactQueueProcessor`, keeping registry URL normalization, explicit principal extraction, base64 `auth` decoding, and auth/cred-helper/legacy entry adapters coupled to the oversized orchestrator. | Added `docker_registry_url_candidate_for_processor`, `docker_auth_principal_candidate_for_processor`, `docker_auth_principal_from_auth_field_for_processor`, `docker_auth_entry_principals_for_processor`, `docker_auth_config_auth_entry_candidates_for_processor`, `docker_auth_config_cred_helper_candidates_for_processor`, and `docker_auth_config_legacy_entry_candidates_for_processor` to `forge.orchestration.artifact_processor_runtime` and exported them through `forge.orchestration`. The processor methods are now compatibility wrappers that preserve registry scheme/host/path normalization, wildcard/invalid scheme suppression, email principal lowering, padded base64 auth decoding, cred-helper registry extraction, metadata-key suppression, and existing Docker auth config call sites. Focused artifact processor runtime verification passed (`329 passed`). |
| B341 | 2026-08-15 | Docker auth env-map and config aggregation still lived inline in `ArtifactQueueProcessor`, keeping env hint detection, env-entry filtering, ordered static batch dispatch, auth/cred-helper/legacy config merging, first-seen dedupe, and JSON/dict payload handling coupled to the oversized orchestrator. | Added `docker_auth_structured_candidates_from_env_map_for_processor`, `docker_auth_structured_env_entry_candidates_for_processor`, `env_value_may_hold_docker_auth_for_processor`, and `docker_auth_config_candidates_for_processor` to `forge.orchestration.artifact_processor_runtime` and exported them through `forge.orchestration`. The processor methods are now compatibility wrappers that preserve Docker/CONTAINER_REGISTRY env detection, inline JSON `auths` detection, safe JSON loading, ordered auth and cred-helper processing, legacy fallback when `auths` is absent, metadata suppression through injected legacy helpers, first-seen dedupe, and existing Docker auth call sites. Focused artifact processor runtime verification passed (`334 passed`). |
| B342 | 2026-08-15 | Scheduled target-import live runs could exceed the intended wall-clock budget because stale startup/recovery time was outside the import timeout and the installer extended Task Scheduler's `ExecutionTimeLimit`; a scheduled live fan-out could also crash when runtime source labels such as `social_profile` were inserted into `engagement_seeds.source`, whose schema only allows neutral source values. | The scheduled runner now treats `-TimeoutMinutes` as a whole-task budget, deducts stale-recovery/startup time from the import timeout, reserves cleanup inside the same budget, and the installer now registers `ExecutionTimeLimit` directly from the timeout. `SeedRunTracker` normalizes non-schema runtime source labels to neutral `discovered`/`artifact` values while preserving the original label as `metadata_json.raw_source`. Reinstalled the local scheduled task with `ExecutionTimeLimit=PT45M`; focused scheduler/import/run-tracking and integration verification passed. |
| B343 | 2026-08-15 | After the scheduled target-import watchdog fixes, live runs could still exit as failures after making useful progress: allocator/state drift could point a new target import at a DB file that already contained an `engagements` row, and completed passive kill-chain subprocesses could return code `2` after generating a report because `--no-auto-run-detected` deliberately skipped optional follow-on modules in non-TTY mode. | Target import now skips allocated DB IDs whose engagement DB already contains an engagement row before creating a new target engagement. Passive child output is captured and replayed to logs; return code `2` is accepted only when output proves `Kill-chain complete` and includes a `Report:` path, while real CLI/parser errors still raise `CalledProcessError`. Regression coverage now proves allocator-collision recovery, completed-run exit-2 acceptance, and real exit-2 failure preservation; runtime checks loaded/imported the live target feed and a scheduled live run generated `reports/engagement_10696_kill_chain_20260815T150918.md`. |
| B344 | 2026-08-15 | The reusable attack-graph export path relied on upstream graph construction to keep metadata safe, but shareable JSON, GraphML, MTGX manifest/workspace, and CSV exports could still serialize sensitive metadata if an in-memory graph was mutated after model validation or supplied by a test/tool adapter. | Attack graph export now sanitizes node and edge metadata once after build/filtering and before any renderer or artifact writer consumes the graph. The guard strips secret-bearing keys and nested values, removes sensitive HTTP(S) query parameters, drops URL userinfo, and keeps reviewable non-sensitive metadata consistent across JSON, Mermaid/DOT, GraphML, MTGX, and CSV outputs. Focused graph export/parser verification passed. |
| B345 | 2026-08-15 | `forge.webui.app.create_app()` still owned in-process rate limiting and production error-handler wiring inline, keeping non-route HTTP policy mixed into the large route-registration function and making the rate-window behavior hard to test without constructing the full FastAPI app. | Added `forge.webui.middleware` with a pure `InMemoryRateLimiter` plus installers for the Web UI rate-limit middleware and traceback-suppressing production 500 handler. `create_app()` now delegates those concerns while preserving `/health` bypass, per-client 60 request/minute limits, and production internal-error responses. Focused Web UI middleware/HTMX wiring tests, Ruff, and py_compile passed. |
| B346 | 2026-08-16 | `forge.webui.app.create_app()` still defined bearer-token dependency, bootstrap-token lookup, and WebSocket token parsing inline, keeping auth/bootstrap policy mixed into route registration and leaving WebSocket query/header/subprotocol token precedence without focused unit coverage. | Added `forge.webui.auth_dependencies` with injectable bearer-principal, bootstrap-secret, and WebSocket-principal helpers that avoid importing FastAPI at module import time. `create_app()` now delegates those helpers while preserving 401/503 error details, bootstrap token trimming, and progress WebSocket token lookup from query, Authorization header, and subprotocols. Focused auth-dependency/HTMX wiring tests, Ruff, and py_compile passed. |
| B347 | 2026-08-16 | `forge.webui.app.create_app()` still scanned engagement SQLite DBs inline to discover live run-progress snapshots, mixing route-app assembly with DB iteration and making multi-DB progress filtering hard to verify without constructing the full web app. | Added `iter_live_run_progress_snapshots` to `forge.webui.run_status` with injected DB discovery, table-check, and connection helpers. `create_app()` now delegates the scan while preserving numeric engagement DB discovery, missing-table/SQLite-error skips, newest-row-per-engagement filtering, terminal/step-less row suppression, and progress fingerprint payloads. Focused run-status/HTMX wiring tests, Ruff, and py_compile passed. |
| B348 | 2026-08-16 | `forge.webui.app.create_app()` still owned workspace RBAC table bootstrap and workspace/engagement access predicates inline, keeping security-sensitive authorization policy mixed into route registration and only indirectly covered by full route tests. | Added `forge.webui.workspace_access` for workspace foundation bootstrapping, membership checks, workspace access, and legacy engagement-owner fallback. `create_app()` now delegates those predicates while preserving default workspace creation, legacy `workspace_id` migration, membership-based access, bootstrap allowance, `workspaces:any`, `workspaces:legacy`, and membership-present legacy fallback denial. Focused workspace access/routes/discovery/HTMX tests, Ruff, and py_compile passed. |
| B349 | 2026-08-16 | `forge.webui.app.create_app()` still shaped run audit-review annotations inline even after run-status payload helpers were split, leaving audit manifest review injection coupled to route assembly and without direct mutation-safety coverage. | Added `annotate_run_audit_review` to `forge.webui.run_status` and delegated the app wrapper to it. The helper preserves non-dict passthrough, run-id/manifest-hash extraction, top-level `audit_review`, nested audit-manifest `review`, and non-mutating payload copies. Focused run-status/HTMX wiring tests, Ruff, and py_compile passed. |
| B350 | 2026-08-16 | `forge.webui.app.create_app()` still duplicated basic engagement row listing and single-row lookup SQL even though engagement lifecycle already owned engagement persistence helpers, leaving discovery context wiring dependent on app-local query snippets. | Added `engagement_rows` to `forge.webui.engagement_lifecycle` and wired `create_app()`/discovery context to reuse lifecycle row helpers. The helper preserves dashboard column shape and ID ordering, while existing single-row lookup remains the shared lifecycle implementation. Lifecycle/discovery tests passed in focused batches, HTMX wiring passed separately after the combined command exceeded 120s, and Ruff/py_compile passed. |
| B351 | 2026-08-16 | `forge.webui.app.create_app()` still queried latest audit timestamps inline for dashboard summary payloads, keeping audit-log tolerance and formatting policy in route assembly instead of the run-status helper module. | Added `latest_audit_timestamp` to `forge.webui.run_status` and delegated the app wrapper to it. The helper preserves newest-audit-row selection by descending id, missing-table tolerance, empty fallback, and injected date formatting. Focused run-status/HTMX wiring tests, Ruff, and py_compile passed. |
| B352 | 2026-08-16 | `forge.webui.app.create_app()` still defined the principal permission guard inline, keeping repeated route authorization failure shaping coupled to route registration instead of the auth dependency layer. | Added `build_principal_permission_guard` to `forge.webui.auth_dependencies` and wired `create_app()` to use it. The guard preserves wildcard/prefix permission matching through `Principal.has_permission`, 403 status mapping, and exact missing-permission detail text. Focused auth-dependency/HTMX wiring tests, Ruff, and py_compile passed. |
| B353 | 2026-08-16 | `forge.webui.app.create_app()` still opened workflow engagement DBs inline, hiding row-factory setup plus migration and canonical-schema validation side effects inside route assembly despite every route sharing the same behavior. | Added `forge.webui.db.open_workflow_db` with injectable connect/migrate/validate hooks and wired `create_app()` to delegate to it. The helper preserves direct-connect use, `sqlite3.Row` row factory setup before migrations, migration-before-validation ordering, and returned open connection semantics. Focused DB-helper/HTMX wiring tests, Ruff, and py_compile passed. |
| B354 | 2026-08-16 | Scheduled target-import runs could still finish with Task Scheduler result `2` after a passive kill-chain generated a report and completed in the engagement DB, because the importer only accepted exit-2 success when completion text was present in captured child output. Windows/Python launcher output splitting can leave durable completion evidence in SQLite while the parent sees a Typer-style exit-2 error string. | Target import now also accepts child exit code `2` when the engagement DB contains a completed `kill_chain` run for the same engagement and seed. The existing output-text success gate remains, real CLI/parser exit-2 failures without DB completion still fail, and regression coverage reproduces the observed `not enough values to unpack` stderr with a completed DB run. Focused target-import tests, Ruff, and py_compile passed. |
| B355 | 2026-08-16 | `forge.webui.app.create_app()` still owned reports-directory, report-file, and audit-file helper policy inline, keeping artifact discovery wiring mixed into route assembly even though artifact payload helpers already own the Web UI artifact surface. | Added `reports_dir`, `report_files`, and `audit_files` to `forge.webui.artifacts` and wired `create_app()` to delegate to them. The helpers preserve `Path.cwd()/reports`, existing report/audit filename patterns, and report/graph/audit payload ordering through the existing artifact payload path. Focused artifact/HTMX wiring tests, Ruff, and py_compile passed. |
| B356 | 2026-08-16 | Scheduled target-import runs could still abort before launching the next passive kill-chain when target-import monitoring seeding hit SQLite's deprecated timestamp converter on existing ISO `T` timestamp rows, surfacing as Typer's generic `Invalid value: not enough values to unpack` and returning Task Scheduler result `2`. | Target import now treats monitoring-policy/snapshot seeding as non-blocking: failures are logged as bounded warnings and import continues to scope manifest generation and passive start logic. Normal monitoring seeding remains intact, and regression coverage proves a monitoring seed failure does not block `--start`. Focused target-import tests, Ruff, py_compile, and a mocked production-data reproduction passed. |
| B357 | 2026-08-16 | `forge.webui.app.create_app()` still aggregated engagement artifact route files inline, mixing report/audit/graph discovery policy and audit-manifest materialization verification into route assembly even after the lower-level artifact file helpers were extracted. | Added `engagement_artifact_files` to `forge.webui.artifacts` with injectable audit materialization and graph discovery hooks, and wired `create_app()` to delegate route-file aggregation to it. The helper preserves report/audit/graph ordering, verified audit materialization, and existing route lookup behavior. Focused artifact/HTMX wiring tests, Ruff, and py_compile passed. |
| B358 | 2026-08-16 | `forge.webui.app.create_app()` still built engagement summary/detail payloads inline, leaving dashboard/report/graph/audit-review shaping and audit-manifest materialization mixed into route assembly. | Added `forge.webui.engagement_payloads` for summary/detail payload construction and wired `create_app()` to pass only report-root and formatter dependencies. The extracted builders preserve summary fields, report history/previews, graph payload state, audit-review annotations, verified audit materialization for detail payloads, and route discovery compatibility. Focused engagement-payload/HTMX wiring tests, Ruff, and py_compile passed. |
| B359 | 2026-08-16 | `forge.webui.app.create_app()` still defined the synchronous progress-publish closure inline, keeping a shared event-shaping adapter in app setup even though progress event construction already lived in `forge.webui.state`. | Added `build_progress_publisher` to `forge.webui.state` and wired `create_app()` to bind it to the app broker. The helper preserves the existing `ProgressEvent` shape for task, lifecycle, seed, run-control, and command-center route publishers. Focused state/HTMX wiring tests, Ruff, and py_compile passed. |
| B360 | 2026-08-16 | `forge.webui.app.create_app()` still defined the launch-time run-control marker clearer inline, leaving data-dir binding for stop/pause stale-marker cleanup in app setup instead of the run-control module that owns marker path policy. | Added `build_run_control_marker_clearer` to `forge.webui.run_control` and wired `create_app()` to use it before kill-chain launches. The helper preserves stale stop/pause marker cleanup and keeps marker path behavior covered by run-control tests. Focused run-control/HTMX wiring tests, Ruff, and py_compile passed. |
| B361 | 2026-08-16 | `forge.webui.app.create_app()` still defined the logs-directory provider inline, leaving data-dir binding for `FORGE_DATA_DIR/logs` creation in app setup instead of the logs module that owns log path policy. | Added `build_logs_dir_provider` to `forge.webui.logs` and wired `create_app()` to use the bound provider wherever routes need the logs directory. The helper preserves existing logs directory creation behavior and keeps path behavior covered by logs tests. Focused logs/HTMX wiring tests, Ruff, and py_compile passed. |
| B362 | 2026-08-16 | `forge.webui.app.create_app()` still defined the reports-directory provider inline, leaving `Path.cwd()/reports` binding in app setup despite report and artifact file policy already living in `forge.webui.artifacts`. | Added `build_reports_dir_provider` to `forge.webui.artifacts` and wired `create_app()` to use the bound provider wherever routes need the reports root. The helper preserves existing reports directory resolution and keeps path behavior covered by artifact tests. Focused artifact/HTMX wiring tests, Ruff, and py_compile passed. |
| B363 | 2026-08-16 | `forge.webui.app.create_app()` still defined report-file and audit-file provider closures inline, leaving reports-root binding for artifact discovery in app setup despite filename discovery already living in `forge.webui.artifacts`. | Added `build_report_files_provider` and `build_audit_files_provider` to `forge.webui.artifacts` and wired `create_app()` to use those providers. The helpers preserve existing report/audit filename discovery behavior and keep path binding covered by artifact tests. Focused artifact/HTMX wiring tests, Ruff, and py_compile passed. |
| B364 | 2026-08-16 | `forge.webui.app.create_app()` still imported `latest_running_engagement_run` and defined an unused `_latest_running_engagement_run` wrapper even though no route or helper called it after the run-status extraction. | Removed the stale import and wrapper from app setup while keeping `forge.webui.run_status.latest_running_engagement_run` as the owned implementation used by run-control and launch helpers. Focused run-status/HTMX wiring tests, Ruff, and py_compile passed. |
| B365 | 2026-08-16 | `forge.webui.app.create_app()` still defined the frontend entry response closure inline, binding frontend index, legacy dashboard template, and `FileResponse` in app setup instead of the shell route module that owns shell/static response policy. | Added `build_frontend_entry_response_provider` to `forge.webui.shell_routes` and wired `create_app()` to use it for dashboard and SPA fallback routes. The helper preserves React-index preference and legacy fallback behavior. Focused shell-route/HTMX wiring tests, Ruff, and py_compile passed. |
| B366 | 2026-08-16 | `forge.webui.app.create_app()` still constructed `EngagementDiscoveryContext` inline, keeping dependency binding for indexed engagement discovery/resolution in app setup instead of the engagement-discovery module that owns the context contract. | Added `build_engagement_discovery_context_provider` to `forge.webui.engagement_discovery` and wired `create_app()` to use it. The helper preserves dependency injection for workspace access, lifecycle rows, summary/detail payloads, artifact files, and tombstone retention. Focused engagement-discovery/HTMX wiring tests, Ruff, and py_compile passed. |
| B367 | 2026-08-16 | `forge.webui.app.create_app()` still defined its workflow DB opener inline, wrapping `open_workflow_db` locally even though row-factory, migration, and validation policy already lived in `forge.webui.db`. | Added `build_workflow_db_opener` to `forge.webui.db` and wired `create_app()` to use the bound opener for all route-level workflow DB opens. The helper preserves row-factory setup, migration-before-validation order, and caller-owned close behavior. Focused Web UI DB/HTMX wiring tests, Ruff, and py_compile passed. |
| B368 | 2026-08-16 | `forge.webui.app.create_app()` still defined engagement summary/detail payload closures inline, binding reports root and dashboard formatters in app setup even though payload construction policy lived in `forge.webui.engagement_payloads`. | Added `build_engagement_payload_providers` to `forge.webui.engagement_payloads` and wired `create_app()` to use the bound summary/detail builders. The helper preserves dashboard/detail payload contracts while keeping report-root and formatter binding beside payload construction. Focused engagement-payload/HTMX wiring tests, Ruff, and py_compile passed. |
| B369 | 2026-08-16 | `forge.webui.app.create_app()` still imported `annotate_run_audit_review` and defined an unused `_annotate_run_audit_review` wrapper after audit-review annotation moved into engagement payload/run-status ownership. | Removed the stale import and wrapper from app setup while preserving the owned `forge.webui.run_status.annotate_run_audit_review` implementation and its engagement-payload call path. Focused run-status plus engagement-payload tests, HTMX wiring tests, Ruff, and py_compile passed. |
| B370 | 2026-08-16 | Target-import monitoring snapshots could hit Python's sqlite timestamp converter on existing ISO `T` timestamps when engagement DBs were opened with declared-type parsing, producing `not enough values to unpack` warnings during scheduled imports. | Monitoring exposure-state selection now casts timestamp-like `*_at` columns to text before snapshot JSON construction. A regression covers ISO timestamp reads with `PARSE_DECLTYPES`; focused monitoring/import tests, Ruff, py_compile, and a local no-start target-import smoke test passed. |
| B371 | 2026-08-16 | `forge.webui.app.create_app()` still bound engagement artifact route-file discovery in an inline wrapper even though report/audit/graph artifact ordering and materialization policy lived in `forge.webui.artifacts`. | Added `build_engagement_artifact_files_provider` to `forge.webui.artifacts` and wired app discovery context to use it. The provider preserves reports-root binding, audit-manifest materialization, graph discovery, and artifact route-file ordering. Focused artifact/HTMX wiring tests, Ruff, and py_compile passed. |
| B372 | 2026-08-16 | `forge.webui.app.create_app()` still defined the route workspace-access checker inline, binding the access predicate and SQLite connection at app setup even though workspace RBAC policy lived in `forge.webui.workspace_access`. | Added `build_workspace_access_checker` to `forge.webui.workspace_access` and wired route payloads to use the provider. The helper preserves connection-bound workspace access decisions while reducing app-factory route policy surface. Focused workspace-access/HTMX wiring tests, Ruff, and py_compile passed. |
| B373 | 2026-08-16 | `forge.webui.app.create_app()` still defined five engagement discovery wrapper functions for list, tombstone list, detail lookup, artifact lookup, and DB resolution even though discovery ownership lived in `forge.webui.engagement_discovery`. | Added `EngagementDiscoveryProviders` and `build_engagement_discovery_providers` so app setup receives bound discovery callables from the discovery module. Existing route call sites keep the same callable contracts while app-factory discovery surface shrinks. Focused engagement-discovery/HTMX wiring tests, Ruff, and py_compile passed. |
| B374 | 2026-08-16 | A real scheduled target-import run reached Task Scheduler's 45-minute hard cap after logging `timeout exceeded; requesting graceful stop`, but child PowerShell/Python import and kill-chain processes remained alive after the parent task ended. | Tightened the Windows scheduled import watchdog timeout path: the import wait now reserves explicit timeout-recovery helper, graceful-stop, and kill-job cleanup time before Task Scheduler's `ExecutionTimeLimit`; timeout recovery uses a short helper budget, waits only while sufficient parent budget remains, then closes the watchdog job and force-stops remaining child IDs before marking timed-out DB state. The installer passes the new timeout-recovery budget. Manually stopped the observed orphaned process tree and confirmed engagement `10702` had no running run rows. Launcher tests, watchdog self-test, Ruff, and py_compile passed. |
| B375 | 2026-08-16 | `forge.webui.app.create_app()` still defined principal workspace and engagement-row access wrappers that only forwarded to `forge.webui.workspace_access` helpers. | Removed the pass-through wrappers and wired app setup directly to `principal_can_access_workspace`, `principal_can_access_engagement_row`, and `build_workspace_access_checker`. Focused workspace-access, engagement-discovery, and HTMX wiring tests, Ruff, and py_compile passed. |
| B376 | 2026-08-16 | `forge.webui.app.create_app()` still bound the live run-progress snapshot scan inline even though live run status payload and scan policy lived in `forge.webui.run_status`. | Added `build_live_run_progress_snapshot_provider` to `forge.webui.run_status` and wired the app bridge to consume the bound provider. The helper preserves data-dir, numeric DB discovery, table-exists, and DB-connect injection while shrinking app-factory run-status surface. Focused run-status/HTMX wiring tests, Ruff, and py_compile passed. |
| B377 | 2026-08-16 | `forge.webui.app.create_app()` still defined a local command-center event publisher that only converted command events into progress events through `forge.webui.command_center_routes`. | Added `build_command_event_publisher` to `forge.webui.command_center_routes` and wired app setup to use the bound publisher. The helper preserves command-event-to-progress-event shaping while reducing app-factory command-center bridge surface. Focused command-center/HTMX wiring tests, Ruff, and py_compile passed. |
| B378 | 2026-08-16 | `forge.webui.app.create_app()` still passed kill-chain launch filesystem, control-marker, progress-publishing, environment, cwd, and process-spawn dependencies directly from the route wrapper into the launch payload helper. | Added `build_kill_chain_run_launcher` to `forge.webui.kill_chain_launch` and wired app setup to use the bound launcher. The route wrapper now keeps auth, DB resolution, and HTTP error mapping while launch dependency binding lives beside kill-chain launch policy. Focused kill-chain launch/HTMX wiring tests, Ruff, and py_compile passed. |
| B379 | 2026-08-16 | `forge.webui.app.create_app()` still passed run-control data-dir, progress publisher, and timestamp formatter dependencies from the stop/pause route wrapper into the run-control route payload helper. | Added `build_run_control_requester` to `forge.webui.run_log_routes` and wired app setup to use the bound requester. The stop/pause route wrapper now keeps auth, engagement DB resolution, connection lifecycle, and HTTP error mapping while run-control dependency binding lives beside run/log route policy. Focused run-log/HTMX wiring tests, Ruff, and py_compile passed. |
| B380 | 2026-08-16 | `forge.webui.app.create_app()` still defined a local command-center body parser wrapper that only mapped `CommandCenterRouteError` from engagement-id extraction into FastAPI HTTP 400 responses. | Added `build_command_body_engagement_id_parser` to `forge.webui.command_center_routes` with injectable HTTP exception construction, and wired app setup to use it. Command-center body parsing now keeps route-error mapping beside command-center route helpers without importing FastAPI there. Focused command-center/HTMX wiring tests, Ruff, and py_compile passed. |
| B381 | 2026-08-16 | Passive package-manager config recursion did not recognize Bun `bunfig.toml` files, so Bun registry URLs, owner contacts, and cloud metadata in scoped artifacts could be missed or fall back to generic TOML labeling. | Added `bun-config` package-manager labeling for local and downloaded Bun config artifacts. Existing artifact queue processing now classifies `bunfig.toml`, extracts sanitized recursive registry URL seeds, owner email seeds, and Firebase metadata, and proves Bun token material is not persisted. Focused package-manager artifact tests, Ruff, and py_compile passed. |
| B382 | 2026-08-16 | Deno/JSR config artifacts had source labels, but static import-map URLs, `npm:`/`jsr:` specifiers, and versioned Deno URLs containing `@` were not covered by focused passive-recursion tests and could be dropped by the generic JS-runtime URL guard. | Added source-gated Deno/JSR candidate extraction for `deno-config`, `deno-import-map`, and `jsr-config`, and adjusted JS-runtime URL normalization to preserve HTTP(S) `@version` path segments while stripping URL userinfo before persistence. Deno configs now recurse static Deno, JSR, npm package, and endpoint URLs without executing Deno or storing token material. Focused JS-runtime/format-label tests, Ruff, and py_compile passed. |
| B383 | 2026-08-16 | Deno lockfiles were not labeled or source-gated into passive JS-runtime recursion, so static remote module URLs and `npm:`/`jsr:` specifiers in scoped `deno.lock` artifacts could be missed. | Added `deno-lock` artifact labeling and included it in the Deno/JSR passive extractor gate. Scoped `deno.lock` artifacts now recurse static Deno module URLs, JSR and npm package registry URLs, and endpoint URLs while stripping URL userinfo and proving token material is not persisted. Focused JS-runtime/format-label tests, Ruff, and py_compile passed. |
| B384 | 2026-08-16 | PNPM workspace manifests were not labeled as first-class dependency artifacts, so scoped `pnpm-workspace.yaml`/`.yml` artifacts could fall back to generic YAML labeling even when they contained workspace registry URLs and owner contacts. | Added `pnpm-workspace` artifact labeling for PNPM workspace manifests. Existing YAML/package-manager artifact processing now preserves narrow passive extraction of registry and endpoint URL seeds plus owner email seeds from scoped PNPM workspace manifests while stripping URL userinfo and proving token material is not persisted. Focused package-manager/format-label tests, Ruff, and py_compile passed. |
| B385 | 2026-08-16 | Turbo/Nx monorepo build configs were not labeled as JS-runtime configuration artifacts, so scoped `turbo.json` and `nx.json` files could miss remote-cache/API endpoint recursion and fall back to generic JSON handling. | Added `turbo-config` and `nx-config` artifact labels and routed them through the existing passive JS-runtime endpoint extractor. Turbo/Nx configs now recurse remote-cache API/login URLs and Nx cache/API URLs while stripping URL userinfo and proving token material is not persisted. Focused JS-runtime/format-label tests, Ruff, and py_compile passed. |
| B386 | 2026-08-16 | Webpack/Rollup/Rspack/Rsbuild bundler configs were not labeled as passive JS-runtime config artifacts, so scoped build configs could miss CDN, dev-server, source-map, and upload endpoint recursion. | Added `webpack-config`, `rollup-config`, `rspack-config`, and `rsbuild-config` artifact labels and routed them through the existing JS-runtime endpoint extractor. The extractor now recognizes bundler-specific `sourcemapBaseUrl`, `sourceMapBaseUrl`, `webSocketUrl`, and `api_url` keys while preserving source gating, URL userinfo stripping, and token non-persistence. Focused JS-runtime/format-label tests, Ruff, and py_compile passed. |
| B387 | 2026-08-16 | Vitest/Jest/Karma test-runner configs were not labeled as passive JS-runtime config artifacts, so scoped test configs could miss test server, coverage/report, and proxy endpoint recursion. | Added `vitest-config`, `jest-config`, and `karma-config` artifact labels and routed them through the existing JS-runtime endpoint extractor. The extractor now recognizes report/output URL key variants while preserving source gating, URL userinfo stripping, and token non-persistence for test-runner endpoints. Focused JS-runtime/format-label tests, Ruff, and py_compile passed. |
| B388 | 2026-08-16 | Docusaurus/VitePress/VuePress/Rspress docs-site configs were not labeled as passive JS-runtime config artifacts, so scoped documentation configs could miss docs, API, CDN, and source-edit URL recursion. | Added `docusaurus-config`, `vitepress-config`, `vuepress-config`, and `rspress-config` artifact labels and routed them through the existing JS-runtime endpoint extractor. The extractor now recognizes explicit `editUrl` keys while preserving source gating, URL userinfo stripping, and token non-persistence for docs-site endpoints. Focused JS-runtime/format-label tests, Ruff, and py_compile passed. |
| B389 | 2026-08-16 | Grype and Syft free/local scanner configs were not labeled as security-scanner policy config artifacts, so scoped vulnerability/SBOM scanner configs could miss database, registry, source, and owner recursion. | Added `grype-config` and `syft-config` labels to the security-scanner config classifier and allowed `update-url`/`updateUrl` style endpoints in the security-scanner structured extractor. Grype/Syft configs now recurse sanitized DB update, registry, source, and owner contacts while proving token material is not persisted. Focused security-scanner tests, Ruff, and py_compile passed. |
| B390 | 2026-08-16 | Hadolint config files were not labeled as security-scanner policy config artifacts, so scoped Dockerfile-linter configs could miss trusted registry and owner recursion. | Added `hadolint-config` labels for `.hadolint.yaml`/`.hadolint.yml` and `hadolint.yaml`/`hadolint.yml`, and allowed `trustedRegistries` through the source-gated security-scanner structured extractor. Hadolint configs now recurse sanitized registry URLs and owner contacts while proving URL userinfo token material is not persisted. Focused security-scanner tests, Ruff, and py_compile passed. |
| B391 | 2026-08-16 | OWASP Dependency-Check config files were not labeled as security-scanner policy config artifacts, so scoped dependency-vulnerability scanner configs could miss NVD API, legacy CVE feed, hosted-suppression, and owner recursion. | Added `dependency-check-config` labels and allowed Dependency-Check endpoint keys through the source-gated security-scanner structured extractor. Dependency-Check configs now recurse sanitized NVD and hosted-suppression URLs plus owner contacts while proving URL userinfo token material is not persisted. Focused security-scanner tests, Ruff, and py_compile passed. |
| B392 | 2026-08-16 | AnchoreCTL config files were not labeled as security-scanner policy config artifacts, so scoped Anchore Enterprise scanner client configs could miss API endpoint and owner recursion. | Added `anchorectl-config` labels for documented AnchoreCTL config locations and routed them through the existing source-gated security-scanner structured extractor. AnchoreCTL configs now recurse sanitized Enterprise API URLs and owner contacts while proving URL userinfo token and password material are not persisted. Focused security-scanner tests, Ruff, and py_compile passed. |
| B393 | 2026-08-16 | Clair config files were not labeled as security-scanner policy config artifacts, so scoped Clair deployment configs could miss API, metrics, indexer, and owner recursion. | Added `clair-config` labels for source-aware Clair config paths and allowed Clair address fields through the source-gated security-scanner structured extractor. Clair configs now recurse sanitized API, metrics, and indexer URLs plus owner contacts while proving database connection-string password material is not persisted. Focused security-scanner tests, Ruff, and py_compile passed. |
| B394 | 2026-08-16 | CVE Binary Tool config files were not labeled as security-scanner policy config artifacts, so scoped CVE scanner configs could miss configured feed endpoints and owner recursion. | Added `cve-bin-tool-config` labels for source-aware CVE Binary Tool config paths and routed them through the existing source-gated security-scanner structured extractor. CVE Binary Tool configs now recurse sanitized feed URLs plus owner contacts while proving API-key material is not persisted. Focused security-scanner tests, Ruff, and py_compile passed. |
| B395 | 2026-08-16 | The package-manager credential artifact regression still lived inline in the Phase 1 mega test file, increasing import/edit surface for an already-heavy orchestrator test module. | Moved the package-manager credential fixture into `tests.phase1.package_manager_artifact_cases` and kept the original pytest node as a thin wrapper. The preserved wrapper test, Ruff, and py_compile passed. No production parsing, scanner, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B396 | 2026-08-16 | The OS package repository artifact regression still lived inline in the Phase 1 mega test file even after adjacent package-manager fixtures were extracted, keeping package repository URL/cloud recursion coverage coupled to the oversized orchestrator test module. | Moved the OS package repository fixture into `tests.phase1.package_manager_artifact_cases` and kept the original pytest node as a thin wrapper. The preserved package-manager wrapper tests, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B397 | 2026-08-16 | The gcloud and AWS CLI config artifact regressions still lived inline in the Phase 1 mega test file, keeping cloud CLI config recursion coverage coupled to the oversized orchestrator test module. | Moved the gcloud and AWS CLI config fixtures into `tests.phase1.cloud_cli_artifact_cases` and kept the original pytest nodes as thin wrappers. The preserved cloud CLI wrapper tests, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B398 | 2026-08-16 | The Azure and OCI CLI config artifact regressions still lived inline in the Phase 1 mega test file, leaving adjacent cloud CLI config recursion coverage coupled to the oversized orchestrator test module. | Moved the Azure and OCI CLI config fixtures into `tests.phase1.cloud_cli_artifact_cases` and kept the original pytest nodes as thin wrappers. The preserved Azure/OCI wrapper tests, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B399 | 2026-08-16 | Backup and sync repository artifact regressions for rclone, Kopia, Restic, Borg, Duplicacy, and Duplicati still lived inline in the Phase 1 mega test file, keeping passive repository/cloud recursion fixtures coupled to the oversized orchestrator test module. | Moved the backup/sync repository fixtures into `tests.phase1.backup_config_artifact_cases` and kept the original six pytest nodes as thin wrappers. The preserved wrapper tests, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B400 | 2026-08-16 | Package-index credential stripping and detect-secrets baseline regressions still lived inline in the Phase 1 mega test file, keeping passive secret-redaction fixture setup coupled to the oversized orchestrator test module. | Moved the package index credential fixture into `tests.phase1.package_manager_artifact_cases` and the detect-secrets baseline fixture into `tests.phase1.security_scanner_artifact_cases`, with the original pytest nodes retained as thin wrappers. The preserved wrapper tests, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B401 | 2026-08-16 | Network DSN credential stripping and ORM/database config artifact regressions still lived inline in the Phase 1 mega test file, keeping database endpoint extraction and secret-redaction fixtures coupled to the oversized orchestrator test module. | Moved the DSN and ORM config fixtures into `tests.phase1.database_config_artifact_cases` and kept the original pytest nodes as thin wrappers. The preserved wrapper tests plus source-aware ORM format-label test, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B402 | 2026-08-16 | Framework database config artifact regressions for Rails, Spring, .NET, Alembic, Laravel, and Django still lived inline in the Phase 1 mega test file, leaving adjacent database framework fixtures coupled to the oversized orchestrator test module. | Moved the framework config fixture into `tests.phase1.database_config_artifact_cases` and kept the original pytest node as a thin wrapper. The preserved wrapper test plus source-aware framework format-label test, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B403 | 2026-08-16 | Network endpoint extractor regressions for remote-access fields, Ansible inventory, Vagrant hostnames, and bounded worker ordering still lived inline in the Phase 1 mega test file, keeping passive endpoint parser fixtures coupled to the oversized orchestrator test module. | Moved the first network endpoint extractor fixture group into `tests.phase1.network_endpoint_artifact_cases` and kept the original pytest nodes as thin wrappers. The preserved wrapper tests, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B404 | 2026-08-16 | Additional network endpoint extractor regressions for DNS zones, hosts files, DHCP leases, VPN endpoints, and bounded per-line worker ordering still lived inline in the Phase 1 mega test file, leaving adjacent passive endpoint parser fixtures coupled to the oversized orchestrator test module. | Moved the DNS/hosts/DHCP/VPN endpoint fixture group into `tests.phase1.network_endpoint_artifact_cases` and kept the original pytest nodes as thin wrappers. The preserved wrapper tests, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B405 | 2026-08-16 | DNS zone and VPN endpoint artifact-queue regressions still lived inline in the Phase 1 mega test file, keeping full passive endpoint artifact fixtures coupled to the oversized orchestrator test module. | Moved the DNS zone and VPN endpoint artifact fixtures into `tests.phase1.network_endpoint_artifact_cases` and kept the original pytest nodes as thin wrappers. The preserved wrapper tests, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B406 | 2026-08-16 | Tunnel config structured-payload and artifact-queue regressions still lived inline in the Phase 1 mega test file, keeping Cloudflared/ngrok/Tailscale/localtunnel endpoint fixture coverage coupled to the oversized orchestrator test module. | Moved the tunnel config fixtures into `tests.phase1.network_endpoint_artifact_cases` and kept the original pytest nodes as thin wrappers. The preserved wrapper tests, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B407 | 2026-08-16 | Ansible inventory and cloud-init endpoint artifact regressions still lived inline in the Phase 1 mega test file, keeping adjacent infrastructure endpoint fixtures coupled to the oversized orchestrator test module. | Moved the Ansible inventory and cloud-init fixtures into `tests.phase1.network_endpoint_artifact_cases` and kept the original pytest nodes as thin wrappers. The preserved wrapper tests, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B408 | 2026-08-16 | OS installer and Ignition/Butane bootstrap artifact regressions still lived inline in the Phase 1 mega test file, coupling bootstrap endpoint fixtures to the oversized orchestrator test module. | Moved the OS installer and Ignition/Butane fixtures into `tests.phase1.network_endpoint_artifact_cases` and kept the original pytest nodes as thin wrappers. The preserved wrapper tests, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B409 | 2026-08-16 | DHCP lease and hosts-file artifact regressions still lived inline in the Phase 1 mega test file after adjacent endpoint parser fixtures had moved, keeping endpoint artifact archive/cloud coverage coupled to the oversized orchestrator test module. | Moved the DHCP lease and hosts-file artifact fixtures into `tests.phase1.network_endpoint_artifact_cases` and kept the original pytest nodes as thin wrappers. The preserved wrapper tests, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B410 | 2026-08-16 | Diagram and design artifact regressions for Draw.io, Excalidraw, Mermaid, PlantUML, and nested diagram archives still lived inline in the Phase 1 mega test file, keeping creative artifact metadata coverage coupled to the oversized orchestrator test module. | Added `tests.phase1.creative_artifact_cases` and moved the diagram/design fixture into it while keeping the original pytest node as a thin wrapper. The preserved wrapper test, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B411 | 2026-08-16 | Apple resource metadata artifact regressions for Storyboard, Xcode privacy manifests, string catalogs, XIB, stringsdict, and asset catalogs still lived inline in the Phase 1 mega test file, keeping resource metadata coverage coupled to the oversized orchestrator test module. | Moved the Apple resource metadata fixture into `tests.phase1.creative_artifact_cases` and kept the original pytest node as a thin wrapper. The preserved wrapper test, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B412 | 2026-08-16 | Game engine metadata artifact regressions for Unity, Unreal, shaders, plugins, and nested game asset archives still lived inline in the Phase 1 mega test file, keeping game-resource metadata coverage coupled to the oversized orchestrator test module. | Moved the game engine metadata fixture into `tests.phase1.creative_artifact_cases` and kept the original pytest node as a thin wrapper. The preserved wrapper test, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B413 | 2026-08-16 | Cargo credential redaction regression still lived inline in the Phase 1 mega test file even though adjacent package-manager credential fixtures had been extracted, keeping Cargo-specific token non-persistence coverage coupled to the oversized orchestrator test module. | Moved the Cargo credentials fixture into `tests.phase1.package_manager_artifact_cases` and kept the original pytest node as a thin wrapper. The preserved wrapper test, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B414 | 2026-08-16 | JVM build metadata artifact regressions for SBT, Maven POM/settings/config, Mill, repositories, and nested JVM build archives still lived inline in the Phase 1 mega test file, keeping package build metadata coverage coupled to the oversized orchestrator test module. | Moved the JVM build metadata fixture into `tests.phase1.package_manager_artifact_cases` and kept the original pytest node as a thin wrapper. The preserved wrapper test, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B415 | 2026-08-16 | Maven XML structured payload worker-order regression still lived inline in the Phase 1 mega test file after the adjacent JVM build fixture moved, keeping package-manager parser batching coverage coupled to the oversized orchestrator test module. | Moved the Maven XML structured payload worker-order fixture into `tests.phase1.package_manager_artifact_cases` and kept the original pytest node as a thin wrapper. The preserved wrapper test, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B416 | 2026-08-16 | Recon-tool structured payload worker-order regression still lived inline in the Phase 1 mega test file even though adjacent recon worker coverage existed elsewhere, keeping recon parser batching coverage coupled to the oversized orchestrator test module. | Added `tests.phase1.recon_tool_artifact_cases` and moved the recon-tool structured payload worker-order fixture into it while keeping the original pytest node as a thin wrapper. The preserved wrapper test, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B417 | 2026-08-16 | Gradle structured payload worker-order regression still lived inline in the Phase 1 mega test file after adjacent Maven/JVM package-manager fixtures moved, keeping repository URL batching coverage coupled to the oversized orchestrator test module. | Moved the Gradle structured payload worker-order fixture into `tests.phase1.package_manager_artifact_cases` and kept the original pytest node as a thin wrapper. The preserved wrapper test, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B418 | 2026-08-16 | JS runtime structured payload worker-order regression still lived inline in the Phase 1 mega test file, and extraction exposed that Deno/JSR package candidates could be sent to the worker batch twice before final output dedupe. | Added `tests.phase1.js_runtime_artifact_cases`, moved the JS runtime worker-order fixture into it, kept the original pytest node as a thin wrapper, and deduped raw JS runtime candidates before worker dispatch while preserving normalized final output. The preserved wrapper test plus adjacent JS runtime worker/config suites, Ruff, and py_compile passed. No provider calls, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B419 | 2026-08-16 | JS runtime host-only promotion regressions for browser-test and frontend framework configs still lived inline in the Phase 1 mega test file, keeping Playwright/TestCafe/Vite batching coverage coupled to the oversized orchestrator test module. | Moved the Playwright, TestCafe, and frontend framework JS runtime host-only promotion fixtures into `tests.phase1.js_runtime_artifact_cases` and kept the original pytest nodes as thin wrappers. The preserved wrapper tests plus adjacent JS runtime worker/config suites, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B420 | 2026-08-16 | JS runtime host-only promotion regressions for mobile and deploy configs still lived inline in the Phase 1 mega test file after adjacent browser/frontend JS fixtures moved, keeping Expo/Capacitor/Cordova/Vercel/Netlify batching coverage coupled to the oversized orchestrator test module. | Moved the mobile/deploy JS runtime host-only promotion fixture into `tests.phase1.js_runtime_artifact_cases` and kept the original pytest node as a thin wrapper. The preserved wrapper test plus adjacent JS runtime worker/config suites, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B421 | 2026-08-16 | Nomad orchestration structured payload worker-order regression still lived inline in the Phase 1 mega test file, keeping Nomad artifact label gating and line-batch coverage coupled to the oversized orchestrator test module. | Added `tests.phase1.orchestration_artifact_cases` and moved the Nomad orchestration worker-order fixture into it while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent orchestration worker tests, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B422 | 2026-08-16 | API spec structured payload regressions for OpenAPI, API Blueprint, Arazzo, and OpenAPI Overlay still lived inline in the Phase 1 mega test file, keeping API-spec parser batching coverage coupled to the oversized orchestrator test module. | Added `tests.phase1.api_spec_artifact_cases` and moved the API spec parser fixtures into it while keeping the original pytest nodes as thin wrappers. The preserved wrappers plus adjacent API spec worker tests, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B423 | 2026-08-16 | API client structured payload regressions for Postman-style collections, SoapUI, JMeter, Artillery, and Gherkin/Karate still lived inline in the Phase 1 mega test file, keeping API-client parser batching coverage coupled to the oversized orchestrator test module. | Added `tests.phase1.api_client_artifact_cases` and moved the first API client parser fixtures into it while keeping the original pytest nodes as thin wrappers. The preserved wrappers plus adjacent API client worker/document tests, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B424 | 2026-08-16 | API client structured payload regressions for Selenium SIDE, Tavern, Dredd, Schemathesis, Pactum, and Pact contract still lived inline in the Phase 1 mega test file after the first API-client batch moved, keeping adjacent API-client parser batching coverage coupled to the oversized orchestrator test module. | Extended `tests.phase1.api_client_artifact_cases` with the second API client parser batch while keeping the original pytest nodes as thin wrappers. The preserved wrappers plus adjacent Pact depth guard and API client worker/document tests, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B425 | 2026-08-16 | API client structured payload regressions for PyRestTest, k6, and Locust still lived inline in the Phase 1 mega test file after adjacent API-client batches moved, leaving the last API-client parser batching fixtures coupled to the oversized orchestrator test module. | Extended `tests.phase1.api_client_artifact_cases` with the final API client parser batch while keeping the original pytest nodes as thin wrappers. The preserved wrappers plus adjacent API client worker/document tests, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B426 | 2026-08-16 | HTTP request and Hurl structured payload regressions still lived inline in the Phase 1 mega test file, keeping request-format parser batching coverage coupled to the oversized orchestrator test module. | Added `tests.phase1.http_request_artifact_cases` and moved the HTTP request/Hurl fixtures into it while keeping the original pytest nodes as thin wrappers. The preserved wrappers plus adjacent HTTP request worker tests, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B427 | 2026-08-16 | GraphQL config structured payload regression still lived inline in the Phase 1 mega test file, keeping GraphQL config parser batching coverage coupled to the oversized orchestrator test module. | Added `tests.phase1.graphql_config_artifact_cases` and moved the GraphQL config fixture into it while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent GraphQL config worker tests, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B428 | 2026-08-16 | Native build config artifact-ingestion regression for CMake, Meson, SCons, Autoconf, Automake, m4, and SConscript files still lived inline in the Phase 1 mega test file, keeping native build artifact coverage coupled to the oversized orchestrator test module. | Added `tests.phase1.native_build_artifact_cases` and moved the native build config fixture into it while keeping the original pytest node as a thin wrapper. The preserved wrapper, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B429 | 2026-08-16 | Template text artifact-ingestion regression for Helm/Jinja/Go/Mustache/Handlebars/ERB/EJS/Liquid templates still lived inline in the Phase 1 mega test file, keeping template artifact coverage coupled to the oversized orchestrator test module. | Added `tests.phase1.template_artifact_cases` and moved the template text fixture into it while keeping the original pytest node as a thin wrapper. The preserved wrapper, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B430 | 2026-08-16 | HTTP and GraphQL local artifact-ingestion regression still lived inline in the Phase 1 mega test file, keeping request/GraphQL artifact coverage coupled to the oversized orchestrator test module. | Added `tests.phase1.http_graphql_artifact_cases` and moved the HTTP/GraphQL text fixture into it while keeping the original pytest node as a thin wrapper. The preserved wrapper, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B431 | 2026-08-16 | API spec and client collection artifact-ingestion regression still lived inline in the Phase 1 mega test file, keeping OpenAPI/API Blueprint/Arazzo/Overlay/Postman/Hoppscotch/Thunder/SoapUI/JMeter/API-test-client coverage coupled to the oversized orchestrator test module. | Added `tests.phase1.api_collection_artifact_cases` and moved the API spec/client collection fixture into it while keeping the original pytest node as a thin wrapper. The preserved wrapper, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B432 | 2026-08-16 | Pact contract artifact-ingestion regression still lived inline in the Phase 1 mega test file, keeping Pact format classification, remote filename selection, nested archive parsing, and Pact seed/cloud extraction coverage coupled to the oversized orchestrator test module. | Added `tests.phase1.pact_artifact_cases` and moved the Pact contract fixture into it while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent Pact unit tests, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B433 | 2026-08-16 | Interface schema artifact-ingestion regression for Proto, AsyncAPI, Avro, Thrift, and GraphQL schema artifacts still lived inline in the Phase 1 mega test file, keeping interface-schema artifact coverage coupled to the oversized orchestrator test module. | Added `tests.phase1.interface_schema_artifact_cases` and moved the interface schema fixture into it while keeping the original pytest node as a thin wrapper. The preserved wrapper, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B434 | 2026-08-16 | Log and trace artifact-ingestion regression still lived inline in the Phase 1 mega test file, keeping log/trace artifact coverage coupled to the oversized orchestrator test module. | Added `tests.phase1.log_artifact_cases` and moved the log/trace fixture into it while keeping the original pytest node as a thin wrapper. The preserved wrapper, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B435 | 2026-08-16 | Crash diagnostic artifact-ingestion regression still lived inline in the Phase 1 mega test file, keeping crash/IPS/stacktrace/tombstone artifact coverage coupled to the oversized orchestrator test module. | Added `tests.phase1.crash_artifact_cases` and moved the crash diagnostic fixture into it while keeping the original pytest node as a thin wrapper. The preserved wrapper, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B436 | 2026-08-16 | Passive scan output artifact-ingestion regression still lived inline in the Phase 1 mega test file, keeping Nmap/GNmap/Nessus scan-output coverage coupled to the oversized orchestrator test module. | Added `tests.phase1.passive_scan_artifact_cases` and moved the scan-output fixture into it while keeping the original pytest node as a thin wrapper. The preserved wrapper, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B437 | 2026-08-16 | Recon tool output artifact-ingestion regression still lived inline in the Phase 1 mega test file, keeping Subfinder/Amass/httpx/Katana/GAU/Wayback coverage separate from the existing recon helper module. | Extended `tests.phase1.recon_tool_artifact_cases` with the recon tool output fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent structured-payload wrapper, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B438 | 2026-08-16 | Imported scanner JSON artifact-ingestion regression still lived inline in the Phase 1 mega test file, keeping Nuclei/Naabu/ffuf/Feroxbuster/dirsearch/ZAP JSON coverage separate from the scan-output helper module. | Extended `tests.phase1.passive_scan_artifact_cases` with the imported scanner JSON fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent passive scan wrapper, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B439 | 2026-08-16 | Screenshot tool output artifact-ingestion regression still lived inline in the Phase 1 mega test file, keeping Gowitness/EyeWitness/Aquatone output coverage coupled to the oversized orchestrator test module. | Extended `tests.phase1.passive_scan_artifact_cases` with the screenshot tool output fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent scan helper wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B440 | 2026-08-16 | Search recon provider export artifact-ingestion regression still lived inline in the Phase 1 mega test file, keeping Shodan/Censys/FOFA/urlscan/ZoomEye export coverage separate from the recon helper module. | Extended `tests.phase1.recon_tool_artifact_cases` with the search recon provider export fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent recon helper wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B441 | 2026-08-16 | Tech/WAF/TLS scanner output artifact-ingestion regression still lived inline in the Phase 1 mega test file, keeping WhatWeb/wafw00f/sslscan/testssl/SSLyze/RustScan coverage coupled to the oversized orchestrator test module. | Extended `tests.phase1.passive_scan_artifact_cases` with the tech/WAF/TLS scanner fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent scan helper wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B442 | 2026-08-16 | CMS scanner output artifact-ingestion regression still lived inline in the Phase 1 mega test file, keeping WPScan/CMSmap/Droopescan/JoomScan/CMSeeK coverage coupled to the oversized orchestrator test module. | Extended `tests.phase1.passive_scan_artifact_cases` with the CMS scanner fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent scan helper wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B443 | 2026-08-16 | DNS resolver and takeover output artifact-ingestion regression still lived inline in the Phase 1 mega test file, keeping MassDNS/PureDNS/dnsrecon/dnsenum/Subjack/Subzy coverage separate from the network endpoint helper module. | Extended `tests.phase1.network_endpoint_artifact_cases` with the DNS resolver/takeover output fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent DNS endpoint helper wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B444 | 2026-08-16 | SARIF scan artifact-ingestion regression still lived inline in the Phase 1 mega test file, keeping SARIF seed/cloud extraction coverage separate from the security scanner helper module. | Extended `tests.phase1.security_scanner_artifact_cases` with the SARIF scan artifact fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent security scanner helper wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B445 | 2026-08-16 | SBOM and bundled security tool output artifact-ingestion regression still lived inline in the Phase 1 mega test file, keeping SPDX/package URL expansion plus nested Nuclei/Gitleaks/Semgrep/Masscan coverage separate from the security scanner helper module. | Extended `tests.phase1.security_scanner_artifact_cases` with the SBOM/security tool output fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent security scanner helper wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B446 | 2026-08-16 | Supply-chain attestation and VEX artifact-ingestion regression still lived inline in the Phase 1 mega test file, keeping in-toto/SLSA/VEX/CSAF/OSV coverage separate from the security scanner helper module. | Extended `tests.phase1.security_scanner_artifact_cases` with the supply-chain attestation/VEX fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent security scanner helper wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B447 | 2026-08-16 | CI test and coverage report artifact-ingestion regression still lived inline in the Phase 1 mega test file, keeping LCOV/TRX/TAP/Cobertura/Jacoco/JUnit coverage separate from the CI workflow helper module. | Extended `tests.phase1.ci_workflow_artifact_cases` with the CI test/coverage report fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent CI helper wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B448 | 2026-08-16 | Dependency lock and manifest artifact-ingestion regression still lived inline in the Phase 1 mega test file, keeping Yarn/Pipfile/go/Gradle/OPAM/Dune/Cabal/Bun/Cargo lock coverage separate from the package-manager helper module. | Extended `tests.phase1.package_manager_artifact_cases` with the dependency lock/manifest fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent package-manager helper wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B449 | 2026-08-16 | SSH known-host and authorized-key artifact regressions still lived inline in the Phase 1 mega test file, keeping recursive SSH host filtering and bounded static-worker coverage separate from the network endpoint helper module. | Extended `tests.phase1.network_endpoint_artifact_cases` with the SSH artifact queue fixture and SSH host worker-order fixture while keeping the original pytest nodes as thin wrappers. The preserved wrappers plus adjacent network endpoint helper wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B450 | 2026-08-16 | Package archive artifact-ingestion regression still lived inline in the Phase 1 mega test file, keeping wheel/NuGet/VSIX/Crate archive extraction coverage separate from the package-manager helper module. | Extended `tests.phase1.package_manager_artifact_cases` with the package archive fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent package-manager archive wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B451 | 2026-08-16 | Debian/IPK and CPIO package artifact-ingestion regressions still lived inline in the Phase 1 mega test file, keeping ar/tar/cpio package extraction coverage separate from the package-manager helper module. | Extended `tests.phase1.package_manager_artifact_cases` with the Debian/IPK and CPIO package fixtures while keeping the original pytest nodes as thin wrappers. The preserved wrappers plus adjacent package archive wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B452 | 2026-08-16 | Electron ASAR artifact-ingestion regressions still lived inline in the Phase 1 mega test file, keeping direct and nested ASAR extraction coverage separate from the JS runtime helper module. | Extended `tests.phase1.js_runtime_artifact_cases` with the direct and nested Electron ASAR fixtures while keeping the original pytest nodes as thin wrappers. The preserved wrappers plus adjacent JS runtime helper wrapper, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B453 | 2026-08-16 | RPM package artifact-ingestion regression still lived inline in the Phase 1 mega test file, keeping RPM header plus compressed CPIO payload extraction coverage separate from the package-manager helper module. | Extended `tests.phase1.package_manager_artifact_cases` with the RPM package fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent system package wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B454 | 2026-08-16 | Terraform plan artifact-ingestion regression still lived inline in the Phase 1 mega test file, keeping tfplan binary payload extraction coverage without a focused Terraform helper module. | Added `tests.phase1.terraform_artifact_cases` with the Terraform plan fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent artifact wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B455 | 2026-08-16 | Columnar data export artifact-ingestion regression still lived inline in the Phase 1 mega test file, keeping Parquet/ORC/Avro/Arrow/Feather/HDF5 binary payload coverage without a focused data-export helper module. | Added `tests.phase1.data_export_artifact_cases` with the columnar data export fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent artifact wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B456 | 2026-08-16 | Model binary artifact-ingestion regression still lived inline in the Phase 1 mega test file, keeping ONNX/Safetensors/Joblib/Pickle/Torch payload coverage separate from the data-export helper module. | Extended `tests.phase1.data_export_artifact_cases` with the model binary fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent data-export/mobile artifact wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B457 | 2026-08-16 | Compiled mobile/JVM artifact-ingestion regression still lived inline in the Phase 1 mega test file, keeping DEX/Class/OAT/ODEX/VDEX payload coverage without a focused mobile binary helper module. | Added `tests.phase1.mobile_binary_artifact_cases` with the compiled mobile/JVM fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent data/model/mobile artifact wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B458 | 2026-08-16 | Keras model archive artifact-ingestion regression still lived inline in the Phase 1 mega test file, keeping `.keras` ZIP plus embedded HDF5 payload coverage separate from the data-export helper module. | Extended `tests.phase1.data_export_artifact_cases` with the Keras model archive fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent model/installer artifact wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B459 | 2026-08-16 | Installer binary artifact-ingestion regression still lived inline in the Phase 1 mega test file, keeping MSI and PKG/XAR payload coverage without a focused installer binary helper module. | Added `tests.phase1.installer_binary_artifact_cases` with the installer binary fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent Keras/WASM artifact wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B460 | 2026-08-16 | WASM binary artifact-ingestion regression still lived inline in the Phase 1 mega test file, keeping direct and nested WebAssembly string/payload extraction coverage separate from the mobile binary helper module. | Extended `tests.phase1.mobile_binary_artifact_cases` with the WASM binary fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent installer/mobile artifact wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B461 | 2026-08-16 | Keystore binary artifact-ingestion regression still lived inline in the Phase 1 mega test file, keeping Keystore, PKCS#12, and nested truststore ZIP payload coverage without a focused key-material binary helper module. | Added `tests.phase1.key_material_binary_artifact_cases` with the keystore binary fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent WASM/certificate artifact wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B462 | 2026-08-16 | Certificate binary artifact-ingestion regression still lived inline in the Phase 1 mega test file, keeping DER, CRL, and nested P7B ZIP payload coverage separate from the key-material binary helper module. | Extended `tests.phase1.key_material_binary_artifact_cases` with the certificate binary fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent keystore/dump artifact wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B463 | 2026-08-16 | Dump/profile binary artifact-ingestion regression still lived inline in the Phase 1 mega test file, keeping Redis RDB, HPROF, PPROF, CPU profile, LevelDB, and heapdump payload coverage without a focused dump binary helper module. | Added `tests.phase1.dump_binary_artifact_cases` with the dump/profile binary fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent certificate and 7z artifact wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B464 | 2026-08-16 | 7z archive artifact-ingestion regression still lived inline in the Phase 1 mega test file, keeping 7z payload extraction coverage without a focused archive helper module. | Added `tests.phase1.archive_binary_artifact_cases` with the 7z archive fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent dump/native artifact wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B465 | 2026-08-16 | Native binary artifact-ingestion regression still lived inline in the Phase 1 mega test file, keeping ELF/shared library and nested DLL ZIP payload coverage without a focused native binary helper module. | Added `tests.phase1.native_binary_artifact_cases` with the native binary fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent 7z/firmware artifact wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B466 | 2026-08-16 | Firmware binary artifact-ingestion regression still lived inline in the Phase 1 mega test file, keeping firmware BIN and nested native-agent ZIP payload coverage without a focused firmware binary helper module. | Added `tests.phase1.firmware_binary_artifact_cases` with the firmware binary fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent native/firmware-image artifact wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B467 | 2026-08-16 | Firmware image artifact-ingestion regression still lived inline in the Phase 1 mega test file, keeping `.fw`, nested ROM, and nested IMG ZIP payload coverage separate from the firmware binary helper module. | Extended `tests.phase1.firmware_binary_artifact_cases` with the firmware image fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent firmware/managed-hosting artifact wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B468 | 2026-08-16 | Managed-hosting artifact promotion regression still lived inline in the Phase 1 mega test file, keeping provider-specific hosting asset promotion and public-host non-promotion coverage coupled to the oversized orchestrator test module. | Added `tests.phase1.managed_hosting_artifact_cases` with the managed-hosting fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent firmware-image/bundle archive wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B469 | 2026-08-16 | Zip-backed bundle archive artifact-ingestion regression still lived inline in the Phase 1 mega test file, keeping AAR, WAR, and JAR archive coverage separate from the archive helper module. | Extended `tests.phase1.archive_binary_artifact_cases` with the zip-backed bundle archive fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent managed-hosting/disk-image artifact wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B470 | 2026-08-16 | Disk image artifact-ingestion regression still lived inline in the Phase 1 mega test file, keeping DMG, ISO, and nested ISO ZIP payload coverage plus remote/content-type classification assertions separate from the archive helper module. | Extended `tests.phase1.archive_binary_artifact_cases` with the disk image fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent bundle archive/virtual disk artifact wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B471 | 2026-08-16 | Virtual disk artifact-ingestion regression still lived inline in the Phase 1 mega test file, keeping VHDX, QCOW2, OVA, nested VMDK, and OVF payload coverage plus remote/content-type classification assertions separate from the archive helper module. | Extended `tests.phase1.archive_binary_artifact_cases` with the virtual disk fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent disk-image/virtual-machine-config artifact wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B472 | 2026-08-16 | Virtual-machine config artifact-ingestion regression still lived inline in the Phase 1 mega test file, keeping VMX, VBox, Vagrantfile, VMXF, and PVS coverage plus remote/content-type classification assertions coupled to the oversized orchestrator test module. | Added `tests.phase1.virtual_machine_config_artifact_cases` with the virtual-machine config fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent virtual-disk/shortcut-link artifact wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B473 | 2026-08-16 | Shortcut/link artifact-ingestion regression still lived inline in the Phase 1 mega test file, keeping URL, website, webloc, desktop link, and LNK coverage plus remote/content-type classification assertions coupled to the oversized orchestrator test module. | Added `tests.phase1.shortcut_link_artifact_cases` with the shortcut/link fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent virtual-machine-config/shell-history artifact wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B474 | 2026-08-16 | Shell-history artifact-ingestion regression still lived inline in the Phase 1 mega test file, keeping bash history, PowerShell history, and nested zsh-history ZIP coverage plus name/remote classification assertions coupled to the oversized orchestrator test module. | Added `tests.phase1.shell_history_artifact_cases` with the shell-history fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent shortcut-link/Windows-registry-export artifact wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B475 | 2026-08-16 | Windows registry export artifact-ingestion regression still lived inline in the Phase 1 mega test file, keeping UTF-16 `.reg` payload coverage plus remote/content-type classification assertions coupled to the oversized orchestrator test module. | Added `tests.phase1.windows_registry_artifact_cases` with the registry export fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent shell-history/Windows-registry-hive artifact wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B476 | 2026-08-16 | Windows registry hive artifact-ingestion regression still lived inline in the Phase 1 mega test file, keeping NTUSER, SOFTWARE, and nested UsrClass hive coverage plus name/remote classification and remote filename normalization assertions coupled to the oversized orchestrator test module. | Extended `tests.phase1.windows_registry_artifact_cases` with the registry hive fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent registry-export/Windows-event-trace artifact wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B477 | 2026-08-16 | Windows event trace artifact-ingestion regression still lived inline in the Phase 1 mega test file, keeping EVTX and ETL coverage plus remote/content-type classification assertions coupled to the oversized orchestrator test module. | Added `tests.phase1.windows_event_artifact_cases` with the Windows event trace fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent registry-hive/Windows-execution-history artifact wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B478 | 2026-08-16 | Windows execution-history artifact-ingestion regression still lived inline in the Phase 1 mega test file, keeping Prefetch, automaticDestinations-ms, and customDestinations-ms coverage plus remote/content-type classification assertions coupled to the oversized orchestrator test module. | Extended `tests.phase1.windows_event_artifact_cases` with the Windows execution-history fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent event-trace/browser-extension artifact wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B479 | 2026-08-16 | Browser extension package artifact-ingestion regression still lived inline in the Phase 1 mega test file, keeping XPI and CRX coverage plus remote classification assertions coupled to the oversized orchestrator test module. | Added `tests.phase1.browser_extension_artifact_cases` with the browser extension package fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent Windows-execution-history/nested-mobile-config artifact wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B480 | 2026-08-16 | Nested mobile config archive-bundle artifact-ingestion regression still lived inline in the Phase 1 mega test file, keeping ZIP-wrapped IPA Firebase plist and Supabase JavaScript payload coverage coupled to the oversized orchestrator test module. | Added `tests.phase1.nested_mobile_artifact_cases` with the nested mobile archive-bundle fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent browser-extension/nested archive-style mobile bundle wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B481 | 2026-08-16 | Nested archive-style mobile bundle artifact-ingestion regression still lived inline in the Phase 1 mega test file, keeping ZIP-wrapped XAPK/APK Firebase, GCS, and Supabase payload coverage coupled to the oversized orchestrator test module. | Extended `tests.phase1.nested_mobile_artifact_cases` with the nested archive-style mobile bundle fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent nested IPA archive-bundle/nested 7z mobile wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B482 | 2026-08-16 | Nested 7z mobile config artifact-ingestion regression still lived inline in the Phase 1 mega test file, keeping optional-py7zr XAPK/APK Firebase, GCS, and Supabase payload coverage coupled to the oversized orchestrator test module. | Extended `tests.phase1.nested_mobile_artifact_cases` with the nested 7z mobile fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent nested archive-style mobile bundle/nested ZIP worker-order wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B483 | 2026-08-16 | Nested ZIP mobile worker-order regression still lived inline in the Phase 1 mega test file, keeping bounded worker concurrency, result-order preservation, Firebase aggregation, Supabase aggregation, and payload ordering checks coupled to the oversized orchestrator test module. | Extended `tests.phase1.nested_mobile_artifact_cases` with the nested ZIP worker-order fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent nested 7z mobile/nested 7z worker-order wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B484 | 2026-08-16 | Nested 7z mobile worker-order regression still lived inline in the Phase 1 mega test file, keeping optional-py7zr bounded worker concurrency, result-order preservation, Firebase aggregation, Supabase aggregation, and payload ordering checks coupled to the oversized orchestrator test module. | Extended `tests.phase1.nested_mobile_artifact_cases` with the nested 7z worker-order fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent nested ZIP worker-order/nested ZIP planning-order wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B485 | 2026-08-16 | Nested ZIP mobile member-planning worker-order regression still lived inline in the Phase 1 mega test file, keeping ZIP member-entry planning, ignored-member filtering, bounded concurrency, result-order preservation, and payload ordering checks coupled to the oversized orchestrator test module. | Extended `tests.phase1.nested_mobile_artifact_cases` with the nested ZIP member-planning fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent nested 7z worker-order/nested ZIP job-planning wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B486 | 2026-08-16 | Nested ZIP mobile member-job planning worker-order regression still lived inline in the Phase 1 mega test file, keeping member-job planning, gate-backed parallel entry, ignored-member exclusion, bounded concurrency, result-order preservation, and payload ordering checks coupled to the oversized orchestrator test module. | Extended `tests.phase1.nested_mobile_artifact_cases` with the nested ZIP member-job planning fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent nested ZIP member-planning/nested TAR worker-order wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B487 | 2026-08-16 | Nested TAR mobile worker-order regression still lived inline in the Phase 1 mega test file, keeping TAR member extraction, bounded worker concurrency, result-order preservation, Firebase aggregation, Supabase aggregation, and payload ordering checks coupled to the oversized orchestrator test module. | Extended `tests.phase1.nested_mobile_artifact_cases` with the nested TAR worker-order fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent nested ZIP job-planning/nested TAR member-planning wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B488 | 2026-08-16 | Nested TAR mobile member-planning worker-order regression still lived inline in the Phase 1 mega test file, keeping TAR member-entry planning, ignored-member filtering, gate-backed parallel entry, bounded concurrency, result-order preservation, and payload ordering checks coupled to the oversized orchestrator test module. | Extended `tests.phase1.nested_mobile_artifact_cases` with the nested TAR member-planning fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent nested TAR worker-order/nested TAR job-planning wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B489 | 2026-08-16 | Nested TAR mobile member-job planning worker-order regression still lived inline in the Phase 1 mega test file, keeping TAR member-job planning, gate-backed parallel entry, ignored-member exclusion, bounded concurrency, result-order preservation, and payload ordering checks coupled to the oversized orchestrator test module. | Extended `tests.phase1.nested_mobile_artifact_cases` with the nested TAR member-job planning fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent nested TAR member-planning/nested mobile result-batch wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B490 | 2026-08-16 | Nested mobile member result-batch worker-order regression still lived inline in the Phase 1 mega test file, keeping bounded result-batch concurrency, payload ordering, Firebase project ordering/extract paths, and Supabase config ordering/extract paths coupled to the oversized orchestrator test module. | Extended `tests.phase1.nested_mobile_artifact_cases` with the nested mobile result-batch fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent nested TAR job-planning/APKM bundle wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B491 | 2026-08-16 | APKM nested mobile config artifact-ingestion regression still lived inline in the Phase 1 mega test file, keeping nested APKM Firebase, GCS, Supabase, key-finding, seed promotion, synthesis root-domain guarding, and artifact metadata assertions coupled to the oversized orchestrator test module. | Extended `tests.phase1.nested_mobile_artifact_cases` with the APKM bundle fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent nested mobile result-batch/Android App Bundle wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B492 | 2026-08-16 | Android App Bundle artifact-ingestion regression still lived inline in the Phase 1 mega test file, keeping AAB Firebase, GCS, Supabase, key-finding, email extraction, seed promotion, and artifact metadata assertions coupled to the oversized orchestrator test module. | Extended `tests.phase1.nested_mobile_artifact_cases` with the AAB fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent APKM bundle/OpenDocument wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B493 | 2026-08-16 | OpenDocument text artifact-ingestion regression still lived inline in the Phase 1 mega test file, keeping ODT email/URL extraction, S3/GCS cloud asset promotion, seed promotion, and artifact metadata format assertions coupled to the oversized orchestrator test module. | Added `tests.phase1.opendocument_artifact_cases` with the ODT fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent Android App Bundle/ODS+ODP suite wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B494 | 2026-08-16 | OpenDocument spreadsheet/presentation artifact-ingestion regression still lived inline in the Phase 1 mega test file, keeping ODS/ODP email and URL extraction, GCS/Supabase/Azure Blob cloud asset promotion, seed promotion, and artifact metadata format assertions coupled to the oversized orchestrator test module. | Extended `tests.phase1.opendocument_artifact_cases` with the ODS/ODP suite fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent ODT/EPUB wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B495 | 2026-08-16 | EPUB artifact-ingestion regression still lived inline in the Phase 1 mega test file, keeping EPUB email/URL extraction, Firebase/GCS cloud asset promotion, seed promotion, and artifact metadata format assertions coupled to the oversized orchestrator test module. | Added `tests.phase1.document_artifact_cases` with the EPUB fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent ODS/ODP suite/MHTML wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B496 | 2026-08-16 | MHTML artifact-ingestion regression still lived inline in the Phase 1 mega test file, keeping MHTML header/body/attachment email extraction, URL seed promotion, Firebase/GCS/Supabase cloud asset promotion, and artifact metadata format assertions coupled to the oversized orchestrator test module. | Extended `tests.phase1.document_artifact_cases` with the MHTML fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent EPUB/EML wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B497 | 2026-08-16 | EML body and nested attachment artifact-ingestion regression still lived inline in the Phase 1 mega test file, keeping EML header/plain/HTML body extraction, nested ZIP and DOCX attachment parsing, Firebase/Supabase/S3 promotion, email and URL seed promotion, and artifact metadata format assertions coupled to the oversized orchestrator test module. | Extended `tests.phase1.document_artifact_cases` with the EML fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent MHTML/PDF wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B498 | 2026-08-16 | Email attachment part concurrency regression still lived inline in the Phase 1 mega test file, keeping bounded EML attachment worker usage, payload order preservation, message metadata extraction, and attachment payload promotion coupled to the oversized orchestrator test module. | Extended `tests.phase1.document_artifact_cases` with the email attachment concurrency fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent EML body/nested-attachment and email part-planning wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B499 | 2026-08-16 | Email part-planning concurrency regression still lived inline in the Phase 1 mega test file, keeping parallel email part-entry planning, nested message handling, attachment job routing, payload order preservation, and bounded worker fanout coupled to the oversized orchestrator test module. | Extended `tests.phase1.document_artifact_cases` with the email part-planning concurrency fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent email attachment and nested-email job-planning wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B500 | 2026-08-16 | Nested email message job-planning concurrency regression still lived inline in the Phase 1 mega test file, keeping nested message job creation, empty nested payload filtering, bounded nested worker fanout, attachment job routing, and payload order preservation coupled to the oversized orchestrator test module. | Extended `tests.phase1.document_artifact_cases` with the nested email message job-planning fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent email part-planning and email part payload entry wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B501 | 2026-08-16 | Email part payload-entry concurrency regression still lived inline in the Phase 1 mega test file, keeping direct payload entries, extraction-job payload entries, empty payload filtering, bounded payload batch fanout, and payload order preservation coupled to the oversized orchestrator test module. | Extended `tests.phase1.document_artifact_cases` with the email part payload-entry fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent nested-email job-planning and nested-email part-message wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B502 | 2026-08-16 | Nested email part-message concurrency regression still lived inline in the Phase 1 mega test file, keeping concurrent nested message payload extraction, nested depth propagation, payload byte routing, and result order preservation coupled to the oversized orchestrator test module. | Extended `tests.phase1.document_artifact_cases` with the nested email part-message fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent email part payload-entry and nested email payload-entry wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B503 | 2026-08-16 | Nested email payload-entry concurrency regression still lived inline in the Phase 1 mega test file, keeping nested message payload batch fanout, empty nested payload filtering, ignored tuple filtering, nested depth propagation, and result order preservation coupled to the oversized orchestrator test module. | Extended `tests.phase1.document_artifact_cases` with the nested email payload-entry fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent nested email part-message and email part decoding wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B504 | 2026-08-16 | Email part decoding charset-order regression still lived inline in the Phase 1 mega test file, keeping parallel charset decode attempts, invalid charset fallback, first successful decoded text selection, and bounded worker fanout coupled to the oversized orchestrator test module. | Extended `tests.phase1.document_artifact_cases` with the email part decoding fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent nested email payload-entry and email metadata-line wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B505 | 2026-08-16 | Email metadata-line concurrency regression still lived inline in the Phase 1 mega test file, keeping bounded concurrent header reads, deterministic metadata line ordering, optional header handling, and message metadata payload inputs coupled to the oversized orchestrator test module. | Extended `tests.phase1.document_artifact_cases` with the email metadata-line fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent email part decoding and email summary/part family wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B506 | 2026-08-16 | Email summary/part family concurrency regression still lived inline in the Phase 1 mega test file, keeping parallel message summary and leaf-part extraction families, metadata payload routing, body part payload routing, bounded worker fanout, and result order preservation coupled to the oversized orchestrator test module. | Extended `tests.phase1.document_artifact_cases` with the email summary/part family fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent email metadata-line and email payload family entry wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B507 | 2026-08-16 | Email payload family-entry concurrency regression still lived inline in the Phase 1 mega test file, keeping parallel summary/part payload family batching, ignored tuple filtering, empty payload filtering, metadata input forwarding, and result order preservation coupled to the oversized orchestrator test module. | Extended `tests.phase1.document_artifact_cases` with the email payload family-entry fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent email summary/part-family and EMLX ingestion wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B508 | 2026-08-16 | EMLX body and nested attachment artifact-ingestion regression still lived inline in the Phase 1 mega test file, keeping EMLX length-prefixed message extraction, nested EMLX-in-ZIP parsing, Firebase/Supabase/S3 promotion, email and URL seed promotion, and artifact metadata format/count assertions coupled to the oversized orchestrator test module. | Extended `tests.phase1.document_artifact_cases` with the EMLX fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent email payload family-entry and MSG ingestion wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B509 | 2026-08-16 | MSG body and nested attachment artifact-ingestion regression still lived inline in the Phase 1 mega test file, keeping fake OLE MSG stream parsing, nested MSG-in-ZIP parsing, S3/Firebase/GCS/Supabase promotion, email and URL seed promotion, and artifact metadata format/count assertions coupled to the oversized orchestrator test module. | Extended `tests.phase1.document_artifact_cases` with the MSG fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent EMLX and SAZ HTTP transcript wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B510 | 2026-08-16 | SAZ HTTP transcript artifact-ingestion regression still lived inline in the Phase 1 mega test file, keeping SAZ request/response transcript parsing, query secret redaction, origin/location URL promotion, S3/Firebase/Supabase promotion, email seed promotion, synthesis root-domain guarding for cloud refs, and artifact metadata payload counts coupled to the oversized orchestrator test module. | Extended `tests.phase1.http_request_artifact_cases` with the SAZ transcript fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent MSG and Charles session wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B511 | 2026-08-16 | Charles session JSON artifact-ingestion regression still lived inline in the Phase 1 mega test file, keeping Charles request/response JSON parsing, query secret redaction, redirect/direct URL promotion, S3/Firebase/Supabase promotion, email seed promotion, synthesis root-domain guarding for cloud refs, and artifact metadata payload counts coupled to the oversized orchestrator test module. | Extended `tests.phase1.http_request_artifact_cases` with the Charles session fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent SAZ and Burp site-map wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B512 | 2026-08-16 | Burp site-map XML artifact-ingestion regression still lived inline in the Phase 1 mega test file, keeping Burp site-map XML parsing, base64 request/response extraction, query secret redaction, redirect URL promotion, S3/Firebase/Supabase promotion, email seed promotion, synthesis root-domain guarding for cloud refs, and artifact metadata payload counts coupled to the oversized orchestrator test module. | Extended `tests.phase1.http_request_artifact_cases` with the Burp site-map fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent Charles session and HAR entry wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B513 | 2026-08-16 | HAR entry concurrency regression still lived inline in the Phase 1 mega test file, keeping HAR summary payload preservation, bounded entry worker fanout, per-entry payload routing, and result order preservation coupled to the oversized orchestrator test module. | Extended `tests.phase1.http_request_artifact_cases` with the HAR entry payload fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent Burp site-map and HAR entry job-planning wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B514 | 2026-08-16 | HAR entry job-planning concurrency regression still lived inline in the Phase 1 mega test file, keeping bounded HAR entry job planning, ordered job creation, per-entry payload routing, worker fanout, and result order preservation coupled to the oversized orchestrator test module. | Extended `tests.phase1.http_request_artifact_cases` with the HAR entry job-planning fixture while keeping the original pytest node as a thin wrapper. The preserved wrapper plus adjacent HAR entry and HAR entry payload wrappers, Ruff, and py_compile passed. No production parsing, provider, live probing, scope, validation, reporting, or scheduler behavior changed. |
| B515 | 2026-08-20 | External CTI/OSINT source research could be misread as permission to run unsafe public snippets, clone broad tool lists, scrape sensitive sources, or feed raw provider bodies into FORGE, while the local scheduled target-import automation was still enabled to consume upstream target feeds. | Disabled the local `FORGE Import theprawnhunter Targets` scheduled task on request and confirmed no active FORGE/telegramhunter/theprawnhunter process remained. Added passive/catalog-only CTI connector definitions for abuse.ch ThreatFox, abuse.ch URLHaus, and STIX/TAXII import; added CTI-specific provider pacing env controls; and added `forge.utils.intel.observations` for normalized observations, target-feed conversion, default-off sensitive source categories, unsafe public artifact tags, raw-artifact hashes, and command/secret redaction. Subagent research treated `ukr.pw`, `orlofv/Cyber-Threat-Intelligence`, and `jivoi/awesome-osint` as unsafe text only. Focused CTI/OSINT observation tests plus connector registry readiness guard, Ruff, and py_compile passed. No third-party command execution, scraping bypass, credential use, live provider call, kill-chain launch, validation/report gate, or persistent non-test engagement data changed. |
| B516 | 2026-08-20 | The safe CTI/OSINT observation model was only a local normalizer and connector catalog entry, so operators still lacked a production workflow path to import offline CTI JSON into an engagement with durable provenance, duplicate handling, audit receipts, and explicit scope-gated promotion into FORGE seeds. | Added `forge.connectors.cti` and `forge connectors import-cti` for offline CTI/OSINT observation JSON imports. The importer creates additive `cti_observations` storage, persists normalized fields plus raw-artifact hashes only, rejects sensitive observations by default, dedupes repeated observations, returns target-feed-shaped items for compatible indicators, audits normalized-only import summaries, and promotes seeds only when `--promote-targets` is set and the existing scope gate accepts the target. CTI connector registry entries now advertise the wired import path. Focused CTI import/CLI/registry tests, Ruff, and py_compile passed. No live provider calls, third-party tool execution, scraping bypass, credential use, kill-chain launch, validation/report gate, or persistent non-test engagement data changed. |
| B517 | 2026-08-20 | Durable CTI/OSINT observations were persisted but still invisible to analyst review/reporting surfaces, and surfacing them naively could expose source credentials/provenance secrets or make raw-provider fallback hashes act as secret oracles. | Added CTI observation inventory to Phase 6 context, template reports, raw CSV export rows, dashboard detail sections, section titles, and dashboard summary counts with explicit `Reportable=no`/`cti_reportable=False` labelling. CTI observations remain outside `vulnerability_findings`, `key_scanner_findings`, exploit counts, and validated-finding rows unless a later independent scanner validates promoted seeds. Hardened observation normalization so source URLs and provenance strings are redacted before persistence/surfacing, and fallback raw-artifact hashes now use sanitized canonical observation fields instead of raw provider objects. Focused CTI/reporting/dashboard/report-synthesizer tests, Ruff, py_compile, and `git diff --check` passed. No live provider calls, third-party command execution, scraping bypass, credential use, kill-chain launch, or active validation/reportability gates changed. |
| B518 | 2026-08-20 | The offline CTI importer accepted neutral observation JSON but still rejected common provider/export shapes such as ThreatFox `data` IOC rows, URLHaus URL rows, and STIX indicator bundles unless operators manually rewrote them into FORGE's neutral schema. | Added local provider-shape adapters inside `forge.connectors.cti` for ThreatFox `ioc`/`ioc_type`/`confidence_level` rows, URLHaus `url`/`url_status`/`dateadded` rows, and STIX indicator objects with simple observable patterns. The adapters convert only local JSON into the existing sanitized observation model, support `objects` payloads for STIX bundles, preserve explicit scope-gated seed promotion, and keep non-indicator STIX objects as rejected observations. Focused CTI provider-shape, connector registry, Ruff, and py_compile checks passed. No live provider calls, API credentials, scraping bypass, third-party command execution, kill-chain launch, active validation, or reportability gates changed. |
| B519 | 2026-08-20 | Operators could validate CTI export parsing only by running the importer, which wrote `cti_observations`, optional promoted seeds, and audit receipts even when they only wanted to preview provider-shape compatibility and scope-gated promotion results. | Added `--dry-run` support to `forge connectors import-cti` and `CtiObservationImportConfig`. Dry-run parses and sanitizes the offline CTI file, returns target-feed preview items, scope-checks promotion candidates, and reports `would_persist_count`/`would_promote_seed_count` while leaving `persisted_count`, `promoted_seed_count`, duplicate counts, `cti_observations`, `engagement_seeds`, and `audit_log` untouched. README documents dry-run preview behavior. Focused CTI dry-run/CLI tests, connector registry checks, Ruff, py_compile, and `git diff --check` passed. No live provider calls, API credentials, scraping bypass, third-party command execution, kill-chain launch, active validation, or reportability gates changed. |
| B520 | 2026-08-20 | CTI dry-run previews reported every parsed observation as `would_persist_count`, so operators could not tell whether an offline export would add new rows or mostly repeat observations already imported or repeated within the same file. | Dry-run now performs read-only duplicate detection against existing `cti_observations` and tracks in-file observation keys, reporting duplicates through `would_duplicate_count` while leaving actual duplicate/write counters at zero. Promotion preview now follows real import semantics by counting `would_promote_seed_count` only for observations that would newly persist and pass scope gates. README documents the duplicate preview counters. Focused CTI duplicate dry-run tests, Ruff, py_compile, and connector registry checks passed. No table creation, row writes, audit writes, seed writes, live provider calls, API credentials, scraping bypass, kill-chain launch, active validation, or reportability gates changed. |
| B521 | 2026-08-20 | Large offline CTI exports had no item budget, so an operator could accidentally process a full provider dump when they intended to test or import only a bounded prefix. | Added optional `limit` support to `CtiObservationImportConfig` and `forge connectors import-cti --limit N`. The importer processes only the bounded prefix when a limit is set and reports `limit`, `total_item_count`, `processed_item_count`, and `limited_item_count` in JSON and console summaries. README documents the limit behavior. Focused CTI limit/CLI tests, connector registry checks, Ruff, py_compile, and `git diff --check` passed. No default import behavior, live provider calls, API credentials, scraping bypass, third-party command execution, kill-chain launch, active validation, or reportability gates changed. |
| B522 | 2026-08-20 | Offline CTI exports could contain low-confidence noise, but operators had no first-class filter to exclude it before persistence, seed-promotion preview, or dry-run would-persist accounting. | Added optional `min_confidence` support to `CtiObservationImportConfig` and `forge connectors import-cti --min-confidence F`. Observations below the threshold are parsed, counted in `filtered_count`, and skipped with reason `below_min_confidence` before persistence or dry-run duplicate/promotion accounting. README documents the threshold behavior. Focused CTI write/dry-run filter tests, connector registry checks, Ruff, py_compile, and `git diff --check` passed. No reportability gates, active validation, live provider calls, API credentials, scraping bypass, third-party command execution, or kill-chain launch changed. |
| B523 | 2026-08-20 | Offline CTI exports could include TLP-restricted observations, but operators had no import-time data-handling gate to exclude observations above the level they were allowed to retain or preview. | Added optional `max_tlp` support to `CtiObservationImportConfig` and `forge connectors import-cti --max-tlp clear|green|amber|red`. Observations above the selected TLP rank are parsed, counted in `filtered_count`, and skipped with reason `above_max_tlp` before persistence, dry-run duplicate/promotion accounting, or seed promotion. README documents the TLP filter. Focused CTI write/dry-run TLP tests, connector registry checks, Ruff, py_compile, and `git diff --check` passed. No reportability gates, active validation, live provider calls, API credentials, scraping bypass, third-party command execution, or kill-chain launch changed. |
| B524 | 2026-08-20 | Large or stale offline CTI exports could not be bounded by observation time, forcing operators to import or preview old/future rows and making provider backfills harder to stage safely. | Added optional observed-time window support to `CtiObservationImportConfig` and `forge connectors import-cti --since ISO --until ISO`. Observations outside the window are parsed, counted in `filtered_count`, and skipped with `before_since` or `after_until` before persistence, dry-run duplicate/promotion accounting, or seed promotion; unparseable observed times are filtered when a window is requested. README documents the time-window filters. Focused CTI write/dry-run window tests, connector registry checks, Ruff, py_compile, and `git diff --check` passed. No reportability gates, active validation, live provider calls, API credentials, scraping bypass, third-party command execution, or kill-chain launch changed. |
| B525 | 2026-08-20 | The offline CTI importer handled JSON exports but still rejected common operator-downloaded CSV exports, requiring manual conversion before ThreatFox or URLHaus rows could enter the same sanitized inventory path. | Added CSV fallback parsing for non-JSON `forge connectors import-cti --report-file` inputs. Headered CSV rows flow through the existing ThreatFox/URLHaus provider adapters, normalizer, filters, duplicate preview, scope-gated seed promotion, and normalized-only persistence; malformed JSON still fails as JSON instead of being interpreted as CSV. README documents JSON or CSV import files, and results include `source_format`. Focused CTI CSV/malformed JSON tests, broader connector sanity tests, Ruff, py_compile, and `git diff --check` passed. No live provider calls, API credentials, scraping bypass, third-party command execution, kill-chain launch, active validation, or reportability gates changed. |
| B526 | 2026-08-20 | Large offline CTI imports returned totals but not composition, so operators had to inspect the DB or raw file to understand which indicator types, TLP levels, target-feed-compatible seeds, or skipped reasons dominated an import. | Added deterministic JSON result summaries: `parsed_indicator_type_counts`, `parsed_tlp_counts`, `target_feed_type_counts`, and `skipped_reason_counts`. The counts are derived from normalized observations and skip decisions, apply to dry-run and write modes, and do not persist extra raw provider data. README documents the summary dictionaries. Focused CTI tests, broader connector sanity tests, Ruff, py_compile, and `git diff --check` passed. No persistence semantics, reportability gates, live provider calls, API credentials, scraping bypass, third-party command execution, kill-chain launch, or active validation changed. |
| B527 | 2026-08-20 | Automated offline CTI import jobs could succeed with zero usable observations when a file was empty, unsupported, or fully filtered, making bad provider exports hard to catch before downstream review. | Added opt-in `fail_on_empty` support to `CtiObservationImportConfig` and `forge connectors import-cti --fail-on-empty`. The gate raises a sanitized error when no observations survive normalization and filters, while duplicate-only re-imports remain accepted because they prove the file still matches known observations. Dry-run failures do not write observations, seeds, or audit rows. README documents the automation gate. Focused CTI tests, broader connector sanity tests, Ruff, py_compile, and `git diff --check` passed. No default behavior, persistence semantics for accepted imports, reportability gates, live provider calls, API credentials, scraping bypass, third-party command execution, kill-chain launch, or active validation changed. |
| B528 | 2026-08-20 | Operators still had to manually decompress common provider `.gz` exports before using the offline CTI importer, adding avoidable handling steps for large local ThreatFox/URLHaus-style files. | Added local `.gz` report-file reading for `forge connectors import-cti`; compressed files are decompressed as UTF-8 text and then flow through the existing JSON/CSV parsing, sanitization, filters, duplicate handling, and optional promotion paths. README documents `.gz` report files. Focused CTI tests, broader connector sanity tests, Ruff, py_compile, and `git diff --check` passed. No live provider calls, API credentials, raw provider persistence, reportability gates, scraping bypass, third-party command execution, kill-chain launch, or active validation changed. |
| B529 | 2026-08-20 | Some offline provider exports arrive as ZIP archives, but CTI import still required operators to extract those archives manually and risk picking the wrong file from mixed archive contents. | Added constrained local `.zip` report-file reading for `forge connectors import-cti`. Archives are inspected without extracting to disk, unsupported archives fail with a sanitized error, the first supported JSON/CSV/GZ member by sorted archive name is selected deterministically, and oversized ZIP members are rejected before the existing sanitized JSON/CSV import path. README documents ZIP member selection. Focused CTI tests, broader connector sanity tests, Ruff, py_compile, and `git diff --check` passed. No live provider calls, API credentials, raw provider persistence, reportability gates, scraping bypass, third-party command execution, kill-chain launch, or active validation changed. |
| B530 | 2026-08-20 | Compressed CTI report imports could still expand too far before parsing because the size guard was ZIP-member focused and did not consistently cap top-level `.gz`, nested `.gz`, or plain local report text. | Added one shared decoded report-size cap for local CTI report-file reads. Plain files are rejected by file size, top-level gzip streams are read through a capped decompressor, ZIP members are capped before read, and nested gzip members inside ZIP archives are capped during decompression before the existing sanitized JSON/CSV parser runs. README documents the 100 MB cap. Focused CTI tests, broader connector sanity tests, Ruff, py_compile, and `git diff --check` passed. No live provider calls, API credentials, raw provider persistence, reportability gates, scraping bypass, third-party command execution, kill-chain launch, or active validation changed. |
| B531 | 2026-08-20 | Containerized CTI imports could be parsed successfully but the JSON result did not identify whether the source was plain, gzip, ZIP, or which ZIP member was selected, making audit and automation review harder for mixed archives. | Added sanitized read metadata to CTI import results: `report_container_format` and `report_member`. Plain and gzip imports report their container format with an empty member; ZIP imports report the deterministically selected supported member name after stripping control characters and bounding length. README documents the metadata fields. Focused CTI tests, broader connector sanity tests, Ruff, py_compile, and `git diff --check` passed. No raw provider body persistence, source credential storage, reportability gates, live provider calls, scraping bypass, third-party command execution, kill-chain launch, or active validation changed. |
| B532 | 2026-08-20 | Automation consuming `forge connectors import-cti --json` still had to infer the result contract from field presence, making future result-shape changes harder to detect safely. | Added `result_schema_version="forge.cti_observation_import.v1"` to CTI import results for direct importer calls and CLI JSON output. README documents the schema marker. Focused CTI tests, broader connector sanity tests, Ruff, py_compile, and `git diff --check` passed. No import semantics, persistence behavior, reportability gates, raw provider persistence, live provider calls, scraping bypass, third-party command execution, kill-chain launch, or active validation changed. |
| B533 | 2026-08-20 | The offline CTI importer handled ThreatFox, URLHaus, and STIX but still required manual conversion for common MISP event JSON exports, limiting production use with standard CTI sharing workflows. | Added `misp_event_import` as a passive offline connector and provider-catalog entry. Local MISP `Event.Attribute` rows are flattened into the existing sanitized observation model with common domain, URL, IP, email, and hash type mapping, TLP tag preservation, conservative `to_ids` confidence, event/attribute provenance, duplicate handling, and explicit scope-gated seed promotion. IP target-feed items now promote back to IPv4/IPv6 seeds when in scope. README documents MISP event JSON support. Focused CTI tests, broader connector sanity tests, Ruff, py_compile, and `git diff --check` passed. No MISP API polling, live provider calls, API credentials, raw provider persistence, reportability gates, scraping bypass, third-party command execution, kill-chain launch, or active validation changed. |
| B534 | 2026-08-20 | MISP event imports accepted `timestamp` fields as raw Unix strings, but observed-time filters only handled ISO-like values, so `--since/--until` could reject otherwise valid MISP rows as unparseable. | MISP attribute/event Unix timestamps are now normalized to UTC ISO timestamps before observation normalization and time-window filtering. README documents the behavior. Focused CTI tests, broader connector sanity tests, Ruff, py_compile, and `git diff --check` passed. No MISP API polling, live provider calls, API credentials, raw provider persistence, reportability gates, scraping bypass, third-party command execution, kill-chain launch, or active validation changed. |
| B535 | 2026-08-20 | MISP support handled full event JSON but not common attribute-search exports such as `response.Attribute` or response lists wrapping individual `Attribute` objects, again forcing manual conversion for standard offline MISP workflows. | Extended offline `misp_event_import` flattening to accept top-level `Attribute` arrays, `response.Attribute` arrays, and response-list `Attribute` wrappers. These rows use the same sanitized observation adapter, timestamp normalization, secret-redacting URL canonicalization, duplicate handling, and optional scope-gated promotion as full MISP events. README documents MISP event/attribute JSON support. Focused CTI tests, broader connector sanity tests, Ruff, py_compile, and `git diff --check` passed. No MISP API polling, live provider calls, API credentials, raw provider persistence, reportability gates, scraping bypass, third-party command execution, kill-chain launch, or active validation changed. |
| B536 | 2026-08-20 | Supabase target-like data had a planning path but no file-only importer, leaving operators with Supabase table exports unable to reuse the CTI observation sanitizer without manual conversion or live API handling. | Added passive offline `supabase_table_import` support for JSON/CSV table exports, including bare arrays, `data`/`items`/`rows` wrappers, explicit neutral target/indicator columns, and conservative URL/domain/IP/email/username aliases with case-insensitive headers. Rows use the same sanitizer, duplicate handling, dry-run/write semantics, time/confidence/TLP filters, and optional scope-gated promotion as other offline CTI imports. README documents file-only Supabase imports and explicitly states this path does not poll Supabase or persist API keys. Focused CTI tests, broader connector sanity tests, Ruff, py_compile, and `git diff --check` passed. No live Supabase API calls, API credentials, raw row persistence, reportability gates, scraping bypass, third-party command execution, kill-chain launch, or active validation changed. |
| B537 | 2026-08-20 | Sensitive CTI/OSINT rows were rejected by default, but import JSON only exposed generic `observation_rejected` skips, making it harder for operators to distinguish harmless unsupported rows from blocked phone/person/private-message/breach-record material without inspecting source files. | Added `rejected_sensitive_type_counts` to CTI import JSON results. The counter records only bounded sensitive type names, never raw values, and applies to write and dry-run paths including CLI JSON output. README documents the aggregate field and default rejection behavior. Focused CTI tests, broader connector sanity tests, Ruff, py_compile, and `git diff --check` passed. No sensitive-value persistence, reportability gates, live provider calls, API credentials, scraping bypass, third-party command execution, kill-chain launch, or active validation changed. |
| B538 | 2026-08-20 | Public CTI/OSINT exports can include command snippets or install instructions from unsafe source lists; although FORGE did not execute or persist them, import JSON gave operators no aggregate signal that such unsafe text was present. | Added `unsafe_text_item_count` to CTI import JSON results. The importer detects command/script/install-like row fields and shell-like text patterns, reports only a count, strips unsafe command-bearing fields before normalization, and persists only normalized observations with sanitized provenance/metadata. README documents that command snippets are unsafe text only, never commands to execute or persist. Focused CTI tests, broader connector sanity tests, Ruff, py_compile, and `git diff --check` passed. No command execution, raw command persistence, reportability gates, live provider calls, API credentials, scraping bypass, kill-chain launch, or active validation changed. |
| B539 | 2026-08-20 | CTI/OSINT provider research from public lists had a safe import path, but the provider catalog lacked a machine-readable aggregate policy view separating default-enabled, opt-in, offline-import, live/API-style, category, safety-tier, and gate coverage for operator review. | Added `provider_catalog_policy_summary()` to return aggregate catalog counts and provider ID lists only. The summary maps default vs opt-in sources, offline import vs live/API-style sources, safety tiers, collection methods, categories, and required gates without embedding commands or external source material. README documents using the summary as the source-selection map for public CTI/OSINT lists. Focused CTI tests, broader connector sanity tests, Ruff, py_compile, and `git diff --check` passed. No provider execution, live provider calls, API credentials, raw provider persistence, reportability gates, scraping bypass, third-party command execution, kill-chain launch, or active validation changed. |
| B540 | 2026-08-20 | Public CTI, awesome-osint, and `ukr.pw` research still existed mostly as prose, so operators could not see those source families in FORGE's default provider catalog without reading handoff text. | Expanded the CTI/OSINT provider catalog with default-visible backlog entries for additional abuse.ch feeds, OTX, urlscan, VirusTotal, CyberGordon, OpenCTI, APT report IOC imports, PhishTool exports, YARA catalog context, sandbox exports, OpenIOC, IODEF/RID, VERIS, TLP, Admiralty reliability, Diamond Model context, social/real-time/general OSINT categories, and `ukr.pw`-style sysadmin/webserver/network/firewall/database/mail/file-service/cloud-bootstrap snippet catalogs. Entries are catalog data only with policy metadata for manual, live/API-style, unsafe-text, location, people-search, breach, sample, sensitive identity, credential, private-network, and secret-redaction handling; no external commands are embedded or executable. README documents the broader default-visible catalog. Focused CTI tests, broader connector sanity tests, Ruff, py_compile, and `git diff --check` passed. No provider execution, live provider calls, API credentials, raw provider persistence, reportability gates, scraping bypass, third-party command execution, kill-chain launch, or active validation changed. |
| B541 | 2026-08-20 | Engagement dashboards could render very large seed/scope lists and long table/chip values in a way that overflowed static report pages, especially after bulk target imports and abandoned/failed runs left many inputs to review. | Hardened generated dashboard/report HTML to wrap long chips, pills, metadata, tables, artifact links, route cards, and timeline text. Seed/scope chip blocks now show a bounded preview with an expandable remainder instead of dumping every input inline. Table sections are wrapped in horizontal scroll containers. Reporting helper tests, Ruff, py_compile, and `git diff --check` passed. No engagement data deletion, report content filtering, kill-chain execution, connector behavior, CTI import behavior, or scheduled task behavior changed. |
| B542 | 2026-08-20 | Phase 6 still defaulted to local `llama_cpp`, so machines with Codex/CLI provider access but no GGUF model produced noisy GGUF fallback lineage unless operators remembered to set `FORGE_LLM_PROVIDER=auto` or pass `--provider auto`. | Changed the code/config/default CLI documentation to make `auto` the default Phase 6 provider. `auto` tries configured LLM CLI/API backends first, then local `llama_cpp`, then deterministic template. `llama_cpp` remains available as an explicit provider for local GGUF use. Focused Phase 6/default-provider, CLI README, platform config, py_compile, and `recon subdomains --help` checks passed. No live provider call, report resume, engagement resume, kill-chain execution, scheduled task behavior, provider credentials, or generated report content filtering changed. |
| B543 | 2026-08-20 | Failed/cancelled latest-run resume audits existed only as ad hoc local SQL/scripts, so operators could not repeat the 49-candidate review safely before deciding whether to resume or fix runs. | Added read-only `forge targets resume-candidates` JSON output backed by a helper that scans numeric engagement DBs in SQLite read-only/query-only mode, classifies latest `kill_chain` failures as `pending_recursive_work`, `watchdog_timeout`, `abandoned`, `stale_run_recovery`, or fallback statuses, and reports bounded resume context without dumping raw metadata or starting work. README documents the command. Focused target-import/resume-candidate tests, CLI registry/docs tests, Ruff, py_compile, `git diff --check`, and local `.forge_data` read-only verification passed; the local scan matched the prior audit at 49 candidates. No engagement mutation, live resume, kill-chain launch, scheduled task change, provider call, credential persistence, or report regeneration changed. |
| B544 | 2026-08-20 | Resume candidates were available through CLI JSON but still invisible in the static dashboard, forcing operators to know and run a separate command before reviewing failed/abandoned imported engagements. | Surfaced sanitized latest-run resume candidates in static dashboard overview/detail payloads and HTML: overview rows now include a `Resume Review` column/filter/stat, detail pages include a `Target Resume Review Candidate` evidence table, and run-summary metadata is sanitized to remove scope-manifest paths and token/secret-like fields before dashboard serialization. Focused dashboard contract/resume-review tests, reporting/CLI slice tests, Ruff, py_compile, and `git diff --check` passed. A full local `.forge_data` dashboard smoke was attempted but exceeded 4 minutes and the dashboard process was stopped; no engagement mutation, live resume, kill-chain launch, scheduled task change, provider call, credential persistence, or report regeneration semantics changed. |
| B545 | 2026-08-20 | The CTI/OSINT provider policy summary existed only as a Python helper, so operators could not inspect default-visible, offline-import, live/API-style, catalog-only, gated, unsafe-text, or sensitive source groups from the normal CLI before deciding what production sources to wire. | Added read-only `forge connectors policy-summary [--json]` over the existing aggregate catalog policy summary. The command emits only counts and provider IDs, with human output explicitly stating no provider is contacted and no third-party command is executed. README documents the command. Focused policy-summary CLI tests, connector registry/docs checks, Ruff, py_compile, `git diff --check`, and a JSON smoke passed. No live provider calls, API credentials, provider execution, raw provider persistence, kill-chain launch, active validation, scheduled task change, or engagement mutation changed. |
| B546 | 2026-08-20 | `forge doctor --json` still omitted the CTI/OSINT source-policy summary from readiness/action-plan output, so operators could miss the default/offline/live/API/manual-gated split before deciding whether live fetchers were production-ready. | Added a read-only `CTI/OSINT Policy` doctor row and `review_cti_osint_policy` action-plan item backed by the aggregate provider policy summary. The row reports counts for total/default-visible/offline-import/live/API-style/manual-opt-in/operator-gated/unsafe-text/blocked-sensitive source groups and remediates to `forge connectors policy-summary --json`. README documents the action id. Full doctor tests, focused policy-summary tests, Ruff, py_compile, and `git diff --check` passed. No live provider calls, API credentials, provider execution, raw provider persistence, kill-chain launch, active validation, scheduled task change, or engagement mutation changed. |
| B547 | 2026-08-20 | The generated dashboard still allowed graph-summary labels and graph-node text to overflow inside their boxes on narrow screens, and `targets import --start` still launched with older passive-only suppressors despite the operator/default kill-chain now enabling attack mode, resume, and detected follow-on execution by default. | Hardened dashboard CSS for graph nodes, graph-summary lists, and summary-line children, and backfilled the ignored local `reports/dashboard` HTML files so existing reports stop bleeding long labels without regenerating engagements. `targets import --start` now invokes scoped `forge kill-chain` with the normal defaults instead of passing `--no-attack-mode` or `--no-auto-run-detected`; ROE and the generated narrow scope manifest remain required, `--start-limit` still caps new launches, and existing running/completed run reuse is preserved. README and installer wording now describe scoped kill-chain starts. Browser overflow smoke on the flagged 10441 page passed at 1440/820 with zero overflow and at 390 with only intentional table-scroll overflow. Focused target-import and reporting tests, Ruff, py_compile, and `git diff --check` passed. No engagement DB mutation, live run launch, scheduled task enablement, report content deletion, provider call, credential persistence, or root report artifact commit changed. |
| B548 | 2026-08-20 | Report-quality review still required ad hoc shell searches over generated dashboard HTML/JSON, so operators could not repeatably answer how many reports existed, which report generations fell back to degraded providers, which runs failed or ran too long, or where dashboard refresh failures occurred. | Added read-only `forge report quality-audit [--reports-dir reports] [--json]`. The command scans static dashboard overview/detail JSON and report artifacts, reports schema-versioned counts for engagement/report/dashboard inventory, run statuses, report backends, fallback/write-error classes, policy flags, resume-review rows, long runs, failed runs, and dashboard refresh failures, and includes bounded sample rows without mutating engagement DBs or launching work. README documents the command. Focused quality-audit/CLI docs tests, Ruff, py_compile, `git diff --check`, and a real local reports smoke passed; the local smoke found 829 dashboard engagements, 3,139 report files, 1,432 root report files, 844 dashboard HTML files, 581 report families, 49 failed runs, 3 long runs, 4 dashboard refresh failures, 0 resume-review rows, and 149 historical GGUF-missing fallback generations. No report regeneration, engagement mutation, live run launch, provider call, scheduled task change, credential persistence, or ignored report artifact commit changed. |
| B549 | 2026-08-20 | The report-quality audit exposed historical dashboard refresh failures from older engagement DBs missing `risk_acceptance_expires_at`; the core remediation review queue still selected that column directly even though reporting section queries had a legacy fallback. | Made `remediation_review_queue()` build a stable remediation-item payload select list from the live table schema, aliasing missing legacy fields to NULL/default values before payload shaping. Added a regression DB missing `risk_acceptance_expires_at` and verified the core queue plus reporting legacy section tests, Ruff, py_compile, `git diff --check`, and a read-only quality-audit smoke. Existing generated dashboard artifacts still show their historical failure rows until regenerated; future queue reads no longer fail on that missing column. No engagement mutation, live run launch, provider call, report deletion, scheduled task enablement, or credential persistence changed. |
| B550 | 2026-08-20 | The local report-quality audit still showed multi-hour kill-chain runs with pending recursive work, and the scheduled-task watchdog could hard-stop the process before FORGE had a chance to preserve final report/DB closeout context. | Added bounded `--max-runtime-minutes` / `FORGE_KILL_CHAIN_MAX_RUNTIME_MINUTES` support to kill-chain runtime options with a default soft budget of 25 minutes. The spider loop checks the budget at iteration boundaries, audits `runtime_budget_exhausted`, records effective budget/phase/elapsed metadata, and then proceeds through the existing graceful finalization path so collected evidence and reports are preserved. Terminal run errors now say `runtime budget exhausted with pending recursive work` when that was the stopping cause instead of mislabeling it as max-iteration exhaustion. README documents the option. Focused runtime-option, CLI help/validation, run-tracking, Ruff, and py_compile checks passed. No feature gate, attack-mode default, resume default, auto-run default, scheduled task enablement, engagement mutation, provider call, or live run launch changed. |
| B551 | 2026-08-20 | The report-quality audit still contained a historical dashboard refresh failure with `[Errno 22] Invalid argument` for a URL-seeded engagement, and dashboard artifact rendering still assumed every discovered report/graph/audit path could be statted, relativized, and rendered without OS-level path errors. | Hardened dashboard artifact payloads, report-history mtime handling, and static artifact cards so missing, disappearing, or OS-invalid artifact paths are skipped or rendered as zero-byte/unmodified placeholders instead of aborting the whole dashboard refresh. Added missing-artifact regressions for JSON payload and HTML card wrappers. Focused reporting tests, Ruff, py_compile, `git diff --check`, and a read-only quality-audit smoke passed. A full local dashboard smoke still exceeded four minutes on the large local corpus and was stopped/cleaned up; no engagement DB mutation, live run launch, provider call, scheduled task enablement, or committed report artifact changed. |
| B552 | 2026-08-20 | The shared state and JSON-first resume workflow referenced `forge targets resume-candidates --json`, but the command rejected `--json` even though it emitted JSON by default. | Added a compatibility `--json` flag to `targets resume-candidates` while preserving the existing default JSON output shape, documented the flag in README, and added a CLI regression for the explicit flag. Focused target-resume/CLI-registry tests, Ruff, py_compile, `git diff --check`, and a real local `forge targets resume-candidates --limit 200 --json` smoke passed with zero current candidates. No engagement mutation, live resume, kill-chain launch, scheduled task change, provider call, report regeneration, or credential persistence changed. |
| B553 | 2026-08-20 | The continuation quality-audit workflow naturally used `forge report quality-audit --top-limit N`, but the CLI only accepted the shorter `--top` spelling even though the internal audit API names the setting `top_limit`. | Added `--top-limit` as a compatibility alias for `--top`, documented both spellings, and added a CLI regression proving the alias bounds sample rows. Focused quality-audit/CLI registry tests (`23 passed`), Ruff, py_compile, `git diff --check`, and a real local `forge report quality-audit --json --top-limit 50` smoke passed; the smoke still reports historical dashboard-refresh/failure/long-run findings until dashboards or runs are regenerated. No engagement mutation, report regeneration, live run launch, provider call, scheduled task change, or credential persistence changed. |
| B554 | 2026-08-20 | After a full current-code dashboard refresh succeeded on the local corpus, `report quality-audit` still counted old `dashboard_review_refresh_failed` audit rows as headline dashboard refresh failures, making fixed historical failures look like current dashboard breakage. | Split dashboard refresh failures into current unresolved failures and timestamped historical failures older than the dashboard `generated_at` time. The headline `dashboard_refresh_failure_count` now tracks unresolved/unknown-current failures, while `historical_dashboard_refresh_failure_count` and samples preserve old evidence. A full local `forge dashboard -o reports\dashboard.html` backfill completed, then the real audit reported 930 dashboard engagements, 945 dashboard HTML pages, 3,341 report files, 0 current dashboard refresh failures, and 4 historical dashboard refresh failures. Focused quality-audit/CLI registry tests (`24 passed`), Ruff, py_compile, `git diff --check`, and real local audit smoke passed. No engagement DB mutation, live run launch, provider call, scheduled task change, report deletion, or committed ignored dashboard artifact changed. |
| B555 | 2026-08-20 | `report quality-audit` counted fallback reasons across every report-history entry, so old GGUF-missing report generations looked like current latest-report health even after provider defaults/model availability were fixed. | Added latest-report-only backend, fallback, and write-error counters beside the existing all-history lineage counters. The audit now preserves `fallback_reason_counts` for historical lineage while `latest_fallback_reason_counts` shows current latest-report degradation. Focused quality-audit/CLI registry tests (`25 passed`), Ruff, py_compile, `git diff --check`, and a real local audit smoke passed; the smoke reports 149 historical GGUF-missing fallback generations but an empty `latest_fallback_reason_counts`, proving the latest report summaries are no longer GGUF-missing. No report regeneration, engagement DB mutation, live run launch, provider call, scheduled task change, report deletion, or committed ignored dashboard artifact changed. |
| B556 | 2026-08-20 | The refreshed dashboard included repo-local legacy `.forge_data` engagement DBs and showed 49 resume-review rows, but `forge targets resume-candidates` defaulted to only the configured data dir and reported 0 candidates, creating an operator-facing mismatch between dashboard review and CLI review. | Extended numeric engagement DB discovery with an opt-in legacy scan and made `targets resume-candidates` include repo-local legacy dashboard DBs only for the default no-`--data-dir` operator view. Explicit `--data-dir` scans remain narrow and report `include_legacy=false`. README documents the default. Focused target-import/resume, quality-audit, and CLI registry tests (`47 passed`), Ruff, py_compile, `git diff --check`, and a real local `forge targets resume-candidates --limit 200 --json` smoke passed; the smoke now scans 930 engagements and returns the same 49 candidates visible in the dashboard (`pending_recursive_work=31`, `watchdog_timeout=7`, `abandoned=6`, `stale_run_recovery=5`). No engagement mutation, live resume, kill-chain launch, scheduled task change, provider call, credential persistence, or report regeneration changed. |
| B557 | 2026-08-20 | `forge doctor` Workspace Access still inspected only the configured data dir, so after B556 the dashboard and resume-candidate CLI reviewed 930 configured-plus-legacy DBs while doctor still reported workspace/index readiness over only 101 configured DBs. | Made the Workspace Access doctor check use the shared legacy-aware numeric DB discovery and label results that include repo-local legacy dashboard DBs. Other operational checks such as retention, monitoring schedules, standards exchange, remediation tickets, and connector secret-store remain configured-data-only so scheduled jobs do not expand unexpectedly. Focused doctor/engagement-id tests (`45 passed`), Ruff, py_compile, `git diff --check`, and a real local `forge doctor --json` smoke passed; the real Workspace Access row now reports `50/930 DB(s)` inspected and explicitly includes repo-local legacy dashboard DBs. No engagement mutation, workspace backfill, live run launch, scheduled task change, provider call, credential persistence, or report regeneration changed. |
| B558 | 2026-08-20 | Doctor told operators to install missing free/local connector binaries but did not provide a first-class FORGE command to produce a safe, repeatable, non-executing install plan, leaving setup guidance split across prose and remediation text. | Added schema-versioned `connector_install_plan()` and `forge connectors install-plan [--json]`. The command groups missing local binaries by affected connector, returns installer labels and suggested commands, and explicitly declares `plan_only_no_commands_executed`; it never installs tools or contacts providers. Doctor's `install_free_binaries` action now points to `forge connectors install-plan --json`, and README documents the command. Focused connector/doctor/CLI registry tests (`102 passed`), Ruff, py_compile, `git diff --check`, and a real local install-plan smoke passed; the smoke reported six missing tools: detect-secrets, gitleaks, katana, nuclei, subfinder, and trufflehog. No external installer execution, provider call, engagement mutation, live run launch, scheduled task change, credential persistence, or report regeneration changed. |
| B559 | 2026-08-20 | The Connector Catalog doctor row still used old manual missing-binary wording even though the action plan had a safe read-only `install-plan` command, so setup guidance remained inconsistent. | Pointed Connector Catalog remediation at `forge connectors install-plan --json` and updated doctor regressions to assert the safe plan command instead of raw installer prose. Focused doctor/connector/CLI tests (`102 passed`), Ruff, py_compile, `git diff --check`, and a real `forge doctor --json` smoke passed with the Connector Catalog row and `install_free_binaries` action both referencing the install-plan command. No installer execution, provider call, engagement mutation, live run launch, scheduled task change, credential persistence, or report regeneration changed. |
| B560 | 2026-08-20 | The resume-candidate audit showed 49 failed/cancelled historical target-import runs, but every candidate was blocked by missing scope-manifest metadata, so operators still could not safely resume them even though ROE and narrow seeds were present. | Added resume readiness fields to `forge targets resume-candidates`: aggregate `resume_ready_count`, `resume_blocker_counts`, per-item `resume_ready`, `resume_blockers`, and a `resume_command` array only when ROE/scope/resume gates are present; suggested resume commands floor `--max-iter` at 3 to avoid repeating old one-iteration pending-work failures. Added dry-run-first `forge targets backfill-scope-manifests [--apply]` to recover narrow scope manifests from engagement scope/seed data and update latest-run metadata without starting or resuming kill-chain runs. Applied the backfill locally to 49 historical candidates; the follow-up local smoke reported 49 candidates, 49 resume-ready, and zero blockers. Focused target-import/CLI-registry/quality-audit tests, Ruff, py_compile, and `git diff --check` passed. No live run launch, scheduled task enablement, provider call, report deletion, credential persistence, or committed ignored local DB/report artifact changed. |
| B561 | 2026-08-20 | After scope-manifest recovery, static dashboards still rendered resume candidates only as generic review rows and run-summary metadata could expose recovered local scope-manifest paths through nested `scope_manifest_source` policy metadata. | Added dashboard resume readiness/blocker fields and overview labels so resume rows render as `ready` or `blocked`; blocker labels are path-neutral in dashboard JSON. Tightened run-summary metadata sanitization to drop any `scope_manifest*` key recursively, including nested live-execution policy sources. Regenerated the ignored local dashboard; generated dashboard JSON now reports 49 resume items, 49 ready, zero blockers, and no recovered scope-manifest paths. Focused dashboard/run-summary/target-import/quality-audit/CLI-registry tests, Ruff, py_compile, `git diff --check`, and local dashboard/quality smokes passed. No live run launch, scheduled task enablement, provider call, report deletion, credential persistence, or committed ignored local dashboard artifact changed. |
| B562 | 2026-08-20 | Doctor's Workspace Access row still warned on legacy/imported engagement DBs missing operator memberships and stale control index rows, but remediation was prose-only and operators had no dry-run-first command to repair the workspace/RBAC surface. | Added `forge workspaces backfill-memberships [--apply] [--json]`, backed by `workspace_backfill`, to plan or seed missing local engagement `workspace_memberships`, control DB memberships, and control index rows using the existing operator role. Dry-run inspects existing control DB state without writing; `--apply` writes only membership/index/schema repairs and appends a redacted control-audit event. Applied the backfill locally across 930 DBs; follow-up dry-run reports 929 skipped items and zero remaining membership/index actions, and `forge doctor --json` now reports Workspace Access `OK`. Doctor remediation now points to the dry-run command. Focused workspace/doctor/CLI-registry tests (`65 passed`), Ruff, py_compile, and real local workspace/doctor smokes passed. No live run launch, scheduled task enablement, provider call, report deletion, finding mutation, or credential persistence changed. |
| B563 | 2026-08-20 | Connector Secret Store doctor remediation still told operators to manually set `FORGE_ENGAGEMENT_KEY`, and the local machine had no engagement key configured, leaving encrypted connector secret storage disabled. | Added `forge connectors secret-key-plan [--json]`, a non-secret setup guidance command that reports whether `FORGE_ENGAGEMENT_KEY` is configured, prints only key length/fingerprint, and returns PowerShell/POSIX commands that generate keys inside the target shell without echoing secret material. Doctor's missing-key remediation now points to this plan. Set a local Windows user-level `FORGE_ENGAGEMENT_KEY` without printing the value; real local `doctor --json` with the user env loaded now reports Connector Secret Store `OK`. Focused connector/doctor/CLI-registry tests (`63 passed`), Ruff, py_compile, `git diff --check`, and local secret-key/doctor smokes passed. No connector secret values, provider credentials, live run launch, scheduled task enablement, report deletion, or finding mutation changed. |
| B564 | 2026-08-20 | Doctor's Monitoring Schedules warning pointed directly at mutating `forge monitoring run-due --json` even when many policies were due, so operators had no bounded policy-level review step before creating scheduled snapshots or running refresh callbacks. | Added read-only `forge monitoring due-plan [--limit N] [--json]`, backed by read-only SQLite opens, to enumerate due policies with engagement/policy IDs, modes, timing, baseline state, and sanitized refresh shape while declaring `plan_only_no_commands_executed`. Doctor now remediates due schedules by reviewing `due-plan` first and includes a `review_due_monitoring` action item before the explicit `run-due`/worker apply step. README documents the public command. Focused monitoring/doctor/CLI-registry tests and Ruff passed; broader verification followed before commit. No monitoring snapshot creation, refresh execution, alert delivery, engagement mutation, live run launch, scheduled task enablement, provider call, report regeneration, or credential persistence changed. |
| B565 | 2026-08-20 | Connector setup still left full/default free-local connector execution mostly unavailable on a fresh machine, and doctor/install-plan/runtime disagreed because doctor only checked raw PATH while install-plan/runtime could use richer tool locations. A long `go install` also exposed that connector setup needed per-tool timeout handling so failed installers cannot hang the operator setup path. | Added a shared connector binary resolver that checks PATH, configured connector binary directories, FORGE/local venv tool dirs, repo `.venv`/`.venv-osint`, and `~/go/bin`, then wired it into connector readiness, connector execution, install-plan, and doctor's Connector Catalog/Action Plan checks. Bootstrap full mode now best-effort installs free/local connector CLIs (`detect-secrets`, `gitleaks`, `katana`, `nuclei`, `subfinder`) with bounded per-tool timeouts and an opt-out env var; TruffleHog is reported as manual guidance because the current upstream Go module is not `go install` friendly. README documents the full/default setup behavior and binary search paths. Focused connector/bootstrap/doctor tests (`93 passed`), Ruff, py_compile, `git diff --check`, local install-plan/doctor smokes, scheduled-task status, and orphan process checks passed; local connector install-plan now reports only TruffleHog missing. No provider execution, connector run launch, engagement mutation, live kill-chain start, scheduled task enablement, secret material logging, or committed local binary/report artifact changed. |
| B566 | 2026-08-20 | Operators could review 49 ready historical target-import resume candidates and copy individual commands, but there was no first-class sequential batch plan or executor with a durable ledger, rerun skip checks, and one-child-at-a-time enforcement. Doctor also still had duplicate secret-tool rows that disagreed with B565 connector binary discovery and TruffleHog guidance. | Added read-only `forge targets resume-plan [--json]` to turn ready candidates into an ordered manual command plan with `execution_policy=plan_only_no_commands_executed`, `concurrency=sequential`, explicit `--max-runtime-minutes`, skipped blocker counts, and estimated serial runtime. Added explicit `forge targets resume-run [--json]`, which re-checks each candidate before launch, holds a resume-batch lock, writes JSONL ledger events under `target_imports/resume_batches`, skips already-non-resumable latest runs on rerun, and executes at most one child `forge kill-chain ... --resume` process at a time. Doctor secret binary rows now use the connector-aware resolver for secrets and TruffleHog remediation points to release-binary/install-plan guidance; `detect-secrets` resolves from `.venv`/connector search paths. Focused resume/doctor/CLI tests, broader target-import/doctor/CLI-registry tests (`72 passed`), Ruff, py_compile, a local `resume-plan --limit 5 --json` smoke, and a doctor smoke passed. No local `resume-run`, kill-chain child, provider execution, scheduled task enablement, engagement mutation, secret material logging, or committed local ledger/report artifact changed. |
| B567 | 2026-08-20 | Connector Secret Store doctor output still looked like setup failure when `FORGE_ENGAGEMENT_KEY` had been configured persistently at Windows user scope but the current shell/service had not inherited it. Operators needed restart/process-env guidance without exposing the key. | Extended `forge connectors secret-key-plan --json` with a non-secret `persistent_key_hint` for Windows User/Machine environment keys that reports only source, length, and fingerprint. `forge doctor` now uses that hint for real process checks and reports Connector Secret Store as `WARN` with restart/process-env remediation instead of `MISSING` when a persistent key exists but this process lacks it. Injected-env doctor/test calls remain deterministic and do not probe host persistent env. Focused key-plan/doctor tests, full doctor tests (`45 passed`), Ruff, py_compile, `git diff --check`, and local secret-key-plan/doctor smokes passed; the local smoke reports user-level key length 44 and fingerprint only. No secret value printing, connector secret mutation, engagement mutation, provider execution, live run launch, scheduled task change, or local env write changed. |
| B568 | 2026-08-20 | Doctor's Monitoring Schedules warning still summarized due work from its bounded first-50-DB readiness sample, so a large configured data dir could show `50 due/overdue` even though the read-only due-plan saw 101 due policies. | Reused `monitoring_due_plan_for_data_dir(..., limit=0)` inside the doctor row after the sampled schema/readiness checks so the details preserve sampled alert/delivery counts while the due-action summary and details show the full read-only due-plan backlog. Focused doctor tests (`43 passed`), Ruff, py_compile, `git diff --check`, and real local `.venv\Scripts\forge.exe doctor --json` / `monitoring due-plan --limit 5 --json` smokes passed; local doctor now reports `101 due/overdue monitoring policy(ies)` and labels the first-50 sample separately. No monitoring snapshot creation, refresh execution, alert delivery, engagement mutation, live run launch, scheduled task enablement, provider call, report regeneration, or credential persistence changed. |
| B569 | 2026-08-20 | Target-import starts now inherited attack/resume defaults, but kill-chain live policy metadata still hard-coded destructive and post-exploitation permission fields to `false`, so generated dashboards/reports could continue showing `Destructive=no` and `Post-Ex=no` even for target-specific ROE manifests that intend those policy flags. | Added a tested `scope_manifest_policy_flag()` helper and changed kill-chain live policy metadata to read destructive/post-ex permission booleans from explicit scope-manifest `policy` fields or top-level aliases while preserving default-false behavior for manifests that do not authorize them. Generated target-import scope manifests now include that explicit policy block, and README documents that these dashboard/report flags come from the target-specific ROE manifest. Focused target-import/runtime tests (`46 passed`), Ruff, py_compile, and `git diff --check` passed. No kill-chain run, resume-run, monitoring execution, scheduled-task enablement, provider call, report regeneration, engagement mutation, or credential persistence changed. |
| B570 | 2026-08-20 | Connector setup and doctor still left TruffleHog as the only missing free/local connector binary, so the GitHub/secret-scanning baseline stayed partially manual even after bootstrap installed the other local tools. | Added a bounded, checksum-verified TruffleHog release-binary installer to `bootstrap.py setup`, updated connector install-plan and doctor remediation to point at bootstrap release installation, and documented TruffleHog in the full connector setup list. Installed TruffleHog `3.97.0` locally under `AppData\Local\FORGE\tools\bin`; real install-plan now reports `missing_binary_count=0`, and doctor reports Connector Catalog/Action Plan `OK`. Focused bootstrap/connector/doctor tests (`52 passed`), Ruff, py_compile, `git diff --check`, local install-plan/doctor smokes, and `trufflehog --version` passed. No provider scan, secret scan, engagement mutation, live run launch, scheduled-task change, report regeneration, or credential persistence changed. |
| B571 | 2026-08-20 | Doctor still warned on an installed-but-disabled TPH target-import scheduled task even when the task had been paused by operator request, so intentional local automation stoppage looked like a broken integration. | `forge doctor` now reports the TPH target-import bridge as `OFF` for an installed disabled scheduled task unless `FORGE_TPH_TARGET_IMPORT_ENABLED=1` explicitly declares the bridge expected to run; explicit enabled state still warns if the task is disabled. README documents the paused/OFF behavior, and a regression covers disabled-task status plus secret redaction. Focused doctor tests (`44 passed`), Ruff, py_compile, `git diff --check`, and a real local doctor smoke passed with `TPH Target Import Bridge` status `OFF`. No scheduled-task enablement, import run, provider call, engagement mutation, monitoring execution, live run launch, report regeneration, or credential persistence changed. |
| B572 | 2026-08-20 | Connector binary discovery was mostly env-aware, but doctor's ProjectDiscovery binary rows still used raw PATH lookup and `connector_install_plan(statuses=..., env=...)` could preserve stale missing-binary rows even when the supplied env resolved those binaries. | Reused the connector-aware resolver for ProjectDiscovery doctor binary rows, matching Connector Catalog/Action Plan and Secrets behavior. `connector_install_plan` now filters supplied missing-binary rows through the env-aware resolver before emitting guidance, so custom `FORGE_CONNECTOR_BIN_DIR(S)` deployments do not get stale install prompts. Regressions cover env-only ProjectDiscovery resolution and env-resolved missing-binary filtering with host-independent synthetic rows. Focused doctor/connector registry tests (`91 passed`), Ruff, py_compile, `git diff --check`, and real local doctor/install-plan smokes passed. No installer execution, provider call, scheduled-task enablement, monitoring apply, engagement mutation, live run launch, report regeneration, or credential persistence changed. |
| B573 | 2026-08-20 | `forge monitoring due-plan` was bounded and read-only, but the mutating `run-due` and worker paths still applied every due policy once invoked, so a 101-policy backlog could be swept in one accidental scheduler/manual run. | Added global execution caps through `run_due_monitoring_policies`, `run_due_monitoring_for_db`, `run_due_monitoring_for_data_dir`, and `run_monitoring_worker`. Public `forge monitoring run-due` now defaults to `--limit 50`, worker defaults to `--run-limit 50`, and both expose `--all` for explicit full-backlog apply. JSON results report full due count, executed run count, `limited_policy_count`, and execution limit; doctor now recommends review first and capped apply second. Focused monitoring/doctor tests (`84 passed`), Ruff, py_compile, `git diff --check`, non-mutating help smokes, doctor smoke, and read-only due-plan smoke passed. No monitoring `run-due`, worker execution, alert delivery, provider call, scheduled-task enablement, engagement mutation, live run launch, report regeneration, or credential persistence changed. |
| B574 | 2026-08-20 | Aggregate `engagement_<id>_stats.json` sidecars were treated as report families and latest report-history entries, causing quality-audit latest backend counts to show `none` for stats metadata instead of the actual latest reports behind them. | Added `is_report_metadata_sidecar()` and excluded stats sidecars from engagement report artifact discovery, report-family grouping, quality-audit report file inventory, and precomputed dashboard `report_history` interpretation. Regressions cover report artifact discovery, latest report-family sorting, and quality-audit filtering of old dashboard history. Focused reporting tests (`14 passed`), Ruff, py_compile, `git diff --check`, and a real local quality-audit smoke passed; local latest backend counts now report `template: 61` instead of stats-sidecar `none: 58`. No dashboard regeneration, report regeneration, engagement mutation, live run launch, provider call, scheduled-task change, or credential persistence changed. |
| B575 | 2026-08-20 | After stats sidecars were removed from latest report history, quality-audit exposed 57 real latest reports whose fallback reason was GGUF-missing, but operators still lacked a bounded read-only repair list and old fallback reasons repeated local model paths. | Added `latest_fallback_reports` samples to `report quality-audit` with engagement/slug/family metadata, sanitized fallback reasons, `repair_status`, and non-executing `forge report generate --engagement <id> --provider auto --yes` command arrays. GGUF-missing latest reports are marked `stale_after_model_available` when the default local GGUF exists; otherwise samples remain generic regenerate guidance. Focused quality-audit tests (`6 passed`), Ruff, py_compile, `git diff --check`, and a real local quality-audit smoke passed showing path-neutral stale-after-model repair rows. No report regeneration, dashboard regeneration, engagement mutation, live run launch, provider call, scheduled-task change, or credential persistence changed. |
| B576 | 2026-08-20 | Safe Mode doctor warning was expected when full legacy mode is active, but the wording made intentional ROE-backed full mode look like broken setup and `.env.example` still suggested `0` for OSINT-only ops. | Kept behavior unchanged but rewrote Safe Mode doctor details/remediation and env docs to distinguish safe/core or production ASM from intentional full legacy offensive mode with written ROE. Focused doctor tests (`44 passed`), Ruff, py_compile, `git diff --check`, and a real local doctor smoke passed. No safe-mode default change, live run, provider call, scheduled-task change, monitoring apply, report regeneration, engagement mutation, or credential persistence changed. |
| B577 | 2026-08-20 | Connector Secret Store doctor could detect a persistent Windows `FORGE_ENGAGEMENT_KEY`, but operators still had to infer the current-process reload command from JSON and plain output did not show the persistent-key hint. | Added value-safe `powershell_reload_persistent_env` guidance to `forge connectors secret-key-plan --json`, surfaced persistent Windows key source/length/fingerprint plus the process-only reload command in plain output, and changed doctor remediation to show the exact reload command when available. Focused connector/doctor tests (`5 passed`), Ruff, py_compile, `git diff --check`, and real local secret-key-plan/doctor smokes passed. No secret material was printed, persisted, or committed; no engagement mutation, provider call, live run, monitoring apply, scheduled-task change, report regeneration, or credential persistence changed. |
| B578 | 2026-08-20 | `forge monitoring due-plan` was read-only and bounded, but the operator JSON still looked like raw DB enumeration: empty limited DB rows obscured the actual plan, and the output did not summarize stale backlog age, capped batch count, policy composition, or safe next commands. | Added schema-versioned due-plan operator metadata: default execution limit, estimated capped invocations, oldest/newest due timestamps, oldest last-run timestamp, stale-backlog summary, policy composition counts, and plan-only action commands. Data-dir due-plan now omits empty planned DB rows by default while preserving stale/error DB rows and exposes `--include-empty-db-results` for verbose troubleshooting; doctor includes stale age and capped-batch estimates. Focused monitoring/doctor tests, Ruff, py_compile, `git diff --check`, and real local due-plan/doctor smokes passed. No monitoring `run-due`, worker execution, alert delivery, engagement mutation, provider call, live run, scheduled-task change, report regeneration, or credential persistence changed. |
| B579 | 2026-08-20 | `report quality-audit` exposed the right backlog samples, but operators still had to infer which findings were historical, which were safe to repair, and which command should be reviewed first; policy `*_no` counts also read like current configuration defects instead of latest-run metadata. | Added a read-only `operator_action_plan` to quality-audit JSON and human output with buckets for stale report regeneration samples, resume-plan review, long-run review, and policy flag explanation. Commands are arrays only and point to `forge report generate ... --provider auto --yes` or `forge targets resume-plan --json --limit N`; the audit never starts `resume-run`, regenerates reports, launches providers, or mutates engagements. Focused quality-audit tests, Ruff, py_compile, `git diff --check`, and real local quality-audit JSON/human smokes passed. |
| B580 | 2026-08-20 | After B579, quality-audit correctly pointed operators to `targets resume-plan`, but the default resume-plan JSON includes local DB and recovered scope-manifest paths, which is useful for execution but too noisy and path-revealing for report/review output. | Added `forge targets resume-plan --redact-paths` and planner-level `redact_paths=True` support. Redacted review output keeps sequence, engagement, seed, ROE, runtime, `db_ref`, and `scope_manifest_ref`, but hides `data_dir`, clears `db_path`, and replaces `--scope-manifest` command arguments with `<scope-manifest:filename>` placeholders. Default resume-plan and resume-run behavior remain executable and unchanged. Quality-audit now points to `forge targets resume-plan --json --redact-paths --limit N`. Focused target resume/quality tests, Ruff, py_compile, `git diff --check`, and real local read-only redacted resume-plan/quality-audit smokes passed. No resume-run, kill-chain, report regeneration, provider call, engagement mutation, scheduled-task change, monitoring apply, or credential persistence changed. |
| B581 | 2026-08-20 | Connector Secret Store doctor rows explained how to set or reload `FORGE_ENGAGEMENT_KEY`, but the row had no action items, so the top-level doctor action plan omitted the secret-key setup/reload step and automation could miss it. | Added value-safe `setup_connector_secret_key` and `reload_connector_secret_key_env` action items to the Connector Secret Store MISSING/WARN rows. Persistent Windows reload action uses only the literal current-process reload command from `secret-key-plan`, never the key value. Focused doctor tests, Ruff, py_compile, `git diff --check`, and a real local filtered doctor smoke passed. No secret material was printed, persisted, or committed; no engagement mutation, provider call, live run, scheduled-task change, monitoring apply, report regeneration, or credential persistence changed. |
| B582 | 2026-08-20 | `report quality-audit` reported 57 stale latest reports but only emitted the sampled repair commands allowed by `--top-limit`, with no omitted count or read-only follow-up command. Doctor also had a safety-relevant `Offline Strict` OFF row with no remediation/action item, and scheduled-task doctor checks could intermittently time out on valid Windows task queries at 1.5 seconds. | Added `sample_limit`, `omitted_count`, and read-only `follow_up_commands` to stale-report operator action plans; human quality-audit output now prints follow-up commands. Added Offline Strict remediation plus a `review_offline_strict` action item without changing defaults. Raised scheduled-task doctor query timeout to a named 3-second constant for TPH/remediation task checks. Focused reporting/doctor tests, Ruff, py_compile, `git diff --check`, and real local read-only doctor/quality smokes passed. No report regeneration, resume-run, kill-chain, provider call, scheduled-task change, monitoring apply, engagement mutation, or credential persistence changed. |
| B583 | 2026-08-20 | Operators still needed to run the broad `report quality-audit` command to extract stale latest-report repair commands, even though stale report repair is the current highest-signal report backlog. | Added read-only `forge report stale-plan [--reports-dir reports] [--limit N] [--json]`, backed by `collect_stale_report_repair_plan()`, to emit a schema-versioned plan-only command list for stale latest-report regeneration. The plan reuses quality-audit's stale-report detection and never regenerates reports. README and CLI registry docs now include the command. Focused reporting and CLI registry tests, Ruff, py_compile, `git diff --check`, and real local read-only stale-plan JSON/human smokes passed. No report regeneration, resume-run, kill-chain, provider call, scheduled-task change, monitoring apply, engagement mutation, or credential persistence changed. |
| B584 | 2026-08-20 | Doctor's Connector Action Plan still emitted `install_free_binaries` as a ready action with summary `none` when all connector binaries were already available, making the top-level action plan include a no-op setup step. | Suppressed the `install_free_binaries` action item unless missing connector binaries actually exist. Connector Action Plan details and status remain unchanged, and missing-binary cases still emit the install-plan action. Focused doctor tests, Ruff, py_compile, `git diff --check`, and a real local doctor smoke passed showing Connector Action Plan `OK` and no `install_free_binaries` action. No installer execution, provider call, report regeneration, resume-run, kill-chain, scheduled-task change, monitoring apply, engagement mutation, or credential persistence changed. |
| B585 | 2026-08-20 | `report quality-audit` identified long-running failed runs but exposed them only as inline samples with an empty command list, leaving operators without a first-class read-only review command for that backlog class. | Added read-only `forge report long-run-plan [--reports-dir reports] [--long-run-seconds N] [--limit N] [--json]`, backed by `collect_long_run_review_plan()`, to emit a schema-versioned plan-only long-run review payload with thresholds, counts, samples, omitted count, and follow-up command. Quality-audit long-run action now points to the dedicated plan. Focused reporting and CLI registry tests, Ruff, py_compile, `git diff --check`, and real local read-only long-run-plan JSON/human smokes passed. No resume-run, kill-chain, report regeneration, provider call, scheduled-task change, monitoring apply, engagement mutation, or credential persistence changed. |
| B586 | 2026-08-20 | After `forge report stale-plan` was added, the `report quality-audit` stale-report follow-up still pointed operators back to broad `quality-audit --top-limit N` instead of the dedicated stale-report plan. | Changed stale-report follow-up commands to `forge report stale-plan --json --limit N` so quality-audit and stale-plan both point to the first-class read-only stale-report repair plan. Focused reporting tests, Ruff, py_compile, `git diff --check`, and real local quality-audit/stale-plan smokes passed. No report regeneration, resume-run, kill-chain, provider call, scheduled-task change, monitoring apply, engagement mutation, or credential persistence changed. |
| B587 | 2026-08-20 | `report quality-audit` still showed policy `*_no` counts only as aggregates, so operators could not inspect which latest-run summaries drove `attack_no`, `destructive_no`, and `post_ex_no` without manually opening dashboard JSON. | Added read-only `forge report policy-plan [--reports-dir reports] [--limit N] [--json]`, backed by `collect_policy_flag_review_plan()`, to emit a schema-versioned plan-only explanation with aggregate counts, sampled latest-run rows, omitted count, and follow-up command. Quality-audit policy action now includes samples and points to the dedicated plan. Focused reporting and CLI registry tests, Ruff, py_compile, `git diff --check`, and real local read-only policy-plan/quality-audit smokes passed. No resume-run, kill-chain, report regeneration, provider call, scheduled-task change, monitoring apply, engagement mutation, or credential persistence changed. |
