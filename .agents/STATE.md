# Agent State

Current task: streamline autonomous live-safe daily operation around `forge automation cycle`, local input queues, and safe startup gates.

Progress:
- Completed B734 locally: added per-artifact import item caps to autonomous source-queue execution. `automation cycle` now derives `queue_import_item_limit` from `imports/autostart.local.json` (default 1000, max 10000), lets individual queue items narrow/override it with `limit` or `import_item_limit`, passes explicit `--limit` to queued CTI/discovery/validation import commands, reports the effective cap in `queue_execution`, and preserves direct guarded-autostart fail-closed queue checks. `DEFAULT_AUTOSTART_CONFIG`, defaults/limits reviews, README, DAILY_USE, and ignored local `imports/autostart.local.json` now include `queue_import_item_limit=1000`. Verification passed on 2026-08-29: focused automation cycle/policy/self-heal tests (`88 passed`), Ruff and `py_compile` on touched Python files, real `automation limits --json` smoke showing `queue_import_item_limit=1000` and `total=25`, temp dry-run `automation cycle --json` queue-planning smoke showing a queued ThreatFox import command with `--limit 77`, `git diff --check`, and pasted-key fragment scan. No queued imports were executed in the smoke, and no live provider/API call, scanner execution, target import/start apply, resume-run apply, monitoring apply, Docker mutation, ticket/webhook write, scheduled-task mutation, or credential persistence was executed for this slice.
- Completed B733 locally: brought `forge connectors import-discovery` closer to the CTI/validation safety contract for autonomous source queues. Discovery imports now expose `schema_version`, `execution_policy`, `dry_run`, `apply_requested`, `total_count`, `selected_count`, `omitted_count`, and selected host/finding/template counts; the CLI accepts `--dry-run` and `--limit`; report-file reads reject files above 10 MiB before parsing; dry-run performs parse/scope checks but skips host/service/seed/URL/finding/template/audit writes and does not commit. README, DAILY_USE, and the HTML plan now document bounded dry-run discovery imports. Verification passed on 2026-08-29: focused discovery-import tests (`10 passed`), connector discovery/registry tests (`60 passed`), combined connector/policy slice (`67 passed`), Ruff and `py_compile` on touched Python files, CLI help smoke showing `--dry-run`/`--limit`, temp ProjectDiscovery Cloud import smoke returning `status=dry_run`, `dry_run_no_writes`, `persisted=0`, real command-review smoke still `status=ready`, `daily_status=complete`, `missing=0`, `git diff --check`, and pasted-key fragment scan. No live provider/API call, scanner execution, target import/start apply, resume-run apply, monitoring apply, Docker mutation, ticket/webhook write, scheduled-task mutation, or credential persistence was executed for this slice.
- Completed B732 locally: made `forge automation command-review --json` expose a top-level machine-readable status so it matches the daily-use automation surfaces. `command_surface_review` now reports `status=ready` when the consolidated daily-use layer is fully documented and `attention` otherwise, while keeping existing counts, recommendations, and `daily_use_status` unchanged; human output now prints the same status. Verification passed on 2026-08-29: focused automation policy tests (`7 passed`), combined automation policy/CLI registry tests (`27 passed`), Ruff and `py_compile` on touched files, real `automation command-review --json` smoke showing `status=ready`, `daily_status=complete`, 9 daily commands, and zero missing docs, `git diff --check`, and pasted-key fragment scan. No live provider/Supabase/CTI call, target import/start apply, resume-run apply, monitoring apply, Docker mutation, ticket/webhook write, scheduled-task mutation, or credential persistence was executed for this slice.
- Completed B731 locally: tightened Supabase feed-build harvesting for the 1024 MB autonomous startup profile. Removed the unused raw row-accumulating Supabase helper, kept the active importer page-by-page, enforced configured per-table row caps before candidate harvesting even if a REST page over-returns rows, and added additive `scanned_table_rows`/`scanned_table_pages` telemetry to `supabase_table_discovery` so all-table/all-column runs expose bounded progress. Existing bare username all-column extraction remains covered by tests (`Alice` -> `@alice`). Verification passed on 2026-08-29: focused feed-build tests (`45 passed`), broader automation-cycle/feed-build/self-heal slice (`125 passed`), Ruff and `py_compile` on touched files, real read-only Supabase feed-build smoke showing `dry_run_no_writes` with no configured projects and no errors, real `automation limits --json` smoke showing `memory_gate=1024`, `feed_sources=all`, `total=24`, real full status smoke preserving known backlog (`resume=49`, `monitor=101`, `report=132`) and Supabase `not_configured`, `git diff --check`, and pasted-key fragment scan. No live Supabase/CTI/provider call, target import/start apply, resume-run apply, monitoring apply, Docker mutation, ticket/webhook write, scheduled-task mutation, or credential persistence was executed for this slice.
- Completed B730 locally: added `forge automation limits [--json]` as a read-only daily-use surface for effective resource/run/provider limits. The payload reports active local autostart config source, count aliases, config errors, memory/disk gates, resume/max_parallel/monitor/queue/start/min-source/runtime/cooldown/backoff/log/feed limits, hard caps for target-feed import, monitoring due execution, connector results, OpenRouter free-only default, and Docker Compose CPU/RAM/autostart timeout defaults without reading or printing secrets. Command-review now includes `automation_limits` in the documented daily layer, and README/DAILY_USE list the command. Verification passed on 2026-08-29: focused automation policy tests (`7 passed`), combined automation policy/CLI registry tests (`27 passed`), Ruff and `py_compile` on touched files, real `automation limits --json` smoke showing local config source and 24 limits, real command-review smoke showing daily layer complete and `automation_limits` documented, `git diff --check`, and pasted-key/smoke-key fragment scan. No target import/start apply, resume-run apply, monitoring apply, live provider/Supabase/CTI call, Docker mutation, ticket/webhook write, scheduled-task mutation, or credential persistence was executed for this slice.
- Completed B729 locally: added free-keyed URLhaus live CTI refresh alongside ThreatFox while keeping dry-run/no-network as the default. `forge automation cti-refresh --provider urlhaus` now defaults to `FORGE_URLHAUS_AUTH_KEY`, fetches the abuse.ch URLhaus recent CSV export only in `--apply`, writes a redacted-source local artifact at `imports/urlhaus-observations.local.json`, and queues `abusech_urlhaus` in `imports/urlhaus-inputs.local.json`. `automation status`/`cycle` CTI readiness now reports both supported public CTI providers, counts ready/unset provider keys, and recommends URLhaus refresh when only the URLhaus key is present, while preserving the legacy top-level ThreatFox fields. Verification passed on 2026-08-29: focused automation-cycle/feed-build tests (`90 passed`), broader automation-cycle/feed-build/self-heal slice (`124 passed`), Ruff and `py_compile` on touched files, URLhaus dry-run CLI smoke with no network/writes, quick-status smoke with a fake URLhaus env key showing URLhaus as the first CTI next action, `git diff --check`, and pasted-key/smoke-key fragment scan. Official abuse.ch URLhaus docs were checked and confirm the community API is free but Auth-Key gated. No live URLhaus/ThreatFox call, target import/start apply, resume-run apply, monitoring apply, Supabase call, Docker mutation, ticket/webhook write, scheduled-task mutation, or credential persistence was executed for this slice.
- Completed B728 locally: made `monitoring_due` in `automation status`/`automation cycle` expose additive daily-use count aliases (`total_count`, `selected_count`, `omitted_count`) alongside the existing `total_due_count`, `planned_policy_count`, and `limited_policy_count`. This keeps old consumers compatible while making monitoring backlog limits parse the same way as feed, queue, resume, and report summaries. Verification passed on 2026-08-29: focused automation-cycle tests (`45 passed`), broader automation-cycle/feed-build/self-heal slice (`121 passed`), Ruff on touched files, `py_compile` on touched files, real read-only full status smoke showing monitoring counts `101/0/101`, `git diff --check`, and pasted-key fragment scan. No monitoring apply, target import/start apply, resume-run apply, live provider/Supabase/CTI call, Docker mutation, ticket/webhook write, scheduled-task mutation, or credential persistence was executed for this slice.
- Completed B727 locally: bounded the Docker autostart profile's outer automation cycle so a hung container-side `forge automation cycle --apply --live` cannot block future guarded attempts forever. `forge-guarded-autostart` now runs the cycle through `timeout --preserve-status "$${FORGE_AUTOSTART_TIMEOUT_SECONDS:-9000}"`, documents the configurable timeout in README/DAILY_USE, and pins it in Docker packaging tests. Verification passed on 2026-08-29: Docker packaging tests (`16 passed, 1 skipped` for missing Helm), `py_compile` on the touched Docker packaging test, Ruff on the touched test, `docker compose -f docker/docker-compose.yml config --quiet` with dummy required env values, `git diff --check`, and pasted-key fragment scan. No Docker build/up/start, live target work, provider call, target import/start apply, resume-run apply, monitoring apply, ticket/webhook write, scheduled-task mutation, or credential persistence was executed for this slice.
- Completed B726 locally: aligned static doctor readiness with Forge's existing OpenRouter free-only discovery policy. `OPENROUTER_API_KEY` is no longer lumped into disabled paid API env options when `FORGE_ALLOW_PAID_BACKENDS` is unset; doctor now reports `openrouter_free_only_key` and the live-probe action says OpenRouter will only accept capable zero-price/free models. If paid backends are explicitly enabled, OpenRouter still counts as a paid API env option. Verification passed on 2026-08-29: focused doctor/provider tests (`67 passed`), Ruff on touched files, `py_compile` on touched files, `git diff --check`, pasted-key fragment scan, a repo-local doctor JSON smoke with a fake OpenRouter key showing free-only readiness, and a quick automation status smoke preserving backlog-first ordering. No live OpenRouter model-list call, provider completion, target import/start apply, resume-run apply, monitoring apply, Supabase/CTI provider call, Docker mutation, ticket/webhook write, scheduled-task mutation, or credential persistence was executed for this slice.
- Completed B725 locally: made full `automation status` include source-readiness setup gates in top-level `next_actions` instead of only exposing them inside nested `cti_refresh`/`supabase_sync`. Quick status still recommends a full status audit first, and full status preserves existing guarded live/backlog ordering when resume/monitoring/report work exists, then appends source setup actions such as setting `FORGE_THREATFOX_AUTH_KEY` and running `forge automation supabase-add PROJECT_REF FORGE_SUPABASE_PROJECT_READ_KEY --apply --json`. Verification passed on 2026-08-29: focused automation-cycle tests (`45 passed`), broader automation-cycle/feed-build/self-heal slice (`121 passed`), Ruff on touched files, `py_compile` on touched files, `git diff --check`, pasted-key fragment scan, real quick-status smoke preserving full-status-first ordering, and real full-status smoke showing CTI `key_env_unset`, Supabase `not_configured`, known backlog actions, plus appended CTI/Supabase setup actions. No Supabase network call, CTI provider network call, target import/start apply, resume-run apply, monitoring apply, Docker container start/build/up, ticket/webhook write, scheduled-task mutation, or credential persistence was executed for this slice.
- Completed B724 locally: added explicit machine-readable status labels to `forge monitoring run-due --json`. The monitoring run-due payload now reports `idle`, `dry_run`, `completed`, `completed_with_errors`, or `failed` from the existing due count, selected/run count, dry-run flag, and errors so automation supervisors no longer see `status=null` for valid dry-runs. Verification passed on 2026-08-29: focused monitoring continuous tests (`43 passed`, existing sqlite timestamp warnings only), Ruff on touched files, `py_compile` on touched files, `git diff --check`, pasted-key fragment scan, and real `monitoring run-due --dry-run --limit 50 --json` smoke showing `status=dry_run`, total `101`, selected `50`, omitted `51`, dry-run `true`, and no errors. No monitoring apply, target import/start apply, resume-run apply, live Supabase/CTI/provider call, Docker container start/build/up, ticket/webhook write, scheduled-task mutation, or credential persistence was executed for this slice.
- Completed B723 locally: added read-only Supabase sync readiness to the daily automation loop. `automation status` and `automation cycle` now include `supabase_sync` with local config path, configured/ready/key-env-missing/invalid counts, per-project project ref, derived URL, all-table/all-column intent, row limit, env-var presence, and safe next actions. This mirrors the existing feed-build behavior where minimal owned project entries (`project_ref` plus `key_env`) derive `https://<project_ref>.supabase.co`, discover all tables, and query all columns only when feed-build is explicitly run with the env key present. Verification passed on 2026-08-29: focused automation-cycle tests (`44 passed`), broader automation-cycle/feed-build/self-heal slice (`120 passed`), Ruff on touched files, `py_compile` on touched files, `git diff --check`, pasted-key fragment scan, real quick-status smoke showing `supabase_status=not_configured`, configured `0`, ready `0`, CTI key unset, and real dry-run live cycle smoke showing `status=live_ready_dry_run`, Supabase not configured, CTI key unset, autostart `ready`, resume `49`, monitoring `101`, and report `132`. No Supabase network call, CTI provider network call, target import/start apply, resume-run apply, monitoring apply, Docker container start/build/up, ticket/webhook write, scheduled-task mutation, or credential persistence was executed for this slice.
- Completed B722 locally: added read-only CTI refresh readiness to the daily automation loop. `automation status` and `automation cycle` now include `cti_refresh` with provider, default key env, key presence, local artifact/queue state, and safe next actions without making abuse.ch network calls or writing artifacts. When `FORGE_THREATFOX_AUTH_KEY` is present and there are no queued inputs/autostart blockers, status can recommend `forge automation cti-refresh --provider threatfox --apply --json` before the guarded live cycle so live CTI ingestion becomes discoverable but still explicit/free-key gated. Verification passed on 2026-08-29: focused automation-cycle tests (`40 passed`), broader automation-cycle/feed-build/self-heal slice (`116 passed`), Ruff on touched files, `py_compile` on touched files, `git diff --check`, pasted-key fragment scan, real quick-status smoke showing `cti_status=key_env_unset`, `key_env=FORGE_THREATFOX_AUTH_KEY`, no key present, and no network/writes, plus real dry-run live cycle smoke showing `status=live_ready_dry_run`, CTI key unset, autostart `ready`, resume `49`, monitoring `101`, and report `132`. No CTI provider network call, target import/start apply, resume-run apply, monitoring apply, live Supabase/provider call, Docker container start/build/up, ticket/webhook write, scheduled-task mutation, or credential persistence was executed for this slice.
- Completed B721 locally: made ThreatFox CTI refresh less manual and corrected its daily-use help text. `forge automation cti-refresh --provider threatfox --apply --json` now defaults to reading the free abuse.ch Auth-Key from `FORGE_THREATFOX_AUTH_KEY` when `--key-env` is omitted, dry-run output reports that default env var, and the CLI help no longer incorrectly calls ThreatFox no-key. Verification passed on 2026-08-29: focused feed-build/CTI-refresh tests (`42 passed`), broader feed-build plus automation-cycle slice (`79 passed`), Ruff on touched files, `py_compile` on touched files, `git diff --check`, pasted-key fragment scan, and real dry-run `automation cti-refresh --provider threatfox --json` smoke showing `execution_policy=dry_run_no_network_or_writes`, `key_env=FORGE_THREATFOX_AUTH_KEY`, `requires_key_env=true`, and no writes. Official abuse.ch documentation was checked and confirms ThreatFox/URLhaus community API/export access requires a free Auth-Key. No CTI provider network call, target import/start apply, resume-run apply, monitoring apply, live Supabase/provider call, Docker container start/build/up, ticket/webhook write, scheduled-task mutation, or credential persistence was executed for this slice.
- Completed B720 locally: made `forge doctor --json` easier for daily-use automation to consume without breaking existing payloads. Doctor now exposes top-level `status` (`ready`, `attention`, or `error`) and additive per-check aliases `id`, `message`, `next_action`, and `next_actions` alongside the existing `component`, `details`, `remediation`, and `action_items` fields. Verification passed on 2026-08-29: focused doctor tests (`46 passed`), Ruff on touched files, `py_compile` on touched files, `git diff --check`, pasted-key fragment scan, and real read-only `forge doctor --json` smoke showing status `attention` with actionable WARN IDs `safe_mode` and `monitoring_schedules`. No target import/start apply, resume-run apply, monitoring apply, live Supabase/CTI/provider call, Docker container start/build/up, ticket/webhook write, scheduled-task mutation, or credential persistence was executed for this slice.
- Completed B719 locally: added a top-level machine-readable `status` to `automation cycle`. Cycle responses now classify existing outcomes as `planned`, `planned_with_inputs`, `planned_with_backlog`, `queue_blocked`, `queue_deferred`, `queue_failed`, `feed_errors`, `attention`, `applied_local`, `applied_local_with_backlog`, `live_ready_dry_run`, `live_ready`, `live_completed`, or live failure/blocker states from the already-collected feed, queue, guarded-autostart, history, resume, monitoring, and report-review summaries. Verification passed on 2026-08-29: focused automation-cycle tests (`37 passed`), broader automation/self-heal/policy/CLI registry slice (`97 passed`), Ruff on touched files, `py_compile` on touched files, `git diff --check`, pasted-key fragment scan, and real read-only `automation cycle --source all --limit 5 --live --docker-probe-mode compose-dependency --json` smoke showing `status=live_ready_dry_run`, autostart `ready`, feed errors `0`, queues `0`, resume `49`, monitoring `101`, and report `132`. No target import/start apply, resume-run apply, monitoring apply, live Supabase/CTI/provider call, Docker container start/build/up, ticket/webhook write, scheduled-task mutation, or credential persistence was executed for this slice.
- Completed B718 locally: corrected quick-status next-action ordering so skipped backlog inventories are not hidden behind an immediate live-cycle recommendation. `automation status --quick --json` now returns top-level next actions `forge automation status --json` first, then the guarded live cycle, when resume/monitoring/report summaries were intentionally skipped. Verification passed on 2026-08-29: focused automation-cycle tests (`35 passed`), broader automation/self-heal/CLI registry slice (`89 passed`), Ruff, `py_compile`, `git diff --check`, pasted-key fragment scan, and real read-only quick status smoke showing `status=ready_unverified_backlog`, autostart `ready`, and next actions ordered as full status audit before guarded live cycle. No target import/start apply, resume-run apply, monitoring apply, live Supabase/CTI/provider call, Docker container start/build/up, ticket/webhook write, scheduled-task mutation, or credential persistence was executed for this slice.
- Completed B717 locally: cleaned up `autostart_history` next-action guidance so aged failures do not look like active repair work. The history summary now computes the status once and only recommends `forge automation self-heal-plan --json` for `recent_failure`; `historical_failure`, `recent_blocked`, success, empty history, and normal states point back to `automation status --json` for review. Verification passed on 2026-08-29: focused automation-cycle tests (`35 passed`), broader automation/self-heal/CLI registry slice (`89 passed`), Ruff, `py_compile`, `git diff --check`, pasted-key fragment scan, and real read-only quick status smoke showing `status=ready_unverified_backlog`, `autostart_history.status=historical_failure`, history next action `forge automation status --json`, autostart `ready`, and no blockers. No target import/start apply, resume-run apply, monitoring apply, live Supabase/CTI/provider call, Docker container start/build/up, ticket/webhook write, scheduled-task mutation, or credential persistence was executed for this slice.
- Completed B716 locally: added `forge automation status --quick --json` for low-cost startup/supervisor health checks. Quick status still reports feed existence/scanability, queue state, guarded-autostart probe, and autostart history, but explicitly skips the slower resume, monitoring, and report backlog inventories with `read_only_quick_status_skipped` summaries and top-level status `ready_unverified_backlog` when otherwise ready. Verification passed on 2026-08-29: focused automation-cycle tests (`35 passed`), broader automation/self-heal/policy/CLI registry slice (`95 passed`), focused doc/CLI slice (`55 passed`), Ruff, `py_compile`, `git diff --check`, pasted-key fragment scan, and real read-only quick status smoke returning in about 7.1s with `quick=true`, `status=ready_unverified_backlog`, skipped backlog policies, and autostart `ready`. No target import/start apply, resume-run apply, monitoring apply, live Supabase/CTI/provider call, Docker container start/build/up, ticket/webhook write, scheduled-task mutation, or credential persistence was executed for this slice.
- Completed B715 locally: added a top-level machine-readable `status` to `automation status`. The new classifier reports `blocked`, `attention`, `ready_with_inputs`, `ready_needs_feed`, `ready_with_backlog`, or `ready` from the already-gathered feed, queue, guarded-autostart, autostart-history, resume, monitoring, and report-review summaries. This lets startup wrappers/operators detect the current loop state without parsing every nested object. Verification passed on 2026-08-29: focused automation-cycle tests (`34 passed`), broader automation/self-heal/policy/CLI registry slice (`94 passed`), Ruff, `py_compile`, `git diff --check`, pasted-key fragment scan, and real read-only `automation status --json` smoke showing `status=ready_with_backlog`, autostart `ready`, history `historical_failure`, queues `0`, resume `49`, monitoring `101`, report `132`, and prioritized next actions for guarded live cycle plus safe backlog dry-runs. No target import/start apply, resume-run apply, monitoring apply, live Supabase/CTI/provider call, Docker container start/build/up, ticket/webhook write, scheduled-task mutation, or credential persistence was executed for this slice.
- Completed B714 locally: made top-level `automation status` next actions include safe backlog follow-ups when the guard is not blocked. Status now computes feed/autostart/resume/monitoring/report summaries once and `_status_next_actions` appends bounded dry-run/review commands from `resume_backlog`, `monitoring_due`, and `report_review` after the guarded live-cycle action, while blocker remediation still takes precedence when autostart is blocked. Verification passed on 2026-08-29: focused automation-cycle tests (`32 passed`), broader automation/self-heal/policy/CLI registry slice (`92 passed`), Ruff, `py_compile`, `git diff --check`, pasted-key fragment scan, and real read-only `automation status --json` smoke showing next actions for guarded live cycle, redacted resume plan, monitoring due-plan/run-due dry-run, and report-driven resume review commands. No target import/start apply, resume-run apply, monitoring apply, live Supabase/CTI/provider call, Docker container start/build/up, ticket/webhook write, scheduled-task mutation, or credential persistence was executed for this slice.
- Completed B713 locally: made `autostart_history` distinguish active/recent failures from historical failures after the configured backoff window expires. The summary now reads `failure_backoff_minutes` from `imports/autostart.local.json` when available, reports it, and classifies old persisted failed/dry-run-failed state as `historical_failure` instead of continuously showing `recent_failure`. Verification passed on 2026-08-29: focused automation-cycle tests (`31 passed`), broader automation/self-heal/CLI registry slice (`85 passed`), Ruff, `py_compile`, `git diff --check`, pasted-key fragment scan, and real read-only `automation status --json` smoke showing `autostart_history.status=historical_failure`, `failure_backoff=60`, previous `roe_id_env_missing` blockers preserved as history, and current autostart probe `ready` with no blockers. No target import/start apply, resume-run apply, monitoring apply, live Supabase/CTI/provider call, Docker container start/build/up, ticket/webhook write, scheduled-task mutation, or credential persistence was executed for this slice.
- Completed B712 locally: surfaced guarded-autostart persisted state/log history in the daily automation layer. `automation status` and `automation cycle` now include read-only `autostart_history` with state/log presence, last status/timestamps/return code, bounded recent JSONL status/mode/blocker counts, last recorded blockers, and safe next actions without exposing absolute log paths or executing guard commands. Corrupt log lines are counted and flagged as `log_attention`. README and DAILY_USE now document using `automation status --json` for guarded-autostart history. Verification passed on 2026-08-29: focused automation-cycle tests (`30 passed`), broader automation/self-heal/CLI registry slice (`90 passed`), docs/status slice (`84 passed`), Ruff, `py_compile`, `git diff --check`, pasted-key fragment scan, and real read-only `automation status --json` smoke showing `autostart_history.status=recent_failure`, previous `roe_id_env_missing` blocker counts from the persisted log, current autostart probe blocked only by `free_memory_below_threshold`, plus `resume=49`, `monitoring=101`, and `report=132`. No target import/start apply, resume-run apply, monitoring apply, live Supabase/CTI/provider call, Docker container start/build/up, ticket/webhook write, scheduled-task mutation, or credential persistence was executed for this slice.
- Completed B711 locally: surfaced report/dashboard review drift in the daily automation layer. `automation status` now accepts `--reports-dir`, and both `automation status` and `automation cycle` include a fail-soft read-only `report_review` summary from `report quality-audit --top-limit 0 --redact-paths`, including report family/file counts, current and historical dashboard refresh failures, resume/failed/long-run review counts, operator action counts, and safe next actions. Verification passed on 2026-08-29: focused automation-cycle tests (`27 passed`), broader automation/report/CLI registry slice (`84 passed`), Ruff, `py_compile`, `git diff --check`, pasted-key fragment scan, real read-only `automation status --json` smoke showing `report_review.status=review_due`, `total_count=132`, `report_file_count=3573`, `report_family_count=581`, `dashboard_refresh_failure_count=0`, `resume_review_count=49`, `long_run_count=3`, and action counts for resume/long-run/policy reviews; real dry-run live `automation cycle --source all --limit 5 --live --docker-probe-mode compose-dependency --json` smoke showed no writes/live commands, `report_review.total_count=132`, `resume_backlog=49`, and `monitoring_due=101`; a follow-up detailed dry-run autostart probe showed status `ready`, no blockers, and no active lock. No target import/start apply, resume-run apply, monitoring apply, live Supabase/CTI/provider call, Docker container start/build/up, ticket/webhook write, scheduled-task mutation, or credential persistence was executed for this slice.
- Completed B710 locally: made command-surface review recognize the consolidated daily-use layer. `automation command-review --json` now includes `daily_use_status` with documented/missing daily command counts, specialist command count, and base-command coverage; when all daily-use commands are documented, the old high `reduce_memory_load` recommendation is replaced with low-priority `daily_layer_ready` while preserving medium findings for oversized specialist groups. Verification passed on 2026-08-29: focused automation policy tests (`6 passed`), broader command-layer slice (`50 passed`), Ruff, `py_compile`, `git diff --check`, pasted-key fragment scan, and real read-only `automation command-review --json` smoke showing `daily_use_status.complete=true`, `daily_command_count=8`, `missing_count=0`, `specialist_command_count=127`, and no high `reduce_memory_load` recommendation. No target import/start apply, resume-run apply, monitoring apply, live Supabase/CTI/provider call, Docker container start/build/up, ticket/webhook write, scheduled-task mutation, or credential persistence was executed for this slice.
- Completed B709 locally: surfaced the target resume backlog in the daily automation layer. `automation status` and `automation cycle` now include a read-only `resume_backlog` summary from `targets resume-plan --limit 0 --redact-paths`, including status, total candidates, ready count, reason counts, and safe next actions without executing resume commands or exposing local DB/report paths. The summary includes legacy engagement DBs only when automation is using the configured Forge data dir, so alternate/test data dirs do not accidentally scan the real backlog. Verification passed on 2026-08-29: focused automation-cycle tests (`24 passed`), broader automation cycle/self-heal/policy/target import/launcher slice (`159 passed, 2 skipped`, existing sqlite timestamp warnings only), Ruff, `py_compile`, `git diff --check` (existing `.agents` CRLF warnings only), pasted-key fragment scan, real read-only `automation status --json` smoke showing `resume_backlog.status=ready`, `total_count=49`, `resume_ready_count=49`, reason counts `pending_recursive_work=31`, `watchdog_timeout=7`, `abandoned=6`, `stale_run_recovery=5`, and real dry-run live `automation cycle --source all --limit 5 --live --docker-probe-mode compose-dependency --json` smoke showing no writes/live commands, `resume_backlog.total_count=49`, `monitoring_due=101`, and detailed guarded-autostart probe ready with no blockers. No target import/start apply, resume-run apply, monitoring apply, live Supabase/CTI/provider call, Docker container start/build/up, ticket/webhook write, scheduled-task mutation, or credential persistence was executed for this slice.
- Completed B708 locally: made Docker compose-dependency health delegation explicit in self-heal JSON. `_docker_status(..., mode="compose-dependency")` now reports `core_service_health_check=delegated_not_inspected`, lists Compose-gated services (`postgres`, `redis`), lists app services not inspected in that mode (`forge-api`, `forge-webui`, `forge-worker`), and gives a host-side `host-compose` probe command as next action. This closes the ambiguity where compose-dependency readiness could be mistaken for a full container health inspection. Verification passed on 2026-08-29: self-heal tests (`34 passed`), Ruff, `py_compile`, real read-only self-heal smoke showing delegated/not-inspected service lists and next action, `git diff --check`, and pasted-key fragment scan. No Docker container start/build/up, live startup apply, target import/start apply, resume-run apply, monitoring apply, live Supabase/CTI/provider call, ticket/webhook write, scheduled-task mutation, or credential persistence was executed for this slice.
- Completed B707 locally: changed Docker autostart from a one-shot profile to a controlled resilient loop. `docker/docker-compose.yml` now runs `forge-guarded-autostart` with `restart: unless-stopped`, conservative CPU/RAM caps, a default 300-second startup delay, a default 9,300-second cadence, and an inner shell loop that runs `forge automation cycle --apply --live --docker-probe-mode compose-dependency --json`, then sleeps before retrying. Forge's own ROE, local config, memory/disk, Docker/tool, cooldown/backoff, queue limit, source-count threshold, and single-instance lock gates still decide whether live work actually runs, so Docker recovery does not create a tight crash loop. README, DAILY_USE, and systemd comments now describe the loop instead of one-shot behavior. Verification passed on 2026-08-29: Docker packaging tests (`16 passed, 1 skipped` for missing Helm), Docker Compose config `--quiet` with dummy process-local required env values, broader Docker/Windows/POSIX launcher plus automation self-heal/cycle slice (`102 passed, 3 skipped`), Ruff, `git diff --check`, stale one-shot wording search with only unrelated fixture text, and pasted-key fragment scan. No Docker container start/build/up, live startup apply, target import/start apply, resume-run apply, monitoring apply, live Supabase/CTI/provider call, ticket/webhook write, scheduled-task mutation, or credential persistence was executed for this slice.
- Completed B706 locally: hardened the Windows guarded-autostart wrapper timeout path to terminate the whole process tree. `scripts/run_guarded_autostart_task.ps1` now uses a local `Stop-ProcessTree` helper that calls `System32\taskkill.exe /PID <root> /T /F` when available, with `Stop-Process` fallback, and logs `process_tree_root_id` on timeout. This closes the startup robustness gap where a timed-out PowerShell wrapper could kill only the direct Python process and leave scanner grandchildren alive. Verification passed on 2026-08-29: focused Windows launcher tests (`22 passed`), wrapper `-SelfTest`, Ruff on launcher tests, broader Windows/POSIX launcher plus automation cycle/self-heal slice (`86 passed, 2 skipped`), `git diff --check`, and pasted-key fragment scan. No live startup registration, target import/start apply, resume-run apply, monitoring apply, live Supabase/CTI/provider call, Docker container start/build/up, ticket/webhook write, or credential persistence was executed for this slice.
- Completed B705 locally: bounded source-queue consumption before guarded live startup. `automation cycle` now reports `queue_execution` with `queue_limit`, selected/deferred ready counts, and execution order; explicit `--queue-limit N` limits ready queue imports, and `--live` defaults to `queue_limit` from the selected `autostart.local.json` so a large inbox cannot monopolize startup before target import/start/resume/monitoring. Deferred ready inputs stay pending for later cycles. `automation cycle` CLI no longer forces the repo-local autostart config when `--imports-dir` is customized; it now lets the core derive the matching imports-dir config unless `--autostart-config` is explicit. Automation defaults and docs now expose `queue_limit` and `min_start_source_count` as daily tuning knobs. This machine's ignored local aggressive config has non-secret `queue_limit: 10`. Verification passed on 2026-08-29: focused automation cycle/self-heal/policy tests (`61 passed`), broader automation cycle/self-heal/policy/target import/launcher slice (`156 passed, 2 skipped`, existing sqlite timestamp warnings only), Ruff, `py_compile`, `git diff --check`, real dry-run live cycle smoke showing no writes/live commands, queue limit 10, ready 0, deferred 0, autostart ready, autostart queue limit 10, threshold 2, and 5 startable preview targets, real defaults smoke showing tunables for `queue_limit` and `min_start_source_count`, plus pasted-key fragment scan. No queue import apply against real inputs, target import/start apply, resume-run apply, monitoring apply, live Supabase/CTI/provider call, Docker container start/build/up, ticket/webhook write, scheduled-task mutation, or credential persistence was executed for this slice.
- Completed B704 locally: made automation status/cycle report real live-start capacity under the configured source-count threshold. `target_feed_scan` now includes `min_start_source_count`, `startable_count`, `eligible_below_start_threshold_count`, and `top_startable_targets`; `scan_policy` echoes the active threshold. `automation cycle` now mirrors `automation status` by reading `autostart.local.json` from the selected imports directory unless an explicit config path is provided, preventing tests or alternate imports dirs from inheriting this machine's ignored aggressive config. Verification passed on 2026-08-29: focused automation-cycle tests (`20 passed`), broader target import/self-heal/launcher/automation cycle slice (`149 passed, 2 skipped`, existing sqlite timestamp warnings only), Ruff, `py_compile`, `git diff --check`, real status smoke showing threshold 2, 4,076 eligible, 3,733 startable, 343 below threshold, autostart ready and no blockers, real dry-run cycle smoke showing no writes/live commands and threshold 2, plus pasted-key fragment scan. No target import/start apply, resume-run apply, monitoring apply, live Supabase/CTI/provider call, Docker container start/build/up, ticket/webhook write, scheduled-task mutation, or credential persistence was executed for this slice.
- Completed B703 locally: added an explicit live-start source-count threshold while preserving import breadth. `forge targets import --start` now accepts `--min-start-source-count N`; targets below the threshold are still imported and monitored but do not spend live kill-chain start budget, and the skipped start reason reports `source_count_below_min_start_threshold:X<Y`. `forge-autopilot.bat`, `forge-autopilot.sh`, and guarded-autostart config now pass the threshold through end to end. The committed default remains `1` for compatibility, while this machine's ignored local aggressive `imports/autostart.local.json` is set to non-secret `min_start_source_count: 2`. Verification passed on 2026-08-29: affected target import/self-heal/Windows launcher/POSIX launcher/automation cycle tests (`149 passed, 2 skipped`, existing sqlite timestamp warnings only), Ruff, `py_compile`, `git diff --check`, real dry-run `targets import --feed-file imports\target-feed.json --dry-run --limit 5 --min-start-source-count 2 --json` smoke showing 5 parsed and 0 started, real read-only guarded-autostart smoke showing status ready, no blockers, config threshold 2, and generated `--min-start-source-count 2`, real dry-run live automation cycle smoke showing no writes/live commands, autostart ready, threshold 2, zero queues, and stale monitoring due summary, plus pasted-key fragment scan. No target import/start apply, resume-run apply, monitoring apply, live Supabase/CTI/provider call, Docker container start/build/up, ticket/webhook write, scheduled-task mutation, or credential persistence was executed for this slice.
- Completed B702 locally: surfaced monitoring due backlog in the daily automation layer and fixed same-cycle propagation for queued artifacts. `automation status` and `automation cycle` now include a read-only `monitoring_due` summary from `monitoring due-plan --limit 0`, including stale backlog status, capped invocation estimate, and next monitoring actions without executing monitoring. `automation cycle --apply` now rebuilds and rewrites the target feed after successful source queue imports, before any guarded live autostart handoff, so targets promoted by CTI/connector/Burp/Censys/runZero/ProjectDiscovery artifact queues are eligible in the same live cycle while guarded autostart still uses `--skip-feed-build`. Verification passed on 2026-08-29: focused automation/feed tests (`61 passed`), broader automation/feed/cycle/policy/CLI registry slice (`87 passed`), Ruff, `py_compile`, `git diff --check`, real read-only `automation status --json` filtered smoke showing `monitoring_due.status=stale_due`, `total_due_count=101`, and `estimated_capped_invocations=3`, real dry-run live `automation cycle --source all --limit 5 --live --docker-probe-mode compose-dependency --json` filtered smoke showing no writes/live commands, autostart ready, zero queues, feed selected 5, and `feed_rebuilt_after_queue_imports=false`, pasted-key fragment scan, and `.agents` secret-pattern review with only existing safe env/secret-store wording. No monitoring apply, target import/start apply, resume-run apply, live Supabase/CTI/provider call, Docker container start/build/up, ticket/webhook write, scheduled-task mutation, or credential persistence was executed for this slice.
- Completed B701 locally: corrected the CTI live-refresh plan against current abuse.ch documentation and added a guarded ThreatFox refresh path without making live CTI a no-key startup dependency. `forge automation cti-refresh --provider threatfox` now defaults to no-network dry-run; `--apply` requires `--key-env ENV` for a free abuse.ch Auth-Key, sends it only as an HTTP header, writes `imports/threatfox-observations.local.json` only when IOCs are returned, and queues that artifact through the existing source-queue path. Empty `no_result` responses perform no artifact/queue write, HTTP failures fail closed before writes, and URLhaus remains rejected from this refresh path because it is also keyed/currently handled as local artifact import. README and DAILY_USE now state that no-key CTI operation means local artifact drops or `automation input-add`, while direct ThreatFox refresh is free-account/keyed and explicit. Verification passed on 2026-08-29: focused feed/setup/CTI refresh tests (`41 passed`), broader automation/feed/cycle/policy/CLI registry slice (`84 passed`), Ruff, `py_compile`, `git diff --check`, real dry-run `automation cti-refresh --provider threatfox --days 1 --json` with no network/write, real apply-without-key smoke failing closed with `key_env_required_for_threatfox_apply`, pasted-key fragment scan, `.agents` secret-pattern review with only existing safe env/secret-store wording, and current abuse.ch ThreatFox/URLhaus docs review showing Auth-Key required for API interaction. No CTI provider apply call, local CTI/source queue write, live Supabase/provider call, target import/start apply, resume-run apply, monitoring apply, Docker container start/build/up, ticket/webhook write, scheduled-task mutation, or credential persistence was executed for this slice.
- Completed B700 locally: added no-secret setup helpers for the remaining local source-input configuration gap. `forge automation supabase-add PROJECT_REF KEY_ENV` now previews or writes ignored Supabase project metadata from only an owned project ref plus env var name, derives the URL/table/column defaults through existing feed-build logic, upgrades pending discoveries, preserves configured refs unless `--replace`, and refuses key-looking values. `forge automation input-add --connector CONNECTOR_ID --file ARTIFACT` now previews or writes the correct ignored per-source queue file for CTI marker drops, ProjectDiscovery Cloud exports, Censys/runZero/asset-delta artifacts, and Burp/JUnit DAST XML, with dedupe, optional engagement/target, local-control-file rejection, and no secret material. Verification passed on 2026-08-29: focused feed-build/setup tests (`35 passed`), broader automation/feed/cycle/policy/CLI registry slice (`78 passed`), Ruff, `py_compile`, `git diff --check`, real dry-run `automation supabase-add https://abc123.supabase.co FORGE_SUPABASE_ABC123_READ_KEY --config %TEMP%\forge-supabase-smoke.local.json --json`, real dry-run `automation input-add --connector abusech_threatfox --file threatfox.json --imports-dir %TEMP% --json`, real read-only `automation command-review --json` showing 134 commands and documented daily-use layer, pasted-key fragment scan, and `.agents` secret-pattern review with only existing safe env/secret-store wording. No local Supabase config entry for a real project, CTI/source queue write, live Supabase/CTI/provider call, target import/start apply, resume-run apply, monitoring apply, Docker container start/build/up, ticket/webhook write, scheduled-task mutation, or credential persistence was executed for this slice.
- Completed B699 locally: ran the remaining live-provider readiness probe without writing credentials or invoking paid backends. `forge doctor --json --live-provider-probes` reports `paid_allowed=False` and detects four usable local/subscription backends (`claude_code`, `codex_cli`, `gemini_cli`, `llama_cpp`), while keeping OpenRouter unclaimed because no OpenRouter key is present in environment. This verifies the provider-discovery path does not guess a current free OpenRouter model when it cannot prove one. Verification passed on 2026-08-29: doctor live-provider probe completed with execution policy `read_only_environment_readiness_no_commands_executed`, LLM Providers `OK`, `32 OK`, `2 WARN`, `6 OFF`, `12 OPTIONAL`, and no secret values printed. No live OpenRouter/model-list call, paid provider call, live Supabase/CTI/provider call, target import/start apply, resume-run apply, monitoring apply, Docker container start/build/up, ticket/webhook write, scheduled-task registration success, or credential persistence was executed for this slice.
- Completed B698 locally: hardened Windows startup when Task Scheduler permission is denied by turning the HKCU Run fallback into a single guarded login-session loop. The scheduled-task path still uses native logon/repetition triggers when registration succeeds; the HKCU fallback now starts `run_guarded_autostart_task.ps1 -Loop -EveryMinutes 155 -StartupDelayMinutes 5`, guarded by a local named mutex so duplicate loop processes exit. Each loop iteration still runs the same `forge automation cycle --apply --live --json` path and therefore remains protected by Forge config, ROE, memory/disk/Docker/tool, cooldown, backoff, and autostart lock gates. Refreshed the actual HKCU Run entry on this host; Task Scheduler is still denied by Windows, but the registry value now includes the safe loop arguments. Verification passed on 2026-08-29: Windows launcher tests (`22 passed`), affected Windows/automation cycle/self-heal tests (`72 passed`), Ruff, `py_compile`, `git diff --check`, runner `-SelfTest`, installer `-DryRun` showing scheduled one-shot action and HKCU loop fallback action, real installer refresh showing Task Scheduler denied and HKCU loop fallback installed, registry verification showing `-Loop -EveryMinutes 155 -StartupDelayMinutes 5`, real dry-run `automation cycle --source all --limit 25 --live --docker-probe-mode compose-dependency --json` showing no writes/live commands, autostart ready, zero blockers, and clean queues, pasted-key fragment scan, and `.agents` secret-pattern review with only existing safe env/secret-store wording. No live provider/model-list probe, live Supabase/CTI/provider call, target import/start apply, resume-run apply, monitoring apply, Docker container start/build/up, ticket/webhook write, scheduled-task registration success, or credential persistence was executed for this slice.
- Completed B697 locally: made `automation feed-build` counts unambiguous for source-only runs that preserve an existing merged feed. JSON counts now include `selected_existing_preserved`, `selected_from_current_sources`, `selected_includes_existing`, `existing_feed_items`, `generated_candidate_total`, and `generated_unique_candidate_total`; human output now prints `current_source` and `preserved_existing`. This closes the audit gap where `feed-build --source supabase --json` could show `selected=6867` while Supabase itself contributed zero. Verification passed on 2026-08-29: focused feed-build tests (`26 passed`), affected automation/feed/startup regression slice (`114 passed, 1 skipped` for missing Helm), Ruff, `py_compile`, `git diff --check`, real Supabase-only dry-run showing `selected=6867`, `selected_from_current_sources=0`, `selected_existing_preserved=6867`, `generated_candidate_total=0`, `generated_unique_candidate_total=0`, `by_source.supabase=0`, and zero source errors, real human Supabase-only feed-build summary showing `current_source=0 preserved_existing=6867`, real all-source limited dry-run showing current-source counters, pasted-key fragment scan, and `.agents` secret-pattern review with only existing safe env/secret-store wording. No live provider/model-list probe, live Supabase/CTI/provider call, target import/start apply, resume-run apply, monitoring apply, Docker container start/build/up, ticket/webhook write, scheduled-task registration success, or credential persistence was executed for this slice.
- Completed B696 locally: fixed the final contradiction-audit gap where Docker compose-dependency readiness depended on the current process inheriting `FORGE_HOST_CONNECTOR_BIN_DIR`. Self-heal now falls back to persistent Windows user/machine environment for `FORGE_HOST_CONNECTOR_BIN_DIR` only when the process env is empty, so newly persisted user env is honored even in process trees that did not inherit it. `automation status` now performs a read-only guarded-autostart probe when `imports/autostart.local.json` exists and reports `autostart_probe`; it recommends the guarded live cycle only when that probe has no blockers, otherwise it points to `automation self-heal-plan`. Verification passed on 2026-08-29: focused automation cycle/self-heal tests (`50 passed`), broader autonomous-loop regression slice (`223 passed, 1 skipped` for missing Helm), Ruff, `py_compile`, `git diff --check`, real self-heal plan without process `FORGE_HOST_CONNECTOR_BIN_DIR` showing persisted user env `C:\Users\bryan\go\bin`, status ready, Docker mount OK, and missing 0, real `automation status --json` showing autostart probe ready and guarded live next action, real dry-run `automation cycle --source all --limit 25 --live --docker-probe-mode compose-dependency --json` showing no writes/live commands, autostart ready, and zero blockers, full local feed rebuild via `automation cycle --apply --source all --json` writing 6,867 selected targets with zero source errors and no live work, target import dry-run (`5` selected, `0` created/started), resume-run dry-run (`5` planned of `49`), monitoring run-due dry-run (`5` planned of `101`), connector run-plan (`20` runnable free/local templates), report quality-audit (`3,573` files, `581` families, `0` current dashboard refresh failures), doctor (`32 OK`, `2 WARN`, `6 OFF`, `12 OPTIONAL`), pasted-key fragment scan, and `.agents` secret-pattern review with only existing safe env/secret-store wording. No live provider/model-list probe, live Supabase/CTI/provider call, target import/start apply, resume-run apply, monitoring apply, Docker container start/build/up, ticket/webhook write, scheduled-task registration success, or credential persistence was executed for this slice.
- Completed B695 locally: aligned `forge automation status` next-action guidance with the requested autonomous live daily loop. When queues are ready it now suggests both the local queue/feed apply path and the guarded live cycle path; when queues are idle it points directly at `forge automation cycle --apply --live --docker-probe-mode compose-dependency --json` instead of the local-only apply command. Refreshed the actual Windows startup registration after B693/B694; Task Scheduler registration is still denied by Windows, but the HKCU Run fallback is installed and points at the corrected hidden runner with quoted repo/config paths. Verification passed on 2026-08-29: affected automation cycle/self-heal tests (`48 passed`), Ruff, `py_compile`, `git diff --check`, real read-only `automation status --json` showing the guarded live next action, HKCU Run registry verification, pasted-key fragment scan, and `.agents` secret-pattern review with only existing safe env/secret-store wording. No live provider/model-list probe, live Supabase/CTI/provider call, target import/start apply, resume-run apply, monitoring apply, Docker container start/build/up, ticket/webhook write, scheduled-task registration success, or credential persistence was executed for this slice.
- Completed B694 locally: closed the startup robustness gaps found by the restarted subagent audit. Guarded-autostart stale lock age now uses the selected local config's `failure_backoff_minutes` instead of the 120-minute default, so this machine's 60-minute backoff also controls stale-lock recovery. Docker Compose autostart now defaults `FORGE_AUTOSTART_MEM_LIMIT` to `1536m` while the Forge internal memory gate remains `1024` MB, preventing the autostart container from self-blocking below its own cgroup headroom. Persisted non-secret user-level startup env values `FORGE_HOST_CONNECTOR_BIN_DIR=C:\Users\bryan\go\bin`, `COMPOSE_PROFILES=autostart`, and `FORGE_AUTOSTART_MEM_LIMIT=1536m`, and mirrored those three non-secret values into ignored local `.env` for Compose runs. Verification passed on 2026-08-29: affected self-heal/Docker/Windows launcher tests (`70 passed, 1 skipped` for missing Helm), Ruff, `py_compile`, `git diff --check`, Docker Compose autostart config `--quiet` with dummy process-only production secrets, read-only self-heal plan showing status ready, zero blockers, 1024 MB memory minimum with 2393 MB free, Docker/delegated Compose OK, mounted Go tool count 13 and missing 0, real dry-run `automation cycle --source all --limit 25 --live --docker-probe-mode compose-dependency --json` showing no writes/live commands, effective engagement 1001, source `all`, threshold 2, autostart ready, and zero blockers, pasted-key fragment scan, and `.agents` secret-pattern review with only existing safe env/secret-store wording. No live provider/model-list probe, live Supabase/CTI/provider call, target import/start apply, resume-run apply, monitoring apply, Docker container start/build/up, ticket/webhook write, scheduled-task registration mutation, or credential persistence was executed for this slice.
- Completed B693 locally: fixed the Windows scheduled/HKCU startup runner command-line quoting so repo and config paths containing spaces survive `Start-Process` when it invokes `python -m forge.cli automation cycle`. The generated runner now joins a safely quoted argument string before `Start-Process`, closing the prior failure where `imports\autostart.local.json` was split at `01 TOOLKITS`. Verification passed on 2026-08-29: Windows/POSIX launcher tests (`31 passed, 2 skipped` for WSL temp paths), affected automation self-heal/cycle/policy tests (`53 passed`), focused Windows launcher tests from the interrupted slice (`22 passed`), Ruff on the touched test file, `py_compile`, `git diff --check`, runner `-SelfTest`, installer `-DryRun` showing quoted scheduled-task and HKCU fallback commands, real dry-run `automation cycle --source all --limit 25 --live --docker-probe-mode compose-dependency --json` showing `dry_run_no_writes_or_live_commands_executed`, effective engagement 1001, no ready or blocked input queues, source `all`, multi-source scan threshold 2, autostart status ready, and zero blockers, pasted-key fragment scan, and `.agents` secret-pattern review with only existing safe env/secret-store wording. No live provider/model-list probe, live Supabase/CTI/provider call, target import/start apply, resume-run apply, monitoring apply, Docker container start/build/up, ticket/webhook write, scheduled-task registration mutation, or credential persistence was executed for this slice.
- Completed B692 locally: fixed a status/cycle consistency gap found during current-state verification. `automation status` now resolves the same default engagement chain as `automation cycle` by reading the selected imports directory's `autostart.local.json` before falling back to `FORGE_DEFAULT_ENGAGEMENT_ID`, and it reports that autostart config path accurately. Added a regression test proving queue readiness uses `engagement_id` from `imports/autostart.local.json`. Updated the ignored local `imports/autostart.local.json` on this machine with non-secret `engagement_id: 1001`, matching existing local `1001.db`, so unattended source-queue imports have a default engagement when startup runs. Verification passed on 2026-08-29: focused automation cycle/status tests (`16 passed`), broader automation cycle/self-heal/Windows/POSIX launcher/policy tests (`84 passed, 2 skipped` for WSL temp paths), Ruff, py_compile, `git diff --check`, real read-only `automation status --json` showing effective engagement 1001 from `imports\autostart.local.json`, 4,076 scan-eligible feed targets, and clean queues, real dry-run `automation cycle --source all --limit 25 --live --docker-probe-mode compose-dependency --json` showing effective engagement 1001, no writes/live commands, zero source errors, clean queues, guarded autostart ready, Docker-mounted tools OK, and 25 eligible preview targets, pasted-key fragment scan, and `.agents` secret-pattern review with only existing safe env/secret-store wording. No live provider/model-list probe, live Supabase/CTI/provider call, target import/start apply, resume-run apply, monitoring apply, Docker container start/build/up, ticket/webhook write, or credential persistence was executed for this slice.
- Completed B691 locally: tightened command consolidation so self-heal/defaults point operators at the queue-aware daily loop instead of direct launcher habits. `automation self-heal-plan --json` now includes `cycle_dry_run` and `cycle_apply_live` command plans and explicitly lists routing startup through `automation cycle` as a guardrail. `automation defaults --json` now exposes `startup_dry_run` as `forge automation cycle --live --json` and keeps direct guarded-autostart only as `guarded_probe`. DAILY_USE and README were updated to match. Verification passed on 2026-08-29: focused automation self-heal/policy/cycle tests (`52 passed`), Ruff, py_compile, `git diff --check`, real read-only `automation defaults --json` and `automation self-heal-plan --json` smokes showing cycle-first startup commands and self-heal status `ready`, real dry-run `automation cycle --source all --limit 25 --live --docker-probe-mode compose-dependency --json` showing no writes/live commands, zero source errors, clean queues, guarded autostart ready, Docker-mounted tools OK, and 25 eligible preview targets, real read-only `automation command-review --json` showing 132 commands, 8 documented daily-use entries, and 0 undocumented daily entries, pasted-key fragment scan, and `.agents` secret-pattern review with only existing safe env/secret-store wording. No live provider/model-list probe, live Supabase/CTI/provider call, target import/start apply, resume-run apply, monitoring apply, Docker container start/build/up, ticket/webhook write, or credential persistence was executed for this slice.
- Completed B690 locally: tightened OpenRouter free-only provider selection for autonomous runs. When paid backends are not explicitly allowed, OpenRouter discovery now requests `/models?sort=newest`, requires numeric zero prompt and completion pricing, rejects weak/unknown free IDs, and selects the newest capable recognized free model family such as Qwen, DeepSeek, Llama, Gemma, Mistral, Kimi, or Phi. README and DAILY_USE document that OpenRouter is skipped unless a capable zero-price/free model is proven. Verification passed on 2026-08-29: provider discovery/router/openai-compatible/cost-table tests (`115 passed`), Ruff, py_compile, `git diff --check`, real read-only `doctor --json` smoke showing Free/Local Baseline `OK`, Paid LLM Backends `OK`/disabled, and LLM Providers preferring local/subscription CLI checks with live HTTP/model-list probes disabled by default, pasted-key fragment scan, and `.agents` secret-pattern review with only existing safe env/secret-store wording. No live OpenRouter/model-list probe, paid provider call, live Supabase/CTI/provider call, target import/start apply, resume-run apply, monitoring apply, Docker container start/build/up, ticket/webhook write, or credential persistence was executed for this slice.
- Completed B689 locally: tightened Supabase all-column extraction for startup-scale use. Supabase table reads now harvest each REST page immediately instead of retaining the full configured table limit in memory, and key hints such as `username`, `usernames`, `handle`, and `screen_name` preserve bare usernames as canonical `@name` targets during JSON/Supabase harvesting. README and DAILY_USE now document page-by-page harvesting and username key hints. Verification passed on 2026-08-29: focused feed-build tests (`25 passed`), broader affected feed/cycle/self-heal/launcher/Docker/CLI/policy tests (`144 passed, 3 skipped` for WSL temp paths and Helm availability), Ruff, py_compile, POSIX launcher syntax, `git diff --check`, Docker Compose autostart config with dummy non-secret env values and `FORGE_HOST_CONNECTOR_BIN_DIR`, pasted-key fragment scan, real dry-run `automation feed-build --source supabase --json` showing no writes/errors with no active local Supabase config, and real dry-run `automation cycle --source all --limit 25 --live --docker-probe-mode compose-dependency --json` showing dry-run policy, no feed write, zero source errors, Supabase discovery summary present, clean queues, guarded autostart ready, Docker-mounted tools OK, and 25 eligible preview targets. No live Supabase/CTI/provider call, target import/start apply, resume-run apply, monitoring apply, Docker container start/build/up, ticket/webhook write, or credential persistence was executed for this slice.
- Completed B688 locally: closed the remaining subagent-reviewed startup robustness gaps around direct guarded-autostart and Supabase all-table visibility. Supabase feed-build payloads now include `supabase_table_discovery` per configured project with `discovered`, `configured`, `blocked_key`, or `blocked_table_discovery` status, scanned table counts, row limits, errors, and a local next action when all-table REST discovery cannot proceed. Direct `forge automation guarded-autostart --apply` now reads local source queues and blocks with `source_queues_require_automation_cycle` if ready local CTI/connector/Burp/import artifacts would be bypassed; startup docs now point Docker/Windows hooks to `forge automation cycle --apply --live --json`. Direct `forge-autopilot` Windows/POSIX launcher defaults now match guarded limits (`start_limit=2`, `resume_limit=10`, `max_parallel=2`, `monitor_limit=10`, `max_runtime_minutes=10`) while remaining dry-run by default and ROE-gated for apply. Verification passed on 2026-08-29: focused feed-build tests (`25 passed`), focused self-heal/launcher/feed tests (`87 passed, 2 skipped` for WSL temp-path execution), broader affected feed/cycle/self-heal/launcher/Docker/CLI/policy tests (`144 passed, 3 skipped` for WSL temp paths and Helm availability), Ruff, py_compile, POSIX launcher syntax, `git diff --check`, Docker Compose autostart config with dummy non-secret env values and `FORGE_HOST_CONNECTOR_BIN_DIR`, pasted-key fragment scan, `.agents` secret-pattern review, real dry-run `automation feed-build --source supabase --json` showing no writes/errors with no active local Supabase config, and real dry-run `automation cycle --source all --limit 25 --live --docker-probe-mode compose-dependency --json` showing dry-run policy, no feed write, zero source errors, Supabase discovery summary present, clean queues, guarded autostart ready, Docker-mounted tools OK, and 25 eligible preview targets. No live Supabase/CTI/provider call, target import/start apply, resume-run apply, monitoring apply, Docker container start/build/up, ticket/webhook write, or credential persistence was executed for this slice.
- Completed B687 locally: made queued local CTI/connector/Burp/import artifacts usable by unattended startup without editing every queue entry. Guarded autostart config now accepts an optional `engagement_id`, `automation status` and `automation cycle` resolve an effective engagement from explicit `--engagement`, local autostart config, or `FORGE_DEFAULT_ENGAGEMENT_ID`, and queue readiness uses that value when pending inputs do not carry their own engagement. Windows startup fallback now launches `forge automation cycle --autostart-config ... --apply --live --json` instead of entering guarded-autostart directly, so source queues are classified and consumed before feed import/start, resume, monitoring, and dashboard refresh. README and DAILY_USE document the unattended engagement fallback. Verification passed on 2026-08-29: focused automation cycle/self-heal/Windows launcher/Docker/CLI/policy tests (`109 passed, 1 skipped` for Helm availability), Ruff, py_compile, `git diff --check`, Docker Compose autostart config with dummy non-secret env values and `FORGE_HOST_CONNECTOR_BIN_DIR`, pasted-key fragment scan, `.agents` secret-pattern review, real read-only `automation status --json` with `FORGE_DEFAULT_ENGAGEMENT_ID=1001` showing effective engagement 1001 and 4,076 scan-eligible feed targets, and real dry-run `automation cycle --source all --limit 25 --live --docker-probe-mode compose-dependency --json` showing dry-run policy, zero source errors, clean queues, guarded autostart ready, Docker-mounted tools OK, and 25 eligible preview targets. No live Supabase/CTI/provider call, target import/start apply, resume-run apply, monitoring apply, Docker container start/build/up, ticket/webhook write, or credential persistence was executed for this slice.
- Completed B686 locally: closed the subagent-reported startup/queue and defaults drift. Docker Compose autostart now enters through `forge automation cycle --apply --live --docker-probe-mode compose-dependency --autostart-config /app/imports/autostart.local.json --json`, so Compose startup classifies inbox drops and consumes ready local source queues before guarded-autostart launches target import/start, resume, monitoring, or dashboard phases. `automation cycle` now accepts and forwards `--docker-probe-mode` to guarded-autostart. `automation self-heal-plan` now emits `docker_tool_mount_status` and treats missing/mismatched mounted runtime Go tools as blockers for Compose-dependency mode, avoiding false Docker readiness when tools are only host-visible or shadowed by PATH shims. The operator-facing `automation defaults` aggressive preset and memory tunables now align with the requested 1024 MB gate. Windows launcher behavioral tests now use the real Python runtime plus a temp fake `forge.cli` instead of copying the venv executable. Verification passed on 2026-08-29: affected automation cycle/defaults/self-heal/Windows launcher/Docker/CLI registry tests (`105 passed, 1 skipped` for Helm availability), focused self-heal mount tests (`3 passed`), focused policy/self-heal/cycle/Docker/Windows tests (`85 passed, 1 skipped`), Ruff, py_compile, `git diff --check`, Docker Compose autostart config with dummy non-secret env values and `FORGE_HOST_CONNECTOR_BIN_DIR`, pasted-key fragment scan, `.agents` secret-pattern review with only existing safe env/secret-store wording, real read-only `automation defaults --json` showing aggressive `min_free_memory_mb=1024`, real read-only `automation self-heal-plan --json --docker-probe-mode compose-dependency` with `FORGE_HOST_CONNECTOR_BIN_DIR=C:\Users\bryan\go\bin` showing status `ready` and 13/13 Docker-mounted runtime tools OK, real read-only `automation guarded-autostart --json --docker-probe-mode compose-dependency` showing ready dry-run commands, and real dry-run `automation cycle --source all --limit 25 --live --docker-probe-mode compose-dependency --json` showing no writes/live commands, zero source errors, clean queues, 25 high-priority scan-eligible preview targets, and guarded autostart ready. No live Supabase/CTI/provider call, target import/start apply, resume-run apply, monitoring apply, Docker container start/build/up, ticket/webhook write, or credential persistence was executed for this slice.
- Completed B685 locally: tightened the remaining standalone/startup audit surface after B684. `automation self-heal-plan` now treats `FORGE_HOST_CONNECTOR_BIN_DIR` as a first-class local tool search path, so the same ignored Go binary directory can satisfy host readiness checks and Docker autostart bind mounts. Added behavioral Windows `forge-autopilot.bat` tests proving apply mode exits before any Python/Forge phase when ROE is missing and stops after a failed apply-mode feed build before target import, resume, or monitoring. Added an explicit test contract for the packaged/free Go tools requested for standalone operation (`nuclei`, `gopls`, `gitleaks`, `amass`, `ffuf`, `gobuster`, `mapcidr`, `tlsx`, `uncover`, `naabu`, `dnsx`, `httpx`, `katana`, `subfinder`). Verification passed on 2026-08-29: focused Windows launcher/self-heal/Docker tests (`64 passed, 1 skipped` for Helm availability), broader affected launcher/Docker/self-heal/provider/feed/cycle/discovery tests (`133 passed, 3 skipped` for WSL bash/Helm availability), Ruff, py_compile, `git diff --check`, Docker Compose autostart config with dummy non-secret env values, pasted-key fragment scan, real read-only `.venv\Scripts\python.exe -m forge.cli automation self-heal-plan --json --docker-probe-mode compose-dependency` showing status `ready`, 1024 MB memory gate passing with 3057 MB free, and all 14 Go tools found with expected sizes, real read-only `automation status --json` showing 6,867 feed targets and 4,076 scan-eligible, real read-only `automation command-review --json`, and real read-only `report quality-audit --json --top-limit 1` showing 3,573 report files, 581 families, and 0 current dashboard refresh failures. No live Supabase/CTI/provider call, target import/start apply, resume-run apply, monitoring apply, Docker container start/build/up, ticket/webhook write, or credential persistence was executed for this slice.
- Completed B684 locally: restarted the interrupted subagent audit and closed its concrete autonomous-loop gaps. `feed-build` now scans the current report corpus scale instead of stopping at 500 report JSON files, and local CTI/connector feed mining now supports bounded passive JSONL/CSV/XML/TXT/LOG/GZ/ZIP target extraction with `cti_file:`/`connector_file:` provenance. Source-queue failures now persist `failed` status, return code, redacted error text, retry-after timestamps, exponential backoff, and a 5-failure hard block instead of retrying every cycle. Apply-mode `forge-autopilot` launchers fail before feed/import/resume/monitoring when ROE is absent, and OpenRouter free-only selection now requires numeric zero pricing. ProjectDiscovery Cloud import now persists bounded template inventory as asset graph evidence nodes, while Censys import now carries service/TLS/software fingerprints and topology relationships through the existing asset graph path. Autostart memory defaults/docs/tests are aligned at 1024 MB, with Linux systemd autostart remaining explicit opt-in via `COMPOSE_PROFILES=autostart`. Verification passed on 2026-08-29: focused launcher/Docker/self-heal/provider/feed/cycle/discovery tests (`129 passed, 3 skipped` for WSL bash/Helm availability), Ruff, py_compile, `git diff --check`, POSIX launcher syntax, BAT help smoke, Docker Compose autostart config with dummy non-secret env values, pasted-key fragment scan, real dry-run `automation feed-build --source all --limit 25 --json` showing 44,227 report candidates and zero source errors, real local `automation cycle --apply --source all --json` writing the feed with 6,867 selected targets and no live work, real read-only `automation status --json`, `automation command-review --json`, and `report quality-audit --json --top-limit 1`. No live Supabase/CTI/provider call, target import/start live apply, resume-run apply, monitoring apply, Docker container start/build/up, ticket/webhook write, or credential persistence was executed for this slice.
- Completed B683 locally: closed the concrete startup/self-heal risks found by the subagent audit after B682. Direct `forge-autopilot` launchers now default to dry-run and require `--apply` for live resume/monitoring/import behavior; guarded-autostart appends `--apply` only after its local config/ROE/resource/lock gates pass. Apply-mode launcher feed-build failures now stop before stale feed import, resume, or monitoring. `automation self-heal-plan` and `guarded-autostart` now support `--docker-probe-mode host-compose|compose-dependency|disabled`; the Docker autostart profile uses `compose-dependency` so it does not require a Docker socket inside the app container. Memory checks now account for Linux cgroup memory limits before comparing against the configured memory gate. Docker autostart now uses a 1024 MB default memory cap matching the local gate, binds local Go binaries from ignored `tools/bin/` or `FORGE_HOST_CONNECTOR_BIN_DIR`, and `.dockerignore` excludes local `imports/` and `tools/bin/` from build context. Verification passed on 2026-08-29: affected automation/launcher/Docker/CLI registry tests (`98 passed, 1 skipped`), Ruff, py_compile, `git diff --check`, POSIX `bash -n forge-autopilot.sh`, BAT `--help` smoke, `docker compose -f docker/docker-compose.yml --profile autostart config --quiet` with dummy non-secret env and `FORGE_HOST_CONNECTOR_BIN_DIR`, pasted-key fragment scan, real read-only `automation self-heal-plan --json --docker-probe-mode compose-dependency`, and real read-only `automation guarded-autostart --json --docker-probe-mode compose-dependency` showing Docker health delegated, ROE present, tools present, and live startup blocked only by current low memory. No live Supabase/CTI/provider call, target import/start apply, resume-run apply, monitoring apply, Docker container start/build/up, ticket/webhook write, or credential persistence was executed for this slice.
- Completed B682 locally: finished the Docker/startup robustness slice for autonomous live-safe operation. The POSIX and Windows `forge-autopilot` launchers now prefer the repo virtualenv but can also run from a packaged PATH runtime without hanging on unattended failures. The runtime Docker image packages `forge-autopilot.sh`/`.bat`, and production Compose now has an opt-in `autostart` profile with a one-shot `forge-guarded-autostart` service, lower default caps (`FORGE_AUTOSTART_CPUS=0.25`, `FORGE_AUTOSTART_MEM_LIMIT=256m`), read-only base hardening, and writable mounts only for `imports/`, `reports/`, and `/data`. README and DAILY_USE document the Docker profile. Verification passed on 2026-08-28: focused launcher/Docker packaging tests (`41 passed, 1 skipped`), automation self-heal/cycle tests (`32 passed`), Ruff, py_compile, `git diff --check`, POSIX `bash -n forge-autopilot.sh`, BAT `--help` smoke, `docker compose -f docker/docker-compose.yml --profile autostart config --quiet` with dummy non-secret env values, pasted-key fragment scan, and real read-only `automation guarded-autostart --json` showing status `ready` with no live commands executed. No live Supabase/CTI/provider call, target import/start apply, resume-run apply, monitoring apply, Docker container start/build/up, ticket/webhook write, or credential persistence was executed for this slice.
- Completed B681 locally: added a read-only scanability summary to `automation status` and `automation cycle` so the autonomous loop reports whether the current target feed can actually consume live scan budget. The new `target_feed_scan` block reports total, eligible, ineligible, high-priority counts, ineligible reason counts, and top eligible scan candidates from either an existing feed file or the in-memory dry-run feed payload. Verification passed on 2026-08-28: Ruff, focused automation status/cycle tests (`2 passed`), broader automation/feed/import slice (`94 passed`, existing sqlite timestamp warnings only), py_compile, `git diff --check`, pasted-key fragment scan, and real `automation status --json` over the current feed showing 6,867 total targets, 4,076 eligible, 3,733 high-priority eligible, 2,791 ineligible due to `non_global_ip`, queues `total=0 ready=0 blocked=0 ignored=0`, and top targets all scan-eligible priority-90 entries. No live Supabase/CTI/provider call, target import/start apply, resume-run apply, monitoring apply, Docker mutation, ticket/webhook write, or credential persistence was executed for this slice.
- Completed B680 locally: made the user's "seen from 2 sources can scan already / prioritize scans" rule operational instead of only advisory. `target-feed.v1` items now carry `source_groups`, `source_count`, `priority`, `scan_eligible`, and `scan_eligibility_reason`; multi-source eligible targets sort first and get priority 90, single-source eligible targets remain priority 60, and non-global IP/URL-host noise is retained as evidence but lowered to priority 10 and skipped by live target starts without consuming `--start-limit`. `targets import --json` now exposes source count, priority, scan eligibility, and start skip reason so dry-runs show exactly what would be scanned first. Verification passed on 2026-08-28: Ruff, focused priority/eligibility/import/start tests (`6 passed`), broader feed/import/automation slice (`93 passed`, existing sqlite timestamp warnings only), py_compile, `git diff --check`, pasted-key fragment scan, real `automation cycle --apply --source all --json` rewriting `imports\target-feed.json` with 6,867 selected targets and zero source errors, feed inspection showing top targets are eligible priority-90 multi-source entries, real `targets import --feed-file imports\target-feed.json --dry-run --limit 3 --json` exposing priority/source_count/scan_eligible, real `automation status --json` showing source queues clean, and real `automation cycle --source connectors --limit 1 --live --json` dry-run showing ROE present but live guarded-autostart blocked safely on `free_memory_below_threshold` at 169 MB available against the 1024 MB gate. No live Supabase/CTI/provider call, target import/start apply, resume-run apply, monitoring apply, Docker mutation, ticket/webhook write, or credential persistence was executed for this slice.
- Completed B679 locally: cleaned the autonomous source-input queue loop so empty local scaffolds and Forge-maintained queue/control files no longer appear as actionable blocked imports. `automation status` and `automation cycle` now classify those entries as ignored with explicit reasons, `doctor --fix-safe` prunes them from source-specific queues and the shared `discovered-inputs.local.json` registry, and feed-build discovery skips empty `*.local.json` scaffolds plus `*-inputs.local.json`/`*-imports.local.json` controls so the next applied cycle does not recreate them. Verification passed on 2026-08-28: Ruff, `git diff --check`, focused automation/feed/import tests (`31 passed`), py_compile, pasted-key fragment scan, real `doctor --fix-safe --json` pruning 10 shared registry entries and 11 source queue placeholders, real `automation status --json` showing feed exists and queues `total=0 ready=0 blocked=0 ignored=0`, real `automation cycle --apply --source all --json` rewriting `imports\target-feed.json` with 6,867 selected targets and zero source errors, real `targets import --feed-file imports\target-feed.json --dry-run --limit 3 --json`, real `automation command-review --json`, real `report quality-audit --json --top-limit 1`, and real `automation cycle --source connectors --limit 1 --live --json` dry-run showing guarded autostart ready on resource/Docker/tool gates but `roe_id_present=false` in the current process. No live Supabase/CTI/provider call, target import/start apply, resume-run apply, monitoring apply, Docker mutation, ticket/webhook write, or credential persistence was executed for this slice.
- Completed B678 locally: made the daily generated target feed truly repeatable and importable. Report extraction now skips the large `reports/dashboard/data/engagements.json` aggregate intentionally instead of treating it as a parse/source error, while still mining detail/report metadata. `targets import` now supports `--json`, exposes schema/execution/count fields, and accepts local feed files up to 64 MiB with an explicit max import limit of 100,000 items so the current 4.25 MB `imports/target-feed.json` can be reread. `feed-build` now loads large existing feeds with the full bounded item cap, so reruns report `new_vs_existing=0` instead of seeing only the old first 100 items. Verification passed on 2026-08-28: focused target-import/feed/automation/autostart/policy/registry/launcher tests (`81 passed` plus narrower `53 passed`/`50 passed`/`47 passed` during repair), Ruff, py_compile, `git diff --check`, pasted-key fragment scan, real `automation cycle --apply --source all --json` writing 6,867 selected targets with zero source errors, real `targets import --feed-file imports\target-feed.json --dry-run --limit 3 --json`, real `automation cycle --source connectors --limit 1 --live --json`, and real `automation status --json` showing feed exists and 10 source queues blocked only on `engagement_required`. No live Supabase/CTI/provider call, target import/start apply, resume-run apply, monitoring apply, Docker mutation, ticket/webhook write, or credential persistence was executed for this slice.
- Completed B677 locally: tightened the live cycle handoff so `forge automation cycle --apply --live` writes/builds the feed once and invokes guarded-autostart with `--skip-feed-build`; standalone `forge automation guarded-autostart` still plans launcher commands with `--feed-build` so startup hooks remain self-contained. Added tests for the parent-cycle skip behavior and direct guarded-autostart skip mode, and documented the distinction in README/DAILY_USE. Verification passed on 2026-08-28: focused automation/autostart/launcher tests (`31 passed`), Ruff, py_compile, `git diff --check`, pasted-key fragment scan, real dry-run `automation cycle --source connectors --limit 1 --live --json` showing guarded `--skip-feed-build`, real dry-run `automation guarded-autostart --json` showing standalone `--feed-build`, and real `automation command-review --json` showing the daily layer documented. No live Supabase/CTI/provider call, target import/start, resume-run apply, monitoring apply, Docker mutation, ticket/webhook write, or credential persistence was executed for this slice.
- Completed B676 locally: added `forge automation status` and `forge automation cycle` as the streamlined daily-use layer. Status is read-only and reports target-feed presence, local source queues, ready/blocked inputs, next actions, and explicit scan policy (`source=all` by default, multi-source threshold 2 gets high priority, new targets scan only when `cycle --apply --live` reaches guarded ROE/resource gates). Cycle runs feed-build, classifies `imports/inbox/` drops into source-specific ignored queues, consumes ready free/local queue items in priority order, and optionally invokes guarded-autostart for live target import/start, resume, monitoring, and dashboard refresh. Added `forge doctor --fix-safe` to create/repair ignored local input scaffolds without live/provider/scanner work; JSON now labels that local-safe mutation clearly. Verification passed on 2026-08-28: focused automation/feed/policy/registry/doctor tests (`51 passed`), Ruff, py_compile, `git diff --check`, pasted-key fragment scan, real `automation status --json`, real dry-run `automation cycle --source connectors --limit 1 --json`, and real `doctor --fix-safe --json` smoke. No live Supabase/CTI/provider call, target import/start, resume-run apply, monitoring apply, Docker mutation, ticket/webhook write, or credential persistence was executed for this slice.
- Completed B675 locally: user clarified that non-Supabase inputs should also get their own maintained files, not only a shared discovered-inputs index. `feed-build --apply` now writes source-specific ignored local queues alongside the shared registry: `threatfox-inputs.local.json`, `urlhaus-inputs.local.json`, `misp-inputs.local.json`, `stix-taxii-inputs.local.json`, `projectdiscovery-cloud-imports.local.json`, `censys-imports.local.json`, `runzero-imports.local.json`, `asset-delta-imports.local.json`, and `burp-dast-imports.local.json`. Dry-run now reports the per-source queue files that would be updated, with pending counts, without writing. Connector harvesting now ignores all `*.local.json` so these maintained lists cannot be mistaken for connector payloads. Verification passed on 2026-08-28: focused automation feed tests (`16 passed`), Ruff, py_compile, `git diff --check`, and pasted-key fragment scan. No live Supabase/CTI/provider call, target import/start, resume-run, monitoring apply, Docker mutation, ticket/webhook write, or credential persistence was executed for this slice.
- Completed B674 locally: user corrected the Supabase-only interpretation and wants all scanned artifacts to grow maintained lists. `forge automation feed-build` now reports and, with `--apply`, appends discovered Supabase project refs from artifact targets into ignored `imports/supabase-projects.local.json` as `status: pending_key` entries with generated `key_env` names. It also maintains ignored `imports/discovered-inputs.local.json` as a generalized local backlog for no-key/free CTI marker drops, ProjectDiscovery Cloud exports, Censys/runZero/asset-delta discovery artifacts, and Burp/JUnit DAST XML validation artifacts. Dry-run reports `discovered_inputs`, `new_discovered_inputs`, and registry update counts without writing. Feed-build excludes its own local registry/config files from connector harvesting, and importable artifact hints come from actual local import files to avoid report-text false positives. Verification passed on 2026-08-28: focused automation feed tests (`15 passed`), Ruff, py_compile, `git diff --check`, pasted-key fragment scan, and real local `forge automation feed-build --source all --limit 10 --json` dry-run showing no writes and five accepted local CTI marker inputs. No live Supabase/CTI/provider call, target import/start, resume-run, monitoring apply, Docker mutation, ticket/webhook write, or credential persistence was executed for this slice.
- Completed B673 locally: user wants Supabase to be greedier under ROE: scan all tables/all columns for configured projects, with less concern about the previous 1,000-row cap. Updated Supabase feed fetch to use paginated table reads (`limit=1000&offset=N` pages), raised default/max per-table rows to 100,000, raised discovered table cap to 1,000 per configured project, and kept arbitrary discovered Supabase URLs as targets only unless an owned project entry and key are explicitly configured. Updated tests for paginated reads and docs/local template for the new greedy cap. Verification passed on 2026-08-28: focused automation feed tests (`11 passed`), Ruff, py_compile, `git diff --check`, no pasted-key fragment scan, real local `forge automation feed-build --source supabase --limit 10 --json` dry-run, and real local `forge automation feed-build --source all --limit 10 --json` dry-run. No live Supabase call, target import/start, resume-run, monitoring apply, Docker mutation, ticket/webhook write, or credential persistence was executed for this slice.
- Completed B672 locally: user amended Supabase requirements so they only provide `project_ref` and `key_env`; Forge must deduce URL and process all exposed tables/columns. Updated the feed builder to derive `https://<project_ref>.supabase.co` when `url` is omitted, default missing `tables` to `["*"]`, default missing `target_columns` to `["*"]`, discover exposed table paths through a read-only Data API root/OpenAPI request, and fetch discovered tables with `select=*` while harvesting target-like strings from all nested row values. Added a focused mocked Supabase test for the minimal config shape and updated README/DAILY_USE plus the ignored local template. Verification passed on 2026-08-28: focused automation feed tests (`10 passed`), Ruff, py_compile, `git diff --check`, no pasted-key fragment scan, and real local `forge automation feed-build --source supabase --limit 10 --json` dry-run parsing the empty local config with zero source errors and `dry_run_no_writes`. No live Supabase call, target import/start, resume-run, monitoring apply, Docker mutation, ticket/webhook write, or credential persistence was executed for this slice.
- Completed B671 locally: user wants CTI to be easier than manual JSON and asked why only a few local files were ignored. Broadened local import ignore coverage to all `imports/*.local.json` and provider-marker CTI drops (`threatfox`, `urlhaus`, `misp`, `stix`, `taxii`) across file extensions. Added empty ignored local CTI observation scaffolds for URLhaus, MISP, STIX, and TAXII alongside the existing ThreatFox file so future runs can populate accepted marker files without manual JSON construction. Updated README and DAILY_USE with the per-provider CTI local drop files and raw export ignore behavior. Verification passed on 2026-08-28: ignored-file checks cover Supabase, ThreatFox, URLhaus, MISP, STIX, TAXII, and raw URLhaus/ThreatFox export names; `forge automation feed-build --source cti --limit 10 --json` parsed all local CTI scaffolds cleanly with zero targets and `dry_run_no_writes`; focused automation feed tests passed (`9 passed`). No live CTI API call, Supabase call, target import/start, resume-run, monitoring apply, Docker mutation, ticket/webhook write, or credential persistence was executed for this slice.
- Completed B670 locally: created ignored local Supabase and CTI feed scaffolds without secrets or placeholder targets. `imports/supabase-projects.local.json` now contains an empty `projects` array plus an example read-only project shape using `key_env`; `imports/threatfox-observations.local.json` is an empty CTI observation drop file whose name is picked up by feed-build. Added a gitignore rule for local observation drops and documented the CTI filename/key requirements in README and DAILY_USE. Lowered the ignored local guarded-autostart memory gate from 2048 MB to 1024 MB after creating the source scaffolds. Fixed feed-build so Forge's own local config files (`supabase-projects.local.json`, `autostart.local.json`, `target-feed.json`) are not harvested as connector-output targets during `--source all`. Verification passed on 2026-08-28: focused automation feed tests (`9 passed`), Ruff, py_compile, `git diff --check`, ignored-file checks for Supabase/CTI/autostart config, `forge automation feed-build --source supabase --limit 10 --json` and `--source cti --limit 10 --json` both parsed cleanly with zero selected targets and `dry_run_no_writes`, `forge automation feed-build --source all --limit 10 --json` reported connector count 0 for local config files, and `forge automation guarded-autostart --json` read the 1024 MB gate and reported ready when memory recovered. No live autostart apply, Supabase API call, target import/start, resume-run, monitoring apply, Docker mutation, ticket/webhook write, or credential persistence was executed.
- Completed B669 locally: user green-lit live startup enablement. Set persistent user-level `FORGE_ROE_ID` and refreshed the current process without printing the value. Fixed GitHub Dependabot alert 10 in `pyproject.toml` by moving optional offensive `asyncssh` from vulnerable `2.18.0` to patched `2.23.1`; repaired the local venv to `asyncssh=2.23.1` with `cryptography=42.0.8`, and `pip check` reports no broken requirements. Added Windows guarded-autostart runner/installer scripts. Task Scheduler registration was denied by Windows even with a current-user limited principal, so the installer fell back successfully to an HKCU Run startup entry named `FORGE Guarded Autostart` that launches `scripts/run_guarded_autostart_task.ps1` hidden at user logon with `imports/autostart.local.json` and `TimeoutMinutes=150`. Also fixed guarded-autostart whole-batch timeout calculation so it budgets for `start_limit * max_runtime_minutes + 30` minutes, capped at 6 hours, instead of timing out after one target runtime. Immediate guarded apply with refreshed ROE/env reached ready preflight but timed out during the old too-short dry-run dashboard refresh before live apply; no target import/start, resume-run apply, monitoring apply, or Docker mutation ran. Current guarded dry-run now sees ROE, memory, disk, Docker, and tools ready, but blocks on the intentional 60-minute `failure_backoff_active` from that failed preflight. Verification passed on 2026-08-28: PowerShell parser checks for the new scripts, py_compile, Ruff, focused guarded-autostart/Windows launcher tests (`39 passed in 32.90s`), targeted installer tests, `pip check`, `asyncssh=2.23.1` import smoke, `git diff --check`, pasted-key fragment scan, HKCU Run registry verification, and guarded-autostart JSON dry-run. No secret value was committed or written to `.agents/`.
- Completed B668 locally: aggressive autonomous startup defaults are now represented in the ignored local `imports/autostart.local.json` (enabled/apply_enabled true, ROE env gate `FORGE_ROE_ID`, memory gate 2048 MB, max_parallel 4, resume_limit 25, monitor_limit 25, start_limit 5, max_runtime_minutes 20, cooldown 30, failure_backoff 60, feed_sources `["all"]`). The committed launchers and guarded-autostart now propagate validated repeatable feed-source settings to `forge automation feed-build`, defaulting to `all`; OpenRouter remains free-only by default because no paid-backend opt-in is set or committed. Fixed target-feed re-read preservation so existing feed items retain `source_group` provenance instead of degrading to source kind. Verification passed on 2026-08-28: py_compile, Ruff, focused/expanded automation feed/autostart/target-import/launcher tests (`117 passed, 132 existing sqlite timestamp warnings in 134.43s`), `git diff --check`, BAT `--help` smoke, real read-only `automation guarded-autostart --json` smoke showing the aggressive local config but blocked on low free memory and missing `FORGE_ROE_ID`, real read-only `automation feed-build --source all --limit 1 --json` smoke scanning DB/report/all configured source classes with dry-run no writes, and real `automation command-review --json` smoke. No live guarded-autostart apply, target import/start, resume-run, monitoring apply, Docker mutation, scheduled-task install, provider API call, ticket/webhook write, or credential persistence was executed.
- Completed B667 locally: added `forge automation defaults [--json]` as a read-only operator-facing defaults surface. It returns automation policy defaults, wildcard scope defaults, guarded-autostart defaults, conservative/current/aggressive startup presets, memory/cadence/log/feed/OpenRouter tunables, safety metadata, and a disabled `imports/autostart.local.json` template without writing config or printing secret values. The command is now part of the documented daily-use layer. Verification passed on 2026-08-28: py_compile, Ruff, focused automation policy + CLI registry tests (`26 passed in 104.16s`), real `automation defaults --json` smoke, real `automation command-review --json` smoke showing `forge automation defaults` documented in README and DAILY_USE, `git diff --check`, and pasted-key fragment scan showing only the redaction regex and fake test key. No local config write, target import/start, resume-run, scanner execution, monitoring apply, Docker mutation, scheduled-task install, provider API call, ticket/webhook write, or credential persistence was executed.
- Completed final free-first audit locally: all requested no-key/free-first implementation slices are now represented as completed in the committed roadmap and state. Final read-only verification on 2026-08-28 passed: no `status next` or `Remaining Free-First Work` markers remain in `forge-autonomous-target-loop-plan.html`; `automation command-review --json` reports all daily-use layer entries documented; `automation feed-build --json --limit 1` still scans local DB/report sources and reports `dry_run_no_writes`; `report quality-audit --json --top-limit 1` still reads old artifacts with 3,573 report files, 581 report families, latest fallback counts empty, and dashboard refresh failures 0. Remaining 49 resume-review rows and 3 long-run rows are historical run-level operator backlog, not unfinished implementation. No live resume, scanner execution, monitoring apply, Docker mutation, scheduled-task install, provider API, ticket/webhook, or credential action was executed.
- Completed B666 locally: finished the free/local autostart follow-up. `self-heal-plan --probe-docker` now runs a read-only `docker compose ps --format json` probe, summarizes container state/health, and blocks startup on unhealthy/starting/exited/dead/paused services. `guarded-autostart --apply` now writes a bounded redacted JSONL history at `FORGE_DATA_DIR/automation/guarded-autostart.jsonl` with configurable `log_max_entries`; dry-run remains non-mutating. README, DAILY_USE, and the HTML plan document the behavior. Verification passed on 2026-08-28: py_compile, Ruff, self-heal/autostart tests (`19 passed in 20.20s` after fixing one missing test import), `git diff --check`, real `automation guarded-autostart --json` dry-run smoke showing fail-closed low-memory/config blockers, Docker JSON health probe OK, and packaged Go binaries resolved from `~/go/bin`, plus pasted-key fragment scan showing only the redaction regex and fake test key. No Docker mutation, service install, target import/start, resume-run, monitoring apply, live provider/API call, scheduled-task change, or credential persistence was executed.
- Completed B665 locally: added no-key `projectdiscovery_cloud` offline export import. `forge connectors import-discovery --connector projectdiscovery_cloud` now accepts operator-supplied ProjectDiscovery Cloud JSON exports, imports in-scope assets/services/seeds/asset graph provenance, parses scoped nuclei-style findings/templates into existing local `vulnerability_findings`, reports parsed/persisted/skipped finding and template counts, and keeps live ProjectDiscovery Cloud API sync plan-only until operator-owned credentials are supplied. README, DAILY_USE, connector registry, and the HTML plan document the free/local path. Verification passed on 2026-08-28: py_compile, Ruff, focused ProjectDiscovery Cloud import/registry tests (`2 passed in 15.07s`), full discovery import + connector registry tests (`57 passed in 112.97s`), `git diff --check`, read-only `connectors list --json` and `connectors run-plan --json` smokes showing `projectdiscovery_cloud`, and pasted-key fragment scan showing only redaction regexes/fake fixtures. No live ProjectDiscovery Cloud API call, scanner execution, target import/start, resume-run, monitoring apply, Docker mutation, scheduled-task change, or credential persistence was executed.
- Completed B664 locally: OpenRouter provider discovery remains free-first when `FORGE_ALLOW_PAID_BACKENDS` is unset. With `OPENROUTER_API_KEY`, Forge now only enables OpenRouter when the mocked/live model-list shape proves a zero-price/free model, prefers capable free model families, and skips OpenRouter instead of falling back to a paid default when free fit cannot be proven. README, DAILY_USE, and the HTML plan document the behavior. Verification passed on 2026-08-28: py_compile, Ruff, focused mocked OpenRouter tests (`3 passed in 20.20s`), broader provider tests (`51 passed in 13.31s`), `git diff --check`, and pasted-key fragment scan showing only the redaction regex and fake test key. No live provider call, target import/start, resume-run, monitoring apply, Docker mutation, scheduled-task change, or credential persistence was executed.
- Completed B663 locally: consolidated the daily-use command surface in read-only command-review output. `forge automation command-review --json` now includes a `daily_use_layer` with automation, doctor, targets resume, connectors plan/run, and report review entrypoints plus README/DAILY_USE documentation status; DAILY_USE now lists the same daily commands. Verification passed on 2026-08-28: focused automation policy tests (`2 passed in 156.52s`), Ruff, py_compile, `git diff --check`, and real `automation command-review --json` smoke showing all daily-use entries documented. No provider call, target import/start, resume-run, monitoring apply, ticket/webhook write, Docker mutation, scheduled-task change, live validation run, or credential persistence was executed.
- Completed planning artifact refresh: created `forge-autonomous-target-loop-plan.html` and uploaded it to PostPlan at `https://ylrghmdr8arh.postplan.dev`. The plan merges the original autonomous target-loop work, completed free/local slices, remaining no-key tasks, packaged Go tool handling, startup robustness findings, and OpenRouter free-model runtime-selection guidance. No secret material was written to the plan.
- Completed B662 locally: hardened guarded-autostart for limited Docker/Windows startup use. Guarded-autostart now reports guarded lock status, replaces stale/dead-PID guarded locks only in apply mode, keeps active locks blocking, runs autopilot through the contained process-tree timeout helper, prefers packaged Go tools in `~/go/bin` before PATH shims, and direct autopilot launchers print only ROE presence. Production Compose now has conservative env-overridable CPU/RAM caps for API/web/worker/Postgres/Redis. Verification passed on 2026-08-28: focused automation self-heal/subprocess/launcher/docker tests (`23 passed in 49.03s`), Ruff, py_compile, `git diff --check`, and real guarded-autostart dry-run smoke that blocked safely on missing config, low memory, and Docker probe timeout while resolving `httpx.exe` from the Go bin. No Docker mutation, service install, target import/start, resume-run, monitoring apply, provider call, live validation run, scheduled-task change, or credential persistence was executed.
- Completed B661 locally: added the free/local closed-loop remediation dashboard slice. Remediation review queues and dashboard remediation rows now derive ticket state, latest ticket sync status, validation proof freshness, stale-proof review reasons, and missing/failed-ticket review counters from existing remediation rows, local ticket events, and active-validation retest metadata. Verification passed on 2026-08-28: focused remediation/reporting tests (`8 passed in 27.58s`), broader remediation/webui tests (`41 passed in 144.76s`), Ruff, py_compile, `git diff --check`, real local `remediation review-queue --engagement 1001 --json` smoke, and pasted-key fragment scan. No schema migration, webhook/API call, ticket write, target import/start, resume-run, monitoring apply, scheduled-task change, live validation run, or credential persistence was executed for this slice.
- Completed B660 locally: added the free/local active-validation matrix. `active_validation_control_coverage` now reports proof type counts, proof freshness counts, fresh/stale/unrun totals, retest-pending count, and per ATT&CK/control/method proof summaries derived only from existing job metadata, method registry `proof_kind`, and latest local runs. Dashboard active-validation coverage rows now include `Proof Types` and `Proof Freshness`, and Burp/JUnit XML imports appear as local artifact proof via `metadata_json.proof_type`. Verification passed on 2026-08-28: focused active-validation/Burp/exposure tests (`10 passed in 50.77s`), Ruff, py_compile, `git diff --check`, leak regression (`2 passed in 29.05s`), and real read-only `active-validation coverage --engagement 4 --json` smoke showing the new empty summary fields. No schema migration, live validation run against external targets, provider call, ticket/webhook write, target import/start, resume-run, monitoring apply, scheduled-task change, engagement mutation outside temp fixtures, or credential persistence was executed for this slice.
- Completed B659 locally: extended free/local discovery artifact import for Censys/runZero-style asset deltas. `forge connectors import-discovery` now accepts `asset_delta_import` and `runzero_asset_export` JSON/CSV exports, scope-gates observed IP/name evidence, persists hosts/services/seeds, preserves fingerprint depth/topology provenance in sanitized metadata, and projects hosts/services/topology into asset graph nodes/relationships. README/DAILY_USE and connector registry/run-plan now expose the new no-key paths. Verification passed on 2026-08-28: discovery import tests (`6 passed in 81.14s`), broader connector/CLI registry slice (`11 passed in 123.16s`), Ruff, py_compile, `git diff --check`, and a temp real CLI import smoke (`parsed=1`, `hosts=1`, `services=1`, `seeds=1`, `graph_nodes=3`, `graph_relationships=1`). No live provider/API calls, scanner execution, external target contact, target import/start, resume-run, monitoring apply, scheduled-task changes, credential persistence, or production data mutation were executed for this slice.
- Completed B658 locally: added free/local exposure-duration metrics. `forge monitoring exposure-metrics` computes first_seen, last_seen, open_days, recurrence, and MTTR from existing local SQLite monitoring changes, vulnerability findings, active-validation runs, and remediation rows without migrations or DB writes. Dashboard detail payloads/pages now include `exposure_duration_metrics`, and README/DAILY_USE list the command. Verification passed on 2026-08-28: exposure metrics/dashboard tests (`6 passed in 24.29s`), broader monitoring/CLI registry slice (`9 passed in 127.76s`), Ruff, py_compile, `git diff --check`, and real read-only CLI smoke over `C:\Users\bryan\.forge\data` (`db_count=101`, `schema_ready_db_count=101`, `total_count=0`, `errors=[]`). Secret-fragment scan found only the generic OpenRouter redaction regex and a fake test key. No migrations, scanner execution, external calls, monitoring snapshot creation, provider calls, target import/start, resume-run, ticket/webhook writes, scheduled-task changes, engagement mutation, or credential persistence were executed for this slice.
- Completed B657 locally: added `forge connectors import-validation --connector burp_dast_xml` for free/local Burp Suite issue XML and JUnit DAST XML import into scoped active-validation evidence. It is offline-only, rejects DOCTYPE/ENTITY XML, caps XML size, strips sensitive URL query parameters, scope-gates URL evidence, does not persist request/response bodies, writes sanitized `active_validation_jobs`/`active_validation_runs` only when not in `--dry-run`, and dedupes repeated imports by stable external ID. Registry/run-plan and README/DAILY_USE public command docs now include the connector. Verification passed: validation-import/connector-registry/CLI-registry tests (`10 passed in 32.91s`), Ruff, py_compile, real CLI dry-run smoke, `git diff --check`, and pasted-key fragment scan. No scanner execution, network request, vulnerability-finding write, provider call, target import/start, resume-run, monitoring apply, scheduled-task change, or credential persistence was executed.
- Completed B656 locally: added `forge automation guarded-autostart` as the startup-safe wrapper for Docker/OS hooks. It reads ignored local `imports/autostart.local.json`, stays dry-run/read-only by default, requires literal JSON booleans plus CLI `--apply`, config `enabled=true`, config `apply_enabled=true`, and a non-empty configured ROE env var for live autopilot, probes self-heal guardrails before apply, fails closed on unknown memory, redacts ROE/secret-shaped command output, enforces a single-instance lock under the Forge data dir, handles lock races as blocked JSON, respects cooldown/failure backoff state, runs bounded autopilot dry-run before live apply, and records guarded-autostart state. Verification passed: guarded-autostart/self-heal/registry tests (`16 passed in 62.98s`), Ruff, py_compile, and `git diff --check`; real CLI smoke blocked safely on missing local config, low memory, Docker probe timeout, and PATH `httpx.exe` size mismatch. No Docker mutation, service install, scheduled-task registration, target import/start, resume-run, monitoring apply, provider call, engagement mutation, or credential persistence was executed.
- Completed B655 locally: added read-only `forge automation self-heal-plan` for smart Docker/startup readiness on limited CPU/RAM hosts. It reports resource guardrails, Docker compose readiness without probing by default, resume-lock/feed paths, packaged Go tool availability/size checks for the supplied binaries (`nuclei`, `gopls`, `gitleaks`, `amass`, `ffuf`, `gobuster`, `mapcidr`, `tlsx`, `uncover`, `naabu`, `dnsx`, `httpx`, `katana`, `subfinder`), fail-closed blockers, startup policy, and bounded autopilot dry-run/apply command arrays. Real smoke showed status blocked because free memory was below 2GB and PATH resolved `httpx.exe` to a Python shim size mismatch, which is intentional fail-closed behavior before auto-start. Verification passed: self-heal/registry tests (`6 passed in 60.19s`), Ruff, py_compile, and real `automation self-heal-plan --json` smoke. No Docker mutation, service install, target import/start, resume-run, monitoring apply, provider call, scheduled-task change, engagement mutation, or credential persistence was executed.
- Completed B654 locally: added `forge automation feed-build` and wired `forge-autopilot.bat`/`.sh` to run feed-build before target import unless `--skip-feed-build`. Feed-build merges local engagement DB seeds, report/dashboard JSON artifacts, CTI observation files, connector output payloads, and optional local-config Supabase read-only REST tables into `imports/target-feed.json` (`target-feed.v1`), dry-run by default and atomic write with `--apply`. Supabase config is ignored local `imports/supabase-projects.local.json`; keys come from env vars or `forge-secret://ENGAGEMENT/CONNECTOR/SECRET_NAME` references and are never printed. JSON exposes total/selected/omitted/new/duplicate counts plus per-source and per-source-group counts; report families keep provenance such as `report_family:<id>`. Verification passed: feed tests (`8 passed in 41.28s`), target-import/registry/launcher slice (`61 passed, 132 existing sqlite timestamp warnings in 127.20s`), Ruff, py_compile, real dry-run `automation feed-build --json --limit 1` (101 DB seed candidates, 44,227 report candidates, 6,867 new unique candidates before limit, no `master.db` error), real `report quality-audit --json --top-limit 1` compatibility smoke (3,573 report files, 581 families, latest fallback counts empty, dashboard refresh failures 0, 49 resume-review rows, 3 long-run rows), and `git diff --check`. No live Supabase call, target import/start, resume-run, monitoring apply, provider call, scheduled-task change, engagement mutation, or credential persistence was executed.
- 2026-08-27: User redirected from implementation to a free-first HTML/PostPlan planning pass for the unfinished autonomous hardening backlog. Uploaded `plan.html` to PostPlan at `https://nepckuwxuhgp.postplan.dev`. Important: an OpenRouter key was pasted in chat and must be treated as exposed; do not write it to repo state, docs, or config. Current working tree still contains uncommitted feed-build/autopilot implementation work from this session: `forge/automation_target_feed.py`, `tests/cli/test_automation_feed_build.py`, updates to `forge/automation_cli.py`, launchers, README/DAILY_USE, `.gitignore`, and launcher/registry tests. Focused verification passed before redirect: `10 passed in 14.69s` for feed-build, CLI registry command registration, and autopilot launcher tests.
- Completed B653 locally: added all-in-one autopilot launchers `forge-autopilot.bat` and `forge-autopilot.sh`, plus main-menu entries in `start_toolkit.bat`/`.sh`. Autopilot defaults to `imports\target-feed.json`, skips import cleanly when the feed file is missing, can import+start new targets with `--roe-id`/`FORGE_ROE_ID`, resumes ready backlog with `--max-parallel`, runs due monitoring, and refreshes the dashboard; `--dry-run` rehearses import/resume/monitoring without live child execution. Updated README/DAILY_USE and launcher tests. Verification passed: launcher tests (`24 passed in 69.07s`), Ruff on launcher tests, BAT `--help` smoke, compact BAT dry-run smoke (`resume-limit=0`, feed missing skipped, 49 total resume candidates observed, exit_code=0), and `git diff --check`. No target import, live resume-run, kill-chain, monitoring apply, provider call, ticket/webhook write, or secret persistence was executed.
- Completed B652 locally: added bounded parallel resume-run support without launching live resume candidates. `execute_target_resume_plan()` now accepts `max_parallel`, reports `concurrency`/`max_parallel` in dry-run and live payloads, preserves default sequential behavior at `max_parallel=1`, holds one batch lock for the whole batch, serializes JSONL ledger writes across parallel child threads, and stops scheduling new parallel children after the first failure when `stop_on_failure` is true while letting in-flight children finish. CLI docs now expose `forge targets resume-run --max-parallel N`. Verification passed: focused mocked resume-run suite (`16 passed, 42 deselected in 23.97s`), full target-import CLI suite (`58 passed, 132 existing sqlite timestamp warnings in 89.21s`), real backlog dry-run smoke (`dry_run_no_commands_executed`, `concurrency=parallel`, `max_parallel=6`, `result_counts={'dry_run': 49}`, `total_count=49`, `selected_count=49`, `lock_missing`), CLI registry/automation policy slice (`25 passed in 57.93s`), command-review smoke (`124 commands`, `23 groups`), Ruff, py_compile, and `git diff --check`. No live resume-run, kill-chain, provider call, scheduled-task change, ticket/webhook write, secret persistence, or public-target action was executed.
- Completed B651 locally: upgraded pytest taxonomy/config after the test audit. Added explicit pytest `pythonpath=["."]`, `--import-mode=importlib` to fix duplicate test module basename collection errors, registered `unit`, `functional`, and `e2e` markers, added a directory-based collection hook that auto-marks `tests/integration` as integration, `tests/e2e` as e2e, CLI/webui tests as functional, and unclassified tests as unit. Added a lightweight pytest-visible E2E harness contract test so `tests/e2e` is no longer an empty selector. Verification passed: default collect-only now collects 6,565/6,607 tests with 42 deselected and no collection errors; duplicate-name targeted collection collected 321 tests; integration selector collected 205 tests; functional CLI selector collected 181 tests; e2e selector collected 1 test and `tests/e2e/test_real_targets.py` passed; focused automation/registry/target-import suite passed (`80 passed, 132 existing sqlite timestamp warnings in 99.60s`); automation module coverage passed configured 75% gate at 78.95%; Ruff, py_compile, and git diff check passed on touched files. Run-coverage backlog was rehearsed only: `targets resume-plan --json --redact-paths --limit 60` and `targets resume-run --dry-run --redact-paths --json --limit 49 --break-stale-lock` both show 49 ready sequential candidates, no lock present, no commands executed, reason counts pending_recursive_work=31, watchdog_timeout=7, abandoned=6, stale_run_recovery=5, estimated serial runtime 1,225 minutes. `report long-run-plan --json --limit 5` shows 3 long failed runs, and `report policy-plan --json --limit 10` confirms old attack/destructive/post flags are generated latest-run metadata, not current global defaults. No live resume-run, broad network run, report regeneration, ticket/webhook write, secret persistence, or scheduled-task mutation was executed.
- Completed B650 locally: expanded automation wildcard scope defaults to include `*.net`, `*.org`, and `http://*.*.*/` in addition to the existing `*.com`, `*.*`, and `https://*.*.*/`. Focused policy tests passed (`5 passed in 29.66s`), `automation policy --json` smoke shows the updated scope, Ruff/py_compile/git diff check passed. Read-only report freshness checks found latest reports still make sense as snapshots: `report stale-plan --json --limit 10` is empty with `total_count=0`, quality-audit reports `latest_fallback_reason_counts={}`, latest backend counts `template=61`, dashboard generated at `2026-08-21 01:54:17`, 3,573 report files, 581 report families, and 0 current dashboard refresh failures. Remaining freshness work is run-level rather than report-artifact staleness: 49 failed/cancelled latest runs need resume review, 3 long runs need review, and policy `*_no` counts are historical latest-run metadata rather than the new global defaults.
- Completed B649 implementation locally: added public `forge automation policy`, `forge automation run`, and `forge automation command-review` commands. The policy captures operator-approved wildcard/broad defaults, approved local path regex allow/deny rules, auto scope generation/expansion/CTI promotion/unknown-source import, active/destructive/post-ex defaults, ProjectDiscovery local connectors with all nuclei templates, offline CTI/discovery imports, stale-lock/resume defaults, ticket/webhook sync defaults, and secret store/update defaults. `forge automation run --apply --json` records apply intent and selects all seven automation steps without launching live commands from the planner. Command review reports 23 CLI groups and 124 source-level commands, with consolidation pressure on osint/connectors and public group count. Verification passed: focused automation/registry tests (`25 passed in 89.59s`), JSON smokes for `automation policy`, `automation run --apply`, and `automation command-review`, Ruff, py_compile, and `git diff --check`. No live broad network run, post-exploitation action, ticket/webhook write, secret persistence, scheduled-task change, or resume-run execution was launched from this session while wiring the policy.
- Implemented B648 resume-lock edge hardening after subagent audit: live PIDs now override stale age, lock status supports redacted local paths and allowlisted metadata, dry-run with `--break-stale-lock` reports lock break decisions without mutation, stale-lock breaking rechecks the inspected lock before unlink, and resume-run cleanup only removes this process's owner token. README/DAILY_USE now document `resume-lock-status`, stale-lock flags, optional kill-chain engagement derivation, and the 6-service cloud-scan wording. Verification passed: expanded focused resume-lock tests (`15 passed in 6.19s`), full `tests/cli/test_targets_import.py` twice (`55 passed, 132 existing sqlite timestamp deprecation warnings in 24.94s` and `55 passed, 132 warnings in 40.31s`), Ruff, py_compile, `git diff --check`, redacted `targets resume-lock-status --json` smoke, `targets resume-run --dry-run --break-stale-lock --json --limit 0` smoke, and docs sanity grep. A stale docs test command failed because `tests/test_readme.py` and `tests/test_agent_docs.py` do not exist in this repo. No live resume-run, kill-chain, provider call, scheduled-task change, engagement mutation, report regeneration, or credential persistence was executed.
- Pushed B647 as `ba0bf43 fix: handle stale resume locks`: added read-only `forge targets resume-lock-status`, explicit `forge targets resume-run --break-stale-lock`, stale/dead-PID lock classification, active-lock refusal, Windows-safe PID liveness checks, focused tests, and `.agents/handoffs/2026-08-21-171500-session-recovery.md` so future sessions/subagents resume from repo state instead of the 241 MB Codex thread. Verification passed: focused resume-lock CLI/helper tests (`7 passed in 28.72s`), full `tests/cli/test_targets_import.py` (`46 passed, 132 existing sqlite timestamp deprecation warnings in 23.24s`), Ruff on touched Python files, py_compile on touched Python files, `git diff --check`, `targets resume-lock-status --json` smoke, and `targets resume-run --help` smoke. No live resume-run, kill-chain, provider call, scheduled-task change, engagement mutation, report regeneration, or credential persistence was executed.
- Pushed B646 as `f591d9d fix: contain target child processes`: target import and resume-run child kill-chain launches now use a shared contained subprocess runner that starts a process group/session and terminates the child tree on timeout. Focused subprocess-tree, target-import, and resume-run tests, Ruff, py_compile, diff check, and a tiny contained-runner smoke passed. No live kill-chain, resume-run, scheduled task registration, provider call, report regeneration, or engagement mutation was executed.
- Pushed B645 as `2648611 fix: sanitize report display text`: dashboard/report-history/timeline display sanitization now shares a render-time cleanup path for GGUF fallback diagnostics, malformed CLI help text, abandoned-run wording, and full `scope_manifest` assignments with spaces. Focused and broader reporting tests, Ruff, py_compile, and diff check passed. No report regeneration, live kill-chain, provider call, scheduled task registration, or engagement mutation was executed.
- Pushed B644 as `f7ba70c fix: budget scheduled target imports`: scheduled theprawnhunter import wrappers now pass `--max-runtime-minutes` end to end and dynamically lower the per-child runtime when needed so `StartLimit` fits the watchdog task budget. Focused launcher tests, Ruff, PowerShell parser checks, and git diff check passed. No scheduled task registration, live kill-chain, provider call, report regeneration, or engagement mutation was executed.
- Pushed B643 as `182657c fix: bound target import start runtime`: target import `--start` now has a public `--max-runtime-minutes` option and passes that budget to the child kill-chain plus a parent-side timeout of budget plus 120s grace. Focused target-import helper/CLI tests, Ruff, py_compile, git diff check, CLI help smoke, and runtime-normalization smoke passed. No live kill-chain, resume-run, scheduled task, provider call, report regeneration, or engagement mutation was executed.
- Pushed B642 as `89f175a fix: redact report review plans`: long-run-plan and policy-plan JSON now support `--redact-paths`, hide local `reports_dir`, and expose `redact_paths=true` while preserving review samples/counts. Focused long-run/policy plan helper/CLI tests, Ruff, py_compile, git diff check, and a synthetic redacted plan smoke passed.
- Pushed B641 as `19b75d7 fix: redact report quality audit`: quality-audit JSON now supports `--redact-paths`, hides local `reports_dir`, and exposes `redact_paths=true` while preserving review counts/action guidance. Focused quality-audit helper/CLI tests, Ruff, py_compile, git diff check, and a synthetic redacted quality-audit smoke passed.
- Pushed B640 as `12d9acc fix: redact stale report plans`: stale-plan JSON now supports `--redact-paths`, hides local `reports_dir`, and exposes `redact_paths=true` while preserving safe command arrays. Focused stale-plan helper/CLI tests, Ruff, py_compile, git diff check, and a synthetic redacted stale-plan smoke passed.
- Pushed B639 as `77c35db fix: redact stale report dry runs`: stale-run dry-run JSON now supports `--redact-paths`, hides local `reports_dir` and generated command output paths, and blocks live redacted stale-run. Focused stale-run CLI/helper tests, Ruff, py_compile, git diff check, and a synthetic redacted dry-run smoke passed.
- Pushed B638 as `02ae30e fix: count stale report dry runs`: stale report repair run JSON now reports `dry_run_count` and no longer counts dry-run preview items as skipped. Focused stale-run tests, Ruff, py_compile, git diff check, and a synthetic dry-run smoke passed.
- Pushed B637 as `d9db719 fix: surface stale report dry runs`: quality-audit stale-report actions now always expose `batch_dry_run_command` and a `stale-run --dry-run --json --limit N` follow-up before the live batch regeneration command. Focused quality-audit tests, Ruff, py_compile, git diff check, and a synthetic quality-audit smoke passed.
- Pushed B636 as `0c97c59 fix: collapse empty dashboard sections`: dashboard evidence sections now render populated tables first and group no-row sections into one collapsible empty-section summary. Focused page-composition/dashboard/style tests, Ruff, py_compile, git diff check, and a direct render smoke passed.
- Pushed B635 as `6623e73 fix: tighten dashboard report rendering`: static and React dashboards now handle long seed/scope lists, mobile table labels, bounded report previews, shortened GGUF/malformed CLI diagnostics, and latest-run policy source labels. Focused reporting/dashboard tests, Ruff, py_compile, git diff check, Vite build, and a renderer smoke passed.
- Pushed B634 as `9697ce3 fix: mark asset ownership writes`: asset graph ownership set/resolve JSON now exposes explicit local write policies and total/selected/omitted claim counts. Focused ownership CLI test, Ruff, py_compile, git diff check, and a temp CLI smoke passed.
- Pushed B633 as `1a6e55c fix: mark asset attribution imports`: asset graph attribution import JSON now exposes explicit local write policy and processed/imported/error total/selected/omitted counts. Focused direct/CLI attribution tests, Ruff, py_compile, git diff check, and a temp direct smoke passed.
- Pushed B632 as `d48392f fix: mark asset graph sync payloads`: asset graph sync CLI/helper JSON now exposes explicit graph-table write policy and node total/selected/omitted counts. Focused graph sync helper/CLI tests, Ruff, py_compile, git diff check, and a temp helper smoke passed.
- Pushed B631 as `6254c74 fix: count asset graph list payloads`: asset graph list/API JSON now exposes read-only execution metadata and bounded node total/selected/omitted counts. Focused asset graph helper/API tests, Ruff, py_compile, git diff check, and a temp helper smoke passed.
- Pushed B630 as `5e62506 fix: mark audit manifest exports`: audit manifest-export JSON now exposes schema/execution/count aliases that distinguish local bundle export from explicit remote-store export. Focused audit manifest CLI tests, Ruff, py_compile, git diff check, and a temp local export smoke passed.
- Pushed B629 as `ca08050 fix: mark audit verification payloads`: audit manifest-verify and manifest-bundle-verify JSON now expose read-only schema/execution/count aliases. Focused audit manifest CLI tests, Ruff, py_compile, git diff check, and a temp signed-bundle verifier smoke passed.
- Pushed B628 as `fc11e16 fix: count active validation list payloads`: active-validation list CLI/API payloads now expose read-only execution metadata and bounded job total/selected/omitted counts. Focused active-validation CLI/API/web-route tests, Ruff, py_compile, git diff check, and a temp-DB CLI smoke passed.
- Pushed B627 as `84a70b8 fix: mark active validation read payloads`: active-validation methods, preview, and coverage JSON now expose schema_version, execution_policy, and total/selected/omitted aliases. Focused active-validation CLI/runner/API tests, Ruff, py_compile, git diff check, and a direct method-catalog smoke passed.
- Pushed B626 as `cea01f5 fix: mark secret prevention plan`: secret-prevention-plan JSON now exposes schema_version, plan-only execution_policy, and total/selected/omitted command counts while preserving value-free secret-prevention payloads. Focused secret lifecycle and connector CLI tests, Ruff, py_compile, git diff check, and a synthetic temp-DB smoke passed with no raw secret leakage.
- Pushed B625 as `16c3810 fix: mark standards stix payloads`: standards import-stix/export-stix JSON now exposes schema_version and total/selected/omitted counts, with dry-run imports explicitly labeled no-write. Focused standards CLI tests, Ruff, py_compile passed; synthetic local standards smoke reports no missing common fields.
- Pushed B624 as `8570bc7 fix: mark monitoring status payload`: monitoring status JSON now exposes schema_version, read-only execution_policy, and total/selected/omitted DB counts. Focused monitoring status tests, Ruff, py_compile passed; real local monitoring status smoke reports 101 selected DBs and no missing top-level fields.
- Pushed B623 as `df17183 fix: mark doctor payload contract`: top-level doctor JSON now exposes schema_version, read-only execution_policy, and total/selected/omitted check counts while preserving the existing schema, summary, checks, and action plan. Focused doctor tests, Ruff, py_compile passed; real local non-live doctor smoke reports 52 selected checks and no missing top-level fields.
- Pushed B622 as `c807041 fix: mark connector catalog json`: connectors list JSON now exposes top-level schema_version, execution_policy, and total/selected/omitted counts while preserving existing connectors and summary. Focused catalog CLI test, Ruff, py_compile passed; real local connectors list smoke reports 22 selected and no missing top-level fields.
- Pushed B621 as `fa0ab16 fix: mark plugin validation summary`: plugin-validate summary now exposes `schema_version`, total/selected/omitted counts, and existing execution policy. Focused plugin validation tests, Ruff, py_compile passed; real local plugin-validate smoke reports no missing summary schema/execution/count fields.
- Pushed B620 as `9d51973 fix: mark cti import results`: CTI observation import results now expose `schema_version`, execution policy, and total/selected/omitted aliases for dry-run and write paths. Focused CTI import tests, Ruff, py_compile passed; synthetic local dry-run smoke reports no missing common fields.
- Pushed B619 as `1d48032 fix: count resume candidate backlog`: resume-candidates now exposes read-only execution policy plus total/selected/omitted counts, and the collector counts omitted candidates after a limit without storing extra rows. Focused target resume tests, Ruff, py_compile passed; real local redacted target/resume smoke reports no missing fields across resume-candidates, resume-plan, and resume-run dry-run.
- Pushed B618 as `cc1f09e fix: mark report review payloads`: quality-audit now exposes read-only execution policy and review-row total/selected/omitted counts, and stale-plan exposes selected_count. Focused report tests, Ruff, py_compile passed; real local report plan-contract smoke reports no missing fields across quality-audit, stale-plan, long-run-plan, and policy-plan.
- Pushed B617 as `e4aca55 fix: mark connector policy summary`: connector policy-summary now exposes `execution_policy`, `selected_count`, and `omitted_count`, and a connector plan-contract smoke reports no missing schema/execution/count fields across run-plan, install-plan, policy-summary, and secret-key-plan. Focused policy-summary tests, Ruff, py_compile, and `git diff --check` passed.
- Pushed B616 as `f3c5736 fix: mark doctor action policies`: remaining doctor monitoring/connector/catalog/gated action rows now expose explicit execution-policy metadata, and a real local non-live `doctor --json` smoke reports zero action rows missing execution/count fields. Focused doctor tests, Ruff, py_compile, and `git diff --check` passed.
- Pushed B615 as `8f7464f fix: mark gated doctor actions`: paid-backend, active-validation, and provider-probe doctor actions now expose explicit execution policy and count metadata; focused doctor tests, Ruff, py_compile, `git diff --check`, and a real local non-live `doctor --json` smoke passed.
- Pushed B614 as `74e50a6 fix: mark config doctor decisions`: Offline Strict and Safe Mode doctor actions now expose explicit non-executing operator-decision metadata with one omitted decision and empty `command_args`; focused doctor tests, Ruff, py_compile, `git diff --check`, and a real local doctor JSON smoke passed.
- Pushed B613 as `e0c9044 fix: add secret key doctor action counts`: Connector Secret Store doctor setup/reload actions now copy plan-only execution/count metadata from `connector_secret_key_plan()` without printing secret material; focused tests, Ruff, py_compile, `git diff --check`, and real local doctor/secret-key-plan smokes passed.
- Pushed B612 as `d69d380 fix: add connector doctor action counts`: connector-related doctor actions now expose count metadata, and `run_free_connectors` derives total/selected/omitted counts from `connector_run_plan()` so it matches `forge connectors run-plan --json`; focused doctor tests, Ruff, py_compile, `git diff --check`, and real local doctor/run-plan smokes passed.
- Pushed B611 as `c87bafa fix: add monitoring doctor action counts`: Monitoring Schedules doctor action items now expose `total_count`, `selected_count`, `omitted_count`, and `estimated_batch_count`; focused monitoring doctor tests, Ruff, py_compile, `git diff --check`, and a real local doctor JSON smoke passed.
- Pushed B610 as `5502af5 fix: add safe mode doctor action`: Safe Mode doctor WARN now has a structured non-executing `review_safe_mode` action item with empty `command_args`; focused doctor tests, Ruff, py_compile, `git diff --check`, and a real local doctor JSON smoke passed.
- Pushed B609 as `31659ef fix: report policy flag totals`: quality-audit `review_policy_flags` now exposes `flag_total_count` alongside row-based total/selected/sample/omitted counts so it matches `policy-plan`; focused tests, Ruff, py_compile, `git diff --check`, and a real local smoke passed.
- Pushed B608 as `99bfef5 fix: normalize report plan counts`: long-run-plan and policy-plan now match the normalized review-plan count contract; policy-plan uses row-based total/selected/omitted counts and preserves aggregate flag occurrences as `flag_total_count`. Focused tests, Ruff, py_compile, `git diff --check`, and real local plan smokes passed.
- Pushed B607 as `8644943 fix: report audit action counts`: quality-audit operator action rows now expose consistent total/selected/sample/omitted count metadata across resume, long-run, stale/fallback, and policy review actions; focused tests, Ruff, py_compile, `git diff --check`, and a real local JSON smoke passed.
- Pushed B606 as `7238a72 fix: add connector setup plan metadata`: connector install-plan and secret-key-plan now expose plan-compatible execution/count metadata without executing installers, printing secrets, or changing setup behavior; focused connector tests, Ruff, py_compile, `git diff --check`, and real local JSON smokes passed.
- Pushed B605 as `6ddcf91 fix: add connector plan metadata`: connector run-plan now adds total/selected/omitted count aliases, and connector policy-summary now has `schema_version="forge.connector_policy_summary.v1"`.
- Pushed B604 as `4849ea3 fix: add doctor command args`: doctor payload action items now add `command_args` arrays for executable `forge ...` commands while preserving existing command strings and leaving env/prose actions as empty arrays.
- Pushed B603 as `d3a83a0 fix: add run due count aliases`: monitoring run-due dry-run/live payloads now add plan-compatible aliases (`schema_version`, `total_count`, `total_due_count`, `selected_count`, `omitted_count`) while preserving existing fields.
- Pushed B602 as `5722b28 fix: add due plan count aliases`: monitoring due-plan now adds compatibility aliases (`schema_version`, `total_count`, `total_due_count`, `selected_count`, `omitted_count`, `oldest_due_age_seconds`) while preserving existing result fields.
- Pushed B601 as `3193952 fix: report resume run batch totals`: resume-run result payloads now carry resume-plan total/selected/omitted and reason/blocker count metadata so limited dry-run batches cannot be mistaken for the full backlog.
- Pushed B600 as `931f80d fix: redact audit resume rehearsal`: quality-audit resume review follow-up now points to `forge targets resume-run --dry-run --redact-paths --json` and long-run guidance names the redacted dry-run rehearsal.
- Pushed B599 as `962cd94 fix: redact resume review rehearsals`: `targets resume-candidates --redact-paths` now hides local candidate paths, `targets resume-run --dry-run --redact-paths` rehearses without ledger/lock/child launch and hides local paths, live redacted resume-run blocks, and `report policy-plan` labels `*_no` counts as generated latest-run metadata with per-sample reasons.
- `FORGE Import theprawnhunter Targets` scheduled task was disabled; no active FORGE/telegramhunter/theprawnhunter process was found.
- Three subagents completed read-only research. No commands from external links were run, cloned, or installed.
- Implemented catalog-only CTI connector entries and local CTI/OSINT observation normalization/redaction tests.
- Updated `SPEC.md` with checkpoint B515. Remote `main` removed the old `docs/` handoff files during rebase, so ongoing shared state now lives here under `.agents/`.
- Pushed B515 as `7b14720 feat: add safe cti osint observations`.
- Implemented offline `forge connectors import-cti` for normalized CTI/OSINT observation JSON, durable `cti_observations` storage, audit logging, duplicate handling, and explicit scope-gated seed promotion.
- Updated README public connector command list for `forge connectors import-cti`.
- Pushed B516 as `6b3bd9f feat: import offline cti observations`.
- Exposed CTI observations as non-reportable inventory in Phase 6 context/export/template reports and dashboard detail JSON.
- Hardened CTI source/provenance redaction and changed fallback raw-artifact hashes to sanitized canonical observation fields only.
- Pushed B517 as `3755cd8 feat: surface cti observation inventory`.
- Added offline provider-shape adapters for common ThreatFox, URLHaus, and STIX indicator JSON exports so `forge connectors import-cti` can ingest real downloaded/exported data without live provider calls.
- Pushed B518 as `30cf1fc feat: accept provider cti export shapes`.
- Documented offline CTI import support in README next to the public command list.
- Pushed README checkpoint as `bfbda54 docs: document offline cti imports`.
- Added `forge connectors import-cti --dry-run` to parse/sanitize offline CTI files and scope-check promotion candidates without writing observations, seeds, or audit rows.
- Pushed B519 as `b6314f0 feat: add cti import dry run`.
- Hardened CTI dry-run preview to distinguish existing/in-file duplicate observations from new observations without creating tables or writing rows.
- Pushed B520 as `eaf0343 feat: preview cti import duplicates`.
- Added `forge connectors import-cti --limit N` and result item-count metadata so large offline CTI exports can be processed in bounded batches.
- Pushed B521 as `c90fbeb feat: bound cti import items`.
- Added `forge connectors import-cti --min-confidence F` to skip low-confidence offline CTI observations before persistence or dry-run would-persist counts.
- Pushed B522 as `af505aa feat: filter cti import confidence`.
- Added `forge connectors import-cti --max-tlp LEVEL` to skip observations above an allowed TLP level before persistence or dry-run preview counts.
- Pushed B523 as `cdb0e42 feat: filter cti import tlp`.
- Added `forge connectors import-cti --since ISO --until ISO` to bound offline CTI imports and dry-runs by observation time.
- Pushed B524 as `6abfd45 feat: filter cti import observed window`.
- Added local CSV fallback parsing for offline `forge connectors import-cti --report-file` inputs so ThreatFox and URLHaus CSV exports flow through the same sanitized import path.
- Verified B525 with focused CTI tests, broader connector sanity tests, Ruff, py_compile, and `git diff --check`.
- Pushed B525 as `fb5aa26 feat: import offline cti csv`.
- Added CTI import JSON summary counts for normalized indicator types, TLP levels, target-feed-compatible types, and skipped reasons.
- Verified B526 with focused CTI tests, broader connector sanity tests, Ruff, py_compile, and `git diff --check`.
- Pushed B526 as `6c26b62 feat: summarize cti import composition`.
- Added opt-in `forge connectors import-cti --fail-on-empty` so automation can fail when no observations survive normalization and filters.
- Verified B527 with focused CTI tests, broader connector sanity tests, Ruff, py_compile, and `git diff --check`.
- Pushed B527 as `3e85cdf feat: fail empty cti imports`.
- Added local `.gz` report-file support for offline `forge connectors import-cti` so compressed JSON/CSV exports use the same sanitized import path.
- Verified B528 with focused CTI tests, broader connector sanity tests, Ruff, py_compile, and `git diff --check`.
- Pushed B528 as `06c5fb0 feat: read compressed cti imports`.
- Added constrained local `.zip` report-file support for offline `forge connectors import-cti` with deterministic supported-member selection and no extraction to disk.
- Verified B529 with focused CTI tests, broader connector sanity tests, Ruff, py_compile, and `git diff --check`.
- Pushed B529 as `3890a87 feat: read zipped cti imports`.
- Added a shared decoded-size cap for offline CTI report-file reads across plain, `.gz`, `.zip`, and nested gzip-in-ZIP inputs.
- Verified B530 with focused CTI tests, broader connector sanity tests, Ruff, py_compile, and `git diff --check`.
- Pushed B530 as `11c350a feat: bound cti report reads`.
- Added sanitized CTI report-read metadata in import JSON: `report_container_format` and ZIP `report_member`.
- Verified B531 with focused CTI tests, broader connector sanity tests, Ruff, py_compile, and `git diff --check`.
- Pushed B531 as `51ce283 feat: report cti import source metadata`.
- Added stable `result_schema_version="forge.cti_observation_import.v1"` to CTI import JSON results.
- Verified B532 with focused CTI tests, broader connector sanity tests, Ruff, py_compile, and `git diff --check`.
- Pushed B532 as `4b3a3d2 feat: version cti import results`.
- Added passive offline `misp_event_import` support for local MISP event JSON through the sanitized CTI observation pipeline.
- Verified B533 with focused CTI tests, broader connector sanity tests, Ruff, py_compile, and `git diff --check`.
- Pushed B533 as `44b3986 feat: import offline misp events`.
- Normalized MISP Unix timestamps to UTC ISO strings so offline MISP imports work with CTI `--since/--until` filters.
- Verified B534 with focused CTI tests, broader connector sanity tests, Ruff, py_compile, and `git diff --check`.
- Pushed B534 as `30186f2 feat: normalize misp import timestamps`.
- Added offline MISP attribute-search export support for top-level `Attribute`, `response.Attribute`, and response-list `Attribute` wrappers.
- Verified B535 with focused CTI tests, broader connector sanity tests, Ruff, py_compile, and `git diff --check`.
- Pushed B535 as `1297a06 feat: import misp attribute exports`.
- Final local checks after push: git clean/synced with `origin/main`, scheduled task `FORGE Import theprawnhunter Targets` is Disabled, no non-check FORGE/telegramhunter/theprawnhunter process was observed, 830 local engagement DBs, 1,432 local `reports/` files, and 0 persisted `cti_observations` rows across local engagement DBs.
- Added passive offline `supabase_table_import` support for Supabase JSON/CSV table exports through the existing sanitized CTI observation importer.
- Verified B536 with focused CTI tests, broader connector sanity tests, Ruff, py_compile, and `git diff --check`.
- Pushed B536 as `764191f feat: import supabase table exports`.
- Added CTI `rejected_sensitive_type_counts` result metadata for default-rejected sensitive rows.
- Verified B537 with focused CTI tests, broader connector sanity tests, Ruff, py_compile, and `git diff --check`.
- Pushed B537 as `3461eb5 feat: summarize sensitive cti rejections`.
- Added CTI `unsafe_text_item_count` result metadata for command/script/install-like source rows and stripped unsafe command-bearing fields before normalization.
- Verified B538 with focused CTI tests, broader connector sanity tests, Ruff, py_compile, and `git diff --check`.
- Pushed B538 as `7191ad1 feat: summarize unsafe cti text`.
- Added aggregate CTI/OSINT `provider_catalog_policy_summary()` for default/opt-in/offline/live-style/gate category review.
- Verified B539 with focused CTI tests, broader connector sanity tests, Ruff, py_compile, and `git diff --check`.
- Pushed B539 as `9a3ec3e feat: summarize osint catalog policy`.
- Expanded the CTI/OSINT provider catalog with default-visible backlog entries from the CTI, awesome-osint, and `ukr.pw` source-list/snippet research.
- Verified B540 with focused CTI tests, broader connector sanity tests, Ruff, py_compile, and `git diff --check`.
- Pushed B540 as `33bf82e feat: expand osint source catalog`.
- Hardened generated dashboard/report wrapping for long chips, tables, metadata, artifact links, route cards, and timelines; seed/scope chips now use a bounded preview with expandable remainder.
- Verified B541 with reporting helper tests, Ruff, py_compile, and `git diff --check`.
- Pushed B541 as `5ffa8aa fix: wrap generated dashboard reports`.
- Changed Phase 6 report defaults from local-only `llama_cpp` to `auto` so configured Codex/CLI/API providers are tried before local GGUF and template fallback.
- Focused B542 checks passed for Phase 6 default fallback, CLI README docs, platform config defaults, py_compile, and current `recon subdomains --help` shape.
- Broader B542 checks passed for targeted Phase 6 auto-provider fallbacks, CLI README docs, platform config defaults, report finalization provider plumbing, Ruff, py_compile, and `git diff --check`; the full combined pytest command exceeded 5 minutes and its orphaned pytest process was stopped.
- Read-only latest-run resume audit found 49 failed/cancelled candidates: pending_recursive_work=31, watchdog_timeout=7, abandoned=6, stale_run_recovery=5. No resume/live action was started.
- Pushed B542 as `c8bbb9d fix: default reports to auto provider`.
- Added read-only `forge targets resume-candidates` JSON output for repeatable latest-run candidate review without engagement mutation or live resume.
- Verified B543 with target resume/import tests, CLI registry/docs tests, Ruff, py_compile, `git diff --check`, and a local `.forge_data` read-only scan matching 49 candidates.
- Pushed B543 as `65c9d68 feat: report target resume candidates`.
- Added sanitized static dashboard visibility for target resume candidates: overview `Resume Review` column/filter/stat, detail candidate table, and run-summary metadata redaction for scope-manifest paths and token-like keys.
- Verified B544 with focused dashboard contract/resume-review tests, reporting/CLI slice tests, Ruff, py_compile, and `git diff --check`; full local `.forge_data` dashboard smoke exceeded 4 minutes and the spawned dashboard process was stopped.
- Pushed B544 as `dd25905 feat: surface resume candidates in dashboard`.
- Added read-only `forge connectors policy-summary [--json]` so operators can inspect CTI/OSINT provider policy counts and provider IDs without running providers or importing Python.
- Verified B545 with focused policy-summary CLI tests, connector registry/docs tests, Ruff, py_compile, `git diff --check`, and a JSON smoke reporting 48 providers, 23 offline imports, 25 live/API-style, and 21 operator-opt-in-gated entries.
- Pushed B545 as `b99bf2d feat: expose cti policy summary`.
- Added read-only `CTI/OSINT Policy` output to `forge doctor` plus the `review_cti_osint_policy` action-plan item backed by aggregate provider policy counts.
- Subagent spawn for the report/UI side audit failed because the current harness reported the agent thread limit was reached; continue old report/product audit locally when B546 is committed.
- Verified B546 with full doctor tests plus focused CTI policy-summary tests (`42 passed`), Ruff, py_compile, `git diff --check`, and a `forge doctor --json` smoke confirming the CTI check/action are present.
- Pushed B546 as `6780226 feat: surface cti policy in doctor`.
- Hardened dashboard graph/list wrapping for long labels and backfilled 844 ignored local `reports/dashboard` HTML files; browser smoke on the flagged 10441 page now shows no desktop/tablet overflow and only intentional mobile table-scroll overflow.
- Changed `targets import --start` to use normal scoped kill-chain defaults instead of passing `--no-attack-mode` and `--no-auto-run-detected`; ROE/scope-manifest and start-limit protections remain.
- Verified B547 with focused target-import/reporting/Windows launcher tests (`46 passed`), Ruff, py_compile, `git diff --check`, suppressor-flag scan, and Playwright static overflow smoke.
- Pushed B547 as `739df24 fix: harden dashboard and target starts`.
- Downloaded the expected local fallback model to `C:\Users\bryan\.cache\forge\models\qwen2.5-1.5b-instruct-q4_k_m.gguf` (1,117,320,736 bytes); `llama_cpp` imports and doctor still detects Codex/Claude/Gemini CLI providers.
- Inspected `ROE-BRYAN-PERMANENT-2026` in local target-import scope manifests: engagement 10441 authorizes only `https://vector.strategy360.live/` / `vector.strategy360.live`, and 824 local target-import manifests reference that ROE id.
- Confirmed the `FORGE Import theprawnhunter Targets` scheduled task is Disabled and no Python process was listed by `Get-Process python`.
- Current read-only `forge targets resume-candidates --limit 200` scan returned 0 candidates.
- Current local report inventory: 3,139 files under `reports/`, 1,432 root report files, 581 grouped report families, 844 dashboard HTML pages, and 843 engagement dashboard detail folders.
- Added read-only `forge report quality-audit [--json]` to classify report/dashboard quality breakpoints from generated dashboard JSON and report artifacts without mutating engagements.
- Verified B548 with focused quality-audit/CLI-doc tests (`3 passed`), Ruff, py_compile, `git diff --check`, and a real local smoke reporting 829 dashboard engagements, 3,139 report files, 581 report families, 49 failed runs, 3 long runs, 4 dashboard refresh failures, 0 resume reviews, and 149 GGUF-missing fallback generations.
- Pushed B548 as `7c723d4 feat: audit report quality breakpoints`.
- Fixed the next concrete report-quality failure class: `remediation_review_queue()` now tolerates legacy engagement DBs missing `risk_acceptance_expires_at` by selecting stable payload aliases with NULL/default values.
- Verified the legacy schema fix with focused remediation/reporting regression tests, the full remediation connector test file (`33 passed`), Ruff, py_compile, `git diff --check`, and a read-only `forge report quality-audit --json` smoke. The smoke still reports four historical dashboard refresh-failure artifacts until those dashboards are regenerated; future queue reads should not hit the missing-column crash.
- Pushed B549 as `e9244ce fix: support legacy remediation queues`.
- Added `forge kill-chain --max-runtime-minutes` / `FORGE_KILL_CHAIN_MAX_RUNTIME_MINUTES` as a default 25-minute soft budget. The loop now stops only at iteration boundaries, audits `runtime_budget_exhausted`, records budget metadata, and proceeds through existing graceful finalization so collected evidence/report closeout is not lost to the external watchdog.
- Terminal run errors now distinguish `runtime budget exhausted with pending recursive work` from max-iteration exhaustion when the soft budget caused the stop.
- Documented the option in README and added SPEC checkpoint B550.
- Verified B550 with focused runtime-option/CLI help/env validation/run-tracking/CLI registry tests (`36 passed`), Ruff, py_compile, `git diff --check`, direct-call search for `normalize_kill_chain_runtime_options`, and a read-only `forge report quality-audit --json` smoke. The smoke still reports historical long-run/dashboard-failure artifacts until future runs/reports are regenerated.
- Pushed B550 as `dc7221a fix: add kill-chain runtime budget`.
- Fixed the next dashboard-refresh robustness class from the quality audit: artifact payloads, report-history mtime reads, and static artifact cards now tolerate missing/disappearing/OS-invalid report, graph, or audit paths instead of aborting dashboard generation.
- Added SPEC checkpoint B551 and missing-artifact regressions for both JSON payloads and HTML card wrappers.
- Verified B551 with focused artifact/report rendering tests (`7 passed`), broader reporting payload/history/detail/rendering slice (`43 passed`), Ruff, py_compile, `git diff --check`, and a read-only `forge report quality-audit --json` smoke returning the baseline 3,139 report files. A full local `forge dashboard` smoke exceeded four minutes on the large ignored local corpus; its spawned Python process was stopped and generated `reports/dashboard-smoke/` output was removed.
- Pushed B551 as `f7b40b1 fix: tolerate dashboard artifact path errors`.
- Fixed a public CLI contract drift found during continuation: `forge targets resume-candidates --json` is now accepted as a compatibility flag while the command still emits JSON by default.
- Documented the flag in README and added SPEC checkpoint B552.
- Verified B552 with focused target resume-candidate/CLI registry tests (`22 passed`), Ruff, py_compile, `git diff --check`, and a real local `forge targets resume-candidates --limit 200 --json` smoke returning zero current candidates.
- Pushed B552 as `92c588d fix: accept resume candidates json flag`.
- Fixed the next public CLI contract drift found during continuation: `forge report quality-audit --top-limit N` is now accepted as a compatibility alias for `--top N`.
- Documented the alias in README, added SPEC checkpoint B553, and added a focused CLI regression proving the alias bounds sample rows.
- Verified B553 with focused quality-audit/CLI registry tests (`23 passed`), Ruff, py_compile, `git diff --check`, and a real local `forge report quality-audit --json --top-limit 50` smoke. The smoke returned the current historical quality backlog: 829 dashboard engagements, 3,139 report files, 49 failed runs, 3 long runs, 4 dashboard refresh failures, and 149 GGUF-missing fallback generations.
- Pushed B553 as `0e77bbf fix: accept quality audit top limit alias`.
- Ran a full local dashboard backfill with current code: `forge dashboard -o reports\dashboard.html` completed in about 8m49s and generated the ignored local static dashboard corpus.
- Fixed quality-audit signal noise: timestamped dashboard refresh failures older than the current dashboard `generated_at` are now reported under `historical_dashboard_refresh_failure_count`, while the headline `dashboard_refresh_failure_count` tracks unresolved/unknown-current failures.
- Verified B554 with focused quality-audit/CLI registry tests (`24 passed`), Ruff, py_compile, `git diff --check`, and a real local `forge report quality-audit --json --top-limit 20` smoke. The refreshed local audit now reports 930 dashboard engagements, 3,341 report files, 49 failed runs, 3 long runs, 0 current dashboard refresh failures, 4 historical dashboard refresh failures, 49 resume-review rows, and 149 GGUF-missing fallback generations.
- Pushed B554 as `fb90e38 fix: separate historical dashboard refresh failures`.
- Fixed quality-audit fallback signal noise: report backend/fallback/write-error counters are now split into all-history lineage counts and latest-report-only counts.
- Verified B555 with focused quality-audit/CLI registry tests (`25 passed`), Ruff, py_compile, `git diff --check`, and a real local `forge report quality-audit --json --top-limit 5` smoke. The refreshed local audit still reports 149 historical GGUF-missing fallback generations, but `latest_fallback_reason_counts` is empty, so latest report summaries are not currently GGUF-missing.
- Pushed B555 as `cf54e94 fix: report latest quality fallback counts`.
- Fixed a dashboard/CLI mismatch: default `targets resume-candidates` now includes repo-local legacy `.forge_data` DBs like the dashboard does, while explicit `--data-dir` scans remain narrow.
- Verified B556 with focused target-import/resume, quality-audit, and CLI registry tests (`47 passed`), Ruff, py_compile, `git diff --check`, and a real local `forge targets resume-candidates --limit 200 --json` smoke. The smoke now scans 930 engagements and returns the same 49 resume candidates visible in the dashboard: pending_recursive_work=31, watchdog_timeout=7, abandoned=6, stale_run_recovery=5.
- Pushed B556 as `c869d61 fix: align resume candidates with dashboard legacy data`.
- Fixed a doctor/operator mismatch: the Workspace Access doctor check now includes repo-local legacy dashboard DBs like dashboard/resume review, and labels that inclusion in the details. Other operational checks remain scoped to the configured data dir.
- Verified B557 with focused doctor/engagement-id tests (`45 passed`), Ruff, py_compile, `git diff --check`, and a real local `forge doctor --json` smoke. The real Workspace Access row now reports 48 engagements across 50/930 DBs checked, 47 usable control index rows, and notes repo-local legacy dashboard DB inclusion.
- Pushed B557 as `512399e fix: align doctor workspace legacy visibility`.
- Added read-only `forge connectors install-plan [--json]` so doctor can point operators at a safe missing-local-binary plan rather than vague manual installation text. The command groups missing binaries by connector, emits suggested install commands, and declares `plan_only_no_commands_executed`.
- Verified B558 with focused connector/doctor/CLI registry tests (`102 passed`), Ruff, py_compile, `git diff --check`, and a real local `forge connectors install-plan --json` smoke. The smoke reports six missing local tools: detect-secrets, gitleaks, katana, nuclei, subfinder, and trufflehog.
- Pushed B558 as `1e39c5d feat: add connector install plan`.
- Subagent spawn was retried for a read-only next-backlog audit, but the harness still returned `agent thread limit reached`; continue locally until an existing agent slot is freed.
- Aligned Connector Catalog doctor remediation with the new read-only install-plan command so the row now tells operators to run `forge connectors install-plan --json` instead of showing stale raw missing-binary installer prose.
- Verified B559 with focused doctor/connector/CLI registry tests (`102 passed`), Ruff, py_compile, `git diff --check`, and a real local `forge doctor --json` smoke confirming the Connector Catalog remediation and `install_free_binaries` action both point to `forge connectors install-plan --json`.
- Pushed B559 as `78a7bc3 fix: point connector catalog to install plan`.
- Added resume readiness/blocker fields to `forge targets resume-candidates` and dry-run-first `forge targets backfill-scope-manifests [--apply]`.
- Real local dry-run found 49 historical candidates all recoverable from `scope_manifest_missing`; applied the backfill locally, writing recovered narrow scope manifests under `.forge_data/target_imports/` and updating latest-run metadata only.
- Follow-up real local `forge targets resume-candidates --limit 200 --json` smoke reports 49 candidates, 49 `resume_ready`, empty `resume_blocker_counts`, and suggested `forge kill-chain ... --resume --max-iter 3` command arrays. No kill-chain run was started.
- Verified B560 with focused target-import/CLI-registry/quality-audit tests (`50 passed` in the broad slice, `45 passed` in the final target/registry slice), Ruff, py_compile, `git diff --check`, and local resume-candidate/backfill smokes.
- Pushed B560 as `c72721f feat: backfill resume scope manifests`.
- Added dashboard resume readiness/blocker fields and overview `ready`/`blocked` labels; dashboard blocker labels are path-neutral.
- Tightened run-summary metadata sanitization to drop nested `scope_manifest*` keys, including `live_execution_policy.scope_manifest_source`.
- Regenerated the ignored local static dashboard with current code. Generated dashboard JSON now has 49 resume items, 49 ready, 0 blocked, empty blocker set, and no `.forge_data/target_imports`, `scope_10001_recovered.json`, or `C:/secret/scope.json` path hits.
- Verified B561 with focused dashboard/run-summary/target-import/quality-audit/CLI-registry tests (`33 passed` plus `25 passed`), Ruff, py_compile, `git diff --check`, and local dashboard/quality smokes.
- Pushed B561 as `e6ea8ca fix: show resume readiness in dashboard`.
- Added dry-run-first `forge workspaces backfill-memberships [--apply] [--json]` to seed missing legacy local/control workspace memberships and refresh control index rows with operator-role permissions only.
- Applied the workspace backfill locally across 930 DBs. It updated 101 local membership rows, 2 control memberships, and 827 control index rows; follow-up dry-run reports 929 skipped items and zero remaining membership/index/schema actions.
- Real local `forge doctor --json` now reports Workspace Access `OK`: 50 engagement(s) across 50/930 DB(s), 50 usable control index rows, operator workspace memberships ready.
- Verified B562 with focused workspace/doctor/CLI-registry tests (`65 passed`), Ruff, py_compile, and real local workspace/doctor smokes.
- Pushed B562 as `09f9b2f feat: backfill workspace memberships`.
- Added `forge connectors secret-key-plan [--json]`, which reports non-secret setup guidance for `FORGE_ENGAGEMENT_KEY` and never prints secret material.
- Set a local Windows user-level `FORGE_ENGAGEMENT_KEY` without printing the value; verification only printed length 44 and fingerprint `sha256:e5e360873854`.
- Real local `forge doctor --json` with the user env key loaded now reports Connector Secret Store `OK`; remaining doctor attention is Safe Mode, Monitoring Schedules, TPH Target Import Bridge, Connector Catalog, and Connector Action Plan.
- Verified B563 with focused connector/doctor/CLI-registry tests (`63 passed`), Ruff, py_compile, `git diff --check`, and local secret-key/doctor smokes.
- Pushed B563 as `45ba1ae feat: add connector secret key plan`.
- Added read-only `forge monitoring due-plan [--limit N] [--json]` so due monitoring policies can be reviewed before `run-due`/worker mutates engagement DBs.
- Doctor's Monitoring Schedules due-policy warning now points first to `forge monitoring due-plan --json` and includes a `review_due_monitoring` action item.
- Verified B564 with focused monitoring/doctor/CLI-registry tests, broader monitoring/doctor/CLI-registry tests (`97 passed`), Ruff, py_compile, `git diff --check`, a local `monitoring due-plan --limit 5 --json` smoke, and a doctor smoke confirming the Monitoring Schedules remediation points to `due-plan` first.
- Pushed B564 as `3bc0c34 feat: add monitoring due plan`.
- Added a shared connector binary resolver and wired it into connector readiness, runtime execution, install-plan, and doctor Connector Catalog/Action Plan checks so full/default free-local connector availability is evaluated consistently outside raw PATH.
- Bootstrap full mode now best-effort installs bounded free/local connector tools (`detect-secrets`, `gitleaks`, `katana`, `nuclei`, `subfinder`) with per-tool timeouts and skip/timeout env controls; TruffleHog remains manual install-plan guidance because the current upstream Go module is not `go install` friendly.
- Locally installed/detected `detect-secrets`, `gitleaks`, `katana`, `nuclei`, and `subfinder`; `forge connectors install-plan --json` and `forge doctor --json` now agree that only connector binary `trufflehog` is missing.
- Verified B565 with focused connector/bootstrap/doctor tests (`93 passed`), Ruff, py_compile, `git diff --check`, local install-plan/doctor smokes, scheduled-task status, and an orphan installer process check.
- Pushed B565 as `e225c9f feat: bootstrap connector tools`.
- Added read-only `forge targets resume-plan [--json]` to convert ready resume candidates into a sequential manual command plan with explicit `--max-runtime-minutes`, blocker summaries, and plan-only execution policy.
- Added explicit `forge targets resume-run [--json]` to execute ready resume candidates one child process at a time with a batch lock, JSONL ledger, latest-run re-checks, rerun skips, and stop-on-failure behavior. No local resume-run was executed.
- Aligned duplicate doctor secret-tool rows with connector binary discovery: `detect-secrets` now resolves from `.venv`/connector search paths and TruffleHog remediation points to manual release-binary/install-plan guidance.
- Verified B566 with focused resume/doctor/CLI tests, broader target-import/doctor/CLI-registry tests (`72 passed`), Ruff, py_compile, local `targets resume-plan --limit 5 --json` smoke, and doctor smoke.
- Pushed B566 as `bf3d13a feat: add target resume batch runner`.
- Added Windows persistent `FORGE_ENGAGEMENT_KEY` detection to `forge connectors secret-key-plan --json` as a non-secret `persistent_key_hint` and changed doctor to report a restart/process-env warning when the key exists at user/machine scope but this shell has not inherited it.
- Verified B567 with focused key-plan/doctor tests, full doctor tests (`45 passed`), Ruff, py_compile, `git diff --check`, and local secret-key-plan/doctor smokes. Local smoke reports user-level key length/fingerprint only and no secret material.
- Pushed B567 as `40e9ba3 fix: detect persistent connector secret key`.
- Aligned the Monitoring Schedules doctor warning with the read-only due-plan total so the row keeps sampled alert/delivery details but summarizes the full due backlog.
- Verified B568 with focused doctor tests (`43 passed`), Ruff, py_compile, `git diff --check`, and real local `.venv\Scripts\forge.exe doctor --json` / `monitoring due-plan --limit 5 --json` smokes. Local doctor now reports 101 due/overdue monitoring policies while labeling the first-50 readiness sample separately.
- Pushed B568 as `fca5ac6 fix: show total monitoring due backlog`.
- Changed kill-chain live policy metadata to read destructive/post-ex permission flags from explicit scope-manifest policy fields instead of hard-coded false defaults, and generated target-import manifests now include those target-specific policy flags.
- Verified B569 with focused target-import/runtime tests (`46 passed`), Ruff, py_compile, and `git diff --check`. No live kill-chain, resume-run, monitoring execution, scheduled-task enablement, provider call, report regeneration, engagement mutation, or credential persistence was started.
- Pushed B569 as `6c23626 fix: honor target import policy flags`.
- Added checksum-verified TruffleHog release-binary installation to `bootstrap.py setup`, updated install-plan/doctor guidance, and locally installed TruffleHog 3.97.0 at `AppData\Local\FORGE\tools\bin\trufflehog.exe`.
- Verified B570 with focused bootstrap/connector/doctor tests (`52 passed`), Ruff, py_compile, `git diff --check`, local `.venv\Scripts\forge.exe connectors install-plan --json` showing `missing_binary_count=0`, local doctor showing Connector Catalog/Action Plan `OK`, and `trufflehog --version` returning 3.97.0. No provider scan, secret scan, engagement mutation, live run, scheduled-task change, report regeneration, or credential persistence was started.
- Pushed B570 as `a8767b3 feat: install trufflehog release binary`.
- Classified the intentionally disabled local `FORGE Import theprawnhunter Targets` scheduled task as paused/OFF in `forge doctor` unless `FORGE_TPH_TARGET_IMPORT_ENABLED=1` explicitly says the bridge should be running.
- Verified B571 with focused doctor tests (`44 passed`), Ruff, py_compile, `git diff --check`, and a real local `.venv\Scripts\forge.exe doctor --json` smoke showing `TPH Target Import Bridge` status `OFF` with `task=disabled`. No scheduled-task enablement, import run, provider call, engagement mutation, monitoring execution, live run, report regeneration, or credential persistence was started.
- Pushed B571 as `1d03fbf fix: treat disabled tph bridge as paused`.
- Freed six completed subagent slots that were still counting against the harness limit; no active subagent work was interrupted.
- Aligned ProjectDiscovery doctor binary rows with connector-aware binary resolution, and filtered `connector_install_plan(statuses=..., env=...)` missing-binary guidance through the env-aware resolver so custom `FORGE_CONNECTOR_BIN_DIR(S)` paths do not produce stale missing prompts.
- Verified B572 with focused doctor/connector registry tests (`91 passed`), Ruff, py_compile, `git diff --check`, and real local `.venv\Scripts\forge.exe doctor --json` / `connectors install-plan --json` smokes showing ProjectDiscovery, Secrets, Connector Catalog, Connector Action Plan, and missing-binary count healthy. No installer execution, provider call, scheduled-task enablement, monitoring apply, engagement mutation, live run, report regeneration, or credential persistence was started.
- Pushed B572 as `8430ec4 fix: honor connector binary env in doctor`.
- Added bounded monitoring apply limits: `forge monitoring run-due` now defaults to `--limit 50`, `forge monitoring worker` defaults to `--run-limit 50`, both support explicit `--all`, and JSON reports full due count plus `limited_policy_count`.
- Doctor's Monitoring Schedules warning now keeps the read-only `due-plan` review action and adds a capped apply action for `forge monitoring run-due --limit 50 --json`.
- Verified B573 with focused monitoring/doctor tests (`84 passed`), Ruff, py_compile, `git diff --check`, non-mutating `run-due --help` / `worker --help` smokes, a doctor JSON smoke showing capped monitoring remediation, and a read-only `monitoring due-plan --limit 0 --json` smoke showing 101 due policies. No monitoring `run-due`, worker execution, alert delivery, provider call, scheduled-task enablement, engagement mutation, live run, report regeneration, or credential persistence was started.
- Pushed B573 as `5fc3550 fix: bound monitoring due execution`.
- Read-only report-quality audit subagent found the next concrete backlog item: root `reports/engagement_<id>_stats.json` sidecars are counted as report families/latest backends, inflating `latest_report_backend_counts.none`.
- Reclassified `engagement_<id>_stats.json` files as report metadata sidecars rather than report families across report artifact discovery, report-family grouping, quality-audit file inventory, and old dashboard `report_history` interpretation.
- Verified B574 with focused reporting tests (`14 passed`), Ruff, py_compile, `git diff --check`, and a real local `report quality-audit --json --top-limit 5` smoke. Local latest backend counts changed from stats-sidecar `none:58, template:3` to actual `template:61`; no dashboard regeneration, report regeneration, engagement mutation, live run, provider call, scheduled-task change, or credential persistence was started.
- The B574 quality-audit smoke surfaced the next real report-health backlog: `latest_fallback_reason_counts` now shows `gguf_model_missing:57`, previously hidden by stats sidecars.
- Pushed B574 as `2c7eedb fix: ignore report stats sidecars`.
- Added read-only `latest_fallback_reports` samples to `report quality-audit` with sanitized fallback reasons, `repair_status`, and non-executing `forge report generate --engagement <id> --provider auto --yes` command arrays. GGUF-missing latest reports are marked `stale_after_model_available` when the default local GGUF model is now present.
- Verified B575 with focused quality-audit tests (`6 passed`), Ruff, py_compile, `git diff --check`, and a real local `report quality-audit --json --top-limit 2` smoke showing path-neutral stale-after-model repair rows. No report regeneration, dashboard regeneration, engagement mutation, live run, provider call, scheduled-task change, or credential persistence was started.
- Pushed B575 as `7cc7b77 feat: report latest fallback repair samples`.
- Clarified Safe Mode doctor and env/docs wording so full mode is described as intentional ROE-backed legacy-offensive posture instead of broken setup; behavior and default status are unchanged.
- Verified B576 with focused doctor tests (`44 passed`), Ruff, py_compile, `git diff --check`, and a real local doctor smoke. No safe-mode default change, live run, provider call, scheduled-task change, monitoring apply, report regeneration, engagement mutation, or credential persistence was started.
- Pushed B576 as `d60a343 fix: clarify safe mode doctor warning`.
- Added value-safe Windows persistent `FORGE_ENGAGEMENT_KEY` reload guidance: `forge connectors secret-key-plan --json` now emits `commands.powershell_reload_persistent_env` when a User/Machine key exists, plain output shows persistent source/length/fingerprint plus the current-process reload command, and doctor remediation shows the exact reload command instead of only generic restart guidance.
- Verified B577 with focused connector/doctor tests (`5 passed`), Ruff, py_compile, `git diff --check`, and real local secret-key-plan/doctor smokes. No secret material was printed, persisted, or committed; no engagement mutation, provider call, live run, monitoring apply, scheduled-task change, report regeneration, or credential persistence was started.
- Pushed B577 as `e731c35 fix: surface connector key reload guidance`.
- Closed three completed subagents. Their read-only audits recommended: improve monitoring due-plan summary/action-plan/stale-backlog fields next, and add report quality-audit operator action-plan buckets after that.
- Improved read-only `forge monitoring due-plan` as an operator plan: added schema version, default execution limit, estimated capped invocations, due/last-run time summaries, stale-backlog age, policy composition counts, action-plan command arrays, and `--include-empty-db-results` for verbose per-DB troubleshooting. Empty planned DB rows are omitted by default while stale/error rows remain visible.
- Doctor Monitoring Schedules now calls due-plan summary-only and includes oldest stale backlog age plus estimated capped run-due batch count in its warning details.
- Verified B578 with focused monitoring/doctor tests (`4 passed`), a broader monitoring/doctor/CLI registry slice (`27 passed`), Ruff, py_compile, `git diff --check`, and real local read-only due-plan/doctor smokes. Local due-plan reports 101 due seed-exposure policies, oldest backlog about 4.96 days, estimated 3 capped batches, and 5 planned rows for `--limit 5`. No monitoring `run-due`, worker execution, alert delivery, engagement mutation, provider call, live run, scheduled-task change, report regeneration, or credential persistence was started.
- Pushed B578 as `c9a03ab feat: summarize monitoring due plans`.
- Added read-only quality-audit `operator_action_plan` buckets for stale latest-report regeneration samples, resume-plan review, long-run review, and policy flag explanation. Human output now prints the plan; JSON commands are arrays only and never invoke `resume-run`.
- Verified B579 with focused quality-audit/CLI registry tests (`27 passed`), Ruff, py_compile, `git diff --check`, and real local read-only quality-audit JSON/human smokes. Local action plan reports 57 stale latest reports, 49 resume-plan reviews, 3 long-run reviews, and policy flag explanation for latest-run metadata. No report regeneration, resume-run, provider call, live run, scheduled-task change, engagement mutation, monitoring apply, or credential persistence was started.
- Pushed B579 as `4491ffd feat: add report quality action plan`.
- Added `forge targets resume-plan --redact-paths` for report/review output. Redacted mode hides local `data_dir`, clears item `db_path`, keeps `db_ref`/`scope_manifest_ref`, and replaces `--scope-manifest` command arguments with `<scope-manifest:filename>` placeholders. Default resume-plan and resume-run remain executable and unchanged.
- Quality-audit `review_resume_plan` now points to `forge targets resume-plan --json --redact-paths --limit N`.
- Verified B580 with focused target resume/quality tests (`9 passed`), broader target-import/quality/CLI registry tests (`58 passed` with existing sqlite timestamp deprecation warnings), Ruff, py_compile, `git diff --check`, and real local read-only redacted resume-plan/quality-audit smokes. No resume-run, kill-chain, report regeneration, provider call, engagement mutation, scheduled-task change, monitoring apply, or credential persistence was started.
- Pushed B580 as `30aae6f fix: redact resume plan review paths`.
- Added Connector Secret Store doctor action items: `setup_connector_secret_key` for missing keys and `reload_connector_secret_key_env` for persistent Windows keys missing from the current process. The reload action uses only the literal process-env reload command and never prints secret material.
- Verified B581 with focused Connector Secret Store doctor tests (`2 passed`), Ruff, py_compile, `git diff --check`, and a real local filtered doctor smoke showing Connector Secret Store `WARN` plus `reload_connector_secret_key_env` action status `ready`. No secret material was printed; no engagement mutation, provider call, live run, scheduled-task change, monitoring apply, report regeneration, or credential persistence was started.
- Pushed B581 as `fa24209 fix: add connector key doctor action`.
- Added report quality-audit sample metadata for stale latest reports: `sample_limit`, `omitted_count`, and read-only `follow_up_commands`; human output prints the follow-up command when the repair sample is truncated.
- Added Offline Strict doctor remediation plus `review_offline_strict` action item without changing the default OFF behavior.
- Raised Windows scheduled-task doctor query timeout from 1.5s to a named 3.0s constant for TPH/remediation task checks to reduce transient `task=error` noise.
- Verified B582 with focused reporting/doctor tests (`13 passed` plus prior quality slice `9 passed`), Ruff, py_compile, `git diff --check`, and real local read-only doctor/quality smokes. Local quality-audit reports 57 stale latest reports, 10 sampled repair commands, 47 omitted commands, and follow-up `forge report quality-audit --json --top-limit 57`; doctor reports Offline Strict `OFF` with `review_offline_strict`, and TPH bridge `OFF task=disabled`. No report regeneration, resume-run, kill-chain, provider call, scheduled-task change, monitoring apply, engagement mutation, or credential persistence was started.
- Pushed B582 as `360be48 fix: make audit action plans complete`.
- Added read-only `forge report stale-plan [--reports-dir reports] [--limit N] [--json]`, backed by `collect_stale_report_repair_plan()`, so stale latest-report regeneration commands can be reviewed as a dedicated plan without running report generation.
- Verified B583 with focused reporting tests (`12 passed`), focused CLI registry tests (`2 passed`), Ruff, py_compile, `git diff --check`, and real local read-only stale-plan JSON/human smokes. Local `stale-plan --json --limit 5` reports schema `forge.report_stale_repair_plan.v1`, 57 stale reports, 5 sampled commands, 52 omitted commands, and execution policy `plan_only_no_commands_executed`. No report regeneration, resume-run, kill-chain, provider call, scheduled-task change, monitoring apply, engagement mutation, or credential persistence was started.
- Pushed B583 as `7fe5c9b feat: add report stale repair plan`.
- Suppressed Connector Action Plan's no-op `install_free_binaries` doctor action when `missing binaries: 0 (none)`; missing-binary environments still emit the install-plan action.
- Verified B584 with focused doctor tests (`3 passed`), Ruff, py_compile, `git diff --check`, and a real local doctor smoke showing Connector Action Plan `OK` and no `install_free_binaries` action. No installer execution, provider call, report regeneration, resume-run, kill-chain, scheduled-task change, monitoring apply, engagement mutation, or credential persistence was started.
- Pushed B584 as `de5b1b1 fix: omit empty connector install action`.
- Added read-only `forge report long-run-plan [--reports-dir reports] [--long-run-seconds N] [--limit N] [--json]`, backed by `collect_long_run_review_plan()`, so long-running failed runs have a dedicated review plan without starting resume-run or kill-chain.
- Quality-audit `review_long_runs` now includes sample counts, omitted counts, and a follow-up command to `forge report long-run-plan --json --limit N`.
- Verified B585 with focused reporting and CLI registry tests (`17 passed`), Ruff, py_compile, `git diff --check`, and real local read-only long-run-plan JSON/human smokes. Local `long-run-plan --json --limit 2` reports schema `forge.report_long_run_review_plan.v1`, 3 long runs, 2 samples, 1 omitted, 0 execution commands, and execution policy `plan_only_no_commands_executed`. No resume-run, kill-chain, report regeneration, provider call, scheduled-task change, monitoring apply, engagement mutation, or credential persistence was started.
- Pushed B585 as `ab81ba0 feat: add report long run plan`.
- Changed stale-report follow-up commands from broad `forge report quality-audit --json --top-limit N` to dedicated `forge report stale-plan --json --limit N`.
- Verified B586 with focused reporting tests (`15 passed`), Ruff, py_compile, `git diff --check`, and real local read-only quality-audit/stale-plan smokes. Local action plan now reports 57 stale reports, 5 sampled, 52 omitted, and follow-up `forge report stale-plan --json --limit 57`. No report regeneration, resume-run, kill-chain, provider call, scheduled-task change, monitoring apply, engagement mutation, or credential persistence was started.
- Pushed B586 as `5ff7eb1 fix: point stale audit to stale plan`.
- Added read-only `forge report policy-plan [--reports-dir reports] [--limit N] [--json]`, backed by `collect_policy_flag_review_plan()`, so latest-run policy `*_no` counts include sampled evidence rows without mutating engagements.
- Quality-audit `review_policy_flags` now includes sample counts, omitted counts, policy samples, and a follow-up command to `forge report policy-plan --json --limit N`.
- Verified B587 with focused reporting and CLI registry tests (`20 passed`), Ruff, py_compile, `git diff --check`, and real local read-only policy-plan/quality-audit smokes. Local `policy-plan --json --limit 5` reports counts `attack_no=72`, `destructive_no=76`, `post_ex_no=76`, 5 sampled rows, 71 omitted rows, 0 execution commands, and execution policy `plan_only_no_commands_executed`. No resume-run, kill-chain, report regeneration, provider call, scheduled-task change, monitoring apply, engagement mutation, or credential persistence was started.
- Pushed B587 as `21107e3 feat: add report policy flag plan`.
- Added read-only `forge connectors run-plan [--domain NAME] [--json]`, backed by `connector_run_plan()`, so runnable free-first connectors have plan-only command templates instead of requiring operators to infer them from `connectors list`.
- Doctor `run_free_connectors` now points to `forge connectors run-plan --json`.
- Verified B588 with focused connector/doctor/CLI registry tests (`7 passed`), Ruff, py_compile, `git diff --check`, and real local read-only connector run-plan/doctor smokes. Local run-plan reports schema `forge.connector_run_plan.v1`, 16 runnable items, and execution policy `plan_only_no_commands_executed`; doctor action `run_free_connectors` now points to `forge connectors run-plan --json`. No connector, provider, installer, report, resume-run, kill-chain, monitoring, scheduled-task, engagement, or credential action was executed.
- Pushed B588 as `33aee7d feat: add connector run plan`.
- Expanded connector run-plan templates for non-generic operator paths: `import-cti`, `import-discovery`, `run-secrets`, `run-identity`, remediation ticket sync, standards STIX import, and artifact intake review.
- Verified B589 with focused connector run-plan tests (`2 passed`), Ruff, py_compile, `git diff --check`, and a real local read-only connector run-plan smoke. The smoke showed required placeholders for ThreatFox, urlscan, gitleaks, HIBP, and subfinder. No connector, provider, installer, report, resume-run, kill-chain, monitoring, scheduled-task, engagement, or credential action was executed.
- Pushed B589 as `0dd0f8f fix: complete connector run templates`.
- Corrected connector operator guidance drift: artifact passive parsers now show a valid `forge kill-chain SEED --engagement N --dry-run` template with local artifact folder notes, and doctor's Connector Action Plan remediation points operators to `forge connectors run-plan --json` first instead of the generic connector run shape.
- Verified B590 with focused connector/doctor tests (`4 passed`), Ruff, py_compile, `git diff --check`, and real local read-only connector run-plan/doctor smokes. No connector, provider, installer, report, resume-run, kill-chain, monitoring, scheduled-task, engagement, or credential action was executed.
- Pushed B590 as `91666dd fix: correct connector plan guidance`.
- Added bounded sequential `forge report stale-run [--limit N] [--provider NAME] [--max-loops N] [--dry-run] [--json]`, backed by `run_stale_report_repair_plan()`, so stale latest reports can be regenerated from the existing stale-plan backlog without copying many commands by hand.
- Verified B591 with focused reporting/CLI registry tests (`5 passed`), Ruff, py_compile, `git diff --check`, stale-run help smoke, and a real local `stale-run --dry-run --limit 3 --provider template --max-loops 0 --json` smoke. The dry-run selected 3 of 57 stale reports and attempted 0 generations. No report regeneration, provider call, resume-run, kill-chain, monitoring, scheduled-task, engagement mutation, or credential action was executed during verification.
- Pushed B591 as `054858c feat: add stale report repair runner`.
- Surfaced the new stale-report runner from `report quality-audit`: `regenerate_stale_reports` now includes `follow_up=forge report stale-run --dry-run --json --limit N` and a machine-readable `batch_run_command` for bounded sequential regeneration.
- Verified B592 with focused reporting tests (`4 passed`), Ruff, py_compile, `git diff --check`, and a real local read-only `quality-audit --top 1` smoke showing `follow_up=forge report stale-run --dry-run --json --limit 57`. No report regeneration, provider call, resume-run, kill-chain, monitoring, scheduled-task, engagement mutation, or credential action was executed.
- Pushed B592 as `d6f4f16 fix: surface stale report runner`.
- Ran one local template-only stale report repair for engagement 10001. It succeeded without provider/live calls, but exposed that the runner wrote report families to the repo root when no output directory was passed. Moved that generated `engagement_10001_report_20260820T162240.*` family into `reports/`.
- Fixed stale-run output placement: effective commands and execution now include `--output <reports-dir>`, and successful runs return `dashboard_refresh_required=true` plus post-run `forge dashboard -o <reports-dir>/dashboard.html` and `forge report quality-audit --json` commands.
- Verified B593 with focused stale-run tests (`3 passed`), Ruff, py_compile, and `git diff --check`. The generated report family remains ignored local artifact data and is not committed. No provider call, resume-run, kill-chain, monitoring, scheduled-task, engagement mutation, or credential action was executed.
- Pushed B593 as `5f63e55 fix: keep stale reports under reports dir`.
- Completed the local stale latest-report repair backlog with template-only report generation: `forge report stale-run --limit 57 --provider template --max-loops 0 --json` attempted 57, completed 57, failed 0, then `forge dashboard -o reports\dashboard.html` refreshed the ignored local dashboard. Follow-up `report quality-audit --json --top-limit 5` reports `latest_fallback_reason_counts={}`, and `report stale-plan --json --limit 5` reports `total_count=0`. Historical fallback lineage still reports old GGUF-missing artifacts, but no latest report remains stale. No provider call, resume-run, kill-chain, monitoring execution, scheduled-task change, engagement mutation, or credential action was executed.
- Fixed limited target resume-plan metadata: `forge targets resume-plan --limit N` now keeps existing selected-slice fields and adds `total_count`, `selected_count`, `omitted_count`, `total_resume_ready_count`, and `total_reason_counts`.
- Verified B594 with focused target resume tests (`3 passed`), Ruff, py_compile, `git diff --check`, and a real local read-only `resume-plan --json --redact-paths --limit 10` smoke. The smoke reports 49 total candidates, 10 selected, 39 omitted, 10 planned, and 49 total ready. No resume-run, kill-chain, provider call, monitoring execution, scheduled-task change, engagement mutation, report regeneration, or credential action was executed.
- Pushed B594 as `2619b23 fix: report total resume plan counts`.
- Added `forge targets resume-run --dry-run` so operators can exercise the exact resume-run batch executor path without creating the batch lock, writing the ledger, or launching child kill-chain processes. Real resume-run behavior remains explicit, sequential, locked, and ledgered when `--dry-run` is omitted.
- Cleaned current dashboard/report rendering for noisy terminal error text: seed-run, engagement-run, and latest-run dashboard fields now strip Rich/terminal box art and Click/Typer `--help` boilerplate, keep secret/scope redaction, and render `abandoned before explicit completion` as `interrupted before finalization`.
- Refreshed the ignored local dashboard twice with `forge dashboard -o reports\dashboard.html`; generated `reports/` artifacts remain untracked local output. A final grep over current dashboard JSON/HTML found no `--help ... for help`, box-art `┌`, or `abandoned before explicit completion` matches.
- Verified B595 with target resume-run focused tests (`5 passed`), broader target/CLI/dashboard/run-summary slice (`64 passed`, existing sqlite timestamp deprecation warnings only), Ruff, py_compile, `git diff --check`, and a real local `targets resume-run --dry-run --limit 1 --batch-id smoke-dry-run --json` smoke. The smoke created no ledger or lock and did not launch kill-chain, provider calls, monitoring execution, scheduled-task changes, engagement mutation, or credential actions.
- Surfaced the new non-mutating resume rehearsal from quality-audit: `review_resume_plan` keeps the redacted read-only `resume-plan` command and now adds follow-up `forge targets resume-run --dry-run --json --limit N`; long-run review guidance now tells operators to rehearse with dry-run before any deliberate live resume.
- Verified B596 with full quality-audit tests plus target dry-run tests (`23 passed`), Ruff, py_compile, `git diff --check`, and real local read-only `report quality-audit --json --top-limit 1` / `report long-run-plan --json --limit 1` smokes. The local quality action plan now exposes dry-run for 49 resume-review candidates without executing resume-run, kill-chain, provider calls, monitoring execution, scheduled-task changes, engagement mutation, or credential actions.
- Added `forge monitoring run-due --dry-run` as a whole-batch non-mutating rehearsal for the exact bounded due-run command. Dry-run delegates to read-only due-plan data, reports `dry_run_no_monitoring_executed`, leaves `run_count/change_count/alert_count` at zero, and does not write snapshots, alerts, audit rows, or schedules.
- Updated monitoring due-plan and doctor action plans to show `dry_run_capped_due_monitoring` before the bounded apply command. Doctor remediation now says to review due-plan, rehearse `run-due --dry-run --limit 50 --json`, then intentionally apply `run-due --limit 50 --json` or worker mode.
- Verified B597 with focused monitoring/doctor/CLI registry tests (`69 passed`), Ruff, py_compile, `git diff --check`, and real local `monitoring run-due --dry-run --limit 3 --json`, `monitoring due-plan --limit 3 --json`, and `doctor --json` smokes. Local dry-run saw 101 due policies, planned 3, limited 98, and executed no monitoring runs, provider calls, scheduled-task changes, engagement mutation, or credential actions.
- Fixed Connector Action Plan command drift: doctor now emits the real CLI shape `forge connectors secret-set --engagement N --connector ID --name ENV_NAME --value-env ENV` instead of invalid `forge connectors secrets set ...`; connector remediation text now matches the actual `secret-set --value-env` command.
- Verified B598 with full doctor/CLI registry/connector registry tests (`115 passed`), Ruff, py_compile, `git diff --check`, and a real local `doctor --json` smoke confirming `configure_optional_keys.command` uses `forge connectors secret-set ... --value-env ENV`. No secret material was printed, persisted, or committed; no connector run, provider call, live run, monitoring apply, scheduled-task change, engagement mutation, or credential action was executed.

Next steps:
- Startup is enabled through the HKCU Run fallback because Task Scheduler registration is denied on this host. The next automatic user logon after the current `failure_backoff_active` window expires should invoke guarded autostart. If recurring cadence is required, run `scripts/install_guarded_autostart_task.ps1` from an elevated shell or allow scheduled-task registration. Do not run live resume, scanner, monitoring apply, Docker mutation, provider API, ticket/webhook, or credential actions unless explicitly requested with the required local config/ROE gates.

<!-- MOLT_AUTO_START -->
## Auto State

- Updated: 2026-08-29 05:25:06 +08:00
- Machine: PRAWN-E14
- Harness: codex
- Event: session-start
- Branch: main
- HEAD: faac599
- Dirty files: 0
- Resume hint: Read .agents/STATE.md, then the latest file in .agents/handoffs/ if present.
<!-- MOLT_AUTO_END -->
